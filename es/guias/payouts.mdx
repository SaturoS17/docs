---
title: "Payouts"
description: "Dispersa fiat a cuentas bancarias locales debitando tu saldo USDT"
---

Un payout envÃ­a dinero en moneda local a una cuenta bancaria del paÃ­s
destino. El monto se convierte de moneda local a USDT con la tasa vigente y
se debita `usdt_amount + fee` de tu saldo.

## 1. Descubre los corredores disponibles

Los paÃ­ses, monedas y mÃ©todos disponibles los define CBPay.
ConsÃºltalos siempre por catÃ¡logo:

```bash
curl https://exchange.qbank.cl/platform/v1/payouts/methods \
  -H "Authorization: Bearer <token>"
```

Corredores tÃ­picos: Chile (CLP), PerÃº (PEN), MÃ©xico (MXN), Venezuela (VES),
Bolivia (BOB/USD) y Paraguay (PYG), con mÃ©todos como `bank_transfer` y
variantes locales segÃºn el paÃ­s.

Para transferencias bancarias necesitas ademÃ¡s el catÃ¡logo de bancos:

```bash
curl "https://exchange.qbank.cl/platform/v1/payouts/banks?country=CL" \
  -H "Authorization: Bearer <token>"
```

## 2. Crea el payout

```bash
curl -X POST https://exchange.qbank.cl/platform/v1/payouts \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "country": "MX",
    "currency": "MXN",
    "method": "bank_transfer",
    "amount": "1500.00",
    "beneficiary": {
      "name": "MarÃ­a LÃ³pez",
      "account_type": "clabe",
      "account_number": "012180001234567895"
    },
    "description": "Pago factura 8841",
    "idempotency_key": "factura-8841"
  }'
```

<Warning>
`beneficiary` es un objeto de pares clave/valor cuyos campos requeridos
dependen del corredor (RUT y banco en Chile, CLABE en MÃ©xico, CCI en PerÃº,
etc.). El catÃ¡logo de mÃ©todos documenta los campos de cada uno.
</Warning>

Respuesta `202 Accepted`:

```json
{
  "payout_id": "0d4fâ€¦",
  "account_id": "â€¦",
  "idempotency_key": "factura-8841",
  "country": "MX",
  "currency": "MXN",
  "method": "bank_transfer",
  "local_amount": "1500.00",
  "fx_rate": "17.50",
  "usdt_amount": "85.714286",
  "fee": "0.300000",
  "total_debit": "86.014286",
  "status": "processing",
  "created_at": "2026-07-06T20:00:00Z"
}
```

En ese momento tu saldo ya refleja el dÃ©bito: `total_debit` pasÃ³ de
`available` a `held`.

## 3. Recibe el estado final

SuscrÃ­bete al evento `payout_status_changed` ([webhooks](/es/webhooks)):

```json
{
  "payout_id": "0d4fâ€¦",
  "account_id": "â€¦",
  "country": "MX",
  "currency": "MXN",
  "local_amount": "1500.00",
  "usdt_amount": "85.714286",
  "total_debit": "86.014286",
  "status": "completed",
  "status_code": ""
}
```

- **`completed`**: el dinero llegÃ³; el hold se consume.
- **`failed`**: se reembolsa el dÃ©bito completo automÃ¡ticamente
  (`payout_refund` en tu ledger).

TambiÃ©n puedes consultar en cualquier momento:

```bash
curl https://exchange.qbank.cl/platform/v1/payouts/0d4fâ€¦ \
  -H "Authorization: Bearer <token>"
```

## Errores frecuentes

| HTTP | `error` | QuÃ© hacer |
|---|---|---|
| 400 | `idempotency_key_required` | EnvÃ­a la clave en body o header |
| 400 | `beneficiary_required` | Incluye el objeto `beneficiary` |
| 402 | `insufficient_funds` | Fondea la cuenta; el payout no se creÃ³ |
| 403 | `account_blocked` | La cuenta no estÃ¡ activa; contacta al equipo de CBPay |
| 422 | `currency_not_supported` | No hay tasa FX para esa moneda |
| 422 | (payout con `status: failed`) | El corredor rechazÃ³ los datos; el dÃ©bito ya fue reembolsado â€” corrige `beneficiary` y reintenta con clave nueva |

## Rechazo inmediato vs fallo posterior

Si el procesador rechaza el payout al crearlo, recibes `422` con el objeto
en `status: failed` y el reembolso ya aplicado. Si falla despuÃ©s (por
ejemplo, cuenta destino inexistente detectada por el banco), te llega el
webhook con `status: failed` y el reembolso automÃ¡tico en ese momento.
