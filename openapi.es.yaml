openapi: 3.1.0
info:
  title: CBPay API
  version: "2.67"
  description: 'CBPay es una plataforma de pagos multimoneda: payouts y cobros fiat

    en toda América Latina, transferencias internas, fondeo y retiros

    on-chain, y screening KYC. Cada cuenta mantiene cuatro saldos virtuales

    independientes — USDT (la moneda operativa), USDC, BTC y GOLD (gramos de

    oro fino) — convertibles a demanda con swaps.

    Los payouts y las comisiones de servicios pueden pagarse desde cualquiera

    de los cuatro saldos (`PUT /v1/settlement` u override por payout con

    `settlement_asset`), y los payins pueden auto-convertirse al asset que

    elijas (`default_payin_asset`).


    Todos los montos son strings decimales en la precisión de cada moneda (6

    decimales para USDT/USDC/GOLD, 8 para BTC). Los errores siempre devuelven

    `{"error": "<code>", "message": "<detail>"}`.

    '
servers:
- url: https://api.qbank.cl/platform
  description: Live (producción, dinero real)
- url: https://cryptobank.qbank.cl/platform
  description: Test (sandbox, dinero simulado — keys pk_test_)
security:
- bearerAuth: []
tags:
- name: Comprobantes
  description: Comprobante PDF brandeado por operacion, con verificacion publica de autenticidad (QR firmado),
    receipt_url en cada respuesta/webhook y envio automatico por email en estados finales.
- name: Autenticación
  description: Registra e inicia sesión de los miembros de la cuenta. Las sesiones duran 24 horas.
- name: Cuenta
  description: Perfil, miembros y llaves de API de la cuenta que llama.
- name: Saldos
  description: Saldos, historial de movimientos y tasas de cambio.
- name: Payouts
  description: Dispersiones fiat que se debitan del saldo de settlement que elijas (USDT por defecto).
- name: Payins
  description: Recargas fiat (QR, transferencias, cuentas dedicadas, cobros pull, tarjetas, links de
    checkout) que se acreditan automáticamente — en USDT por defecto, o convertidas al settlement asset
    que elijas.
- name: Checkout
  description: >-
    Links de checkout universal (`POST /v1/payins` con `method: "checkout"`) y los endpoints
    públicos de la página de pago — fiat multi-país, crypto con wallet efímera por link y pago
    directo CBPay, liquidado en el asset que elijas.
- name: Tarjetas guardadas
  description: >-
    Tarjetas guardadas con el consentimiento explícito del pagador durante un pago 3-D Secure
    (COF). Listarlas, revocarlas y cobrarlas bajo demanda (MIT) sin pedir la tarjeta de nuevo.
- name: Suscripciones
  description: >-
    Cobros recurrentes sobre una tarjeta guardada gestionados por el scheduler de la plataforma
    — diarios, semanales, mensuales o anuales, con pausa/reanudación/cancelación y dunning
    automático.
- name: Transferencias
  description: Transferencias internas gratuitas entre cuentas CBPay (persona o empresa, cualquier combinación).
- name: Contacts
  description: Libreta de beneficiarios por cuenta (destinos CBPay, bancarios y crypto) con match por
    teléfono y auto-guardado en cada envío.
- name: Swaps
  description: Conversión instantánea entre los saldos USDT, USDC, BTC y GOLD de la cuenta a la tasa
    cotizada de la cuenta.
- name: Crypto
  description: Fondeo y retiros on-chain (TRON, Ethereum y Bitcoin).
- name: Wallets segregadas
  description: Wallets on-chain con saldo propio (empresas ilimitadas; personas 1 por combinación red+activo)
    — crear, importar, enviar, exportar la llave privada y auto-forward. El saldo vive on-chain, nunca
    en el ledger.
- name: Pruebas de firma
  description: Firma criptográfica de mensajes con wallets (EIP-191 ETH/EVM, TIP-191 TRON) — crear,
    listar, consultar y revocar pruebas de firma con verificación pública.
- name: Vínculos de wallets
  description: Vincula wallets externas (custody=client) a tu cuenta firmando un desafío nonce —
    crear desafíos, verificar firmas, listar y revocar vínculos.
- name: QR Crypto POS
  description: "Cobros QR crypto con monto para procesadores con POS físicos (cuentas empresa): merchants verificados, dirección exclusiva + QR por cobro, detección temprana del pago, conciliación por merchant y devoluciones por el riel de retiro crypto."
- name: KYC / KYB
  description: 'Verificación de identidad: KYC para personas, KYB para empresas — tu propio onboarding
    y verificaciones de terceros para cuentas empresa.'
- name: Screening de wallets
  description: Evaluación de riesgo AML de direcciones blockchain (sanciones, exposición a fondos ilícitos)
    con comisión por scan, más protección automática gratuita en retiros y depósitos.
- name: AML screening
  description: Screening AML standalone de personas y empresas contra listas de sanciones, PEP y medios
    adversos, con re-screening, monitoreo continuo e informe PDF descargable.
- name: Qscore
  description: Buró de crédito API-first. Emite informes crediticios completos con score 1-999 (bandas A-E, o SC cuando
    no hay datos), consulta el último score de un sujeto y gestiona disputas ARCO. Chile primero, con diseño
    agnóstico al país. Cada informe emitido lleva un código de verificación pública.
- name: Analytics
- name: Webhooks
  description: Suscripciones para recibir notificaciones de eventos firmadas.
- name: Estado
  description: Disponibilidad del servicio.
- name: Banking
  description: 'Cuentas bancarias reales: recibe, mantén y envía dinero por rieles bancarios internacionales.'
- name: Cards
  description: Tarjetas virtuales y físicas que gastan Just-In-Time del saldo que elijas (USDT, USDC,
    BTC o GOLD), con límites de gasto por tarjeta.
- name: Seguridad (OTP)
  description: Códigos de verificación de un solo uso por SMS/WhatsApp/email que protegen acciones sensibles,
    más las preferencias de 2FA self-service. Aplican solo a sesiones de usuario — las API keys quedan
    exentas.
- name: Passkeys
  description: Inicio de sesión sin contraseña con la biometría del dispositivo (Face ID, Touch ID, Windows
    Hello, llaves de seguridad) vía WebAuthn, apps autenticadoras (TOTP) con códigos de respaldo, y gestión
    de sesiones/dispositivos.
- name: Login social
  description: Registro e inicio de sesión sin contraseña con Google, Apple, Microsoft y Facebook vía
    token exchange. El front obtiene la credencial del proveedor; la API la verifica y emite la sesión
    CBPay.
- name: Eventos en tiempo real
  description: >-
    Stream Server-Sent Events con todo lo que pasa en la cuenta (o en toda la organización para administradores), replay con `Last-Event-ID`, snapshot inicial opcional e historial consultable de 90 días.
paths:
  /v1/payouts/{payoutID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getPayoutReceipt
      summary: Descargar el comprobante del payout (PDF)
      description: Comprobante PDF brandeado del payout, con los datos del beneficiario, montos, badge
        de estado y el QR de verificación firmado. Los estados no finales llevan marca de agua diagonal.
        Solo el dueño del payout (o el admin de la org) puede descargarlo.
      parameters:
      - name: payoutID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/{payinID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getPayinReceipt
      summary: Descargar el comprobante del payin (PDF)
      description: Comprobante PDF brandeado del cobro, con la referencia, montos, badge de estado y el
        QR de verificación firmado. Los estados no finales llevan marca de agua diagonal.
      parameters:
      - name: payinID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/transfers/{transferID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getTransferReceipt
      summary: Descargar el comprobante de la transferencia (PDF)
      description: Comprobante PDF brandeado de la transferencia interna (las dos partes pueden descargarlo;
        la contraparte se muestra por display name).
      parameters:
      - name: transferID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/crypto/withdrawals/{withdrawalID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getCryptoWithdrawalReceipt
      summary: Descargar el comprobante del retiro crypto (PDF)
      description: Comprobante PDF brandeado del retiro on-chain, con la red, dirección de destino, tx
        hash y el QR de verificación firmado.
      parameters:
      - name: withdrawalID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/crypto/deposits/{depositID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getCryptoDepositReceipt
      summary: Descargar el comprobante del depósito crypto (PDF)
      description: Comprobante PDF brandeado de un depósito on-chain acreditado. El `depositID` es el
        campo `deposit_id` que devuelve `GET /v1/crypto/transactions`.
      parameters:
      - name: depositID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/swaps/{swapID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getSwapReceipt
      summary: Descargar el comprobante del swap (PDF)
      description: Comprobante PDF brandeado de la conversión de saldos, con las dos puntas, la tasa aplicada
        y el QR de verificación firmado.
      parameters:
      - name: swapID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/cards/{cardID}/transactions/{transactionID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getCardTransactionReceipt
      summary: Descargar el comprobante de la compra con tarjeta (PDF)
      description: Comprobante PDF brandeado de una transacción de tarjeta, con el comercio, monto USD,
        saldo debitado y el QR de verificación firmado.
      parameters:
      - name: cardID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: transactionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/banking/operations/{operationID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getBankingOperationReceipt
      summary: Descargar el comprobante de la operación bancaria (PDF)
      description: Comprobante PDF brandeado de una operación bancaria (monto, moneda, contraparte, estado)
        con el QR de verificación firmado. Disponible para operaciones creadas desde que el espejo de
        trazabilidad quedó activo.
      parameters:
      - name: operationID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/segregated-wallets/{walletID}/sends/{sendID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getWalletSendReceipt
      summary: Descargar el comprobante del envío desde wallet segregada (PDF)
      description: Comprobante PDF brandeado de un envío on-chain desde una wallet segregada, con la red,
        dirección destino, hash de la transacción y el QR de verificación firmado.
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: sendID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/segregated-wallets/{walletID}/deposits/{depositID}/receipt:
    get:
      tags:
      - Comprobantes
      operationId: getWalletDepositReceipt
      summary: Descargar el comprobante del depósito en wallet segregada (PDF)
      description: Comprobante PDF brandeado de un depósito on-chain recibido en una wallet segregada,
        con la red, dirección de origen, hash de la transacción y el QR de verificación firmado.
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: depositID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/receiptLang'
      responses:
        '200':
          $ref: '#/components/responses/ReceiptPDF'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /verify/reports/{code}:
    get:
      security: []
      tags:
      - KYC / KYB
      operationId: verifyReport
      summary: Verificar la autenticidad de un informe (público)
      description: >-
        Endpoint público (sin credenciales) detrás del QR impreso en cada informe de verificación. El
        código va firmado criptográficamente - uno adulterado o inventado responde 404 con valid=false.
        La respuesta solo expone el tipo (KYC/KYB), el estado VIGENTE de la decisión y su fecha - jamás
        la identidad del sujeto, sus documentos ni resultados AML. Los navegadores reciben una página
        HTML brandeada; los clientes API reciben JSON. Rate limit por IP.
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código de verificación impreso en el informe (también embebido en la URL del QR).
      responses:
        '200':
          description: El informe es auténtico; hechos vigentes de la verificación.
          content:
            application/json:
              example:
                valid: true
                type: verification_report
                kind: kyb
                status: ok
                decision: approved
                date: '2026-07-21'
                issued_by: CBPay
        '404':
          description: Código de verificación inválido, adulterado o desconocido.
          content:
            application/json:
              example:
                valid: false
                message: this verification code does not correspond to any verification report issued by this platform
        '429':
          description: Demasiados intentos de verificación desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many verification attempts; wait a moment and retry
  /verify/qscore/{code}:
    get:
      security: []
      tags:
      - Qscore
      operationId: verifyQscoreReport
      summary: Verificar la autenticidad de un informe Qscore (público)
      description: 'Endpoint público (sin credenciales) detrás del código de verificación impreso en cada informe
        crediticio Qscore. El código va firmado criptográficamente: uno adulterado o inventado responde 404 con
        `valid: false`. La respuesta solo expone hechos no sensibles: que el informe existe, su BANDA de score
        (nunca el score numérico) y su fecha de emisión — jamás la identidad del sujeto ni el detalle crediticio.
        Los navegadores reciben una página HTML brandeada; los clientes API reciben JSON. Rate limit por IP.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código de verificación impreso en el informe (el campo `verify_code` del recurso informe).
      responses:
        '200':
          description: El informe es auténtico; hechos no sensibles.
          content:
            application/json:
              example:
                valid: true
                type: verification_report
                kind: qscore
                status: ready
                decision: B
                date: '2026-08-08'
                issued_by: CBPay
        '404':
          description: Código de verificación inválido, adulterado o desconocido.
          content:
            application/json:
              example:
                valid: false
                message: this verification code does not correspond to any verification report issued by this platform
        '429':
          description: Demasiados intentos de verificación desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many verification attempts; wait a moment and retry
  /verify/qscore/seal/{code}:
    get:
      security: []
      tags:
      - Qscore
      operationId: verifyQscoreSeal
      summary: Consultar la vigencia actual de un sello Qscore (público)
      description: 'Endpoint público (sin credenciales) detrás del sello Qscore que una
        empresa activa con `POST /v1/qscore/my-seal`. El código va firmado criptográficamente:
        uno adulterado o inventado responde 404 con `valid: false`. La elegibilidad se evalúa
        EN VIVO en cada llamada: la banda (A o B) solo se muestra mientras el sello está activo
        Y el sujeto sigue calificando (banda A/B con una evaluación de no más de 90 días). Un
        sello que dejó de calificar responde `seal_status: not_current` — anti-oráculo: la banda
        a la que cayó, el score numérico y la razón jamás se revelan. El score numérico NUNCA
        aparece en ninguna superficie pública. Los navegadores reciben una página HTML
        brandeada (`Accept: text/html`); los clientes API reciben JSON. Rate limit por IP.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código público de verificación del sello (el campo `verify_code` del recurso sello).
      responses:
        '200':
          description: El estado del sello, evaluado en vivo.
          content:
            application/json:
              examples:
                active:
                  summary: Sello vigente (activo y todavía elegible)
                  value:
                    valid: true
                    type: qscore_seal
                    seal_status: active
                    band: A
                    evaluated_at: '2026-08-07'
                    company_name: Comercial Andes SpA
                    doc_id: 76.543.210-3
                    country: CL
                revoked:
                  summary: Sello revocado por el titular (permanente para ese código)
                  value:
                    valid: false
                    seal_status: revoked
                    revoked_at: '2026-08-09'
                not_current:
                  summary: Sello ya no elegible (banda cayó o score envejeció) — la razón nunca se revela
                  value:
                    valid: false
                    seal_status: not_current
        '404':
          description: Código de verificación inválido, adulterado o desconocido.
          content:
            application/json:
              example:
                valid: false
                seal_status: not_current
        '429':
          description: Demasiados intentos de verificación desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many verification attempts; wait a moment and retry
  /verify/qscore/seal/{code}/badge.svg:
    get:
      security: []
      tags:
      - Qscore
      operationId: verifyQscoreSealBadge
      summary: Badge SVG embebible con el estado vivo del sello (público)
      description: 'Endpoint público (sin credenciales) que renderiza el sello Qscore
        embebible como imagen SVG (180x64, fondo oscuro). Mientras el sello está activo
        y el sujeto sigue calificando, el badge muestra la letra de la banda actual (A o B)
        en verde con la etiqueta "VERIFICADO"; si no, la MISMA URL renderiza un badge gris
        con la etiqueta "NO VIGENTE" — nunca muestra una banda desactualizada y nunca rompe
        la página que lo hospeda. `Cache-Control: public, max-age=300` (los visitantes pueden
        cachear la imagen hasta 5 minutos). Responde 404 con cuerpo vacío solo para códigos
        inválidos o adulterados.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código público de verificación del sello (el campo `verify_code` del recurso sello).
      responses:
        '200':
          description: El badge SVG con el estado vivo del sello.
          content:
            image/svg+xml:
              schema:
                type: string
              example: '<svg xmlns="http://www.w3.org/2000/svg" width="180" height="64">...</svg>'
        '404':
          description: Código de verificación inválido, adulterado o desconocido (cuerpo vacío).
  /verify/receipts/{code}:
    get:
      security: []
      tags:
      - Comprobantes
      operationId: verifyReceipt
      summary: Verificar la autenticidad de un comprobante (público)
      description: 'Endpoint PÚBLICO (sin credenciales) detrás del QR impreso en cada comprobante. El
        código viene firmado criptográficamente: uno adulterado o inventado responde 404 con `valid: false`.
        La respuesta muestra el estado y monto REALES y vigentes de la operación — jamás datos personales
        del beneficiario. Los navegadores reciben una página HTML brandeada; los clientes API reciben
        JSON. Rate-limited por IP.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código de verificación impreso en el comprobante (también embebido en la URL del
          QR).
      responses:
        '200':
          description: El comprobante es auténtico; datos vigentes de la operación.
          content:
            application/json:
              example:
                valid: true
                type: payout
                status: ok
                raw_status: completed
                amount: 800.00 VES
                detail: Venezuela — Pago Móvil
                date: 2026-07-11 15:29 UTC
                issued_by: CBPay
        '404':
          description: Código de verificación inválido, adulterado o desconocido.
          content:
            application/json:
              example:
                valid: false
                message: this verification code does not correspond to any receipt issued by this platform
        '429':
          description: Demasiados intentos de verificación desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many verification attempts; wait a moment and retry
  /v1/track-link:
    get:
      tags:
      - Comprobantes
      operationId: getTrackLink
      summary: Obtener el link público de seguimiento de una transacción
      description: 'Devuelve el link público compartible de cualquier transacción
        de tu alcance de lectura, sin descargar el PDF del comprobante — la pieza
        base de un botón "Compartir link" en tu UI. El `code` es el mismo código
        determinista firmado con HMAC que se imprime como QR en cada comprobante,
        así que llamar el endpoint dos veces para la misma transacción devuelve
        siempre el mismo link, y quien reciba el link puede seguir la operación en
        la página pública de seguimiento (`GET /v1/public/track/{code}`, estilo
        Wise: timeline, descarga del PDF, tres idiomas). Disponible para la propia
        cuenta (API key o sesión de miembro), para admins de la organización y para
        admins de plataforma — el mismo alcance de lectura que los comprobantes.
        Una transacción fuera de tu alcance (o un `kind` desconocido) responde 404,
        nunca 403: la existencia nunca se revela.'
      parameters:
      - name: kind
        in: query
        required: true
        schema:
          type: string
          enum: [payout, payin, payin_refund, transfer, crypto_withdrawal, crypto_deposit, swap, card_purchase, banking_operation, wallet_send, wallet_deposit]
        description: Familia de la transacción.
      - name: id
        in: query
        required: true
        schema:
          type: string
        description: Identificador de la transacción — el mismo id que devuelven
          los endpoints del producto y los webhooks del `kind` elegido.
      responses:
        '200':
          description: El link público de seguimiento y su código firmado.
          content:
            application/json:
              example:
                track_url: 'https://business.cbpayapp.com/t/P9b1deb4d3b7d4bad9bdd2b0d7b3dcb6d8f4a2c1e5b7'
                code: P9b1deb4d3b7d4bad9bdd2b0d7b3dcb6d8f4a2c1e5b7
        '400':
          description: Falta `kind` o `id`.
          content:
            application/json:
              example:
                error: invalid_payload
                message: kind and id are required
        '404':
          description: La transacción no existe, el `kind` es desconocido o está fuera
            de tu alcance (respuesta uniforme — anti-enumeración).
          content:
            application/json:
              example:
                error: not_found
                message: transaction not found
  /v1/public/track/{code}:
    get:
      security: []
      tags:
      - Comprobantes
      operationId: trackReceipt
      summary: Seguir una operación por su link público (público)
      description: 'Endpoint PÚBLICO (sin credenciales) detrás del link de seguimiento compartible
        impreso en cada comprobante y email de comprobante. El código viene firmado criptográficamente:
        uno adulterado o inventado responde un 404 uniforme. Los estados de revisión se generalizan a
        `processing` (anti tipping-off). La `timeline` usa una secuencia fija de pasos por familia de
        operación y solo lleva timestamps REALES (created/updated) — jamás fabricados. `lang` cambia
        las etiquetas (`en` por defecto vía la página hospedada, `es`, `zh`). Toda respuesta lleva
        `X-Robots-Tag: noindex` y `Cache-Control: no-store`. Comparte el rate limit por IP del endpoint
        de verificación.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código de seguimiento firmado impreso en el comprobante (el mismo código de la URL de verificación).
      - name: lang
        in: query
        required: false
        schema:
          type: string
          enum: [en, es, zh]
        description: Idioma de las etiquetas, el subtítulo y `whats_next`.
      responses:
        '200':
          description: Estado público vigente de la operación.
          content:
            application/json:
              example:
                kind: payout
                id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                status: in_transit
                status_class: pending
                subtitle: Venezuela — Pago Móvil
                fields:
                - {label: 'ID DE TRANSACCIÓN', value: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d, mono: true}
                - {label: 'FECHA Y HORA', value: '8 de agosto de 2026 a las 15:29 UTC'}
                - {label: 'BENEFICIARIO', value: 'María Pérez'}
                - {label: 'BANCO', value: 'Banco de Venezuela'}
                - {label: 'TELÉFONO', value: 0414-1234567}
                - {label: 'MENSAJE', value: 'Factura 1042'}
                amounts:
                - {label: 'MONTO RECIBIDO', value: '1250000.00 VES'}
                - {label: 'TASA DE CAMBIO', value: '950.00'}
                - {label: 'DÉBITO TOTAL', value: '1315.79 USDT'}
                created_at: '2026-08-08T15:29:04Z'
                timeline:
                - {key: initiated, state: complete, at: '2026-08-08T15:29:04Z'}
                - {key: processing, state: complete, at: null}
                - {key: in_transit, state: in_progress, at: null}
                - {key: completed, state: upcoming, at: null}
                branding: {name: CBPay, logo_url: 'https://cdn.example.com/logo.png', website: 'https://www.cbpayapp.com', accent: '#FBC140'}
                receipt_pdf_url: 'https://api.qbank.cl/platform/v1/public/track/Cb4f8a…/receipt.pdf'
                whats_next: payout.in_transit
                support: {website: 'https://www.cbpayapp.com'}
        '404':
          description: Código de seguimiento inválido, adulterado o desconocido (respuesta uniforme — anti-enumeración).
          content:
            application/json:
              example:
                error: not_found
                message: this tracking link does not correspond to any transaction issued by this platform
        '429':
          description: Demasiados intentos desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /v1/public/track/{code}/receipt.pdf:
    get:
      security: []
      tags:
      - Comprobantes
      operationId: trackReceiptPdf
      summary: Descargar el PDF del comprobante desde el link de seguimiento (público)
      description: 'Endpoint PÚBLICO (sin credenciales) que genera el PDF del comprobante al vuelo con
        el mismo renderer de los endpoints autenticados, en el idioma pedido (`lang=es|en|zh`; el chino
        se dibuja con la fuente Noto Sans SC). Mismo código firmado del tracker JSON. Las respuestas
        llevan `X-Robots-Tag: noindex` y `Cache-Control: no-store`. Comparte el rate limit por IP.'
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
        description: Código de seguimiento firmado impreso en el comprobante.
      - name: lang
        in: query
        required: false
        schema:
          type: string
          enum: [en, es, zh]
        description: Idioma del PDF.
      responses:
        '200':
          description: El PDF del comprobante, generado al vuelo.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          description: Código de seguimiento inválido, adulterado o desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: this tracking link does not correspond to any transaction issued by this platform
        '429':
          description: Demasiados intentos desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /v1/public/signature-proofs/{code}:
    get:
      security: []
      tags:
      - Pruebas de firma
      operationId: verifySignatureProofPublic
      summary: Verificación pública de una prueba de firma (sin auth)
      description: "Verifica públicamente una prueba de firma de wallet por su código\n
        de capacidad `proof_code` (empieza con `G`). Sin autenticación: quien tiene el\n
        link puede verificar la prueba. Si el cliente pide HTML (`Accept: text/html`),\n
        devuelve una página brandeada; en cualquier otro caso devuelve JSON. La firma\n
        y el hash del mensaje se exponen porque son el material de la prueba pública.\n\n
        Anti-enumeración: un código inválido, adulterado o inexistente devuelve el MISMO\n
        404 genérico. Rate limit por IP."
      parameters:
      - name: code
        in: path
        required: true
        description: Código público de la prueba (capability code HMAC, empieza con `G`).
        schema:
          type: string
        example: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
      responses:
        '200':
          description: Detalle verificable de la prueba (JSON) o página brandeada (HTML).
          content:
            application/json:
              example:
                proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                status: signed
                valid: true
                chain: eth
                address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                purpose: wallet_ownership
                statement: I control this wallet
                issued_at: '2026-08-21T14:03:11Z'
                expires_at: '2026-08-21T14:13:11Z'
                signed_at: '2026-08-21T14:03:11Z'
                message_hash: '3f8a1c4e9b2d6f70a5c8e1b4d7f2093a4c6e8f10b3d5f709e2a4c6d8f0b1c3d5'
                signature: '8ba1f109551bd432803012645ac136ddd64dba72a0c9e0b1f5b1a2c3d4e5f60718293a4b5c6d7e8f90112233445566778899aabbccddeeff00112233445566771b'
                verified_live: true
        '404':
          description: Código inválido, adulterado o desconocido (anti-enumeración).
          content:
            application/json:
              example:
                error: not_found
                message: this verification link does not correspond to any signature proof issued by this platform
        '429':
          description: Demasiados intentos desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many verification attempts; wait a moment and retry
  /pay/{token}:
    get:
      security: []
      tags:
      - Checkout
      operationId: checkoutPage
      summary: Página de cobro universal (pública)
      description: 'Página HTML pública brandeada detrás de la `checkout_url` que devuelve `POST /v1/payins`
        con `method: "checkout"`. El pagador la abre sin credenciales, elige un país y método fiat,
        una opción crypto (dirección de depósito con QR escaneable) o paga directo desde la app
        CBPay (QR + alias del comercio siempre visibles), y la página se actualiza sola cuando el
        pago se confirma. El token es derivado criptográficamente; uno inválido responde 404. Rate
        limit por IP.'
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en la `checkout_url`.
      responses:
        '200':
          description: Página de cobro brandeada (HTML).
          content:
            text/html:
              example: <!doctype html>…
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /pay/{token}/state:
    get:
      security: []
      tags:
      - Checkout
      operationId: checkoutState
      summary: Estado del cobro universal (público)
      description: Estado JSON de un link de cobro universal — para el polling de la propia página y
        para integradores que rendericen su propio front de pago sobre el mismo link. Muestra el
        estado, el asset y monto de settlement, el método ganador una vez pagado, las
        materializaciones fiat congeladas (`fiat_methods`), el progreso en vivo de cada dirección
        de depósito crypto (monto adeudado vs recibido) y el `conversion_status` de la
        auto-conversión. Sin credenciales; rate limit por IP.
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en la `checkout_url`.
      responses:
        '200':
          description: Estado actual del link de cobro.
          content:
            application/json:
              examples:
                pending:
                  summary: Pendiente con cotización fiat congelada y progreso crypto
                  value:
                    status: pending
                    settlement_asset: USDT
                    asset_amount: '50'
                    description: Order 8841
                    expires_at: '2026-07-17T17:57:44Z'
                    fiat_methods:
                    - method: qr
                      country: MX
                      currency: MXN
                      local_amount: '941.00'
                    crypto:
                    - method: crypto:tron:usdt
                      chain: tron
                      asset: USDT
                      address: TWkfyUCXSdRfNHrRc9CJisFLYd6Ha3XWvS
                      due: '50.000000'
                      received: '5.000000'
                paid:
                  summary: Pagado vía crypto, conversión hecha
                  value:
                    status: paid
                    settlement_asset: GOLD
                    asset_amount: '2'
                    description: Order 8841
                    expires_at: '2026-07-17T17:57:44Z'
                    conversion_status: done
                    paid_method: crypto:tron:usdt
                    paid_at: '2026-07-16T18:03:11Z'
                    redirect_url: https://your-app.com/payment/ok
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /pay/{token}/quote:
    get:
      security: []
      tags:
      - Checkout
      operationId: checkoutQuote
      summary: Cotizar un link de cobro antes de elegir (público)
      description: >-
        Cotizaciones indicativas de un link de cobro universal ANTES de materializar —
        el catálogo de países con métodos fiat vivos (tarjeta excluida — ver `cards`), las
        opciones de tarjeta listadas aparte por país+moneda en `cards[]` con el monto adeudado
        cotizado (un corredor puede aceptar varias monedas de cargo, ej. BOB y USD), el monto
        adeudado por opción crypto (con el costo de conversión incluido cuando el asset de pago
        difiere del de settlement) y los montos de pago directo CBPay por saldo habilitado.
        Cada país lista sus corredores en `options[]` — una fila por método+moneda (un país
        puede ofrecer el mismo método en varias monedas, ej. QR en BOB y USD) con
        `collect: true` en los métodos pull que piden los datos del pagador en la página (ver
        `POST /pay/{token}/collect`). Con `?country=XX` devuelve además el monto local cotizado
        por opción de ese país. La materialización congela la cotización definitiva. Sin
        credenciales; rate limit por IP.
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en la `checkout_url`.
      - name: country
        in: query
        required: false
        schema:
          type: string
        description: País ISO-3166 alpha-2 opcional para cotizar el monto local.
      responses:
        '200':
          description: Cotizaciones indicativas del link.
          content:
            application/json:
              example:
                status: pending
                settlement_asset: USDT
                asset_amount: '50'
                countries:
                - country: BO
                  currency: BOB
                  methods:
                  - qr
                  options:
                  - method: qr
                    currency: BOB
                  - method: qr
                    currency: USD
                - country: VE
                  currency: VES
                  methods: []
                  options:
                  - method: c2p
                    currency: VES
                    collect: true
                  - method: debito_inmediato
                    currency: VES
                    collect: true
                - country: CL
                  currency: CLP
                  methods:
                  - bank_transfer
                  - fintoc
                  options:
                  - method: bank_transfer
                    currency: CLP
                  - method: fintoc
                    currency: CLP
                cards:
                - country: BO
                  currency: BOB
                  local_amount: '543.30'
                - country: BO
                  currency: USD
                  local_amount: '50.75'
                crypto:
                - method: crypto:tron:usdt
                  chain: tron
                  asset: USDT
                  due: '50.000000'
                - method: crypto:btc:btc
                  chain: btc
                  asset: BTC
                  due: '0.00046120'
                  quote_ttl_seconds: 900
                cbpay:
                  alias: tayl.code
                  dues:
                    USDT: '50.000000'
                    USDC: '50.100200'
                    BTC: '0.00046120'
                    GOLD: '0.462100'
                country_quote:
                  country: BO
                  currency: BOB
                  methods:
                  - qr
                  local_amount: '543.30'
                  options:
                  - method: qr
                    currency: BOB
                    local_amount: '543.30'
                  - method: qr
                    currency: USD
                    local_amount: '50.75'
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '422':
          description: El país pedido no tiene métodos de pago disponibles.
          content:
            application/json:
              example:
                error: country_unavailable
                message: no payment methods are available for that country
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
        '503':
          description: Cotización temporalmente no disponible.
          content:
            application/json:
              example:
                error: pricing_unavailable
                message: pricing is temporarily unavailable; retry in a moment
  /pay/{token}/methods/{method}:
    post:
      security: []
      tags:
      - Checkout
      operationId: checkoutMaterialize
      summary: Materializar una opción de pago del checkout (público)
      description: >-
        Crea (lazy) la opción de pago que el pagador eligió en un link de cobro universal
        y devuelve sus instrucciones — la URL de pago para tarjeta/métodos hosted, el QR para
        métodos QR, la referencia de anuncio para transferencias bancarias, o una dirección de
        depósito crypto exclusiva con el monto exacto cotizado más un QR escaneable
        (`qr_payload` + `qr_png_base64` — siempre la dirección cruda para BTC/TRON/ETH,
        legible por wallets y exchanges; las URIs BIP-21/EIP-681 las rechazan apps
        como Binance). Los métodos fiat exigen `?country=XX`; el monto
        local se cotiza y CONGELA en ese momento. Cuando el país ofrece el método en más de una
        moneda (ver `options[]` del quote — ej. QR en BOB y USD, o los corredores de tarjeta)
        exige además `&currency=YYY`; cada moneda es una materialización independiente. Las
        transferencias bancarias en México emiten una CLABE dedicada exclusiva del link
        (`destination` con `dedicated: true`): el pagador transfiere el monto exacto SIN
        referencia — el abono se rutea al link automáticamente. Si la cuenta dedicada no puede
        emitirse, el payload degrada al camino clásico (cuenta del comercio + `reference`
        obligatoria en la glosa). Los métodos pull
        (`collect: true` en el quote, ej. `c2p` y `debito_inmediato` en Venezuela) congelan la
        cotización y devuelven el formulario que el pagador debe llenar (catálogo `banks[]`,
        `requires_otp_request`); el cargo corre por `POST /pay/{token}/collect`. Re-POST del
        mismo país+moneda+método devuelve la MISMA materialización; elegir un método nunca
        bloquea los demás mientras nadie pague. Sin credenciales; rate limit por IP.
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en la `checkout_url`.
      - name: method
        in: path
        required: true
        schema:
          type: string
        description: Método a materializar — un método fiat del catálogo multi-país (`qr`,
          `card`, `fintoc`, `bank_transfer`, `c2p`, `debito_inmediato`; exige `?country=XX`)
          o una opción crypto (`crypto:tron:usdt`, `crypto:eth:usdt`, `crypto:eth:usdc`,
          `crypto:btc:btc`; sin país).
      - name: country
        in: query
        required: false
        schema:
          type: string
        description: País ISO-3166 alpha-2 del método fiat a materializar. Obligatorio para
          métodos fiat, ignorado para crypto.
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Moneda del cargo de la materialización, elegida del catálogo `options[]`
          (o `cards[]`) del quote (ej. `BOB` o `USD`). Obligatoria cuando el país ofrece el
          método en más de una moneda; ignorada cuando hay una sola (la determina el país).
      responses:
        '200':
          description: Instrucciones del método de pago elegido.
          content:
            application/json:
              examples:
                crypto:
                  summary: Crypto — dirección de depósito exclusiva con QR escaneable
                  value:
                    method: crypto:btc:btc
                    payload:
                      chain: btc
                      asset: BTC
                      address: bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh
                      due: '0.00046120'
                      received: '0.00000000'
                      qr_payload: bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh
                      qr_png_base64: iVBORw0KGgo…
                      quote_expires_at: '2026-07-16T18:15:00Z'
                      note: send the exact amount to the address; the payment is confirmed on-chain
                        automatically
                bank_transfer:
                  summary: Transferencia bancaria — referencia de anuncio (cotización congelada)
                  value:
                    method: bank_transfer
                    country: CL
                    currency: CLP
                    local_amount: '47180'
                    payload:
                      reference: CB68JZCT46QE
                      note: include the reference in the transfer description so the payment is
                        detected automatically
                bank_transfer_dedicated_account:
                  summary: Transferencia bancaria con CLABE dedicada exclusiva del link (MX)
                  value:
                    method: bank_transfer
                    country: MX
                    currency: MXN
                    local_amount: '941.00'
                    payload:
                      destination:
                        type: clabe
                        account: '710969000012345678'
                        beneficiary: Acme Store S.A.
                        dedicated: true
                      note: transfer the exact amount to this dedicated account; no reference
                        is needed — the payment is detected automatically
                collect:
                  summary: Método pull (C2P VE) — cotización congelada + formulario del pagador
                  value:
                    method: c2p
                    country: VE
                    currency: VES
                    local_amount: '4266.77'
                    payload:
                      collect: true
                      currency: VES
                      amount: '4266.77'
                      requires_otp_request: false
                      banks:
                      - code: '0102'
                        name: Banco de Venezuela
                      - code: '0134'
                        name: Banesco
                card:
                  summary: Tarjeta — página de pago hosted en la moneda elegida
                  value:
                    method: card
                    country: BO
                    currency: USD
                    local_amount: '50.75'
                    payload:
                      charge_id: 8f6a2c1e-4b7d-4a1e-9c3f-2d5e6f7a8b9c
                      payment_url: https://api.qbank.cl/pay/cards/9f3XkT2m…
                      expires_at: '2026-07-17T18:15:00Z'
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '409':
          description: El link ya se pagó por otro método.
          content:
            application/json:
              example:
                error: already_paid
                message: this payment link was already paid
        '410':
          description: El link venció sin pago.
          content:
            application/json:
              example:
                error: checkout_expired
                message: this payment link expired
        '400':
          description: Método fiat sin el país obligatorio, o tarjeta sin la moneda del cargo.
          content:
            application/json:
              examples:
                country_required:
                  summary: Método fiat sin país
                  value:
                    error: country_required
                    message: 'send ?country=XX to choose the payment corridor'
                currency_required:
                  summary: Método ofrecido en varias monedas sin elegir una
                  value:
                    error: currency_required
                    message: 'this country accepts this method in more than one currency; send ?currency=XXX'
        '422':
          description: El método no está disponible para este link o país.
          content:
            application/json:
              examples:
                method_unavailable:
                  summary: Método no ofrecido
                  value:
                    error: method_unavailable
                    message: this payment method is not available for this link
                country_unavailable:
                  summary: País sin métodos vivos
                  value:
                    error: country_unavailable
                    message: no payment methods are available for that country
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
        '503':
          description: Cotización temporalmente no disponible.
          content:
            application/json:
              example:
                error: pricing_unavailable
                message: pricing is temporarily unavailable; retry in a moment
  /pay/{token}/collect/otp:
    post:
      security: []
      tags:
      - Checkout
      operationId: checkoutCollectOTP
      summary: Solicitar la clave OTP de un cobro pull (público)
      description: >-
        Para métodos pull cuyo rail envía al pagador una clave de un solo uso a
        demanda (`requires_otp_request: true` en la materialización, ej. débito
        inmediato de Venezuela). Dispara el envío del OTP al pagador y devuelve el
        `otp_reference` que debe acompañar el `POST /pay/{token}/collect` final. La
        opción pull debe materializarse primero; el monto SIEMPRE es el congelado ahí.
        Rate limit estricto por IP (cada llamada es un SMS/push real al pagador).
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en el `checkout_url`.
      requestBody:
        required: true
        content:
          application/json:
            example:
              country: VE
              currency: VES
              method: debito_inmediato
              payer_document: V12345678
              payer_bank: '0102'
              payer_account: '01020123456789012345'
      responses:
        '200':
          description: OTP enviado al pagador.
          content:
            application/json:
              example:
                otp_reference: '783412'
                status: sent
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '409':
          description: El link ya fue pagado por otro método.
          content:
            application/json:
              example:
                error: already_paid
                message: this payment link was already paid
        '410':
          description: El link expiró sin pago.
          content:
            application/json:
              example:
                error: checkout_expired
                message: this payment link expired
        '422':
          description: La opción pull no está materializada, o el rail rechazó la solicitud de OTP.
          content:
            application/json:
              examples:
                not_materialized:
                  summary: Opción pull aún no preparada
                  value:
                    error: method_unavailable
                    message: prepare this payment option first (POST /methods/{method})
                rail_rejected:
                  summary: El rail rechazó la solicitud de OTP
                  value:
                    error: collect_otp_failed
                    message: the destination account does not accept immediate debit
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /pay/{token}/collect:
    post:
      security: []
      tags:
      - Checkout
      operationId: checkoutCollect
      summary: Ejecutar un cobro pull con los datos del pagador (público)
      description: >-
        Ejecuta el cargo pull de una opción collect materializada (`c2p` /
        `debito_inmediato`) con los datos que el pagador escribió en la página del
        checkout — banco, documento, teléfono o cuenta, y el OTP (generado en la app
        bancaria del pagador para C2P, o solicitado vía `POST /pay/{token}/collect/otp`
        para débito inmediato, devolviendo su `otp_reference`). El monto SIEMPRE es el
        congelado en la materialización — el pagador nunca lo envía. Si el rail
        confirma síncrono, el link se liquida en la misma llamada (`paid: true`); un
        rechazo NO mata el link — el pagador puede corregir los datos o elegir otro
        método. Reintentar con los mismos datos jamás cobra dos veces. Rate limit
        estricto por IP.
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del checkout embebido en el `checkout_url`.
      requestBody:
        required: true
        content:
          application/json:
            examples:
              c2p:
                summary: C2P — OTP generado por el pagador en su app bancaria
                value:
                  country: VE
                  currency: VES
                  method: c2p
                  payer_document: V12345678
                  payer_phone: '04141234567'
                  payer_bank: '0102'
                  otp: '123456'
              immediate_debit:
                summary: Débito inmediato — OTP solicitado vía /collect/otp
                value:
                  country: VE
                  currency: VES
                  method: debito_inmediato
                  payer_document: V12345678
                  payer_bank: '0102'
                  payer_account: '01020123456789012345'
                  otp: '654321'
                  otp_reference: '783412'
      responses:
        '200':
          description: Resultado del intento de cobro.
          content:
            application/json:
              examples:
                paid:
                  summary: El rail confirmó síncrono — link liquidado
                  value:
                    paid: true
                    status: paid
                    status_message: approved
                rejected_soft:
                  summary: El rail reportó un rechazo recuperable en el body
                  value:
                    paid: false
                    status: failed
                    status_code: '05'
                    status_message: the document does not match the C2P registration
        '404':
          description: Token inválido o link de cobro desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: payment link not found
        '409':
          description: El link ya fue pagado por otro método.
          content:
            application/json:
              example:
                error: already_paid
                message: this payment link was already paid
        '410':
          description: El link expiró sin pago.
          content:
            application/json:
              example:
                error: checkout_expired
                message: this payment link expired
        '422':
          description: La opción pull no está materializada, o el rail rechazó el cargo
            (el link queda pendiente para que el pagador reintente).
          content:
            application/json:
              examples:
                not_materialized:
                  summary: Opción pull aún no preparada
                  value:
                    error: method_unavailable
                    message: prepare this payment option first (POST /methods/{method})
                rail_rejected:
                  summary: El rail rechazó el cargo
                  value:
                    error: collect_rejected
                    message: invalid or expired OTP
        '429':
          description: Demasiadas peticiones desde esta IP.
          content:
            application/json:
              example:
                error: too_many_attempts
                message: too many requests; wait a moment and retry
  /v1/branding:
    get:
      tags:
      - Cuenta
      operationId: getBranding
      summary: Obtener el branding de la plataforma
      description: Branding efectivo de la plataforma donde operas (nombre, colores, logos en base64)
        para que un front white-label se auto-tematice desde la API en vez de hardcodear assets. `logo_url`/`symbol_url`
        son URLs públicas de CDN de los mismos logos — prefiérelas por sobre los payloads base64. Solo
        lectura, sin PII. `timezone` es la zona horaria IANA de la organización (default `America/New_York`) — cada filtro de fecha `from`/`to` y cada bucket diario de la plataforma se interpreta en esa zona.
      responses:
        '200':
          description: Branding efectivo.
          content:
            application/json:
              example:
                brand_name: CBPay
                website: www.cbpayapp.com
                footer_address: 'Miami: 25 SW 9th STREET, Suite 406, Miami, FL 33130'
                support_email: ''
                colors:
                  primary: '#FBC140'
                  header: '#2E2E2E'
                  accent: '#A16207'
                logo_png_base64: iVBORw0KGgoAAAANSUhEUg...
                symbol_png_base64: iVBORw0KGgoAAAANSUhEUg...
                logo_url: https://cdn.cbpayapp.com/public/branding/default/logo.png
                symbol_url: https://cdn.cbpayapp.com/public/branding/default/symbol.png
                timezone: America/New_York
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/auth/register:
    post:
      tags:
      - Autenticación
      security: []
      operationId: register
      summary: Registrar una cuenta
      description: 'Crea una cuenta de persona o empresa con su miembro de acceso owner y

        devuelve un token de sesión. Un solo endpoint para ambos tipos: `type` es un dato.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - type
              - email
              - password
              - display_name
              properties:
                org:
                  type: string
                  description: Slug de la organización CBPay (`cbpay`).
                type:
                  type: string
                  enum:
                  - person
                  - company
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 8
                display_name:
                  type: string
                tax_id:
                  type: string
                phone:
                  type: string
                country:
                  type: string
                  description: Código de país ISO 3166-1 alpha-2.
                locale:
                  type: string
                  enum:
                  - en
                  - es
                  - zh
                  description: Locale preferido al registrarse. Si se omite, sigue Accept-Language, luego el default de la org, luego ingles.
                profile:
                  type: object
                  description: Datos de perfil de formato libre.
            example:
              org: cbpay
              type: person
              email: ana@example.com
              password: a-secure-password
              display_name: Ana Perez
              country: CL
              locale: en
      responses:
        '201':
          description: Cuenta creada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    $ref: '#/components/schemas/Account'
                  access_token:
                    type: string
                    description: JWT de sesión, válido por 24 horas.
                  expires_at:
                    type: string
                    format: date-time
                  refresh_token:
                    type: string
                    description: Refresh token rotatorio de un solo uso para `POST /v1/auth/refresh`.
                  refresh_expires_at:
                    type: string
                    format: date-time
              example:
                account: &id001
                  id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  org_id: 1c9e7a2b-5f6d-4e3a-8c1b-2a9d8e7f6a5b
                  type: person
                  status: active
                  kyc_status: approved
                  email: ana@example.com
                  display_name: Ana Perez
                  tax_id: 12345678-9
                  phone: '+56912345678'
                  country: CL
                  locale: en
                  profile: &id002 {}
                  created_at: '2026-07-07T12:00:00Z'
                  updated_at: '2026-07-07T12:00:00Z'
                access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                expires_at: '2026-07-08T12:00:00Z'
                refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                refresh_expires_at: '2026-08-06T12:00:00Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          description: Servicio suspendido (`org_suspended`); contacta al equipo de CBPay.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: org_suspended
                message: organization is not active
        '409':
          description: Ya existe una cuenta con ese email (`duplicate`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: duplicate
                message: resource already exists
  /v1/auth/environment-handoff:
    post:
      tags:
      - Autenticación
      operationId: createEnvironmentHandoff
      summary: Obtener un token de handoff a modo test
      description: 'Emite un token de un solo uso (TTL 60 segundos) que traslada la sesión ACTUAL del
        member al ambiente de test — es lo que alimenta el switch test/live de un click del dashboard,
        sin segundo login ni nuevo desafío OTP. Solo el ambiente live emite handoffs, y solo las sesiones
        de member (JWT) pueden pedir uno: las API keys jamás cambian de ambiente (cada ambiente administra
        sus propias keys).'
      responses:
        '201':
          description: Token de handoff emitido. Cánjéalo de inmediato en el `POST /v1/auth/handoff` del
            ambiente de test.
          content:
            application/json:
              schema:
                type: object
                properties:
                  handoff_token:
                    type: string
                    description: Token firmado de un solo uso (expira en 60 segundos).
                  expires_at:
                    type: string
                    format: date-time
                  exchange_url:
                    type: string
                    description: URL del ambiente de test donde se canjea el token.
              example:
                handoff_token: eyJqdGkiOiI2ZjFjMDJhYS0uLi4ifQ.9zXk1v…
                expires_at: '2026-07-13T12:01:00Z'
                exchange_url: https://cryptobank.qbank.cl/platform/v1/auth/handoff
        '403':
          description: El caller no es una sesión de member (`session_required`) — las API keys no cambian
            de ambiente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: session_required
                message: environment handoff requires a member session
  /v1/auth/handoff:
    post:
      tags:
      - Autenticación
      security: []
      operationId: exchangeEnvironmentHandoff
      summary: Canjear un token de handoff por una sesión de test
      description: Solo en el ambiente de test (live siempre responde 404). Canjea el token de un solo
        uso emitido por `POST /v1/auth/environment-handoff` en live por una sesión propia del ambiente
        de test. Si la cuenta aún no existe en test, su espejo se provisiona automáticamente — el modo
        test siempre existe para todo usuario.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - token
              properties:
                token:
                  type: string
                  description: El `handoff_token` emitido por el ambiente live.
            example:
              token: eyJqdGkiOiI2ZjFjMDJhYS0uLi4ifQ.9zXk1v…
      responses:
        '200':
          description: Sesión de test emitida.
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  refresh_token:
                    type: string
                  refresh_expires_at:
                    type: string
                    format: date-time
                  account:
                    type: object
                    description: La cuenta espejo en el ambiente de test.
                  livemode:
                    type: boolean
                    description: Siempre false en el ambiente de test.
              example:
                access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                expires_at: '2026-07-14T12:00:00Z'
                refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                refresh_expires_at: '2026-08-12T12:00:00Z'
                account:
                  id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  type: person
                  display_name: Ana Test
                livemode: false
        '401':
          description: El token es inválido, expiró o ya fue usado (`invalid_handoff`, `handoff_replayed`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_handoff
                message: handoff token is invalid or expired
  /v1/auth/login:
    post:
      tags:
      - Autenticación
      security: []
      operationId: login
      summary: Iniciar sesión
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - email
              - password
              properties:
                org:
                  type: string
                  description: Slug de la organización CBPay (`cbpay`).
                email:
                  type: string
                  format: email
                password:
                  type: string
            example:
              org: cbpay
              email: ana@example.com
              password: a-secure-password
      responses:
        '200':
          description: Sesión emitida — o, cuando la política de la cuenta exige OTP en el login, una
            respuesta intermedia con `otp_required` true y un `pending_token` para canjear en `POST /v1/auth/login/otp`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  refresh_token:
                    type: string
                    description: Refresh token rotatorio de un solo uso (`rt_…`) para renovar la sesión
                      en `POST /v1/auth/refresh` sin re-login.
                  refresh_expires_at:
                    type: string
                    format: date-time
                  account_id:
                    type: string
                    format: uuid
                  role:
                    type: string
                    enum:
                    - owner
                    - operator
                    - viewer
                  otp_required:
                    type: boolean
                    description: Presente (true) cuando el login necesita un segundo paso.
                  pending_token:
                    type: string
                    description: Token intermedio para `POST /v1/auth/login/otp`. No autentica requests
                      de la API.
                  challenge_id:
                    type: string
                    format: uuid
                  channel:
                    type: string
                    enum:
                    - sms
                    - whatsapp
                    - email
                    - totp
                    description: Canal efectivo del desafío de login. Con el teléfono en cooldown de enlace
                      el desafío cae automáticamente a un factor más fuerte (app autenticadora, luego email
                      de login) — el código jamás viaja a un número recién enlazado sin verificar.
                  phone:
                    type: string
                    description: Teléfono enmascarado al que viajó el código.
              examples:
                session:
                  summary: Sesión emitida (sin política OTP)
                  value:
                    access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                    expires_at: '2026-07-08T12:00:00Z'
                    refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                    refresh_expires_at: '2026-08-06T12:00:00Z'
                    account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                    role: owner
                otpRequired:
                  summary: Login en dos pasos (política OTP activa)
                  value:
                    otp_required: true
                    pending_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                    challenge_id: 6f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
                    channel: sms
                    phone: '********5678'
                    expires_at: '2026-07-08T21:10:00Z'
                    note: verify the code with POST /v1/auth/login/otp to receive the session token
        '403':
          description: La política de login resuelve a SMS/WhatsApp pero el teléfono se enlazó hace menos
            de 24 horas sin verificar y la cuenta no tiene factor alternativo (`phone_binding_cooldown`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: phone_binding_cooldown
                message: the phone number was linked recently without verification; SMS/WhatsApp login
                  codes unlock 24 hours after the change and no alternative factor (authenticator app or
                  login email) is available
        '401':
          description: Email o contraseña incorrectos (`invalid_credentials`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_credentials
                message: email or password is incorrect
  /v1/auth/login/otp:
    post:
      tags:
      - Autenticación
      security: []
      operationId: loginOTP
      summary: Completar el login en dos pasos
      description: Canjea el `pending_token` de `POST /v1/auth/login` más el código OTP recibido por SMS/WhatsApp
        por el token de sesión real. El token intermedio expira a los 10 minutos y jamás puede llamar
        a la API por sí solo.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - pending_token
              - code
              properties:
                pending_token:
                  type: string
                code:
                  type: string
                  minLength: 4
                  maxLength: 10
            example:
              pending_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
              code: '482913'
      responses:
        '200':
          description: Sesión emitida.
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  refresh_token:
                    type: string
                  refresh_expires_at:
                    type: string
                    format: date-time
                  account_id:
                    type: string
                    format: uuid
                  role:
                    type: string
              example:
                access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                expires_at: '2026-07-09T21:00:00Z'
                refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                refresh_expires_at: '2026-08-07T21:00:00Z'
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                role: owner
        '401':
          description: El código no coincide (`invalid_code`) o el token intermedio expiró (`invalid_pending_token`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_code
                message: the code is incorrect; check it and try again
        '409':
          description: El desafío expiró o ya se usó (`challenge_not_pending`); vuelve a iniciar sesión.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: challenge_not_pending
                message: the challenge already expired or was used; log in again
  /v1/auth/refresh:
    post:
      tags:
      - Autenticación
      security: []
      operationId: refreshSession
      summary: Renovar la sesión
      description: 'Canjea un refresh token de un solo uso (`rt_…`, emitido con cada login) por un par
        nuevo de access token + refresh token sin volver a pedir credenciales. Rotación estricta: al canjear
        se revoca el access token anterior de ese dispositivo, y presentar un refresh token ya canjeado
        revoca la cadena completa del dispositivo (respuesta ante robo) y registra un evento de seguridad
        `refresh_token_reuse`. Los refresh tokens duran 30 días por rotación con un tope absoluto de 90
        días desde el login original; cerrar sesión, revocar sesiones o cambiar la contraseña también
        los invalida.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - refresh_token
              properties:
                refresh_token:
                  type: string
                  description: El token `rt_…` del último login o refresh.
            example:
              refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
      responses:
        '200':
          description: Par nuevo emitido; el par anterior queda inválido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
                  refresh_token:
                    type: string
                    description: Reemplaza al token canjeado (un solo uso).
                  refresh_expires_at:
                    type: string
                    format: date-time
                  account_id:
                    type: string
                    format: uuid
                  role:
                    type: string
              example:
                access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
                expires_at: '2026-07-15T12:00:00Z'
                refresh_token: rt_9f2b1c30-51ab-4dd0-93e2-7a6b5c4d3e2f.YYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
                refresh_expires_at: '2026-08-13T12:00:00Z'
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                role: owner
        '401':
          description: El refresh token es inválido, expiró, ya se usó o fue revocado (`invalid_refresh_token`).
            Manda al usuario a la pantalla de login.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_refresh_token
                message: the refresh token is invalid or expired; log in again
        '429':
          description: Demasiados intentos de refresh desde esta dirección (`too_many_attempts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: too_many_attempts
                message: too many refresh attempts from this address; retry later
  /v1/auth/oauth:
    post:
      tags:
      - Login social
      security: []
      operationId: oauthLogin
      summary: Iniciar sesión o registrarse con un proveedor social
      description: Token exchange. El front obtiene la credencial del proveedor (Google/Apple/Microsoft
        `id_token`, Facebook `access_token`) y la envía aquí. La API la verifica contra el proveedor y
        devuelve la sesión CBPay — creando la cuenta en el primer uso. Respeta la política de OTP en login
        de la cuenta (puede devolver `otp_required`).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - provider
              - credential
              properties:
                org:
                  type: string
                  description: Slug de la organización CBPay (`cbpay`).
                provider:
                  type: string
                  enum:
                  - google
                  - apple
                  - microsoft
                  - facebook
                credential:
                  type: string
                  description: Credencial del proveedor obtenida por el SDK del front.
                type:
                  type: string
                  enum:
                  - person
                  - company
                  description: Tipo de cuenta al crear una nueva (default person; se ignora si ya existe).
            example:
              org: cbpay
              provider: google
              credential: eyJhbGciOiJSUzI1Ni…
              type: person
      responses:
        '200':
          description: Usuario existente inició sesión (o `otp_required` si la cuenta exige OTP en login).
          content:
            application/json:
              example:
                access_token: eyJhbGciOiJIUzI1Ni…
                expires_at: '2026-07-09T21:00:00Z'
                refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                refresh_expires_at: '2026-08-07T21:00:00Z'
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                role: owner
        '201':
          description: Cuenta nueva creada y sesión iniciada.
          content:
            application/json:
              example:
                account:
                  id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  type: person
                  email: ana@gmail.com
                  display_name: Ana
                access_token: eyJhbGciOiJIUzI1Ni…
                expires_at: '2026-07-09T21:00:00Z'
                refresh_token: rt_6a1e6c22-93a1-4f0e-a7d1-1f2e3c4b5a69.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
                refresh_expires_at: '2026-08-07T21:00:00Z'
                created: true
        '401':
          description: La credencial es inválida, expiró o es de otra app (`invalid_credential`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_credential
                message: the login credential is invalid or expired
        '400':
          description: Org desconocida, proveedor inválido o no habilitado para la org.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: provider_not_configured
                message: this login provider is not enabled for this organization
        '409':
          description: Ya existe una cuenta con ese email (`email_conflict`); inicia sesión y vincula
            desde tu sesión.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: email_conflict
                message: an account with this email already exists; sign in with your current method and
                  link this provider from your session
  /v1/auth/oauth/providers:
    get:
      tags:
      - Login social
      security: []
      operationId: oauthProviders
      summary: Listar proveedores sociales habilitados
      description: Público. Devuelve los proveedores sociales habilitados de la org y su `client_id`,
        para que el front pinte los botones correctos.
      parameters:
      - name: org
        in: query
        required: true
        schema:
          type: string
        description: Slug de la organización CBPay.
      responses:
        '200':
          description: Proveedores habilitados.
          content:
            application/json:
              example:
                providers:
                - provider: google
                  client_id: 1234567890-abc.apps.googleusercontent.com
                - provider: apple
                  client_id: com.yourcompany.cbpay.web
  /v1/me/identities:
    get:
      tags:
      - Login social
      operationId: listIdentities
      summary: Listar mis proveedores vinculados
      responses:
        '200':
          description: Identidades vinculadas del member que llama.
          content:
            application/json:
              example:
                identities:
                - account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  provider: google
                  email: ana@gmail.com
                  display_name: Ana
                  created_at: '2026-07-08T21:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
    post:
      tags:
      - Login social
      operationId: linkIdentity
      summary: Vincular un proveedor a mi cuenta
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - provider
              - credential
              properties:
                provider:
                  type: string
                  enum:
                  - google
                  - apple
                  - microsoft
                  - facebook
                credential:
                  type: string
            example:
              provider: apple
              credential: eyJhbGciOiJSUzI1Ni…
      responses:
        '201':
          description: Proveedor vinculado.
          content:
            application/json:
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                provider: apple
                email: ana@privaterelay.appleid.com
                created_at: '2026-07-08T21:05:00Z'
        '409':
          description: Esa identidad ya está vinculada a otra cuenta (`identity_taken`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: identity_taken
                message: this provider identity is already linked to another account
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/me/identities/{provider}:
    delete:
      tags:
      - Login social
      operationId: unlinkIdentity
      summary: Desvincular un proveedor
      description: Bloqueado con `409 last_login_method` si es el único método de acceso (sin contraseña
        y sin otro proveedor vinculado).
      parameters:
      - name: provider
        in: path
        required: true
        schema:
          type: string
          enum:
          - google
          - apple
          - microsoft
          - facebook
      responses:
        '200':
          description: Proveedor desvinculado.
          content:
            application/json:
              example:
                provider: apple
                unlinked: true
        '409':
          description: No se puede desvincular el único método de acceso (`last_login_method`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: last_login_method
                message: cannot unlink the only sign-in method; set a password or link another provider
                  first
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/me:
    get:
      tags:
      - Cuenta
      operationId: getMe
      summary: Obtener mi cuenta
      responses:
        '200':
          description: La cuenta que llama.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
              example: *id001
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags:
      - Cuenta
      operationId: updateMe
      summary: Actualizar mi perfil
      description: Con la verificación de identidad aprobada, `display_name`,
        `tax_id` y `country` se rellenan desde la identidad verificada y quedan
        bloqueados (`409 identity_locked`); `phone` sigue editable con su
        propio flujo de verificación.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                tax_id:
                  type: string
                phone:
                  type: string
                country:
                  type: string
                locale:
                  type: string
                  description: Locale preferido (`en`, `es` o `zh`). Un string vacio guarda `en`. Valores invalidos devuelven 400 `invalid_locale`.
            example:
              phone: '+56987654321'
              locale: en
      responses:
        '200':
          description: Cuenta actualizada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
              example:
                id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                org_id: 1c9e7a2b-5f6d-4e3a-8c1b-2a9d8e7f6a5b
                type: person
                status: active
                kyc_status: approved
                email: ana@example.com
                display_name: Ana Perez
                tax_id: 12345678-9
                phone: '+56987654321'
                country: CL
                locale: en
                profile: *id002
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '400':
          description: Locale invalido (`invalid_locale`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_locale
                message: locale must be en, es or zh
        '409':
          description: Los campos de identidad quedan bloqueados tras aprobar la verificación.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: identity_locked
                message: display_name, tax_id and country come from your approved identity verification and cannot be changed here; contact support to update your verified identity
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/members:
    get:
      tags:
      - Cuenta
      operationId: listMembers
      summary: Listar miembros
      description: Miembros de acceso de la cuenta que llama (cuentas de empresa).
      responses:
        '200':
          description: Miembros.
          content:
            application/json:
              schema:
                type: object
                properties:
                  members:
                    type: array
                    items:
                      $ref: '#/components/schemas/Member'
              example:
                members:
                - &id003
                  id: 5a4b3c2d-1e0f-9a8b-7c6d-5e4f3a2b1c0d
                  email: ops@example.com
                  role: operator
                  status: active
                  created_at: '2026-07-07T12:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - Cuenta
      operationId: addMember
      summary: Agregar un miembro
      description: Agrega un miembro de acceso. Disponible solo para cuentas de empresa.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 8
                role:
                  type: string
                  enum:
                  - owner
                  - operator
                  - viewer
                  default: operator
            example:
              email: ops@example.com
              password: another-secure-pass
              role: operator
      responses:
        '201':
          description: Miembro creado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Member'
              example: *id003
        '403':
          description: No es una cuenta de empresa (`company_only`) o falta la credencial de cuenta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: company_only
                message: members are only available for company accounts
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/api-keys:
    post:
      tags:
      - Cuenta
      operationId: createApiKey
      summary: Crear una llave de API
      description: 'Emite una llave de API server-to-server ligada a la cuenta que llama. El

        token en texto plano (`pk_...`) se devuelve exactamente una vez y no puede

        recuperarse de nuevo.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
            example:
              label: production-backend
      responses:
        '201':
          description: Llave emitida.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyIssued'
              example:
                api_key_id: 7f6e5d4c-3b2a-1908-a7b6-c5d4e3f2a1b0
                key_id: a1b2c3d4e5f60718
                token: pk_a1b2c3d4e5f60718.k9J2mX4pQ7wR5tY8uZ0aB3cD6eF1gH2i
                label: production-backend
                note: store this token now; it cannot be retrieved again
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/balances:
    get:
      tags:
      - Saldos
      operationId: getBalances
      summary: Obtener saldos
      description: Los cuatro saldos virtuales independientes de la cuenta que llama (USDT — la moneda
        operativa —, USDC, BTC y GOLD en gramos de oro fino). Todos los activos soportados vienen siempre,
        con ceros si la cuenta no ha usado esa moneda. Si la cuenta usa Banking, sus saldos espejo BANK_USD/BANK_EUR
        también se listan con el marcador `custody` en `banking` (el saldo autoritativo vive en el banco;
        estos saldos no son gastables dentro de la plataforma).
      responses:
        '200':
          description: Saldos de la cuenta que llama (uno por activo soportado).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalancesResponse'
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                balances:
                - asset: USDT
                  available: '125.430000'
                  held: '10.000000'
                  updated_at: '2026-07-07T12:00:00Z'
                - asset: USDC
                  available: '50.000000'
                  held: '0.000000'
                  updated_at: '2026-07-08T09:00:00Z'
                - asset: BTC
                  available: '0.00060000'
                  held: '0.00000000'
                - asset: GOLD
                  available: '12.500000'
                  held: '0.000000'
                - asset: BANK_USD
                  available: '7500.00'
                  held: '0.00'
                  custody: banking
                  updated_at: '2026-07-08T09:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/balances/history:
    get:
      tags:
      - Saldos
      operationId: getBalancesHistory
      summary: Obtener historial de saldos
      description: 'Evolución diaria de los saldos de la cuenta para gráficos de dashboard: una serie
        por asset (el saldo disponible de cierre de cada día, arrastrado en días sin movimientos) más
        una serie agregada en USD que valoriza BTC/GOLD al precio de referencia histórico de cada día.
        Incluye las entradas/salidas totales del período y el snapshot actual (available + held), para
        que el front arme una tarjeta de saldo estilo dashboard con su variación porcentual. La serie
        refleja el saldo **disponible** (los holds no tienen historia); `current` trae el `held` de hoy
        por asset. Los días valorizados con el precio spot de hoy (antes de que exista precio histórico)
        se declaran en `spot_priced_dates` — los valores jamás se inventan.


        El mapa `assets` incluye también los espejos de las cuentas banking (`BANK_USD`, `BANK_EUR`) como
        serie propia en su propia moneda (2 decimales). **No** forman parte del agregado `total_usd`,
        que cubre solo los saldos operativos (USDT, USDC, BTC, GOLD).

        '
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive). Rango máximo 366 días.
      responses:
        '200':
          description: Serie diaria de saldo por asset más el agregado USD.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_id:
                    type: string
                  from:
                    type: string
                  to:
                    type: string
                  granularity:
                    type: string
                    example: day
                  timezone:
                    type: string
                    example: America/New_York
                  assets:
                    type: object
                    description: 'Una serie de saldo de cierre diario por asset. Incluye los saldos operativos
                      (USDT, USDC, BTC, GOLD) y los espejos de las cuentas banking (BANK_USD, BANK_EUR)
                      en su propia moneda; las series banking quedan fuera de `total_usd`.

                      '
                    additionalProperties:
                      type: object
                      properties:
                        series:
                          type: array
                          items:
                            type: object
                            properties:
                              date:
                                type: string
                                example: '2026-07-10'
                              balance:
                                type: string
                                example: '125.430000'
                        first:
                          type: string
                        last:
                          type: string
                        change_pct:
                          type: string
                          nullable: true
                          description: Variación porcentual con signo primero→último (null cuando la serie
                            parte en cero).
                  total_usd:
                    type: object
                    description: Valor agregado en USD de todos los saldos por día.
                    properties:
                      series:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                            balance_usd:
                              type: string
                      first:
                        type: string
                      last:
                        type: string
                      change_pct:
                        type: string
                        nullable: true
                      spot_priced_dates:
                        type: array
                        items:
                          type: string
                        description: Días valorizados con el precio spot de hoy porque no existía precio
                          histórico.
                      unpriced_assets:
                        type: array
                        items:
                          type: string
                        description: Assets excluidos del agregado USD por falta de precio.
                  period:
                    type: object
                    description: Entradas/salidas totales del período en USD.
                    properties:
                      in_usd:
                        type: string
                      out_usd:
                        type: string
                      net_usd:
                        type: string
                  current:
                    type: object
                    description: Snapshot actual (available + held, valorizado a spot).
                    properties:
                      items:
                        type: array
                        items:
                          type: object
                          properties:
                            asset:
                              type: string
                            available:
                              type: string
                            held:
                              type: string
                            usd_estimate:
                              type: string
                              nullable: true
                      net_worth_usd_estimate:
                        type: string
                        nullable: true
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                from: '2026-06-12'
                to: '2026-07-11'
                granularity: day
                timezone: America/New_York
                assets:
                  USDT:
                    series:
                    - date: '2026-06-12'
                      balance: '100.000000'
                    - date: '2026-06-13'
                      balance: '100.000000'
                    - date: '2026-07-11'
                      balance: '125.430000'
                    first: '100.000000'
                    last: '125.430000'
                    change_pct: '25.43'
                  BTC:
                    series:
                    - date: '2026-06-12'
                      balance: '0.00060000'
                    - date: '2026-07-11'
                      balance: '0.00060000'
                    first: '0.00060000'
                    last: '0.00060000'
                    change_pct: '0.00'
                  BANK_USD:
                    series:
                    - date: '2026-06-12'
                      balance: '1500.00'
                    - date: '2026-07-11'
                      balance: '1725.50'
                    first: '1500.00'
                    last: '1725.50'
                    change_pct: '15.03'
                total_usd:
                  series:
                  - date: '2026-06-12'
                    balance_usd: '163.73'
                  - date: '2026-07-11'
                    balance_usd: '191.34'
                  first: '163.73'
                  last: '191.34'
                  change_pct: '16.86'
                  spot_priced_dates: []
                  unpriced_assets: []
                period:
                  in_usd: '280.20'
                  out_usd: '254.77'
                  net_usd: '25.43'
                current:
                  items:
                  - asset: USDT
                    available: '125.430000'
                    held: '10.000000'
                    usd_estimate: '135.43'
                  - asset: USDC
                    available: '0.000000'
                    held: '0.000000'
                    usd_estimate: '0.00'
                  - asset: BTC
                    available: '0.00060000'
                    held: '0.00000000'
                    usd_estimate: '65.91'
                  - asset: GOLD
                    available: '0.000000'
                    held: '0.000000'
                    usd_estimate: '0.00'
                  net_worth_usd_estimate: '201.34'
        '400':
          description: Rango de fechas faltante o inválido (`invalid_range`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_range
                message: from and to are required (YYYY-MM-DD)
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/movements:
    get:
      tags:
      - Saldos
      operationId: listMovements
      summary: Listar movimientos
      description: Historial inmutable del ledger de la cuenta que llama.
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: asset
        in: query
        description: Filtra por moneda del saldo.
        schema:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          example: USDT
      - name: type
        in: query
        description: Filtra por tipo de asiento del ledger.
        schema:
          type: string
          enum:
          - payout_debit
          - payout_refund
          - payin_credit
          - adjustment
          - transfer_in
          - transfer_out
          - funding
          - withdrawal_debit
          - withdrawal_refund
          - compliance_fee
          - compliance_refund
          - wallet_creation_fee
          - wallet_creation_refund
          - card_debit
          - card_refund
          - card_fee
          - card_fee_refund
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Filtra desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Filtra hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: format
        in: query
        description: 'Exporta las mismas filas como archivo: `csv` o `xlsx` (descarga contable, hasta
          10.000 filas; mismos filtros y rango). Omite para la vista JSON.'
        schema:
          type: string
          enum:
          - csv
          - xlsx
      responses:
        '200':
          description: Movimientos paginados.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovementsResponse'
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                page: 1
                page_size: 50
                movements:
                - id: 0f1e2d3c-4b5a-6978-8796-a5b4c3d2e1f0
                  asset: USDT
                  amount: '-86.014286'
                  type: payout_debit
                  reference_type: payout
                  reference_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
                  description: ''
                  balance_after: '39.415714'
                  created_at: '2026-07-07T12:00:00Z'
                - id: 9a8b7c6d-5e4f-3a2b-1c0d-e9f8a7b6c5d4
                  asset: USDT
                  amount: '-1.000000'
                  type: wallet_creation_fee
                  reference_type: crypto_wallet
                  reference_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d:tron:usdt
                  description: 'service fee: wallet_creation'
                  balance_after: '125.430000'
                  created_at: '2026-07-07T12:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/rates:
    get:
      tags:
      - Saldos
      operationId: getRates
      summary: Obtener mis tasas de cambio y comisiones
      description: Devuelve las tasas de cambio que aplican a la cuenta que llama para cada país — las
        mismas tasas usadas cuando las operaciones se ejecutan (`local_amount / rate = USDT`) — más la
        configuración de comisiones **efectiva** de la cuenta — los defaults de la organización ya resueltos
        contra los overrides de tu cuenta — para calcular el costo exacto de una operación antes de crearla.
        `rate` es la punta de payouts (dispersiones) y `payin_rate` la de payins (depósitos). `asset_prices`
        trae el precio USD de referencia de cada saldo virtual (BTC por unidad, GOLD por gramo; los stablecoins
        valen 1 por convención) para valorizar. El bloque `settlement` muestra, por asset habilitado,
        el **precio efectivo de settlement** que tu cuenta obtendría ahora mismo si pagara una operación
        desde ese saldo (`settlement_rate`, spread incluido) y si el asset está `available` — úsalo para
        estimar antes de enviar `settlement_asset` en un payout.
      responses:
        '200':
          description: Tasas, precios de referencia por activo y comisiones.
          content:
            application/json:
              schema:
                type: object
                properties:
                  base:
                    type: string
                    example: USD
                  rates:
                    type: object
                    additionalProperties:
                      type: object
                      properties:
                        currency:
                          type: string
                          example: CLP
                        rate:
                          type: string
                          description: Tasa aplicada a los payouts (dispersiones).
                          example: '950.123456'
                        payin_rate:
                          type: string
                          description: Tasa aplicada a los payins (depósitos/cobros fiat).
                          example: '955.10'
                  asset_prices:
                    type: object
                    description: Precio USD de referencia por activo del saldo virtual (display/valorización).
                    additionalProperties:
                      type: object
                      properties:
                        currency:
                          type: string
                          example: USD
                        unit:
                          type: string
                          description: Unidad a la que refiere el precio (`btc`, `gram`, `usdt`, `usdc`).
                          example: gram
                        price:
                          type: string
                          example: '107.5341'
                        updated_at:
                          type: string
                          description: Cuándo se actualizó este precio por última vez.
                        settlement_grade:
                          type: boolean
                          description: True cuando el precio está lo bastante fresco para ejecutar settlements
                            (operaciones pagadas desde ese saldo).
                  settlement:
                    type: object
                    description: Pricing de settlement efectivo para la cuenta que llama.
                    properties:
                      default_asset:
                        type: string
                        description: El saldo desde el que se debitan las operaciones cuando no se envía
                          un override `settlement_asset`.
                        example: USDT
                      assets:
                        type: array
                        items:
                          type: object
                          properties:
                            asset:
                              type: string
                              example: BTC
                            available:
                              type: boolean
                              description: False cuando el asset no puede usarse en este momento (deshabilitado
                                o sin precio de ejecución).
                            settlement_rate:
                              type: string
                              description: Precio USD efectivo por unidad al que se valoraría tu saldo
                                (spread incluido); vacío cuando no está disponible.
                              example: '109029.34'
                  updated_at:
                    type: string
                  fees:
                    type: array
                    items:
                      $ref: '#/components/schemas/FeeConfig'
              example:
                base: USD
                rates:
                  chile:
                    currency: CLP
                    rate: '950.25'
                    payin_rate: '955.10'
                  mexico:
                    currency: MXN
                    rate: '17.50'
                    payin_rate: '17.62'
                  peru:
                    currency: PEN
                    rate: '3.72'
                    payin_rate: '3.75'
                asset_prices:
                  USDT:
                    currency: USD
                    unit: usdt
                    price: '1'
                  USDC:
                    currency: USD
                    unit: usdc
                    price: '1'
                  BTC:
                    currency: USD
                    unit: btc
                    price: '109853.24'
                    updated_at: '2026-07-07T11:59:41Z'
                    settlement_grade: true
                  GOLD:
                    currency: USD
                    unit: gram
                    price: '107.5341'
                    updated_at: '2026-07-07T09:12:05Z'
                    settlement_grade: true
                settlement:
                  default_asset: USDT
                  assets:
                  - asset: USDT
                    available: true
                    settlement_rate: '1'
                  - asset: USDC
                    available: true
                    settlement_rate: '0.99900000'
                  - asset: BTC
                    available: true
                    settlement_rate: '109029.34070000'
                  - asset: GOLD
                    available: true
                    settlement_rate: '106.99642950'
                updated_at: '2026-07-07T12:00:00Z'
                fees:
                - id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                  org_id: 1c9e7a2b-5f6d-4e3a-8c1b-2a9d8e7f6a5b
                  account_id: ''
                  service: payout
                  country: CL
                  asset: USDT
                  percent: '0'
                  fixed_amount: '0.30'
                  status: active
                  updated_at: '2026-07-07T12:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '502':
          description: Tasas de cambio temporalmente no disponibles (`rates_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: rates_unavailable
                message: could not fetch fx rates
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/rates/history:
    get:
      tags:
      - Saldos
      operationId: getRatesHistory
      summary: Obtener historial de tasas de cambio
      description: 'Serie temporal de las tasas de cambio que aplican a la cuenta que llama, para gráficos
        de evolución. Cada punto trae las mismas tasas que obtendrías operando en ese momento — `rate`
        (punta de payouts) y `payin_rate` (punta de depósitos) — y cada país incluye `first`, `last` y
        el `change_pct` con signo entre ambos, listo para el badge "+3.4% / -3.0%". `asset_prices` trae
        la serie de referencia USD de BTC y GOLD. Los buckets sin datos arrastran el valor anterior; los
        días previos al inicio del historial se omiten (jamás se inventan).

        '
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: granularity
        in: query
        schema:
          type: string
          enum:
          - day
          - hour
          default: day
        description: Tamaño del bucket. Rango máximo 366 días para `day` y 31 días para `hour`.
      - name: currency
        in: query
        schema:
          type: string
        description: Filtro opcional por moneda local (ej. CLP).
      responses:
        '200':
          description: Serie de tasas por país más series de precios de assets.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_id:
                    type: string
                  base:
                    type: string
                    example: USD
                  from:
                    type: string
                  to:
                    type: string
                  granularity:
                    type: string
                    example: day
                  rates:
                    type: object
                    description: Una serie por país (mismas llaves que `GET /v1/rates`).
                    additionalProperties:
                      type: object
                      properties:
                        currency:
                          type: string
                          example: CLP
                        series:
                          type: array
                          items:
                            type: object
                            properties:
                              date:
                                type: string
                                example: '2026-07-10'
                              rate:
                                type: string
                                description: Tasa punta payout en ese bucket.
                                example: '950.123456'
                              payin_rate:
                                type: string
                                description: Tasa punta payin en ese bucket.
                                example: '978.912345'
                        first:
                          type: string
                          description: Primera tasa (punta payout) del rango.
                        last:
                          type: string
                          description: Última tasa (punta payout) del rango.
                        change_pct:
                          type: string
                          nullable: true
                          description: Variación porcentual con signo primero→último (píntala verde/roja
                            en el front).
                  asset_prices:
                    type: object
                    description: Series de precio USD de referencia de BTC y GOLD (solo display).
                    additionalProperties:
                      type: object
                      properties:
                        currency:
                          type: string
                          example: USD
                        unit:
                          type: string
                          example: gram
                        series:
                          type: array
                          items:
                            type: object
                            properties:
                              date:
                                type: string
                              price:
                                type: string
                        first:
                          type: string
                        last:
                          type: string
                        change_pct:
                          type: string
                          nullable: true
                  retrieved_at:
                    type: string
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                base: USD
                from: '2026-06-12'
                to: '2026-07-11'
                granularity: day
                rates:
                  chile:
                    currency: CLP
                    series:
                    - date: '2026-06-12'
                      rate: '939.068965'
                      payin_rate: '967.452325'
                    - date: '2026-06-13'
                      rate: '940.886699'
                      payin_rate: '969.325000'
                    - date: '2026-07-11'
                      rate: '910.896551'
                      payin_rate: '938.428400'
                    first: '939.068965'
                    last: '910.896551'
                    change_pct: '-3.00'
                  mexico:
                    currency: MXN
                    series:
                    - date: '2026-06-12'
                      rate: '17.241379'
                      payin_rate: '17.762500'
                    - date: '2026-07-11'
                      rate: '17.832512'
                      payin_rate: '18.371500'
                    first: '17.241379'
                    last: '17.832512'
                    change_pct: '3.43'
                asset_prices:
                  BTC:
                    currency: USD
                    unit: btc
                    series:
                    - date: '2026-06-12'
                      price: '106214.55'
                    - date: '2026-07-11'
                      price: '109853.24'
                    first: '106214.55'
                    last: '109853.24'
                    change_pct: '3.43'
                  GOLD:
                    currency: USD
                    unit: gram
                    series:
                    - date: '2026-06-12'
                      price: '106.8812'
                    - date: '2026-07-11'
                      price: '107.5341'
                    first: '106.8812'
                    last: '107.5341'
                    change_pct: '0.61'
                retrieved_at: '2026-07-11T15:00:00Z'
        '400':
          description: Rango de fechas o granularidad faltante/inválida (`invalid_range`, `invalid_granularity`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_range
                message: from and to are required (YYYY-MM-DD)
        '403':
          $ref: '#/components/responses/AccountRequired'
        '502':
          description: Historial de tasas temporalmente no disponible (`rates_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: rates_unavailable
                message: could not fetch fx rate history
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payouts:
    post:
      tags:
      - Payouts
      operationId: createPayout
      summary: Crear un payout
      description: 'Dispersa fiat a una cuenta bancaria local. El monto local se convierte a USDT a **la
        tasa de tu cuenta** (la misma que devuelve `GET /v1/rates`); se debita `usdt_amount` más la comisión
        fija (cuando está configurada) y queda retenido hasta que la dispersión se complete. Si falla,
        se reembolsa el débito completo.


        **Paga desde cualquier saldo**: por defecto el débito sale del asset de settlement predeterminado
        de tu cuenta (USDT salvo que lo cambies con `PUT /v1/settlement`). Envía `settlement_asset` (USDT,
        USDC, BTC o GOLD) para pagar esta operación puntual desde otro saldo: el total en USDT se convierte
        a ese asset al precio efectivo de settlement del momento (ver el bloque `settlement` de `GET /v1/rates`)
        y el débito, la retención y — si falla — el reembolso viven en ese asset por el monto exacto.
        Si el precio de ejecución de BTC/GOLD no está disponible, el request devuelve `503 pricing_unavailable`;
        los assets volátiles además tienen un límite por operación (`422 settlement_limit_exceeded`).


        Requiere una llave de idempotencia (campo del body o header `Idempotency-Key`). Reintentar con
        la misma llave devuelve el payout original (`200` con `idempotency_hit: true`).'
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - country
              - currency
              - amount
              properties:
                country:
                  type: string
                  description: País de destino ISO 3166-1 alpha-2 (ej. CL, PE, MX).
                currency:
                  type: string
                  description: Moneda local (ej. CLP, PEN, MXN).
                method:
                  type: string
                  description: Método de payout de `GET /v1/payouts/methods` (ej. `bank_transfer`).
                amount:
                  type: string
                  description: Monto en moneda local como string decimal positivo.
                  example: '50000.00'
                beneficiary:
                  type: object
                  additionalProperties:
                    type: string
                  description: 'Datos de la cuenta de destino. Las claves requeridas dependen del

                    país y del método; ver `GET /v1/payouts/methods` y

                    `GET /v1/payouts/banks`. Ejemplos: RUT + banco en Chile, CLABE en

                    México, CCI en Perú. PIX Brasil: `pix_key` + `pix_key_type` (cpf,

                    cnpj, phone, email, evp), o cuenta bancaria sin llave con

                    `bank_code` (ISPB) + `branch_code` + `account_number` + `tax_id`.

                    Ecuador (corredor de remesas): `document_value` (cédula) más los

                    datos del ordenante planos en el mismo objeto (`sender_name`,

                    `sender_document_value`, ...); los nombres estructurados opcionales

                    (`given_name`, `first_surname`, ... y sus pares `sender_*`)

                    mandan sobre la separación automática de `name`.

                    Argentina: `tax_id` (CUIT/CUIL de 11 dígitos) + `account_number`

                    (CBU o CVU de 22 dígitos; USD solo CBU) — sin `bank_code`.

                    Estados Unidos (USD): identidad y dirección postal completas del

                    beneficiario en cada transferencia — `name`, `email`,

                    `account_number`, `country_code`, `address`, `city`,

                    `postal_code`, `bank_name`, `bank_country` y `bank_code`

                    (routing ABA para `ach`/`wire`, BIC SWIFT para `swift`);

                    `account_type` (`CHECKING`/`SAVING`) es requerido para `ach`.

                    El beneficiario puede vivir en CUALQUIER país (`country_code`

                    es libre — ej. un ACH a un banco de EE. UU. para alguien que

                    vive en Alemania); `state` es requerido solo cuando

                    `country_code` es `US`. El banco receptor debe estar en EE.

                    UU. para `ach`/`wire` (`bank_country: "US"` — los rails

                    domésticos no pagan a bancos fuera de EE. UU.) junto con su

                    bloque de dirección completo (`bank_address`, `bank_city`,

                    `bank_postal_code` y `bank_state` para bancos de EE. UU.);

                    para `swift` el bloque de dirección del banco es opcional (el

                    rail lo deriva del BIC) y `bank_country` puede ser cualquier

                    país. Las jurisdicciones del Anexo B (CU/IR/KP/SY) siguen

                    bloqueadas para el país del beneficiario Y del banco (`422

                    prohibited_country`). Toda transferencia USD por rail

                    bancario exige además un documento de respaldo

                    (invoice/recibo) subido antes con `POST

                    /v1/payouts/documents` — ver la guía de payouts.

                    '
                beneficiary_contact_id:
                  type: string
                  format: uuid
                  description: Usa el beneficiario guardado más reciente del contacto para este país (y
                    método, si lo envías) en vez de tipear `beneficiary` de nuevo. Un `beneficiary` explícito
                    siempre gana; 422 no_saved_destination si el contacto no tiene.
                save_contact:
                  type: boolean
                  default: true
                  description: El beneficiario se guarda como contacto automáticamente; envía false para
                    no guardarlo.
                description:
                  type: string
                settlement_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  description: Saldo virtual a debitar para esta operación. Omítelo para usar el asset
                    de settlement predeterminado de tu cuenta (USDT salvo que lo cambies con `PUT /v1/settlement`).
                idempotency_key:
                  type: string
            examples:
              cl_bank_transfer:
                summary: Chile — transferencia bancaria (CLP)
                value:
                  country: CL
                  currency: CLP
                  method: bank_transfer
                  amount: '100000'
                  beneficiary:
                    name: Pedro Soto Fuentes
                    tax_id: 12.345.678-5
                    bank_code: '012'
                    account_type: checking
                    account_number: '123456789'
                  description: Supplier payment
                  idempotency_key: cl-prov-0091
              pe_bank_transfer:
                summary: Perú — transferencia bancaria vía CCI (PEN)
                value:
                  country: PE
                  currency: PEN
                  method: bank_transfer
                  amount: '1000.00'
                  beneficiary:
                    name: Rosa Alvarez Diaz
                    account_number: 00219300123456789012
                  idempotency_key: pe-cci-3310
              pe_yape:
                summary: Perú — Yape a un número de teléfono (PEN)
                value:
                  country: PE
                  currency: PEN
                  method: yape
                  amount: '150.00'
                  beneficiary:
                    name: Luis Ramos Vega
                    phone: '51987654321'
                  idempotency_key: pe-yape-8874
              mx_clabe:
                summary: México — SPEI a una CLABE (MXN)
                value:
                  country: MX
                  currency: MXN
                  method: bank_transfer
                  amount: '1500.00'
                  beneficiary:
                    name: Maria Lopez
                    account_type: clabe
                    account_number: 012180001234567895
                  idempotency_key: mx-clabe-8841
              mx_debit_card:
                summary: México — SPEI a una tarjeta de débito (MXN)
                value:
                  country: MX
                  currency: MXN
                  method: bank_transfer
                  amount: '800.00'
                  beneficiary:
                    name: Jorge Herrera
                    account_type: debit_card
                    account_number: '4152313412341234'
                    bank_code: '40012'
                  idempotency_key: mx-card-1102
              ve_pago_movil:
                summary: Venezuela — Pago Móvil (VES)
                value:
                  country: VE
                  currency: VES
                  method: pago_movil
                  amount: '2000.00'
                  beneficiary:
                    name: Carmen Delgado
                    phone: '04141234567'
                    bank_code: '0102'
                    document_value: V12345678
                  idempotency_key: ve-pm-5567
              ve_bank_transfer:
                summary: Venezuela — transferencia bancaria (VES)
                value:
                  country: VE
                  currency: VES
                  method: bank_transfer
                  amount: '5000.00'
                  beneficiary:
                    name: Carmen Delgado
                    account_number: 01020123456789012345
                    document_value: V87654321
                  idempotency_key: ve-bank-7810
              bo_bank_transfer:
                summary: Bolivia — transferencia ACH (BOB)
                value:
                  country: BO
                  currency: BOB
                  method: bank_transfer
                  amount: '1382.00'
                  beneficiary:
                    name: Juan Quispe Mamani
                    tax_id: '4567890'
                    bank_code: '1016'
                    account_number: '1234567890'
                  idempotency_key: bo-ach-2204
              br_pix:
                summary: Brasil — PIX por llave (BRL)
                value:
                  country: BR
                  currency: BRL
                  method: pix
                  amount: '350.00'
                  beneficiary:
                    name: João da Silva
                    tax_id: 123.456.789-69
                    pix_key_type: cpf
                    pix_key: '12345678909'
                  idempotency_key: br-pix-3321
              ec_bank_transfer:
                summary: Ecuador — transferencia bancaria (USD, remesa con datos del ordenante)
                value:
                  country: EC
                  currency: USD
                  method: bank_transfer
                  amount: '250.00'
                  beneficiary:
                    name: Carlos Andrade Vera
                    document_value: '1712345678'
                    account_number: '2203456789'
                    sender_name: Ana Torres Silva
                    sender_document_value: V23456789
                    sender_country: US
                  idempotency_key: ec-bank-4471
              ec_deuna:
                summary: Ecuador — billetera DE UNA por número de celular (USD)
                value:
                  country: EC
                  currency: USD
                  method: deuna
                  amount: '80.00'
                  beneficiary:
                    name: Lucia Paredes Mora
                    document_value: '0923456781'
                    phone: '0998765432'
                    sender_name: Ana Torres Silva
                  idempotency_key: ec-deuna-5520
              ec_cash_pickup:
                summary: Ecuador — efectivo en ventanilla (USD)
                value:
                  country: EC
                  currency: USD
                  method: cash_pickup
                  amount: '120.00'
                  beneficiary:
                    name: Miguel Zambrano Loor
                    document_value: '1309876543'
                    sender_name: Ana Torres Silva
                  idempotency_key: ec-cash-6612
              ec_cnb:
                summary: Ecuador — efectivo en corresponsal no bancario (USD)
                value:
                  country: EC
                  currency: USD
                  method: cnb
                  amount: '60.00'
                  beneficiary:
                    name: Rosa Cedeño Vera
                    document_value: '0801234567'
                    sender_name: Ana Torres Silva
                  idempotency_key: ec-cnb-7703
              py_bank_transfer:
                summary: Paraguay — transferencia bancaria (PYG)
                value:
                  country: PY
                  currency: PYG
                  method: bank_transfer
                  amount: '500000'
                  beneficiary:
                    name: Sofia Benitez
                    tax_id: '4123456'
                    bank_code: '0011'
                    account_number: '600123456'
                  idempotency_key: py-bank-9917
              ar_bank_transfer:
                summary: Argentina — transferencia bancaria a CBU/CVU (ARS)
                value:
                  country: AR
                  currency: ARS
                  method: bank_transfer
                  amount: '50000.00'
                  beneficiary:
                    name: Julieta Fernandez
                    tax_id: 27-23456789-1
                    account_number: '2850590940090418135201'
                  idempotency_key: ar-ars-3311
              ar_usd_cbu:
                summary: Argentina — transferencia en USD (solo CBU)
                value:
                  country: AR
                  currency: USD
                  method: bank_transfer
                  amount: '100.00'
                  beneficiary:
                    name: Julieta Fernandez
                    tax_id: 27-23456789-1
                    account_number: '2850590940090418135201'
                  idempotency_key: ar-usd-3312
              us_ach:
                summary: Estados Unidos — ACH a cuenta corriente (USD)
                value:
                  country: US
                  currency: USD
                  method: ach
                  amount: '250.00'
                  beneficiary:
                    name: John Carter
                    email: john.carter@example.com
                    account_number: '123456789012'
                    account_type: CHECKING
                    country_code: US
                    address: 1200 Brickell Ave
                    city: Miami
                    state: FL
                    postal_code: '33131'
                    bank_name: Example Bank
                    bank_code: '021000089'
                    bank_address: 500 Main Street
                    bank_city: New York
                    bank_state: NY
                    bank_postal_code: '10001'
                    bank_country: US
                  description: Factura 2210
                  idempotency_key: us-ach-2210
              us_ach_international_beneficiary:
                summary: Estados Unidos — ACH con beneficiario fuera de EE. UU. (banco en EE. UU.)
                value:
                  country: US
                  currency: USD
                  method: ach
                  amount: '250.00'
                  beneficiary:
                    name: Anna Schmidt
                    email: anna.schmidt@example.com
                    account_number: '123456789012'
                    account_type: CHECKING
                    country_code: DE
                    address: Friedrichstrasse 100
                    city: Berlin
                    postal_code: '10117'
                    bank_name: Example Bank
                    bank_code: '021000089'
                    bank_address: 500 Main Street
                    bank_city: New York
                    bank_state: NY
                    bank_postal_code: '10001'
                    bank_country: US
                  description: Factura 2215
                  idempotency_key: us-ach-2215
              us_wire:
                summary: Estados Unidos — wire doméstico (USD, mín 25.00)
                value:
                  country: US
                  currency: USD
                  method: wire
                  amount: '1000.00'
                  beneficiary:
                    name: John Carter
                    email: john.carter@example.com
                    account_number: '123456789012'
                    country_code: US
                    address: 1200 Brickell Ave
                    city: Miami
                    state: FL
                    postal_code: '33131'
                    bank_name: Example Bank
                    bank_code: '021000089'
                    bank_address: 500 Main Street
                    bank_city: New York
                    bank_state: NY
                    bank_postal_code: '10001'
                    bank_country: US
                    bank_phone: '+12125550100'
                  description: Factura 2211
                  idempotency_key: us-wire-2211
              us_swift:
                summary: Estados Unidos — wire internacional USD vía SWIFT (mín 25.00)
                value:
                  country: US
                  currency: USD
                  method: swift
                  amount: '1000.00'
                  beneficiary:
                    name: John Carter
                    email: john.carter@example.com
                    account_number: '123456789012'
                    country_code: US
                    address: 1200 Brickell Ave
                    city: Miami
                    state: FL
                    postal_code: '33131'
                    bank_name: Example Bank
                    bank_code: CHASUS33XXX
                    bank_country: US
                  description: Factura 2212
                  idempotency_key: us-swift-2212
              cl_paid_from_btc:
                summary: Chile — pagado desde el saldo BTC (settlement_asset)
                value:
                  country: CL
                  currency: CLP
                  method: bank_transfer
                  amount: '100000'
                  beneficiary:
                    name: Pedro Soto Fuentes
                    tax_id: 12.345.678-5
                    bank_code: '012'
                    account_type: checking
                    account_number: '123456789'
                  settlement_asset: BTC
                  idempotency_key: cl-btc-0192
      responses:
        '202':
          description: Payout aceptado y en despacho.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example: &id005
                payout_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: invoice-8841
                country: MX
                currency: MXN
                method: bank_transfer
                local_amount: '1500.00'
                fx_rate: '17.50'
                usdt_amount: '85.714286'
                fee: '0.300000'
                total_debit: '86.014286'
                settlement_asset: USDT
                settlement_amount: '86.014286'
                settlement_rate: '1'
                beneficiary: &id004
                  name: Maria Lopez
                  account_type: clabe
                  account_number: 012180001234567895
                description: Invoice 8841
                status: processing
                status_code: ''
                status_message: ''
                bank_reference: ''
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '200':
          description: Respuesta idempotente de un payout existente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example:
                payout_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: invoice-8841
                country: MX
                currency: MXN
                method: bank_transfer
                local_amount: '1500.00'
                fx_rate: '17.50'
                usdt_amount: '85.714286'
                fee: '0.300000'
                total_debit: '86.014286'
                settlement_asset: USDT
                settlement_amount: '86.014286'
                settlement_rate: '1'
                beneficiary: *id004
                description: Invoice 8841
                status: processing
                status_code: ''
                status_message: ''
                bank_reference: ''
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
                idempotency_hit: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: Cuenta no activa (`account_blocked`) o falta la credencial de cuenta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: account_blocked
                message: account is not active
        '422':
          description: 'No hay tasa de cambio para la moneda (`currency_not_supported`), o el

            payout fue rechazado de entrada — la respuesta es el objeto payout

            con `status: failed` y el débito ya reembolsado.

            '
          content:
            application/json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/Error'
                - $ref: '#/components/schemas/Payout'
              example:
                payout_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: invoice-8841
                country: MX
                currency: MXN
                method: bank_transfer
                local_amount: '1500.00'
                fx_rate: '17.50'
                usdt_amount: '85.714286'
                fee: '0.300000'
                total_debit: '86.014286'
                settlement_asset: USDT
                settlement_amount: '86.014286'
                settlement_rate: '1'
                beneficiary: *id004
                description: Invoice 8841
                status: failed
                status_code: core_rejected
                status_message: beneficiary account is invalid
                bank_reference: ''
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '503':
          description: El canal de payout está temporalmente no disponible (`channel_unavailable`);
            reintenta más tarde con la MISMA clave de idempotencia.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: channel_unavailable
                message: the payout channel is temporarily unavailable, please retry later
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Payouts
      operationId: listPayouts
      summary: Listar payouts
      description: Devuelve los payouts de la cuenta que llama.
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - processing
          - completed
          - failed
      - name: country
        in: query
        schema:
          type: string
        description: Filtra por pais de destino (ISO 3166-1 alfa-2, ej. CL, PE, MX).
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Filtra desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Filtra hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: format
        in: query
        description: 'Exporta las mismas filas como archivo: `csv` o `xlsx` (descarga contable, hasta
          10.000 filas; mismos filtros y rango). Omite para la vista JSON.'
        schema:
          type: string
          enum:
          - csv
          - xlsx
      responses:
        '200':
          description: Payouts paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  payouts:
                    type: array
                    items:
                      $ref: '#/components/schemas/Payout'
              example:
                page: 1
                page_size: 50
                payouts:
                - *id005
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payouts/documents:
    post:
      tags:
      - Payouts
      operationId: uploadPayoutDocument
      summary: Subir el documento de respaldo de un payout
      description: |-
        Sube el documento de respaldo (factura/recibo) que el corredor US/USD exige en TODA transferencia saliente (`ach`, `wire` y `swift`). Envía el binario crudo con su `Content-Type` (PDF, PNG, JPEG, WEBP, TXT, CSV, DOC(X) o XLS(X), hasta 50 MB) y el nombre del archivo en el query param `name`. El `document_key` devuelto viaja como `options.supporting_document_key` al crear el payout (`POST /v1/payouts`); el opcional `options.document_reference_number` se envía al banco solo en `ach`/`wire`. Un payout US/USD creado sin el documento falla con `400 supporting_document_required`; una key subida por otra cuenta falla con `400 invalid_document_key`.
      parameters:
      - name: name
        in: query
        required: true
        description: Nombre original del archivo (máx. 200 caracteres, sin separadores de ruta).
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/pdf:
            schema:
              type: string
              format: binary
          image/jpeg:
            schema:
              type: string
              format: binary
          image/png:
            schema:
              type: string
              format: binary
          text/csv:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: Documento almacenado; usa `document_key` como `options.supporting_document_key` del payout.
          content:
            application/json:
              example:
                document_key: payout-docs/9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d/7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f/a3f2b1c4-0000-4000-8000-0000000000ab
                file_name: invoice-221.pdf
                content_type: application/pdf
                size_bytes: 482110
        '400':
          description: Error de validación.
          content:
            application/json:
              examples:
                invalid_name:
                  summary: Falta el nombre del archivo
                  value:
                    error: invalid_file_name
                    message: query param name is required (1-200 chars, no path separators)
                empty_file:
                  summary: Body vacío
                  value:
                    error: empty_file
                    message: the file body is empty or could not be read
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '413':
          description: El archivo supera los 50 MB.
          content:
            application/json:
              example:
                error: file_too_large
                message: the file exceeds the 50 MiB limit
        '415':
          description: Tipo de contenido no soportado.
          content:
            application/json:
              example:
                error: unsupported_media_type
                message: unsupported content type (pdf, images, office documents, text)
        '503':
          description: Almacenamiento privado no disponible.
          content:
            application/json:
              example:
                error: storage_unavailable
                message: the file could not be stored right now; try again
  /v1/payouts/{payoutID}:
    get:
      tags:
      - Payouts
      operationId: getPayout
      summary: Obtener un payout
      parameters:
      - name: payoutID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El payout.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example:
                payout_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: invoice-8841
                country: MX
                currency: MXN
                method: bank_transfer
                local_amount: '1500.00'
                fx_rate: '17.50'
                usdt_amount: '85.714286'
                fee: '0.300000'
                total_debit: '86.014286'
                settlement_asset: USDT
                settlement_amount: '86.014286'
                settlement_rate: '1'
                beneficiary: *id004
                description: Invoice 8841
                status: completed
                status_code: ''
                status_message: ''
                bank_reference: '00761123456'
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payouts/methods:
    get:
      tags:
      - Payouts
      operationId: getPayoutMethods
      summary: Listar métodos de payout
      description: >-
        Corredores de payout disponibles (país, moneda, método) ofrecidos por CBPay.
        Pass-through del catálogo del core de procesamiento. Cada ítem trae `availability`
        (`operational` | `degraded` | `down`) — la salud en vivo del corredor, para decidir
        al renderizar si muestras, adviertes u ocultas un canal (ver la guía de estado del
        servicio). Un corredor sin incidentes registrados siempre es `operational`.
      responses:
        '200':
          description: Catálogo de métodos.
          content:
            application/json:
              schema:
                type: object
                description: Catálogo de corredores disponibles.
              example:
                items:
                - country: BO
                  currency: BOB
                  method: bank_transfer
                  availability: operational
                - country: BO
                  currency: BOB
                  method: qr
                  availability: operational
                - country: CL
                  currency: CLP
                  method: bank_transfer
                  availability: operational
                - country: EC
                  currency: USD
                  method: bank_transfer
                  availability: operational
                - country: EC
                  currency: USD
                  method: deuna
                  availability: operational
                - country: EC
                  currency: USD
                  method: cash_pickup
                  availability: operational
                - country: EC
                  currency: USD
                  method: cnb
                  availability: operational
                - country: MX
                  currency: MXN
                  method: bank_transfer
                  availability: operational
                - country: PE
                  currency: PEN
                  method: bank_transfer
                  availability: operational
                - country: PE
                  currency: PEN
                  method: yape
                  availability: operational
                - country: PY
                  currency: PYG
                  method: bank_transfer
                  availability: operational
                - country: US
                  currency: USD
                  method: ach
                  availability: operational
                - country: US
                  currency: USD
                  method: wire
                  availability: operational
                - country: US
                  currency: USD
                  method: swift
                  availability: operational
                - country: VE
                  currency: VES
                  method: bank_transfer
                  availability: down
                - country: VE
                  currency: VES
                  method: pago_movil
                  availability: down
                meta:
                  retrieved: 16
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payouts/banks:
    get:
      tags:
      - Payouts
      operationId: getPayoutBanks
      summary: Listar bancos de destino
      description: Catálogo de bancos para los payouts de un país. Sin `method` devuelve la unión de los
        bancos de todos los métodos del país (deduplicada por código); con `method` acota al catálogo
        de ese canal. Algunos métodos no tienen catálogo de bancos (ej. QR) y no aportan entradas.
      parameters:
      - name: country
        in: query
        schema:
          type: string
          example: CL
      - name: method
        in: query
        schema:
          type: string
          example: bank_transfer
        description: Método de payout opcional para acotar el catálogo (ver GET /v1/payouts/methods).
      responses:
        '200':
          description: Catálogo de bancos para el país.
          content:
            application/json:
              schema:
                type: object
              example:
                items:
                - code: '0001'
                  name: Banco de Chile
                - code: '0012'
                  name: BancoEstado
                - code: '0037'
                  name: Santander
                - code: '0051'
                  name: Banco Falabella
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payouts/bank-directory/lookup:
    get:
      tags:
      - Payouts
      operationId: lookupBankDirectory
      summary: Busca un banco por routing number o SWIFT/BIC
      description: 'Autocompleta el banco beneficiario desde un directorio bancario
        público embebido — pasa exactamente uno de `routing_number` (9 dígitos, solo
        US) o `swift` (8 u 11 caracteres; un sufijo `XXX` se normaliza a la casa
        matriz). Úsalo para autodetectar el banco mientras el pagador escribe el
        routing o SWIFT/BIC en formularios de payout o contraparte: un 404 significa
        "no está en el directorio" y el formulario simplemente sigue manual. Datos
        estáticos servidos con `Cache-Control: public, max-age=86400`; se pueden
        cachear por un día.'
      parameters:
      - name: routing_number
        in: query
        description: ABA routing number, 9 dígitos con dígito verificador válido
          (solo bancos US). Mutuamente excluyente con `swift`.
        schema:
          type: string
          pattern: '^[0-9]{9}$'
        example: '021000021'
      - name: swift
        in: query
        description: Código SWIFT/BIC, 8 u 11 caracteres (un sufijo `XXX` se normaliza
          a la casa matriz). Mutuamente excluyente con `routing_number`.
        schema:
          type: string
          pattern: '^[A-Za-z0-9]{8}([A-Za-z0-9]{3})?$'
        example: CHASUS33
      - name: country
        in: query
        description: Filtro opcional ISO 3166-1 alpha-2 para lookups por routing; solo
          se soporta `US` (otro valor devuelve 400 `invalid_country`).
        schema:
          type: string
          pattern: '^[A-Z]{2}$'
        example: US
      responses:
        '200':
          description: Banco encontrado en el directorio. `bank_phone` y
            `directory_vintage` solo aparecen en lookups por routing;
            `bank_state`/`bank_branch` solo cuando el dataset los tiene.
          content:
            application/json:
              schema:
                type: object
              examples:
                routing:
                  summary: Por ABA routing number
                  value:
                    routing_number: '021000021'
                    bank_name: JPMORGAN CHASE
                    bank_city: TAMPA
                    bank_state: FL
                    bank_postal_code: '33610'
                    bank_country: US
                    bank_phone: 813-432-3700
                    source: directory
                    directory_vintage: fed_ach_2019
                swift:
                  summary: Por SWIFT/BIC
                  value:
                    swift: CHASUS33
                    bank_name: JPMORGAN CHASE BANK, N.A.
                    bank_city: NEW YORK, NY
                    bank_country: US
                    source: directory
        '400':
          description: Parámetros faltantes/en conflicto, formato inválido, o un
            `country` distinto de `US`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              examples:
                invalid_payload:
                  summary: Parámetros faltantes o en conflicto
                  value:
                    error: invalid_payload
                    message: provide exactly one of routing_number or swift
                invalid_country:
                  summary: Lookup por routing con un país no-US
                  value:
                    error: invalid_country
                    message: routing number lookup is only available for US
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '404':
          description: El routing number o SWIFT/BIC no está en el directorio — el
            formulario sigue manual.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: bank_not_found
                message: no bank found for the given routing number
  /v1/payins:
    post:
      tags:
      - Payins
      operationId: createPayin
      summary: Crear un payin (cobro de recarga)
      description: 'Crea una recarga fiat. Con `method: "qr"` (por defecto) se genera un cobro QR activo
        a través del procesador — en Bolivia el QR interoperable local, en Brasil un QR PIX dinámico cuyo
        `charge` trae la imagen del QR y el payload "copia e cola".         Con `method: "bank_transfer"`
        el depósito queda anunciado y la respuesta trae la referencia que el remitente debe incluir para
        que la transferencia entrante se concilie y acredite automáticamente — un código corto de 12 caracteres
        alfanuméricos que cabe en cualquier concepto bancario (algunos rieles lo limitan a 20 caracteres
        sin caracteres especiales). Con `method: "fintoc"` (Chile) la respuesta trae una `payment_url`
        hosted: el pagador la abre y transfiere desde cualquier banco o billetera chilena; el depósito
        se detecta, valida y acredita automáticamente. Con `method: "card"` la respuesta trae
        una `payment_url` hosted de un checkout de tarjeta con 3-D Secure y la marca de tu organización —
        Bolivia (BOB) y tarjetas internacionales en USD (`country: "US"`; Visa, Mastercard,
        American Express, Discover y Diners emitidas en cualquier país); los datos de la tarjeta
        jamás tocan tu integración. Con `method: "checkout"` la respuesta trae
        una `checkout_url` pública — un link de cobro universal denominado en el saldo virtual que
        elijas (`settlement_asset`: USDT, USDC, BTC o GOLD). El pagador elige cualquier país con
        corredor de payin vivo (monto local cotizado), cualquiera de las 4 opciones crypto (dirección
        de depósito exclusiva con QR escaneable) o paga al instante desde la app CBPay vía el
        QR/alias del comercio; cada pago se convierte automático al asset de settlement salvo que se
        pague en el mismo asset. El primer método que completa el pago gana. Cobrar es gratis; la
        comisión de payin aplica cuando el depósito se acredita.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - country
              - currency
              - amount
              properties:
                country:
                  type: string
                  example: BO
                  description: País del corredor. Obligatorio para todos los métodos salvo
                    `checkout`, donde es opcional y solo preselecciona el país del pagador en la
                    página.
                currency:
                  type: string
                  example: BOB
                  description: Moneda del corredor. Obligatoria para todos los métodos salvo
                    `checkout`, que la rechaza con 400 — el cobro se denomina en
                    `settlement_asset`.
                amount:
                  type: string
                  description: String decimal positivo. Monto en moneda local para métodos de
                    corredor; para `checkout` se denomina en el `settlement_asset` ("50" USDT,
                    "0.001" BTC, "2" gramos de oro).
                description:
                  type: string
                channel:
                  type: string
                  description: Indicación opcional de canal que se pasa al core.
                expires_in:
                  type: integer
                  description: Expiración del cobro en segundos. Para `checkout` acepta de 600 a 604800
                    (10 minutos a 7 días; default 24 horas).
                method:
                  type: string
                  enum:
                  - qr
                  - bank_transfer
                  - fintoc
                  - card
                  - checkout
                  default: qr
                  description: Modo de cobro. `qr` crea un cobro QR activo a través del procesador; `bank_transfer`
                    anuncia un depósito entrante y devuelve la referencia a incluir en la descripción
                    de la transferencia; `fintoc` (solo Chile) devuelve una `payment_url` hosted donde
                    el pagador transfiere desde cualquier banco o billetera chilena y el depósito se detecta
                    automáticamente; `card` devuelve una `payment_url` hosted de un checkout de tarjeta con
                    3-D Secure (Bolivia en BOB, o tarjetas internacionales en USD cuando `country`
                    es `US`); `checkout` devuelve una `checkout_url` pública donde el pagador
                    elige el método de pago (QR, tarjeta, transferencia bancaria o crypto). Usa
                    /v1/payins/collect para cobros pull.
                idempotency_key:
                  type: string
                  description: >-
                    Clave de idempotencia opcional (métodos `bank_transfer`, `fintoc`, `card` y
                    `checkout`; también se acepta como header `Idempotency-Key`). Un retry con la
                    misma clave devuelve el payin original con HTTP 200 e `idempotency_hit: true` —
                    la misma `payment_url`/`checkout_url` en los métodos hosted, o la MISMA
                    `reference` en una transferencia anunciada — en vez de abrir un segundo cobro.
                    En las transferencias anunciadas esto define si la plata se puede acreditar: dos
                    anuncios vivos con el mismo monto y sin identidad del pagador son
                    indistinguibles, así que el depósito que llegue quedaría `unassigned`. Si omites
                    la clave igual colapsamos un retry idéntico (misma cuenta, moneda, monto y
                    documento del pagador) en el anuncio original; manda una clave distinta cuando
                    de verdad necesites dos cobros separados del mismo monto y pagador.
                customer:
                  type: object
                  description: Prefill opcional de facturación para el checkout de tarjeta (método `card`) —
                    `email`, `first_name`, `last_name`, `address`, `city`, `country` (texto plano, máx 120
                    caracteres por campo). El pagador puede completarlos o corregirlos en la página.
                success_url:
                  type: string
                  description: URL https pública opcional (métodos `card` y `checkout`) a la que se redirige
                    al pagador tras un pago aprobado.
                failure_url:
                  type: string
                  description: URL https pública opcional (métodos `card` y `checkout`) a la que se redirige
                    al pagador cuando el pago falla o expira.
                expires_at:
                  type: string
                  format: date-time
                  description: Expiración RFC3339 opcional de la sesión de pago con tarjeta (método `card`,
                    mínimo 15 minutos hacia adelante; default 24 horas).
                settlement_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  default: USDT
                  description: Solo checkout — el saldo virtual en que se denomina y liquida el
                    cobro. Todo pago se convierte automático a este asset al acreditar (pagos en el
                    mismo asset no convierten). Debe estar habilitado para tu organización
                    (`422 settlement_asset_disabled` si no).
                save_card:
                  type: boolean
                  default: false
                  description: Solo método card — ofrece al pagador el checkbox "guardar esta tarjeta"
                    en la página hosted. La credencial se guarda SOLO si el pagador lo marca
                    (consentimiento explícito) y el pago 3-D Secure se aprueba; el flujo
                    payin_received/payin_credited lleva entonces un bloque `stored_credential` y la
                    tarjeta aparece en GET /v1/stored-cards.
                payer_reference:
                  type: string
                  maxLength: 120
                  description: Solo método card — tu identificador propio del pagador (ej. tu ID de
                    cliente). Las tarjetas guardadas quedan acotadas a esta referencia para listar y
                    cobrar las tarjetas del cliente correcto. Texto plano, máx 120 caracteres.
                stored_card_id:
                  type: string
                  format: uuid
                  description: Solo método card — paga con una tarjeta guardada previamente (ver GET
                    /v1/stored-cards). La página hosted salta la captura de tarjeta y muestra la
                    guardada; el 3-D Secure corre igual. Mutuamente excluyente con `save_card`.
                    Tarjeta desconocida o revocada responde 404. Los datos de facturación guardados con la credencial se aplican automáticamente en la página hosted (resumen enmascarado con enlace para editar — el pagador no re-tipea nada).
                payer_name:
                  type: string
                  maxLength: 140
                  description: >-
                    Solo transferencia anunciada — nombre de la persona o empresa que hará la
                    transferencia, cuando NO es el titular de la cuenta. Opcional.
                payer_document:
                  type: string
                  maxLength: 40
                  description: >-
                    Solo transferencia anunciada — documento tributario o de identidad del pagador
                    (al menos 5 caracteres, uno de ellos un dígito). Opcional: si lo omites se usa
                    el documento verificado del titular, así un depósito propio se reconoce aunque
                    el pagador olvide la referencia. Envíalo cuando paga un tercero.
                payer_account:
                  type: string
                  maxLength: 40
                  description: >-
                    Solo transferencia anunciada — número de cuenta bancaria del pagador (al menos
                    5 caracteres). Opcional; es una señal extra de conciliación cuando el corredor
                    informa la cuenta de origen.
            examples:
              bo_qr:
                summary: Bolivia — cobro QR (BOB)
                value:
                  country: BO
                  currency: BOB
                  method: qr
                  amount: '700.00'
                  description: App top-up
                  expires_in: 3600
              br_pix_qr:
                summary: Brasil — QR PIX dinámico (BRL)
                value:
                  country: BR
                  currency: BRL
                  method: qr
                  amount: '120.00'
                  description: Order 7719
                  expires_in: 1800
              cl_announced_transfer:
                summary: Chile — transferencia bancaria anunciada (CLP)
                value:
                  country: CL
                  currency: CLP
                  method: bank_transfer
                  amount: '500000'
                  payer_document: '17438319-7'
                  idempotency_key: topup-9912
              py_announced_transfer:
                summary: Paraguay — transferencia bancaria anunciada (PYG, montos enteros)
                value:
                  country: PY
                  currency: PYG
                  method: bank_transfer
                  amount: '596000'
              us_announced_transfer:
                summary: Estados Unidos — transferencia bancaria anunciada (USD, ACH / wire)
                value:
                  country: US
                  currency: USD
                  method: bank_transfer
                  amount: '1250.00'
                  payer_name: Acme Holdings LLC
                  idempotency_key: invoice-1042
              cl_fintoc:
                summary: Chile — página de pago hosted, cualquier banco (CLP)
                value:
                  country: CL
                  currency: CLP
                  method: fintoc
                  amount: '150000'
                  description: Top-up order 8841
                  idempotency_key: topup-8841
              bo_card:
                summary: Bolivia — checkout de tarjeta 3-D Secure (BOB)
                value:
                  country: BO
                  currency: BOB
                  method: card
                  amount: '700.00'
                  description: App top-up
                  customer:
                    email: payer@example.com
                    first_name: Ana
                    last_name: Rojas
                  success_url: https://your-app.com/payment/ok
                  failure_url: https://your-app.com/payment/error
                  idempotency_key: topup-7719
              us_card:
                summary: Tarjetas internacionales — checkout 3-D Secure (US/USD)
                value:
                  country: US
                  currency: USD
                  method: card
                  amount: '49.90'
                  description: Plan Pro
                  customer:
                    email: pagador@ejemplo.com
                    first_name: Ana
                    last_name: Rojas
                  success_url: https://tu-app.com/pago/ok
                  failure_url: https://tu-app.com/pago/error
                  save_card: true
                  payer_reference: cliente-7719
                  idempotency_key: plan-pro-7719
              checkout_usdt:
                summary: Link de cobro universal — 50 USDT (multi-país, CL preseleccionado)
                value:
                  method: checkout
                  amount: '50'
                  settlement_asset: USDT
                  country: CL
                  description: Order 8841
                  success_url: https://your-app.com/payment/ok
                  failure_url: https://your-app.com/payment/error
                  expires_in: 86400
                  idempotency_key: order-8841
              checkout_btc:
                summary: Link de cobro universal — 0.001 BTC (liquidación en BTC)
                value:
                  method: checkout
                  amount: '0.001'
                  settlement_asset: BTC
                  description: Invoice 1204
                  idempotency_key: invoice-1204
      responses:
        '201':
          description: Cobro creado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  payin_id:
                    type: string
                    format: uuid
                  status:
                    type: string
                    example: pending
                  charge:
                    type: object
                    description: Detalles del cobro (datos del QR, referencias) desde el core.
              examples:
                qr:
                  summary: 'Cobro QR (method: qr)'
                  value:
                    payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
                    status: pending
                    charge:
                      charge_id: c9d8e7f6-a5b4-c3d2-e1f0-a9b8c7d6e5f4
                      our_reference: QR-778812
                      qr_image: data:image/png;base64,iVBORw0KGgo…
                      qr_image_url: https://cdn.cbpayapp.com/public/payin-qr/c9d8e7f6-a5b4-c3d2-e1f0-a9b8c7d6e5f4.png
                bank_transfer:
                  summary: 'Depósito anunciado (method: bank_transfer)'
                  value:
                    payin_id: 4f81c1d0-aaaa-bbbb-cccc-ddddeeeeffff
                    status: pending
                    reference: CBJ6T3W9M2K5
                    note: include the reference in the transfer description so the deposit is credited
                      automatically
                bank_transfer_us:
                  summary: 'Depósito anunciado con instrucciones US (method: bank_transfer, US/USD — doble riel)'
                  value:
                    payin_id: 8f4e2a1b-1111-2222-3333-444455556666
                    status: pending
                    reference: CBM4X8Q2T7K9
                    note: include the reference in the transfer description so the deposit is credited
                      automatically
                    payer_source: declared
                    payer_name: Acme Holdings LLC
                    deposit_instructions:
                      bank_name: Partner Bank, N.A.
                      account_number: '000123456789'
                      account_type: checking
                      holder_name: CBPay Operations LLC
                      holder_tax_id: 88-1234567
                      routing_number: '021000021'
                      holder_address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                      reference_required: true
                      qr_payload: "Bank: Partner Bank, N.A.\nAccount type: checking\nAccount number: 000123456789\nRouting number (ABA): 021000021\nHolder: CBPay Operations LLC\nHolder address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US\nTax ID: 88-1234567\nAmount: 1250.00 USD\nReference: CBM4X8Q2T7K9"
                      qr_png_base64: iVBORw0KGgoAAAANSU...
                    deposit_instructions_swift:
                      bank_name: Partner Bank International
                      account_number: '9870001234'
                      account_type: checking
                      holder_name: CBPay Operations LLC
                      holder_tax_id: 88-1234567
                      swift: PRTBPRI3
                      bank_address: 200 Example Blvd, San Juan, PR 00901, PR
                      intermediary_bank_name: Intermediary Bank N.A.
                      intermediary_bank_swift: INTRUS33
                      holder_address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                      notes: Select Puerto Rico as the final beneficiary bank country
                      reference_required: true
                      qr_payload: "Bank: Partner Bank International\nAccount type: checking\nAccount number: 9870001234\nSWIFT: PRTBPRI3\nBank address: 200 Example Blvd, San Juan, PR 00901, PR\nIntermediary bank: Intermediary Bank N.A.\nIntermediary SWIFT: INTRUS33\nHolder: CBPay Operations LLC\nHolder address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US\nTax ID: 88-1234567\nAmount: 1250.00 USD\nReference: CBM4X8Q2T7K9\nNote: Select Puerto Rico as the final beneficiary bank country"
                      qr_png_base64: iVBORw0KGgoAAAANSU...
                fintoc:
                  summary: 'Página de pago hosted (method: fintoc, Chile)'
                  value:
                    payin_id: 7a2b3c4d-1111-2222-3333-444455556666
                    status: pending
                    reference: 7a2b3c4d-1111-2222-3333-444455556666
                    payment_url: https://pay.fintoc.com/plink_K2zwNNSxPyx8w3GZ
                    expires_at: '2026-07-08T18:48:25Z'
                    note: share the payment_url with the payer; the deposit is credited automatically
                      once the transfer is detected
                card:
                  summary: 'Página de pago con tarjeta (method: card, Bolivia)'
                  value:
                    payin_id: b41c9a70-1111-2222-3333-444455556666
                    status: pending
                    reference: b41c9a70-1111-2222-3333-444455556666
                    payment_url: https://api.qbank.cl/pay/cards/9f3XkT2m…
                    expires_at: '2026-07-16T18:30:00Z'
                    note: share the payment_url with the payer; the balance is credited automatically
                      once the card payment is approved
                card_international:
                  summary: 'Página de pago con tarjeta (method: card, internacional USD)'
                  value:
                    payin_id: 3ab7c1d2-1111-2222-3333-444455556666
                    status: pending
                    reference: 3ab7c1d2-1111-2222-3333-444455556666
                    payment_url: https://api.qbank.cl/pay/cards/Kt9XmQ7p…
                    expires_at: '2026-07-26T18:30:00Z'
                    note: share the payment_url with the payer; the balance is credited automatically
                      once the card payment is approved
                checkout:
                  summary: 'Link de cobro universal (method: checkout)'
                  value:
                    payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                    kind: checkout
                    status: pending
                    settlement_asset: USDT
                    asset_amount: '50'
                    country: CL
                    description: Order 8841
                    reference: CB68JZCT46QE
                    checkout_url: https://api.qbank.cl/platform/pay/fc4981b8e7c7…
                    expires_at: '2026-07-17T17:57:44Z'
                    receipt_url: https://api.qbank.cl/platform/v1/payins/d0135ed5-8e9c-4f8b-a522-8ec100470426/receipt
        '200':
          description: >-
            Replay idempotente de un anuncio de transferencia (`bank_transfer`): el anuncio
            ORIGINAL, con la misma `reference`. Tambien se devuelve cuando un POST sin clave
            de idempotencia calza con un anuncio vivo indistinguible (misma cuenta, moneda,
            monto y pagador).
          content:
            application/json:
              schema:
                type: object
                properties:
                  payin_id:
                    type: string
                    format: uuid
                  status:
                    type: string
                  reference:
                    type: string
                  payer_source:
                    type: string
                  idempotency_hit:
                    type: boolean
              example:
                payin_id: 4f81c1d0-aaaa-bbbb-cccc-ddddeeeeffff
                status: pending
                reference: CBJ6T3W9M2K5
                note: include the reference in the transfer description so the deposit is credited
                  automatically
                payer_source: declared
                payer_document: 17438319-7
                idempotency_hit: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: >-
            `idempotency_conflict` — esta clave de idempotencia ya se uso con otro metodo de
            payin (QR, checkout, tarjeta). Las claves son unicas por cuenta: usa una clave por
            operacion logica.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_conflict
                message: this idempotency key was used with a different payin method
        '403':
          $ref: '#/components/responses/AccountRequired'
        '422':
          description: El core rechazó el cobro (`core_rejected`), o el corredor de
            transferencia anunciada exige una cuenta de destino registrada y tu organización
            aún no la configura (`deposit_instructions_unavailable` — hoy CL, PY y US).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              examples:
                core_rejected:
                  summary: Rechazo del corredor
                  value:
                    error: core_rejected
                    message: amount is below the minimum for this corridor
                deposit_instructions_unavailable:
                  summary: Sin instrucciones de depósito configuradas en el corredor
                  value:
                    error: deposit_instructions_unavailable
                    message: no deposit instructions configured for this corridor; contact your
                      organization admin
        '502':
          description: Core no disponible (`core_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: core_unavailable
                message: could not create the charge
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Payins
      operationId: listPayins
      summary: Listar payins
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - credited
          - unassigned
          - expired
          - failed
      - name: country
        in: query
        schema:
          type: string
        description: Filtra por pais de origen (ISO 3166-1 alfa-2, ej. BO, MX, VE).
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Filtra desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Filtra hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: format
        in: query
        description: 'Exporta las mismas filas como archivo: `csv` o `xlsx` (descarga contable, hasta
          10.000 filas; mismos filtros y rango). Omite para la vista JSON.'
        schema:
          type: string
          enum:
          - csv
          - xlsx
      responses:
        '200':
          description: Payins paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  payins:
                    type: array
                    items:
                      $ref: '#/components/schemas/Payin'
              example:
                page: 1
                page_size: 50
                payins:
                - &id006
                  payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  kind: qr
                  country: BO
                  currency: BOB
                  method: ''
                  local_amount: '700.00'
                  status: credited
                  reference: QR-778812
                  created_at: '2026-07-07T12:00:00Z'
                  updated_at: '2026-07-07T12:00:00Z'
                  fx_rate: '6.91'
                  usdt_gross: '101.302460'
                  fee: '1.000000'
                  usdt_credited: '100.302460'
                - payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  kind: checkout
                  country: ''
                  currency: ''
                  method: checkout
                  local_amount: ''
                  settlement_asset: USDT
                  asset_amount: '50'
                  status: pending
                  reference: CB68JZCT46QE
                  created_at: '2026-07-21T12:00:00Z'
                  updated_at: '2026-07-21T12:00:00Z'
                - payin_id: 8b3e1c4d-5a6f-7b8c-9d0e-1f2a3b4c5d6e
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  kind: card
                  country: US
                  currency: USD
                  method: card
                  local_amount: '498.75'
                  status: credited
                  reference: CARD-8842
                  created_at: '2026-08-10T14:08:33Z'
                  updated_at: '2026-08-10T14:08:40Z'
                  fx_rate: '1.010202'
                  usdt_gross: '493.811881'
                  fee: '27.159654'
                  usdt_credited: '466.652227'
                  settle_at: '2026-08-12T14:08:33Z'
                  settlement_pending: true
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/{payinID}:
    get:
      tags:
      - Payins
      operationId: getPayin
      summary: Obtener un payin
      parameters:
      - name: payinID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El payin.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payin'
              example: *id006
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/{payinID}/refunds:
    parameters:
    - name: payinID
      in: path
      required: true
      schema:
        type: string
        format: uuid
    post:
      tags:
      - Payins
      operationId: createPayinRefund
      summary: Devolver un cobro con tarjeta
      description: >-
        Devuelve la plata al tarjetahabiente y reversa el abono en tu saldo. Solo
        se devuelven cobros con tarjeta en estado `credited` (`refund_not_supported`
        en los demás rieles) — y solo cuando su saldo ya está disponible: un payin
        `credited` cuyo settlement sigue pendiente (`settlement_pending: true`, el
        saldo cae al llegar `settle_at`) se rechaza con `422 settlement_pending`.
        Se debita el bruto: la comisión y el spread FX del
        cobro original NO son reembolsables. La `idempotency_key` es obligatoria;
        un reintento devuelve la MISMA devolución con `idempotency_hit` y jamás
        manda una segunda devolución al procesador. Exige OTP cuando el llamante
        es una sesión (las API keys están exentas).
      parameters:
      - name: X-OTP-Token
        in: header
        required: false
        schema:
          type: string
        description: Token OTP de un solo uso (ver la guía de OTP). Solo para llamantes con sesión.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - idempotency_key
              properties:
                amount:
                  type: string
                  description: Monto en la moneda local original. Omítelo para devolver todo lo que queda por devolver.
                kind:
                  type: string
                  enum:
                  - refund
                  - void
                  description: '`refund` (default) devuelve un cargo ya liquidado; `void` anula uno que aún no liquida.'
                reason:
                  type: string
                idempotency_key:
                  type: string
            example:
              amount: '150.00'
              kind: refund
              reason: solicitud del cliente
              idempotency_key: refund-order-8842
      responses:
        '200':
          description: Reintento con la misma clave de idempotencia — la MISMA devolución, con `idempotency_hit`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayinRefund'
              example:
                refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
                payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                kind: refund
                status: completed
                currency: USD
                local_amount: '150.00'
                usdt_debited: '150.000000'
                requested_by: account
                idempotency_key: refund-order-8842
                reason: solicitud del cliente
                idempotency_hit: true
                receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
                created_at: '2026-07-25T13:10:00Z'
                updated_at: '2026-07-25T13:10:03Z'
        '201':
          description: Devolución creada y aprobada por el procesador. El saldo quedó debitado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayinRefund'
              example:
                refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
                payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                kind: refund
                status: completed
                currency: USD
                local_amount: '150.00'
                usdt_debited: '150.000000'
                requested_by: account
                idempotency_key: refund-order-8842
                reason: solicitud del cliente
                receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
                created_at: '2026-07-25T13:10:00Z'
                updated_at: '2026-07-25T13:10:03Z'
        '202':
          description: >-
            Despacho ambiguo: el procesador pudo haber devuelto. La devolución
            queda `pending` con tu saldo reservado y se resuelve por conciliación.
            NUNCA reintentamos por nuestra cuenta — reintenta con la MISMA
            `idempotency_key` y recibirás esta misma devolución.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayinRefund'
              example:
                refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
                payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                kind: refund
                status: pending
                currency: USD
                local_amount: '150.00'
                usdt_debited: '150.000000'
                requested_by: account
                idempotency_key: refund-order-8842
                reconciliation_required: true
                receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
                created_at: '2026-07-25T13:10:00Z'
                updated_at: '2026-07-25T13:10:00Z'
        '400':
          description: '`idempotency_key_required` o `invalid_amount` / `invalid_payload`.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: idempotency_key is required
        '402':
          description: '`insufficient_funds` — fondea el saldo y reintenta con la misma `idempotency_key`.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: insufficient_funds
                message: the account balance is not enough to fund this refund; top up and retry with the same idempotency_key
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: '`idempotency_conflict` — otra devolución con esta clave está en vuelo.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_conflict
                message: another refund with this key is in flight
        '422':
          description: >-
            `payin_not_refundable`, `refund_not_supported`, `refund_exceeds_payin`,
            `settlement_pending` (el payin está confirmado pero su saldo sigue
            programado para un `settle_at` futuro — espera al vencimiento o pide a
            un org-admin que libere el settlement primero),
            o una devolución que el procesador rechazó (el cuerpo es la devolución
            con `status: failed` y `failure_reason`; el débito se reversó completo).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: refund_exceeds_payin
                message: the requested amount exceeds what is left to refund on this payin
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Payins
      operationId: listPayinRefundsByPayin
      summary: Listar devoluciones de un cobro
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      - name: page
        in: query
        schema:
          type: integer
      - name: page_size
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Página de devoluciones.
          content:
            application/json:
              schema:
                type: object
              example: &id_payin_refunds_page
                page: 1
                page_size: 50
                refunds:
                - refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
                  payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  kind: refund
                  status: completed
                  currency: USD
                  local_amount: '150.00'
                  usdt_debited: '150.000000'
                  requested_by: account
                  idempotency_key: refund-order-8842
                  receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
                  created_at: '2026-07-25T13:10:00Z'
                  updated_at: '2026-07-25T13:10:03Z'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payin-refunds:
    get:
      tags:
      - Payins
      operationId: listPayinRefunds
      summary: Listar devoluciones
      description: Todas las devoluciones de la cuenta. Filtra por `payin_id`, `status` (`pending` | `completed` | `failed`) y `kind` (`refund` | `void` | `chargeback`).
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      - name: payin_id
        in: query
        schema:
          type: string
          format: uuid
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - completed
          - failed
      - name: kind
        in: query
        schema:
          type: string
          enum:
          - refund
          - void
          - chargeback
      - name: page
        in: query
        schema:
          type: integer
      - name: page_size
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Página de devoluciones.
          content:
            application/json:
              schema:
                type: object
              example: *id_payin_refunds_page
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payin-refunds/{refundID}:
    get:
      tags:
      - Payins
      operationId: getPayinRefund
      summary: Consultar una devolución
      parameters:
      - name: refundID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: La devolución.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayinRefund'
              example:
                refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
                payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                kind: chargeback
                status: completed
                currency: USD
                local_amount: '150.00'
                usdt_debited: '150.000000'
                requested_by: issuer
                receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
                created_at: '2026-07-25T13:10:00Z'
                updated_at: '2026-07-25T13:10:03Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payin-refunds/{refundID}/receipt:
    get:
      tags:
      - Payins
      operationId: getPayinRefundReceipt
      summary: Comprobante de la devolución (PDF)
      description: Comprobante PDF brandeado con código de verificación pública. Usa `?lang=en|es|zh` (default `en`).
      parameters:
      - name: refundID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - es
          - en
      responses:
        '200':
          description: Comprobante PDF.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/methods:
    get:
      tags:
      - Payins
      operationId: getPayinMethods
      summary: Listar métodos de payin
      description: >-
        Corredores de payin disponibles. Cada ítem trae `availability`
        (`operational` | `degraded` | `down`) — la salud en vivo del corredor
        (ver la guía de estado del servicio).
      responses:
        '200':
          description: Catálogo de corredores de payin disponibles.
          content:
            application/json:
              schema:
                type: object
              example:
                items:
                - country: BO
                  currency: BOB
                  method: qr
                  delivery: push
                  availability: operational
                - country: CL
                  currency: CLP
                  method: bank_transfer
                  delivery: push
                  availability: operational
                - country: MX
                  currency: MXN
                  method: bank_transfer
                  delivery: push
                  availability: operational
                - country: PE
                  currency: PEN
                  method: bank_transfer
                  delivery: polling
                  availability: operational
                - country: PY
                  currency: PYG
                  method: bank_transfer
                  delivery: polling
                  availability: operational
                - country: VE
                  currency: VES
                  method: c2p
                  delivery: push+polling
                  availability: operational
                meta:
                  retrieved: 6
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/transfers:
    post:
      tags:
      - Transferencias
      operationId: createTransfer
      summary: Crear una transferencia interna
      description: 'Mueve saldo entre dos cuentas CBPay, de forma atómica y siempre gratuita — funciona
        cualquier combinación: persona a persona, persona a empresa, empresa a persona o empresa a empresa.
        Funciona con los cuatro saldos virtuales (`USDT` default, `USDC`, `BTC`, `GOLD`) y siempre entre
        saldos de la **misma moneda** (sin conversión). El destino se resuelve por `to_account_id`, `to_email`,
        `to_phone` (solo teléfonos verificados por OTP) o `to_contact_id`. Cada transferencia guarda al
        destinatario como contacto automáticamente (desactívalo con `save_contact: false`). Los suscriptores
        de webhooks del receptor reciben `transfer_received`.'
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              properties:
                to_account_id:
                  type: string
                  format: uuid
                to_email:
                  type: string
                  format: email
                to_phone:
                  type: string
                  description: Teléfono E.164 de la cuenta destino. Solo resuelven cuentas con el teléfono
                    verificado por OTP (si no, 404 recipient_not_found; 422 recipient_ambiguous si más
                    de una cuenta comparte el número).
                to_contact_id:
                  type: string
                  format: uuid
                  description: Envía directo a un contacto de tu libreta (usa su cuenta CBPay enlazada,
                    o su teléfono verificado).
                to_qr_token:
                  type: string
                  description: Token del QR de perfil de la cuenta destino (escaneado desde su QR
                    CBPay). También acepta el payload extendido del QR de checkout
                    (`cbpay:pay?to=…&checkout=…`) — el checkout token embebido se honra
                    automáticamente.
                checkout_token:
                  type: string
                  description: Token opcional de un link de cobro universal para pagarlo directo. El
                    destino se fuerza a la cuenta del link y el monto debe cubrir el due cotizado
                    server-side en el asset elegido (`422 checkout_amount_mismatch` con el due
                    actualizado si no). Al completar, el link se liquida y auto-convierte a su
                    `settlement_asset`.
                asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  default: USDT
                  description: Moneda del saldo a mover; el destino recibe en esta misma moneda.
                amount:
                  type: string
                  description: String decimal positivo, hasta los decimales de la moneda (6 para USDT/USDC/GOLD,
                    8 para BTC).
                  example: '10.500000'
                description:
                  type: string
                idempotency_key:
                  type: string
                save_contact:
                  type: boolean
                  default: true
                  description: El destino se guarda como contacto automáticamente; envía false para no
                    guardarlo.
            examples:
              by_phone:
                summary: Por teléfono verificado
                value:
                  to_phone: '+56987654321'
                  amount: '25.000000'
                  description: Almuerzo
                  idempotency_key: lunch-2026-07-10-a
              by_contact:
                summary: A un contacto guardado
                value:
                  to_contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                  amount: '10.000000'
                  idempotency_key: t-991
              by_email:
                summary: Persona → persona, por email
                value:
                  to_email: carlos@example.com
                  amount: '25.000000'
                  description: Expense split
                  idempotency_key: split-2026-07-06-a
              by_account_id:
                summary: Persona → empresa, por account_id
                value:
                  to_account_id: ae8cf540-22a9-414d-82cc-8ac04732be4f
                  amount: '120.500000'
                  description: Monthly service payment
                  idempotency_key: serv-2026-07-a
              payroll:
                summary: Empresa → persona (nómina)
                value:
                  to_email: employee@example.com
                  amount: '850.000000'
                  description: July salary
                  idempotency_key: payroll-2026-07-emp01
              gold:
                summary: En otra moneda (GOLD, gramos de oro)
                value:
                  to_email: carlos@example.com
                  asset: GOLD
                  amount: '2.500000'
                  description: Gold gift
                  idempotency_key: gold-2026-07-09-a
      responses:
        '201':
          description: Transferencia completada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
              example:
                transfer_id: 2a7b9c1d-4e5f-6a70-8b9c-0d1e2f3a4b5c
                from_account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                to_account_id: 5e6f7a8b-9c0d-1e2f-3a4b-5c6d7e8f9a0b
                asset: USDT
                amount: '25.000000'
                description: Expense split
                status: completed
                created_at: '2026-07-07T12:00:00Z'
        '200':
          description: Respuesta idempotente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transfer'
              example:
                transfer_id: 2a7b9c1d-4e5f-6a70-8b9c-0d1e2f3a4b5c
                from_account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                to_account_id: 5e6f7a8b-9c0d-1e2f-3a4b-5c6d7e8f9a0b
                asset: USDT
                amount: '25.000000'
                description: Expense split
                status: completed
                created_at: '2026-07-07T12:00:00Z'
                idempotency_hit: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '404':
          description: Destino no encontrado (`recipient_not_found`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: recipient_not_found
                message: no account matches that email
        '422':
          description: El destino no puede recibir transferencias (`recipient_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: recipient_unavailable
                message: destination account cannot receive transfers
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Transferencias
      operationId: listTransfers
      summary: Listar transferencias
      description: Lista las transferencias de la cuenta (enviadas y recibidas), más recientes primero.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Filtra desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Filtra hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: format
        in: query
        description: 'Exporta las mismas filas como archivo: `csv` o `xlsx` (descarga contable, hasta
          10.000 filas; mismos filtros y rango). Omite para la vista JSON.'
        schema:
          type: string
          enum:
          - csv
          - xlsx
      responses:
        '200':
          description: Transferencias.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                page: 1
                page_size: 50
                transfers:
                - &id014
                  transfer_id: 77b1c2d3-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                  from_account_id: 389d34a3-6c61-4782-b822-95ac173068dc
                  to_account_id: ae8cf540-22a9-414d-82cc-8ac04732be4f
                  asset: USDT
                  amount: '25.000000'
                  description: Split de gastos
                  status: completed
                  direction: sent
                  created_at: '2026-07-06T20:10:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/crypto/withdrawals:
    post:
      tags:
      - Crypto
      operationId: createCryptoWithdrawal
      summary: Crear un retiro on-chain
      description: 'Envía USDT, USDC o BTC on-chain desde su propio saldo. Pares soportados: `tron`/`USDT`,
        `eth`/`USDT`, `eth`/`USDC`, `btc`/`BTC` (GOLD no tiene riel on-chain). `amount + comisión de retiro`
        se debita atómicamente y queda retenido; el estado final llega vía el webhook `crypto_withdrawal_status_changed`.
        `failed` reembolsa el débito completo.'
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              properties:
                chain:
                  type: string
                  enum:
                  - tron
                  - eth
                  - btc
                  default: tron
                asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  default: USDT
                  description: Activo a enviar (USDC solo por `eth`; en `btc` el activo por defecto
                    es `BTC`).
                to_address:
                  type: string
                to_contact_id:
                  type: string
                  format: uuid
                  description: Usa la dirección guardada del contacto para esta chain en vez de `to_address`
                    (un `to_address` explícito siempre gana).
                amount:
                  type: string
                  description: String decimal positivo, hasta 6 decimales (8 para BTC).
                idempotency_key:
                  type: string
                contact_name:
                  type: string
                  description: Nombre del contacto que se auto-guarda con esta dirección.
                save_contact:
                  type: boolean
                  default: true
                  description: La dirección se guarda como contacto automáticamente; envía false para
                    no guardarla.
                travel_address:
                  type: string
                  description: Travel Rule (FATF R.16) — la travel address de la institución beneficiaria
                    (código `ta…`) para retiros sobre el umbral de la org hacia otra institución. La dirección
                    de pago la entrega la institución receptora, por lo que `to_address` puede omitirse.
                wallet_type:
                  type: string
                  enum:
                  - self_hosted
                  description: Travel Rule — declara `self_hosted` cuando el destino es una wallet del
                    propio titular (exige `beneficiary_name`).
                beneficiary_name:
                  type: string
                  description: Travel Rule — nombre del titular del destino. Obligatorio sobre el umbral
                    (con `travel_address` o `wallet_type=self_hosted`).
            examples:
              usdt_tron:
                summary: USDT por TRON
                value:
                  chain: tron
                  to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                  amount: '100.000000'
                  idempotency_key: withdrawal-2026-07-07-a
              usdc_eth:
                summary: USDC por Ethereum
                value:
                  chain: eth
                  asset: USDC
                  to_address: '0x8f3Ba1c2De4567890aBcDeF1234567890aBcDeF1'
                  amount: '50.000000'
                  idempotency_key: withdrawal-usdc-2026-07-09-a
              btc_bitcoin:
                summary: BTC por Bitcoin
                value:
                  chain: btc
                  to_address: bc1qw508d6qejxtdg4y5r3zarvary0c5xw7kv8f3t4
                  amount: '0.00050000'
                  idempotency_key: withdrawal-btc-2026-07-15-a
              travel_rule_self_hosted:
                summary: Sobre el umbral Travel Rule a wallet propia (self-hosted)
                value:
                  chain: tron
                  to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                  amount: '1500.000000'
                  wallet_type: self_hosted
                  beneficiary_name: Maria Perez
                  idempotency_key: withdrawal-2026-07-12-a
              travel_rule_vasp:
                summary: Sobre el umbral hacia otra institución (travel address)
                value:
                  chain: tron
                  amount: '1500.000000'
                  travel_address: ta2AQSjBotWQf38c8sxYYK2KfisPoMsTPiYkviErEcoSn1uK
                  beneficiary_name: Maria Perez
                  idempotency_key: withdrawal-2026-07-12-b
      responses:
        '202':
          description: Retiro aceptado y en transmisión.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CryptoWithdrawal'
              example: &id007
                withdrawal_id: 6c5d4e3f-2a1b-0c9d-8e7f-6a5b4c3d2e1f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: withdrawal-2026-07-07-a
                chain: tron
                asset: USDT
                to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                amount: '100.000000'
                fee: '1.000000'
                total_debit: '101.000000'
                status: processing
                status_code: ''
                status_message: broadcasted
                tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '200':
          description: Respuesta idempotente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CryptoWithdrawal'
              example:
                withdrawal_id: 6c5d4e3f-2a1b-0c9d-8e7f-6a5b4c3d2e1f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: withdrawal-2026-07-07-a
                chain: tron
                asset: USDT
                to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                amount: '100.000000'
                fee: '1.000000'
                total_debit: '101.000000'
                status: processing
                status_code: ''
                status_message: broadcasted
                tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
                idempotency_hit: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '422':
          description: El core rechazó el retiro; el débito fue reembolsado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CryptoWithdrawal'
              example:
                withdrawal_id: 6c5d4e3f-2a1b-0c9d-8e7f-6a5b4c3d2e1f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: withdrawal-2026-07-07-a
                chain: tron
                asset: USDT
                to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                amount: '100.000000'
                fee: '1.000000'
                total_debit: '101.000000'
                status: failed
                status_code: core_rejected
                status_message: destination address is invalid
                tx_id: ''
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '503':
          description: Retiros no habilitados para este corredor (`withdrawals_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: withdrawals_unavailable
                message: on-chain withdrawals are not enabled for this corridor yet
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/crypto/withdrawals/{withdrawalID}:
    get:
      tags:
      - Crypto
      operationId: getCryptoWithdrawal
      summary: Obtener un retiro
      parameters:
      - name: withdrawalID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El retiro.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CryptoWithdrawal'
              example:
                withdrawal_id: 6c5d4e3f-2a1b-0c9d-8e7f-6a5b4c3d2e1f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: withdrawal-2026-07-07-a
                chain: tron
                asset: USDT
                to_address: TVJ6njG5Fyrq6XwYok3xPQx8kR7HQx6vXk
                amount: '100.000000'
                fee: '1.000000'
                total_debit: '101.000000'
                status: completed
                status_code: ''
                status_message: broadcasted
                tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/crypto/transactions:
    get:
      tags:
      - Crypto
      operationId: listCryptoTransactions
      summary: Listar actividad on-chain
      description: Créditos de fondeo y retiros de la cuenta que llama.
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Filtra desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Filtra hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      responses:
        '200':
          description: Depósitos y retiros.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  deposits:
                    type: array
                    items:
                      type: object
                      properties:
                        asset:
                          type: string
                        amount:
                          type: string
                          description: Monto neto acreditado.
                        reference:
                          type: string
                        tx_id:
                          type: string
                        chain:
                          type: string
                        from_address:
                          type: string
                        created_at:
                          type: string
                          format: date-time
                  withdrawals:
                    type: array
                    items:
                      $ref: '#/components/schemas/CryptoWithdrawal'
              example:
                page: 1
                page_size: 50
                deposits:
                - asset: USDT
                  amount: '499.000000'
                  reference: b1946ac9…:0
                  tx_id: b1946ac92492d2347c6235b4d26111…
                  chain: tron
                  from_address: TNPeeaaFB7K9cmo4uQpcU32zGK8G1NYqeL
                  created_at: '2026-07-07T12:00:00Z'
                withdrawals:
                - *id007
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/screenings:
    post:
      tags:
      - AML screening
      operationId: submitAmlScreening
      summary: Enviar un screening AML
      description: Screenea una persona o empresa (detectado desde el payload) contra listas de sanciones,
        PEP y prensa adversa vía el servicio de compliance. Es solo screening de listas — la verificación
        de identidad con documentos y prueba de vida vive en /v1/kyc y /v1/kyb. Todos los campos de identidad
        son opcionales y se reenvían íntegros al motor; mientras más datos envíes, más preciso es el análisis.
        Si hay comisión de compliance, se debita antes de la llamada y se reembolsa si falla. El resultado
        queda guardado localmente para historial (`GET /v1/aml/screenings`). `idempotency_key` es obligatoria
        — repetir con la misma clave devuelve el screening original con `idempotency_hit` true sin cobrar
        dos veces.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - idempotency_key
              properties:
                idempotency_key:
                  type: string
                  description: Obligatoria (el screening cobra comisión). También aceptada en el header
                    Idempotency-Key.
                customer:
                  type: object
                  description: 'Identity data. Provide `person` or `company`; when omitted

                    it is filled from the account profile. Additional fields are accepted

                    and forwarded to the screening engine.

                    '
                  properties:
                    person:
                      type: object
                      description: Person identity. Every field is optional; more identity data yields
                        a more precise screening.
                      properties:
                        full_name:
                          type: string
                        first_name:
                          type: string
                        middle_name:
                          type: string
                        last_name:
                          type: string
                        date_of_birth:
                          type: object
                          description: 'Date of birth as an object: {year, month, day} (integers). A plain "YYYY-MM-DD" string is rejected by the screening engine.'
                          properties:
                            year:
                              type: integer
                            month:
                              type: integer
                            day:
                              type: integer
                        nationality:
                          type: array
                          items:
                            type: string
                          description: Nationalities as an ARRAY of ISO-3166 codes (a plain string is rejected by the screening engine).
                        country_of_birth:
                          type: string
                        residential_information:
                          type: array
                          items:
                            type: object
                            properties:
                              country_of_residence:
                                type: string
                        personal_identification:
                          type: array
                          description: 'Strong identity documents (national ID, passport, ...) as {number, issuing_country}. Do not send a `type` field: the screening engine rejects it.'
                          items:
                            type: object
                            properties:
                              issuing_country:
                                type: string
                              number:
                                type: string
                        alias:
                          type: array
                          items:
                            type: string
                          description: Other known names.
                    company:
                      type: object
                      description: Company identity. Every field is optional; more identity data yields
                        a more precise screening.
                      properties:
                        legal_name:
                          type: string
                        alias:
                          type: array
                          items:
                            type: string
                          description: Trade / brand names.
                        registration_authority_identification:
                          type: string
                          description: Identificador ante el registro mercantil (ej.
                            RUT/número de registro).
                        place_of_registration:
                          type: string
                          description: País de registro/constitución (ISO-3166).
                        incorporation_date:
                          type: object
                          description: 'Fecha de constitución como objeto: {year,
                            month, day} (enteros).'
                          properties:
                            year:
                              type: integer
                            month:
                              type: integer
                            day:
                              type: integer
                        address:
                          type: array
                          items:
                            type: object
                            properties:
                              country:
                                type: string
                    email:
                      type: string
                    country:
                      type: string
                monitor:
                  type: boolean
                  default: false
                  description: Enable continuous monitoring from the start.
            examples:
              person:
                summary: Person (KYC)
                value:
                  customer:
                    person:
                      full_name: Ana Perez Rojas
                      date_of_birth:
                        year: 1990
                        month: 4
                        day: 12
                      personal_identification:
                      - issuing_country: CL
                        number: 12.345.678-5
                    email: ana@example.com
                    country: CL
                  monitor: false
              personFull:
                summary: Person (KYC) — full identity for the most precise screening
                value:
                  customer:
                    person:
                      full_name: Ana Perez Rojas
                      date_of_birth:
                        year: 1990
                        month: 4
                        day: 12
                      nationality:
                      - CL
                      country_of_birth: CL
                      residential_information:
                      - country_of_residence: CL
                      personal_identification:
                      - issuing_country: CL
                        number: 12.345.678-5
                      - issuing_country: CL
                        number: P0123456
                    email: ana@example.com
                    country: CL
                  monitor: false
              company:
                summary: Company (KYB)
                value:
                  customer:
                    company:
                      legal_name: Comercial Andina SpA
                      registration_authority_identification: 76.543.210-3
                    email: legal@andina.cl
                    country: CL
                  monitor: true
              companyFull:
                summary: Company (KYB) — full identity for the most precise screening
                value:
                  customer:
                    company:
                      legal_name: Comercial Andina SpA
                      alias:
                      - Andina Market
                      registration_authority_identification: 76.543.210-3
                      place_of_registration: CL
                      incorporation_date:
                        year: 2015
                        month: 8
                        day: 1
                      address:
                      - country: CL
                    email: legal@andina.cl
                    country: CL
                  monitor: true
              minimal:
                summary: Minimal — autofilled from the account
                value:
                  customer: {}
                  monitor: false
      responses:
        '201':
          description: Screening enviado y guardado localmente.
          content:
            application/json:
              schema:
                type: object
                description: Espejo local del screening más campos de cobro. El resultado completo está
                  en `result`.
                properties:
                  screening_id:
                    type: string
                  kind:
                    type: string
                    enum:
                    - screening
                  service:
                    type: string
                    enum:
                    - compliance_person
                    - compliance_company
                  subject_type:
                    type: string
                    enum:
                    - person
                    - company
                  subject_name:
                    type: string
                  risk:
                    type: string
                  monitor:
                    type: boolean
                  screening_fee:
                    type: string
                  fee_asset:
                    type: string
                  idempotency_key:
                    type: string
                  idempotency_hit:
                    type: boolean
                  created_at:
                    type: string
                    format: date-time
                  result:
                    type: object
                    description: Resultado neutro del servicio de compliance.
              example:
                screening_id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                kind: screening
                service: compliance_person
                subject_type: person
                subject_name: Ana Perez Rojas
                risk: low
                monitor: false
                screening_fee: '2.000000'
                fee_asset: USDT
                idempotency_key: aml-screen-ana-1
                created_at: '2026-07-12T18:00:00Z'
                result:
                  customer_id: e1d2c3b4-a596-8778-695a-4b3c2d1e0f9a
                  status: completed
                  risk_level: low
                  screening_result: no_hits
                  monitored: false
        '400':
          description: Falta idempotency_key (`idempotency_key_required`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '502':
          description: Screening temporarily unavailable (`compliance_unavailable`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: compliance_unavailable
                message: screening temporarily unavailable
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - AML screening
      operationId: listAmlScreenings
      summary: Listar screenings AML
      description: Historial de screenings AML de la cuenta (persona, empresa y rescreens), del más nuevo
        al más antiguo. Exige el rango from/to; admite paginación y filtro por riesgo.
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
        description: Fecha inicio YYYY-MM-DD (zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
        description: Fecha fin YYYY-MM-DD (zona horaria de la organización, inclusive).
      - name: risk
        in: query
        schema:
          type: string
        description: Filtrar por nivel de riesgo.
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Página de screenings.
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                screenings:
                - screening_id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                  kind: screening
                  service: compliance_person
                  subject_type: person
                  subject_name: Ana Perez Rojas
                  risk: low
                  monitor: false
                  screening_fee: '2.000000'
                  fee_asset: USDT
                  idempotency_key: aml-screen-ana-1
                  created_at: '2026-07-12T18:00:00Z'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/screenings/{screeningID}:
    get:
      tags:
      - AML screening
      operationId: getAmlScreening
      summary: Obtener un screening AML
      description: Detalle de un screening, incluyendo el resultado guardado. Un id de otra cuenta devuelve
        404.
      parameters:
      - name: screeningID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: El screening.
          content:
            application/json:
              example:
                screening_id: a1b2c3d4-e5f6-7890-abcd-ef1234567890
                kind: screening
                service: compliance_person
                subject_type: person
                subject_name: Ana Perez Rojas
                risk: low
                monitor: false
                screening_fee: '2.000000'
                fee_asset: USDT
                idempotency_key: aml-screen-ana-1
                created_at: '2026-07-12T18:00:00Z'
                result:
                  customer_id: e1d2c3b4-a596-8778-695a-4b3c2d1e0f9a
                  status: completed
                  risk_level: low
                  screening_result: no_hits
                  monitored: false
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/screenings/{screeningID}/report:
    get:
      tags:
      - AML screening
      operationId: getAmlScreeningReport
      summary: Descargar el informe del screening AML (PDF)
      description: Informe PDF brandeado del screening — portada ejecutiva con la decisión, indicadores
        de riesgo (sanciones, watchlists, PEP, prensa adversa...), coincidencias consolidadas, alias,
        glosario y una sección final de respaldo con las fuentes internacionales consultadas. Trilingüe
        vía `lang` (default inglés). Lectura pura — sin comisión ni clave de idempotencia. Un id de otra
        cuenta devuelve 404.
      parameters:
      - name: screeningID
        in: path
        required: true
        schema:
          type: string
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - en
          - es
          - zh
          default: en
        description: Idioma del informe PDF (default inglés).
      responses:
        '200':
          description: Informe PDF brandeado del screening.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
        '400':
          description: Idioma no soportado (`invalid_language`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_language
                message: 'lang must be one of: en, es, zh'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/rescreen:
    post:
      tags:
      - AML screening
      operationId: rescreenAml
      summary: Re-ejecutar un screening AML
      description: Re-ejecuta el screening de la cuenta. Requiere un screening previo. Cobra `compliance_rescreen`
        si está configurado; se reembolsa si falla el upstream. Cada rescreen queda en el historial (`GET
        /v1/aml/screenings`). `idempotency_key` es obligatoria — repetir con la misma clave devuelve el
        rescreen original con `idempotency_hit` true sin cobrar dos veces.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - idempotency_key
              properties:
                idempotency_key:
                  type: string
                  description: Obligatoria (el rescreen cobra comisión). También aceptada en el header
                    Idempotency-Key.
            example:
              idempotency_key: aml-rescreen-2026-07-12
      responses:
        '200':
          description: Resultado del rescreen guardado localmente.
          content:
            application/json:
              example:
                screening_id: b2c3d4e5-f6a7-4b8c-9d0e-1f2a3b4c5d6e
                kind: rescreen
                service: compliance_rescreen
                risk: low
                screening_fee: '1.000000'
                fee_asset: USDT
                idempotency_key: aml-rescreen-2026-07-12
                created_at: '2026-07-12T19:00:00Z'
                result:
                  customer_id: e1d2c3b4-a596-8778-695a-4b3c2d1e0f9a
                  status: completed
                  screening_result: no_hits
        '400':
          description: Falta idempotency_key (`idempotency_key_required`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '409':
          description: Account has no prior screening (`no_screening`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_screening
                message: account has no AML screening to rescreen
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/monitoring:
    patch:
      tags:
      - AML screening
      operationId: setAmlMonitoring
      summary: Habilitar o deshabilitar el monitoreo AML
      description: Activa o desactiva el monitoreo continuo de compliance de la identidad verificada de
        la cuenta. Habilitar cobra la comisión `compliance_monitoring` si está configurada; deshabilitar
        es gratis. Requiere un screening previo. Cada cambio de estado queda guardado en el historial
        (`GET /v1/aml/screenings`, filtro kind monitoring). Cuando el estado cambia de verdad, `idempotency_key`
        es obligatoria — repetir con la misma clave devuelve el evento original con `idempotency_hit`
        true. Si ya está en el estado pedido, devuelve unchanged true sin cobrar.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - enabled
              properties:
                enabled:
                  type: boolean
                idempotency_key:
                  type: string
                  description: Obligatoria cuando cambia el estado (habilitar cobra comisión). Also accepted
                    as the Idempotency-Key header.
            example:
              enabled: true
              idempotency_key: aml-monitor-enable-1
      responses:
        '200':
          description: Estado del monitoreo guardado localmente.
          content:
            application/json:
              example:
                screening_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                kind: monitoring
                service: compliance_monitoring
                monitor: true
                monitoring_enabled: true
                screening_fee: '1.000000'
                fee_asset: USDT
                idempotency_key: aml-monitor-enable-1
                created_at: '2026-07-12T20:00:00Z'
                compliance_service: compliance_monitoring
                compliance_fee: '1.000000'
        '400':
          description: Falta idempotency_key cuando cambia el estado (`idempotency_key_required`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
                compliance_fee: '0.500000'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '409':
          description: Account has no prior screening (`no_screening`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_screening
                message: account has no AML screening to monitor
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/aml/catalogs:
    get:
      tags:
      - AML screening
      operationId: getAmlCatalogs
      summary: Catálogos para formularios de compliance
      description: 'Devuelve todos los catálogos (listas de pares value/label) que el front debe usar
        para construir formularios de compliance y verificación consistentes: géneros, estados de empresa,
        tipos de dirección, formas jurídicas (lista global más cascada por país), fuentes de ingreso/patrimonio,
        estándares de industria con su default por país, y las listas ISO-3166 completas de países y subdivisiones.
        Data estática — seguro cachearla por horas. `value` es lo que envías a la API; `label` lo que
        muestras.'
      responses:
        '200':
          description: Todos los catálogos más un bloque `meta` que describe las cascadas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  genders:
                    type: array
                    description: Valores exactos requeridos al screenear personas.
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  company_status:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  address_types:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  company_types:
                    type: array
                    description: Lista plana de formas jurídicas (idioma local oficial) como fallback.
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  source_of_income:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  source_of_wealth:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  industries:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  industry_code_types:
                    type: array
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  company_types_by_country:
                    type: object
                    description: Formas jurídicas por país ISO (GLEIF ELF); si el país falta, usar `company_types`.
                  industries_by_code_type:
                    type: object
                    description: Códigos de industria por estándar (`value` = código, `label` = descripción).
                  industry_code_type_by_country:
                    type: object
                    description: Estándar de industria fijado por país (default ISIC).
                  countries:
                    type: array
                    description: Países ISO-3166-1.
                    items:
                      $ref: '#/components/schemas/CatalogItem'
                  country_subdivisions:
                    type: object
                    description: Subdivisiones ISO-3166-2 por país.
                  meta:
                    type: object
                    description: Conteos más una nota que describe cómo se encadenan las cascadas.
              example:
                genders:
                - value: male
                  label: Male
                - value: female
                  label: Female
                company_status:
                - value: active
                  label: Active
                address_types:
                - value: residential
                  label: Residential
                company_types:
                - value: Sociedad Anónima
                  label: Sociedad Anónima
                source_of_income:
                - value: salary
                  label: Salary
                source_of_wealth:
                - value: business_ownership
                  label: Business ownership
                industries:
                - value: financial_services
                  label: Financial services
                industry_code_types:
                - value: ISIC
                  label: ISIC Rev.4
                company_types_by_country:
                  CL:
                  - value: Sociedad por Acciones
                    label: Sociedad por Acciones
                industries_by_code_type:
                  ISIC:
                  - value: '6419'
                    label: Other monetary intermediation
                industry_code_type_by_country:
                  CL: ISIC
                countries:
                - value: CL
                  label: Chile
                country_subdivisions:
                  CL:
                  - value: CL-RM
                    label: Región Metropolitana de Santiago
                meta:
                  country_count: 249
                  note: value = send to the API; label = display in the UI.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/aml/catalogs/cities:
    get:
      tags:
      - AML screening
      operationId: getAmlCatalogCities
      summary: Catálogo de ciudades de un país
      description: 'Devuelve las ciudades de un país ISO 3166-1 alpha-2, agrupadas por subdivisión:
        las claves de `states` son los mismos códigos ISO 3166-2 de `country_subdivisions` en
        `GET /v1/aml/catalogs`, y `country_cities` lista las ciudades cuya región no pudo
        mapearse a una subdivisión (ofrécelas también). Ningún campo es jamás `null`. Un país
        sin cobertura responde 200 con listas vacías — cae a un campo de ciudad de texto libre.
        Data estática servida con `Cache-Control: public, max-age=86400`; se puede cachear por
        un día. Una llamada por país y luego filtras por estado en el cliente.'
      parameters:
      - name: country
        in: query
        required: true
        description: Código de país ISO 3166-1 alpha-2 (ej. `US`).
        schema:
          type: string
          pattern: '^[A-Z]{2}$'
        example: US
      responses:
        '200':
          description: Ciudades del país agrupadas por subdivisión (listas vacías cuando el país no tiene cobertura).
          content:
            application/json:
              schema:
                type: object
                properties:
                  country:
                    type: string
                    description: Código de país ISO 3166-1 alpha-2 devuelto tal cual.
                  states:
                    type: object
                    description: Ciudades por código de subdivisión ISO 3166-2 (`<CC>-<code>`).
                    additionalProperties:
                      type: array
                      items:
                        type: string
                  country_cities:
                    type: array
                    description: Ciudades cuya región no pudo mapearse a una subdivisión.
                    items:
                      type: string
                  meta:
                    type: object
                    description: Conteos de cobertura (`state_count`, `city_count`).
              example:
                country: US
                states:
                  US-FL:
                  - Miami
                  - Orlando
                country_cities: []
                meta:
                  state_count: 51
                  city_count: 3200
        '400':
          description: Código de país mal formado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_country
                message: country is required (ISO 3166-1 alpha-2)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '404':
          description: Código de país desconocido.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: country_not_found
                message: unknown country code
  /v1/aml/catalogs/postal-code:
    get:
      tags:
      - AML screening
      operationId: getAmlCatalogPostalCode
      summary: Busca ciudad y estado por código postal
      description: 'Resuelve un código postal a su ciudad y estado desde un dataset
        público embebido — hoy solo hay cobertura de ZIP codes de US. Úsalo para
        autocompletar los campos ciudad/estado mientras el usuario escribe el ZIP en
        formularios con dirección: un 404 significa "ZIP desconocido" (o un país sin
        dataset) y los campos simplemente siguen manuales. Datos estáticos servidos
        con `Cache-Control: public, max-age=86400`; se pueden cachear por un día.'
      parameters:
      - name: country
        in: query
        required: true
        description: Código de país ISO 3166-1 alpha-2. Hoy solo `US` tiene dataset.
        schema:
          type: string
          pattern: '^[A-Z]{2}$'
        example: US
      - name: code
        in: query
        required: true
        description: Código postal a resolver (ZIP de US de 5 dígitos).
        schema:
          type: string
          pattern: '^[0-9]{5}$'
        example: '33130'
      responses:
        '200':
          description: Ciudad y estado del código postal.
          content:
            application/json:
              schema:
                type: object
                properties:
                  country:
                    type: string
                    description: Código de país ISO 3166-1 alpha-2 devuelto tal cual.
                  postal_code:
                    type: string
                  city:
                    type: string
                  state:
                    type: string
                    description: Sufijo de subdivisión ISO 3166-2 (ej. `FL`).
              example:
                country: US
                postal_code: '33130'
                city: Miami
                state: FL
        '400':
          description: Parámetros faltantes o código postal malformado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_payload
                message: code must be a 5-digit US ZIP
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '404':
          description: Código postal desconocido, o un país sin dataset — los campos
            de dirección siguen manuales.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: postal_code_not_found
                message: no place found for the given postal code
  /v1/me/verification/link:
    post:
      tags:
      - KYC / KYB
      operationId: createMyVerificationLink
      summary: Pedir mi link de verificación (onboarding)
      description: 'Devuelve el link de verificación de identidad PROPIO de la cuenta (persona ⇒ link
        KYC, empresa ⇒ link KYB — derivado del tipo de cuenta). El wizard hosteado cubre el flujo completo:
        formulario, documentos y prueba de vida en video. Gratis. Si ya hay un link vigente se devuelve
        el mismo con 200; una cuenta ya aprobada recibe 409 already_verified. Hasta aprobar la verificación,
        toda acción de dinero saliente responde 403 verification_required (el fondeo y las lecturas siguen
        disponibles).'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                expires_in_days:
                  type: integer
                  description: Vencimiento opcional del link (1-30 días). Omitido, no vence.
            example:
              expires_in_days: 14
      responses:
        '201':
          description: Link de onboarding creado (200 cuando se reutiliza uno vigente).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationLink'
              example:
                link_id: a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d
                kind: kyc
                url: https://verify.example.com/on/usd/individual/new?invite=abc123
                status: pending
                label: Ana Perez
                created_at: '2026-07-10T12:00:00Z'
                updated_at: '2026-07-10T12:00:00Z'
        '409':
          description: La cuenta ya completó su verificación (already_verified).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: already_verified
                message: this account already completed its identity verification
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/me/verification/report:
    get:
      tags:
      - KYC / KYB
      operationId: getMyVerificationReport
      summary: Descargar mi informe de verificación (PDF/JSON)
      description: >-
        Informe de compliance de tu propia verificación de onboarding, generado por la plataforma:
        identidad verificada, ciclo de vida, documentos + OCR, liveness (una entrada por sesión - el
        check de onboarding más cualquier recaptura de evidencia posterior, cada una con su propio
        session_id y purpose) y una sección AML AGREGADA (estado de sanciones / PEP / medios adversos,
        jamás el detalle de coincidencias). Usa format=pdf (default) o format=json, y lang=en|es|zh.
        Gratis - la verificación ya se cobró al crearse. El PDF imprime un código público de
        verificación + QR consultable por cualquiera en /verify/reports/{code}.
      parameters:
      - name: format
        in: query
        schema:
          type: string
          enum:
          - pdf
          - json
        description: Formato del informe - pdf (default) o json.
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - en
          - es
          - zh
        description: Idioma del informe (en default, es, zh).
      responses:
        '200':
          description: El informe de verificación (application/pdf, o application/json con format=json).
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
            application/json:
              example:
                report_id: IDR-1A2B3C4D5E6F
                kind: kyc
                scope: self
                generated_at: '2026-07-28T15:04:05Z'
                generated_by: CBPay
                language: en
                aml_detail: false
                submission_id: 1a2b3c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                status: approved
                risk_band: low
                submitted_at: '2026-07-18T10:00:00Z'
                decided_at: '2026-07-18T10:42:00Z'
                documents_received:
                - identity
                - proofOfAddress
                subject:
                  type: person
                  name: Taylor Code
                  person:
                    first_name: Taylor
                    last_name: Code
                    date_of_birth: '1990-06-15'
                    nationality: CL
                    tax_residence_country: CL
                    id_type: national_id
                    id_number: 17438319-7
                    occupation: Software engineer
                  address:
                    city: Santiago
                    country: CL
                  economic_profile:
                    source_of_funds: salary
                    primary_purpose: personal_payments
                    monthly_payments_usd: '3000'
                    employment_status: employed
                  bank_account:
                    bank_name: Banco Estado
                    account_masked: '****7788'
                    country: CL
                documents:
                - category: identity
                  filename: cedula.pdf
                  status: validated
                  outcome: MATCH
                  score: '0.99'
                  validated_at: '2026-07-18T10:31:00Z'
                liveness:
                - role: subject
                  session_id: lv_e763e3465bf34f1dab826a263c1eaaaa
                  purpose: gate
                  status: completed
                  outcome: PASS
                  passed_gate: true
                  liveness_score: '0.98'
                  antispoof_score: '0.96'
                  face_similarity: '0.97'
                  gestures_expected: 3
                  gestures_passed: 3
                  updated_at: '2026-07-18T10:35:00Z'
                  media:
                    has_selfie: true
                    has_video: false
                    expires_in_sec: 900
                    selfie_sha256: 6f8a3b1c2d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e
                - role: subject
                  session_id: lv_4cdd3a82903940cebd8cc95a77cdacb3
                  purpose: media_recapture
                  status: completed
                  outcome: FAIL
                  passed_gate: false
                  liveness_score: '0.98'
                  antispoof_score: '0.59'
                  face_similarity: '0.55'
                  gestures_expected: 3
                  gestures_passed: 2
                  reasons:
                  - Smile was not detected.
                  updated_at: '2026-07-18T10:38:00Z'
                  media:
                    has_selfie: true
                    has_video: true
                    frame_gestures:
                    - center
                    - turn_right
                    - smile
                    video_mime_type: video/mp4
                    expires_in_sec: 900
                    selfie_sha256: a1b2c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9
                    frame_sha256:
                    - c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2
                    - d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2c3
                aml:
                  risk_level: low
                  status: clear
                  sanctions: clear
                  pep: clear
                  adverse_media: clear
                content_sha256: 9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b
                verification_code: K1a2b3c4d5e6f4a7b8c9d0e1f2a3b4c5d9f8e7d6c5b4a3928
                verification_url: https://api.example.com/verify/reports/K1a2b3c4d5e6f4a7b8c9d0e1f2a3b4c5d9f8e7d6c5b4a3928
        '400':
          description: Formato o idioma inválido (invalid_format, invalid_language).
          content:
            application/json:
              example:
                error: invalid_format
                message: format must be pdf or json
        '404':
          description: Esta cuenta aún no tiene ninguna submission de verificación (verification_not_found).
          content:
            application/json:
              example:
                error: verification_not_found
                message: no verification submission exists for this account yet
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/me/verification:
    get:
      tags:
      - KYC / KYB
      operationId: getMyVerification
      summary: Mi estado de verificación (onboarding)
      description: Estado efectivo de verificación de la cuenta que llama — `kyc_status`, el kind requerido
        (kyc para personas, kyb para empresas) más el último link y submission propios.
      responses:
        '200':
          description: Estado de onboarding.
          content:
            application/json:
              schema:
                type: object
                properties:
                  kyc_status:
                    type: string
                    enum:
                    - none
                    - pending
                    - approved
                    - rejected
                  required_kind:
                    type: string
                    enum:
                    - kyc
                    - kyb
                  verified:
                    type: boolean
                  link:
                    $ref: '#/components/schemas/VerificationLink'
                  submission:
                    $ref: '#/components/schemas/VerificationSubmission'
              example:
                kyc_status: pending
                required_kind: kyc
                verified: false
                link:
                  link_id: a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d
                  kind: kyc
                  url: https://verify.example.com/on/usd/individual/new?invite=abc123
                  status: completed
                  created_at: '2026-07-10T12:00:00Z'
                  updated_at: '2026-07-10T12:30:00Z'
                submission:
                  submission_id: f0e1d2c3-b4a5-4968-8776-5a4b3c2d1e0f
                  kind: kyc
                  status: in_review
                  liveness_pending: false
                  created_at: '2026-07-10T12:30:00Z'
                  updated_at: '2026-07-10T13:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/screenings/addresses:
    post:
      tags:
      - Screening de wallets
      operationId: createAddressScreening
      summary: Screenear una dirección blockchain
      description: 'Evalúa el riesgo AML de una dirección blockchain (entidad sancionada, exposición a
        fondos ilícitos) contra inteligencia on-chain global y devuelve un nivel de riesgo normalizado
        (Low/Medium/High/Severe) con la evidencia completa. Agnóstico a la red: la dirección se evalúa
        sobre todas las chains soportadas a la vez; `chain` solo etiqueta tu registro. Cobra la comisión
        fija `address_screening` (se reembolsa automáticamente si el screening falla), por lo que `idempotency_key`
        es obligatoria — reintentar con la misma clave devuelve el screening original con `idempotency_hit:
        true` y jamás cobra dos veces. Requiere el servicio `screenings` habilitado y la verificación
        de la cuenta aprobada.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - address
              - idempotency_key
              properties:
                address:
                  type: string
                  description: Dirección blockchain a evaluar (máx 128 caracteres).
                chain:
                  type: string
                  description: Etiqueta opcional (tron, eth, ...) para tu registro; no restringe la evaluación.
                idempotency_key:
                  type: string
                  description: Obligatoria (el scan cobra comisión). También se acepta como header Idempotency-Key.
            example:
              address: TN2BBWc9EF8MMB6i1c4HZHXAssTEXstMDo
              chain: tron
              idempotency_key: scan-cliente-742-1
      responses:
        '201':
          description: Screening ejecutado y guardado.
          content:
            application/json:
              examples:
                low_risk:
                  summary: Dirección limpia
                  value:
                    screening_id: 5f0b1c9a-2f3e-4a7b-9c1d-8e6f5a4b3c2d
                    account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                    address: TN2BBWc9EF8MMB6i1c4HZHXAssTEXstMDo
                    chain: tron
                    risk: Low
                    screening_fee: '0.500000'
                    fee_asset: USDT
                    idempotency_key: scan-cliente-742-1
                    created_at: '2026-07-12T14:30:00Z'
                    assessment:
                      address: TN2BBWc9EF8MMB6i1c4HZHXAssTEXstMDo
                      risk: Low
                      address_identifications: []
                      exposures:
                      - category: exchange
                        value_usd: '1250.75'
                      triggers: []
                severe_risk:
                  summary: Dirección sancionada
                  value:
                    screening_id: 7a2c4e6f-8b1d-4c3a-9e5f-1a2b3c4d5e6f
                    account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                    address: '0x098B716B8Aaf21512996dC57EB0615e2383E2f96'
                    chain: eth
                    risk: Severe
                    risk_reason: Identified as Sanctioned Entity
                    screening_fee: '0.500000'
                    fee_asset: USDT
                    idempotency_key: scan-sospechosa-9
                    created_at: '2026-07-12T14:31:00Z'
                    assessment:
                      address: '0x098B716B8Aaf21512996dC57EB0615e2383E2f96'
                      risk: Severe
                      risk_reason: Identified as Sanctioned Entity
                      cluster_name: OFAC SDN Ronin Bridge Exploiter
                      cluster_category: sanctioned entity
                      address_identifications:
                      - name: 'SANCTIONS: OFAC SDN Ronin Bridge Exploiter'
                        category: sanctioned entity
                      exposures: []
                      triggers: []
        '200':
          description: Replay idempotente — devuelve el screening original sin cobrar de nuevo.
          content:
            application/json:
              example:
                screening_id: 5f0b1c9a-2f3e-4a7b-9c1d-8e6f5a4b3c2d
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                address: TN2BBWc9EF8MMB6i1c4HZHXAssTEXstMDo
                chain: tron
                risk: Low
                screening_fee: '0.500000'
                fee_asset: USDT
                idempotency_key: scan-cliente-742-1
                created_at: '2026-07-12T14:30:00Z'
                idempotency_hit: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          description: La dirección no pudo evaluarse (revisa el formato).
          content:
            application/json:
              example:
                error: invalid_address
                message: the address could not be screened (check the format)
        '502':
          description: Screening temporalmente no disponible (la comisión se reembolsó). Reintenta con
            la MISMA clave.
          content:
            application/json:
              example:
                error: screening_unavailable
                message: address screening temporarily unavailable
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Screening de wallets
      operationId: listAddressScreenings
      summary: Listar screenings de direcciones
      description: Historial de screenings de la cuenta, del más reciente al más antiguo. Exige el rango
        from/to; soporta paginación y filtro por riesgo.
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
        description: Fecha inicial YYYY-MM-DD (zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
        description: Fecha final YYYY-MM-DD (zona horaria de la organización, inclusive).
      - name: risk
        in: query
        schema:
          type: string
        description: Filtrar por nivel de riesgo (low, medium, high, severe).
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Página de screenings.
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                screenings:
                - screening_id: 7a2c4e6f-8b1d-4c3a-9e5f-1a2b3c4d5e6f
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  address: '0x098B716B8Aaf21512996dC57EB0615e2383E2f96'
                  chain: eth
                  risk: Severe
                  risk_reason: Identified as Sanctioned Entity
                  screening_fee: '0.500000'
                  fee_asset: USDT
                  idempotency_key: scan-sospechosa-9
                  created_at: '2026-07-12T14:31:00Z'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/screenings/addresses/{screeningID}:
    get:
      tags:
      - Screening de wallets
      operationId: getAddressScreening
      summary: Obtener un screening de dirección
      description: Detalle de un screening, incluido el assessment completo guardado. Un id de otra cuenta
        responde 404.
      parameters:
      - name: screeningID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: El screening.
          content:
            application/json:
              example:
                screening_id: 7a2c4e6f-8b1d-4c3a-9e5f-1a2b3c4d5e6f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                address: '0x098B716B8Aaf21512996dC57EB0615e2383E2f96'
                chain: eth
                risk: Severe
                risk_reason: Identified as Sanctioned Entity
                screening_fee: '0.500000'
                fee_asset: USDT
                idempotency_key: scan-sospechosa-9
                created_at: '2026-07-12T14:31:00Z'
                assessment:
                  address: '0x098B716B8Aaf21512996dC57EB0615e2383E2f96'
                  risk: Severe
                  risk_reason: Identified as Sanctioned Entity
                  cluster_name: OFAC SDN Ronin Bridge Exploiter
                  cluster_category: sanctioned entity
                  address_identifications:
                  - name: 'SANCTIONS: OFAC SDN Ronin Bridge Exploiter'
                    category: sanctioned entity
                  exposures: []
                  triggers: []
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/links:
    post:
      tags:
      - KYC / KYB
      operationId: createKycLink
      summary: Crear un link KYC para un cliente
      description: 'Solo cuentas empresa: genera un link KYC hosteado para uno de TUS clientes finales.
        El wizard cubre formulario, documentos y prueba de vida en video. Cobra el fee fijo `kyc_verification`
        cuando está configurado (se reembolsa si la creación falla); `idempotency_key` es obligatoria
        — un retry con la misma clave devuelve el link original y nunca cobra dos veces.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationLinkRequest'
            example:
              external_customer_id: cust_123
              label: Ana Perez
              expires_in_days: 14
              idempotency_key: kyc-link-cust-123-1
      responses:
        '201':
          description: Link KYC creado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationLink'
              example:
                link_id: b2c3d4e5-f6a7-4b8c-9d0e-1f2a3b4c5d6e
                kind: kyc
                external_customer_id: cust_123
                url: https://verify.example.com/on/usd/individual/new?invite=def456
                status: pending
                label: Ana Perez
                expires_at: 1721209600
                verification_fee: '1.500000'
                idempotency_key: kyc-link-cust-123-1
                created_at: '2026-07-10T12:00:00Z'
                updated_at: '2026-07-10T12:00:00Z'
        '400':
          description: idempotency_key_required / invalid_payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: verification_required / company_account_required / service_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: company_account_required
                message: third-party KYC/KYB verification is available to company accounts only
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKycLinks
      summary: Listar links KYC
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - opened
          - completed
          - expired
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      responses:
        '200':
          description: Links KYC paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationLink'
              example:
                page: 1
                page_size: 50
                links:
                - link_id: b2c3d4e5-f6a7-4b8c-9d0e-1f2a3b4c5d6e
                  kind: kyc
                  external_customer_id: cust_123
                  url: https://verify.example.com/on/usd/individual/new?invite=def456
                  status: completed
                  created_at: '2026-07-10T12:00:00Z'
                  updated_at: '2026-07-11T09:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/links/{linkID}:
    get:
      tags:
      - KYC / KYB
      operationId: getKycLink
      summary: Consultar un link KYC
      parameters:
      - name: linkID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El link con su estado vivo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationLink'
              example:
                link_id: b2c3d4e5-f6a7-4b8c-9d0e-1f2a3b4c5d6e
                kind: kyc
                external_customer_id: cust_123
                url: https://verify.example.com/on/usd/individual/new?invite=def456
                status: completed
                submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                created_at: '2026-07-10T12:00:00Z'
                updated_at: '2026-07-11T09:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/links:
    post:
      tags:
      - KYC / KYB
      operationId: createKybLink
      summary: Crear un link KYB para un cliente
      description: 'Solo cuentas empresa: genera un link KYB hosteado para uno de TUS clientes finales
        (empresas). Cobra el fee fijo `kyb_verification` cuando está configurado (se reembolsa si la creación
        falla); `idempotency_key` obligatoria. `country` elige el corredor del wizard: us, cl, ve, br,
        mx, co, pe, bo, py, ar o generic (con `generic_country` ISO alpha-2 y `currency` opcional).'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationLinkRequest'
            example:
              external_customer_id: cust_456
              country: cl
              label: Comercial Andina SpA
              expires_in_days: 14
              idempotency_key: kyb-link-cust-456-1
      responses:
        '201':
          description: Link KYB creado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationLink'
              example:
                link_id: d4e5f6a7-b8c9-4d0e-1f2a-3b4c5d6e7f80
                kind: kyb
                external_customer_id: cust_456
                url: https://verify.example.com/on/cl/business/new?invite=ghi789
                status: pending
                country: cl
                label: Comercial Andina SpA
                expires_at: 1721209600
                verification_fee: '2.000000'
                idempotency_key: kyb-link-cust-456-1
                created_at: '2026-07-10T12:00:00Z'
                updated_at: '2026-07-10T12:00:00Z'
        '400':
          description: idempotency_key_required / invalid_payload / invalid_country
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: verification_required / company_account_required / service_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: company_account_required
                message: third-party KYC/KYB verification is available to company accounts only
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKybLinks
      summary: Listar links KYB
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - opened
          - completed
          - expired
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Links KYB paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  links:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationLink'
              example:
                page: 1
                page_size: 50
                links:
                - link_id: d4e5f6a7-b8c9-4d0e-1f2a-3b4c5d6e7f80
                  kind: kyb
                  external_customer_id: cust_456
                  url: https://verify.example.com/on/cl/business/new?invite=ghi789
                  status: pending
                  country: cl
                  created_at: '2026-07-10T12:00:00Z'
                  updated_at: '2026-07-10T12:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/links/{linkID}:
    get:
      tags:
      - KYC / KYB
      operationId: getKybLink
      summary: Consultar un link KYB
      parameters:
      - name: linkID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El link con su estado vivo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationLink'
              example:
                link_id: d4e5f6a7-b8c9-4d0e-1f2a-3b4c5d6e7f80
                kind: kyb
                external_customer_id: cust_456
                url: https://verify.example.com/on/cl/business/new?invite=ghi789
                status: opened
                country: cl
                created_at: '2026-07-10T12:00:00Z'
                updated_at: '2026-07-10T15:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions:
    post:
      tags:
      - KYC / KYB
      operationId: createKycSubmission
      summary: Crear una submission KYC con datos por API
      description: 'Solo cuentas empresa: verifica a uno de TUS clientes finales enviando los datos directo
        (sin wizard). Países en ISO alpha-3, fechas YYYY-MM-DD, id_type passport | id_card | drivers_license.
        Los documentos son opcionales al crear (súbelos con el flujo presign); no se exige prueba de vida
        al crear — la submission queda con liveness_pending: true y se cierra con un liveness link. Re-enviar
        con el mismo external_customer_id mientras la submission siga abierta la actualiza sin cobrar
        de nuevo. Cobra el fee fijo `kyc_verification`; `idempotency_key` obligatoria.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - external_customer_id
              - idempotency_key
              properties:
                external_customer_id:
                  type: string
                  description: Tu referencia del cliente verificado.
                idempotency_key:
                  type: string
                person:
                  type: object
                  description: Campos de la persona (first_name, last_name, email, phone, nationality,
                    date_of_birth, tax_id, id_type, id_number, address, primary_purpose, most_recent_occupation,
                    source_of_funds).
                metadata:
                  type: object
            example:
              external_customer_id: cust_789
              idempotency_key: kyc-sub-cust-789-1
              person:
                first_name: Ana
                last_name: Perez
                email: ana@example.com
                phone: '+56912345678'
                nationality: CHL
                date_of_birth: '1990-04-12'
                tax_id: 12.345.678-5
                id_type: id_card
                id_number: '12345678'
                address:
                  line1: Av. Siempre Viva 123
                  city: Santiago
                  state: RM
                  postal_code: '8320000'
                  country: CHL
                primary_purpose: personal_or_living_expenses
                most_recent_occupation: Engineer
                source_of_funds: salary
      responses:
        '201':
          description: Submission creada (entra a la cola de revisión de compliance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationSubmission'
              example:
                submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                kind: kyc
                external_customer_id: cust_789
                status: pending_review
                liveness_pending: true
                verification_fee: '1.500000'
                idempotency_key: kyc-sub-cust-789-1
                created_at: '2026-07-10T12:05:00Z'
                updated_at: '2026-07-10T12:05:00Z'
        '400':
          description: idempotency_key_required / invalid_payload / missing_person
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_payload
                message: external_customer_id is required (your reference for the customer being verified)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: verification_required / company_account_required / service_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: company_account_required
                message: third-party KYC/KYB verification is available to company accounts only
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKycSubmissions
      summary: Listar submissions KYC
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending_review
          - in_review
          - changes_requested
          - more_info_required
          - escalated
          - approved
          - approved_partial
          - rejected
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Submissions KYC paginadas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  submissions:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationSubmission'
              example:
                page: 1
                page_size: 50
                submissions:
                - submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                  kind: kyc
                  external_customer_id: cust_789
                  status: approved
                  risk_band: low
                  liveness_pending: false
                  created_at: '2026-07-10T12:05:00Z'
                  updated_at: '2026-07-12T10:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions/{submissionID}:
    get:
      tags:
      - KYC / KYB
      operationId: getKycSubmission
      summary: Consultar una submission KYC
      description: 'Estado agregado sin datos sensibles internos: status, risk_band, pending_documents
        (documentos que compliance pidió), rejection_reason, changes_requested_comments, liveness_pending
        y documents_received.'
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: La submission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationSubmission'
              example:
                submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                kind: kyc
                external_customer_id: cust_789
                status: more_info_required
                risk_band: medium
                liveness_pending: false
                pending_documents:
                - proofOfResidence
                documents_gate:
                  ok: false
                  matched: 2
                  total: 10
                  unresolved:
                  - category: proofOfResidence
                    effective_outcome: REVIEW
                created_at: '2026-07-10T12:05:00Z'
                updated_at: '2026-07-11T16:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions:
    post:
      tags:
      - KYC / KYB
      operationId: createKybSubmission
      summary: Crear una submission KYB con datos por API
      description: 'Solo cuentas empresa: verifica a uno de TUS clientes finales (una empresa) enviando
        los datos directo. Body: external_customer_id, country?, business (los campos de la empresa),
        ubos?, directors?, signers?, bank_info?, metadata?. Cobra el fee fijo `kyb_verification`; `idempotency_key`
        obligatoria. Re-enviar mientras siga abierta actualiza la misma submission sin cobrar de nuevo.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - external_customer_id
              - idempotency_key
              properties:
                external_customer_id:
                  type: string
                idempotency_key:
                  type: string
                country:
                  type: string
                business:
                  type: object
                  description: Campos de la empresa (razón social, tax id, domicilio, giro...).
                ubos:
                  type: array
                  items:
                    type: object
                directors:
                  type: array
                  items:
                    type: object
                metadata:
                  type: object
            example:
              external_customer_id: cust_456
              idempotency_key: kyb-sub-cust-456-1
              country: cl
              business:
                legal_name: Comercial Andina SpA
                tax_id: 76.543.210-3
              ubos:
              - first_name: Carlos
                last_name: Soto
                ownership_percent: 60
      responses:
        '201':
          description: Submission creada (entra a la cola de revisión de compliance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationSubmission'
              example:
                submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
                kind: kyb
                external_customer_id: cust_456
                status: pending_review
                verification_fee: '2.000000'
                idempotency_key: kyb-sub-cust-456-1
                created_at: '2026-07-10T12:10:00Z'
                updated_at: '2026-07-10T12:10:00Z'
        '400':
          description: idempotency_key_required / invalid_payload / invalid_country
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: verification_required / company_account_required / service_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: company_account_required
                message: third-party KYC/KYB verification is available to company accounts only
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKybSubmissions
      summary: Listar submissions KYB
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending_review
          - in_review
          - changes_requested
          - more_info_required
          - escalated
          - approved
          - approved_partial
          - rejected
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Submissions KYB paginadas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  submissions:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationSubmission'
              example:
                page: 1
                page_size: 50
                submissions:
                - submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
                  kind: kyb
                  external_customer_id: cust_456
                  status: in_review
                  created_at: '2026-07-10T12:10:00Z'
                  updated_at: '2026-07-10T14:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions/{submissionID}:
    get:
      tags:
      - KYC / KYB
      operationId: getKybSubmission
      summary: Consultar una submission KYB
      description: 'Estado agregado: status, risk_band, pending_documents, rejection_reason, changes_requested_comments
        y aml_decision.'
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: La submission.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerificationSubmission'
              example:
                submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
                kind: kyb
                external_customer_id: cust_456
                status: approved
                risk_band: low
                aml_decision: approved
                documents_gate:
                  ok: true
                  matched: 10
                  total: 10
                  unresolved: []
                created_at: '2026-07-10T12:10:00Z'
                updated_at: '2026-07-13T09:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions/{submissionID}/documents:
    post:
      tags:
      - KYC / KYB
      operationId: presignKycDocument
      summary: Presign de un documento KYC
      description: 'Paso 1 del flujo de documentos: devuelve una URL de subida temporal. Luego PUT del
        binario a upload_url con el mismo Content-Type (paso 2) y confirm (paso 3). Categorías: identity,
        proofOfResidence. Tipos: application/pdf, image/png, image/jpeg; máximo 15 MB; la URL vence en
        15 minutos.'
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationDocumentPresignRequest'
            example:
              category: identity
              filename: cedula.jpg
              content_type: image/jpeg
              file_size: 482133
      responses:
        '200':
          description: URL de subida temporal.
          content:
            application/json:
              schema:
                type: object
                properties:
                  upload_url:
                    type: string
                  key:
                    type: string
                  expires_in:
                    type: integer
              example:
                upload_url: https://storage.example.com/presigned/abc
                key: public-api/2026/07/10/cedula.jpg
                expires_in: 900
        '400':
          description: invalid_category / invalid_payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_category
                message: category must be one of identity, proofOfResidence
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKycDocuments
      summary: Resultados OCR de documentos KYC
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Resultado OCR por documento subido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationDocumentValidation'
              example:
                items:
                - id: 7a1c2d3e-4f5a-4b6c-8d9e-0f1a2b3c4d5e
                  category: identity
                  status: completed
                  outcome: MATCH
                  effective_outcome: MATCH
                  score: 0.97
                  summary: Document matches the submitted identity
                  filename: cedula.jpg
                meta:
                  retrieved: 1
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions/{submissionID}/documents/confirm:
    post:
      tags:
      - KYC / KYB
      operationId: confirmKycDocument
      summary: Confirmar un documento KYC subido
      description: Paso 3 del flujo de documentos. Adjunta el archivo subido a la submission y encola
        su validación OCR; el resultado llega por el webhook kyc_document_validated.
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationDocumentConfirmRequest'
            example:
              key: public-api/2026/07/10/cedula.jpg
              category: identity
              filename: cedula.jpg
              content_type: image/jpeg
      responses:
        '200':
          description: Documento recibido; OCR encolado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  ocr:
                    type: string
              example:
                status: received
                ocr: queued
        '400':
          description: invalid_key / invalid_category
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_key
                message: the upload key does not match a presigned upload
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions/{submissionID}/documents:
    post:
      tags:
      - KYC / KYB
      operationId: presignKybDocument
      summary: Presign de un documento KYB
      description: 'Mismo flujo de 3 pasos que los documentos KYC. Categorías: legalPresence, ownershipStructure,
        controlStructure, companyDetails.'
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationDocumentPresignRequest'
            example:
              category: legalPresence
              filename: vigencia.pdf
              content_type: application/pdf
              file_size: 1204833
      responses:
        '200':
          description: URL de subida temporal.
          content:
            application/json:
              schema:
                type: object
                properties:
                  upload_url:
                    type: string
                  key:
                    type: string
                  expires_in:
                    type: integer
              example:
                upload_url: https://storage.example.com/presigned/def
                key: public-api/2026/07/10/vigencia.pdf
                expires_in: 900
        '400':
          description: invalid_category / invalid_payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_category
                message: category must be one of legalPresence, ownershipStructure, controlStructure,
                  companyDetails
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: listKybDocuments
      summary: Resultados OCR de documentos KYB
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Resultado OCR por documento subido.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/VerificationDocumentValidation'
              example:
                items:
                - id: 7a1c2d3e-4f5a-4b6c-8d9e-0f1a2b3c4d5e
                  category: legalPresence
                  status: completed
                  outcome: REVIEW
                  effective_outcome: REVIEW
                  score: 0.72
                  summary: Registry extract requires manual review
                  filename: vigencia.pdf
                meta:
                  retrieved: 1
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions/{submissionID}/documents/confirm:
    post:
      tags:
      - KYC / KYB
      operationId: confirmKybDocument
      summary: Confirmar un documento KYB subido
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerificationDocumentConfirmRequest'
            example:
              key: public-api/2026/07/10/vigencia.pdf
              category: legalPresence
              filename: vigencia.pdf
              content_type: application/pdf
      responses:
        '200':
          description: Documento recibido; OCR encolado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  ocr:
                    type: string
              example:
                status: received
                ocr: queued
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions/{submissionID}/liveness_link:
    post:
      tags:
      - KYC / KYB
      operationId: createKycLivenessLink
      summary: Crear un liveness link
      description: Página hosteada donde tu cliente completa SOLO la prueba de vida en video (gestos frente
        a la cámara + face match server-side contra el documento de identidad subido). Gratis — el servicio
        se cobró al crear la submission. Si hay un link vigente se devuelve el mismo; si la prueba ya
        fue superada, 400 liveness_already_completed.
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                expires_in_days:
                  type: integer
                  description: 1-30, default 7.
            example:
              expires_in_days: 7
      responses:
        '200':
          description: Liveness link.
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                  status:
                    type: string
                  expires_at:
                    type: integer
              example:
                url: https://verify.example.com/on/liveness/tok_abc123
                status: pending
                expires_at: 1751234567
        '400':
          description: liveness_already_completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: liveness_already_completed
                message: the liveness check already passed for this submission
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - KYC / KYB
      operationId: getKycLivenessLink
      summary: Consultar el liveness link y su estado
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Último link más el estado actual del check.
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                  status:
                    type: string
                  liveness:
                    type: object
                    properties:
                      status:
                        type: string
                      outcome:
                        type: string
                      passed:
                        type: boolean
              example:
                url: https://verify.example.com/on/liveness/tok_abc123
                status: completed
                liveness:
                  status: completed
                  outcome: PASS
                  passed: true
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyc/submissions/{submissionID}/verification-report:
    get:
      tags:
      - KYC / KYB
      operationId: getKycVerificationReport
      summary: Informe de verificación de un KYC tuyo (PDF/JSON)
      description: >-
        Informe de compliance COMPLETO de una verificación que corriste sobre tu cliente (solo cuentas
        empresa): perfil verificado, ciclo de vida, documentos + OCR, liveness (una entrada por sesión -
        el check de onboarding más cualquier recaptura de evidencia posterior, cada una con su propio
        session_id y purpose; en un KYB, cada parte relacionada puede tener más de una sesión también) y
        la sección AML COMPLETA con detalle de coincidencias - eres el responsable de los datos de tus
        terceros. Si la submission resulta ser tu propio onboarding, la sección AML baja a la versión
        agregada. Usa format=pdf (default) o format=json, y lang=en|es|zh. Gratis - es lectura de una
        verificación ya cobrada. Si la submission aún no tiene screening AML enlazado, se dispara uno
        on-demand (idempotente, sin fee); si no está disponible, el informe sale igual con el hueco
        declarado en el campo partial.
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Id de la submission.
      - name: format
        in: query
        schema:
          type: string
          enum:
          - pdf
          - json
        description: Formato del informe - pdf (default) o json.
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - en
          - es
          - zh
        description: Idioma del informe (en default, es, zh).
      responses:
        '200':
          description: El informe de verificación (application/pdf, o application/json con format=json).
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
            application/json:
              example:
                report_id: IDR-3C4D5E6F7A8B
                kind: kyc
                scope: third_party
                generated_at: '2026-07-28T15:04:05Z'
                generated_by: CBPay
                language: en
                aml_detail: true
                submission_id: 3c4d5e6f-7a8b-4c9d-8e0f-1a2b3c4d5e6f
                external_customer_id: customer-1042
                status: approved
                risk_band: medium
                aml_decision: approved
                submitted_at: '2026-07-19T14:00:00Z'
                updated_at: '2026-07-19T14:52:00Z'
                decided_at: '2026-07-19T14:52:00Z'
                documents_received:
                - identity
                - proofOfAddress
                subject:
                  type: person
                  name: Maria Fernanda Rojas
                  person:
                    first_name: Maria
                    middle_name: Fernanda
                    last_name: Rojas
                    date_of_birth: '1988-11-02'
                    country_of_birth: PE
                    nationality: PE
                    tax_residence_country: PE
                    id_type: national_id
                    id_number: '45678912'
                    id_issuing_country: PE
                    id_expires_at: '2029-05-20'
                    occupation: Regional sales manager
                    email: maria.rojas@example.com
                    phone: +51 9 8765 4321
                  address:
                    line1: Av. Arequipa 1200
                    city: Lima
                    state: Lima
                    postal_code: '15046'
                    country: PE
                  economic_profile:
                    source_of_funds: salary
                    primary_purpose: personal_payments
                    monthly_payments_usd: '5000'
                    monthly_transaction_count: '20'
                    employment_status: employed
                    employer_name: Andes Retail SAC
                    expected_chains:
                    - tron
                  attestations:
                  - key: att_money_services
                    value: false
                  - key: att_prohibited_countries
                    value: false
                  bank_account:
                    bank_name: BCP
                    account_holder: Maria Fernanda Rojas
                    account_masked: '****9911'
                    country: PE
                    currency: PEN
                    third_party_account: false
                documents:
                - category: identity
                  filename: dni.pdf
                  status: validated
                  outcome: MATCH
                  score: '0.99'
                  summary: Name, document number and date of birth match the verified profile.
                  validated_at: '2026-07-19T14:40:00Z'
                - category: proofOfAddress
                  filename: recibo-luz.pdf
                  status: validated
                  outcome: REVIEW
                  score: '0.72'
                  error: Address line partially legible.
                  validated_at: '2026-07-19T14:41:00Z'
                liveness:
                - role: subject
                  session_id: lv_e763e3465bf34f1dab826a263c1eaaaa
                  purpose: gate
                  status: completed
                  outcome: PASS
                  passed_gate: true
                  liveness_score: '0.98'
                  antispoof_score: '0.96'
                  face_similarity: '0.94'
                  gestures_expected: 3
                  gestures_passed: 3
                  updated_at: '2026-07-19T14:35:00Z'
                  media:
                    has_selfie: true
                    has_video: false
                    expires_in_sec: 900
                    selfie_sha256: 6f8a3b1c2d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e
                - role: subject
                  session_id: lv_4cdd3a82903940cebd8cc95a77cdacb3
                  purpose: media_recapture
                  status: completed
                  outcome: FAIL
                  passed_gate: false
                  liveness_score: '0.98'
                  antispoof_score: '0.59'
                  face_similarity: '0.55'
                  gestures_expected: 3
                  gestures_passed: 2
                  reasons:
                  - Smile was not detected.
                  updated_at: '2026-07-19T14:38:00Z'
                  media:
                    has_selfie: true
                    has_video: true
                    frame_gestures:
                    - center
                    - turn_right
                    - smile
                    video_mime_type: video/mp4
                    expires_in_sec: 900
                    selfie_sha256: a1b2c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9
                    frame_sha256:
                    - c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2
                    - d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2c3
                aml:
                  screening_id: 6b7c8d9e-0f1a-4b2c-9d3e-4f5a6b7c8d9e
                  risk_level: medium
                  status: under_review
                  monitor: true
                  screened_at: '2026-07-19T14:50:00Z'
                  sanctions: clear
                  pep: under_review
                  adverse_media: under_review
                  screening_result: has_hits
                  rationale: One PEP profile and one adverse media article matched the declared identity.
                  indicators:
                  - key: ind_sanctions
                    hit: false
                  - key: ind_pep
                    hit: true
                  - key: ind_adverse_media
                    hit: true
                  - key: ind_warning
                    hit: false
                  - key: ind_fitness_probity
                    hit: false
                  subject_rows:
                  - key: full_name
                    value: Maria Fernanda Rojas
                  - key: date_of_birth
                    value: '1988-11-02'
                  - key: nationality
                    value: PE
                  matches_total: 1
                  matches:
                  - name: Maria F. Rojas
                    score: 0.88
                    match_types:
                    - name_fuzzy
                    - year_of_birth
                    risk_kinds:
                    - pep
                    - adverse_media
                    aliases:
                    - M. Rojas
                    - Maria Rojas Vega
                    identity:
                    - key: date_of_birth
                      value: '1988'
                    - key: nationality
                      value: PE
                    - key: national_id
                      value: 4567****
                    peps:
                    - name: Advisor, regional government
                      country: PE
                      since: '2018'
                      until: '2021'
                      info: Non-elected advisory role.
                    adverse_media:
                    - title: Local outlet reports contract review
                      published_at: '2020-09-14'
                      source: Diario Local
                      snippet: Regional contract under administrative review.
                    associates:
                    - name: Luis Rojas
                      relation: relative
                  cases:
                  - case_id: CASE-4821
                    status: closed
                    decision: no_action
                    opened_at: '2026-07-19T14:51:00Z'
                    closed_at: '2026-07-19T15:20:00Z'
                content_sha256: 1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2c
                verification_code: K3c4d5e6f7a8b4c9d8e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5
                verification_url: https://api.example.com/verify/reports/K3c4d5e6f7a8b4c9d8e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5
        '400':
          description: Formato o idioma inválido (invalid_format, invalid_language).
          content:
            application/json:
              example:
                error: invalid_format
                message: format must be pdf or json
        '403':
          description: Solo cuentas empresa pueden descargar informes de sus terceros (company_account_required).
          content:
            application/json:
              example:
                error: company_account_required
                message: this action requires a company account
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions/{submissionID}/verification-report:
    get:
      tags:
      - KYC / KYB
      operationId: getKybVerificationReport
      summary: Informe de verificación de un KYB tuyo (PDF/JSON)
      description: >-
        Informe de compliance COMPLETO de una verificación que corriste sobre tu cliente (solo cuentas
        empresa): perfil verificado, ciclo de vida, documentos + OCR, liveness (una entrada por sesión -
        el check de onboarding más cualquier recaptura de evidencia posterior, cada una con su propio
        session_id y purpose; en un KYB, cada parte relacionada puede tener más de una sesión también) y
        la sección AML COMPLETA con detalle de coincidencias - eres el responsable de los datos de tus
        terceros. Si la submission resulta ser tu propio onboarding, la sección AML baja a la versión
        agregada. Usa format=pdf (default) o format=json, y lang=en|es|zh. Gratis - es lectura de una
        verificación ya cobrada. Si la submission aún no tiene screening AML enlazado, se dispara uno
        on-demand (idempotente, sin fee); si no está disponible, el informe sale igual con el hueco
        declarado en el campo partial.
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: Id de la submission.
      - name: format
        in: query
        schema:
          type: string
          enum:
          - pdf
          - json
        description: Formato del informe - pdf (default) o json.
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - en
          - es
          - zh
        description: Idioma del informe (en default, es, zh).
      responses:
        '200':
          description: El informe de verificación (application/pdf, o application/json con format=json).
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
            application/json:
              example:
                report_id: IDR-9B2C41A0D3E5
                kind: kyb
                scope: third_party
                generated_at: '2026-07-28T15:04:05Z'
                generated_by: CBPay
                language: en
                aml_detail: true
                submission_id: 9b2c41a0-d3e5-4f6a-8b7c-1d2e3f4a5b6c
                status: approved
                risk_band: low
                aml_decision: approved
                submitted_at: '2026-07-20T12:30:00Z'
                updated_at: '2026-07-21T09:15:00Z'
                decided_at: '2026-07-21T09:15:00Z'
                documents_received:
                - certificateOfIncorporation
                - proofOfAddress
                - uboIdentity:0
                subject:
                  type: company
                  name: JVA SpA
                  company:
                    legal_name: JVA SpA
                    trade_name: JVA
                    registration_number: 77123456-7
                    business_type: corporation
                    company_status: active
                    incorporation_date: '2019-04-12'
                    incorporation_country: CHL
                    website: https://jva.example
                    countries_of_operation:
                    - CHL
                    - PER
                  address:
                    line1: Av. Apoquindo 1234
                    city: Santiago
                    state: RM
                    postal_code: '7550000'
                    country: CL
                  registered_address:
                    line1: Av. Apoquindo 1234
                    city: Santiago
                    country: CL
                  industry:
                    code: K64
                    label: Financial service activities
                  economic_profile:
                    source_of_funds: business_revenue
                    primary_purpose: payments
                    annual_revenue_usd: '250000'
                    monthly_payments_usd: '40000'
                    monthly_transaction_count: '120'
                    average_transaction_usd: '330'
                    expected_chains:
                    - tron
                    - ethereum
                    customer_funds_description: Collections from merchants in CL and PE.
                  attestations:
                  - key: att_money_services
                    value: false
                  - key: att_third_party_funds
                    value: true
                    detail: Settlement on behalf of merchants.
                  - key: att_high_risk_activities
                    items:
                    - none
                  bank_account:
                    bank_name: Banco de Chile
                    account_holder: JVA SpA
                    account_masked: '****4321'
                    country: CL
                    currency: CLP
                    third_party_account: false
                  fields:
                    operatingName: JVA Payments
                parties:
                - source: ubo
                  index: 0
                  kind: ubo
                  roles:
                  - beneficial_owner
                  - control_person
                  name: Javier Andres Villablanca
                  person:
                    first_name: Javier
                    middle_name: Andres
                    last_name: Villablanca
                    date_of_birth: '1985-03-04'
                    nationality: CL
                    tax_residence_country: CL
                    id_type: national_id
                    id_number: 12345678-9
                    id_issuing_country: CL
                    id_expires_at: '2030-01-31'
                    occupation: Director
                  address:
                    line1: Av. Siempre Viva 742
                    city: Santiago
                    state: RM
                    country: CL
                  ownership_percent: '60'
                  has_ownership: true
                  has_control: true
                  is_signer: false
                  documents:
                  - category: uboIdentity:0
                    filename: cedula.pdf
                    status: validated
                    outcome: MATCH
                    score: '0.97'
                    validated_at: '2026-07-21T09:12:00Z'
                    party_index: 0
                  liveness:
                    role: ubo:0
                    session_id: lv_e763e3465bf34f1dab826a263c1eaaaa
                    purpose: gate
                    status: completed
                    outcome: PASS
                    passed_gate: true
                    liveness_score: '0.99'
                    antispoof_score: '0.97'
                    face_similarity: '0.95'
                    gestures_expected: 3
                    gestures_passed: 3
                    updated_at: '2026-07-20T09:05:00Z'
                    media:
                      has_selfie: true
                      has_video: false
                      expires_in_sec: 900
                      selfie_sha256: 6f8a3b1c2d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e
                  liveness_sessions:
                  - role: ubo:0
                    session_id: lv_e763e3465bf34f1dab826a263c1eaaaa
                    purpose: gate
                    status: completed
                    outcome: PASS
                    passed_gate: true
                    liveness_score: '0.99'
                    antispoof_score: '0.97'
                    face_similarity: '0.95'
                    gestures_expected: 3
                    gestures_passed: 3
                    updated_at: '2026-07-20T09:05:00Z'
                    media:
                      has_selfie: true
                      has_video: false
                      expires_in_sec: 900
                      selfie_sha256: 6f8a3b1c2d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e
                  - role: ubo:0
                    session_id: lv_4cdd3a82903940cebd8cc95a77cdacb3
                    purpose: media_recapture
                    status: completed
                    outcome: FAIL
                    passed_gate: false
                    liveness_score: '0.98'
                    antispoof_score: '0.59'
                    face_similarity: '0.55'
                    gestures_expected: 3
                    gestures_passed: 2
                    reasons:
                    - Smile was not detected.
                    updated_at: '2026-07-31T22:29:32Z'
                    media:
                      has_selfie: true
                      has_video: true
                      frame_gestures:
                      - center
                      - turn_right
                      - smile
                      video_mime_type: video/mp4
                      expires_in_sec: 900
                      selfie_sha256: a1b2c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9
                      frame_sha256:
                      - c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2
                      - d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2c3
                  aml:
                    screening_id: 7c1d2e3f-4a5b-4c6d-8e9f-0a1b2c3d4e5f
                    risk_level: medium
                    status: under_review
                    monitor: true
                    screened_at: '2026-07-21T09:13:00Z'
                    sanctions: clear
                    pep: under_review
                    adverse_media: clear
                    screening_result: has_hits
                    indicators:
                    - key: ind_sanctions
                      hit: false
                    - key: ind_pep
                      hit: true
                    - key: ind_adverse_media
                      hit: false
                    matches_total: 1
                    matches:
                    - name: Javier Villablanca
                      score: 0.91
                      match_types:
                      - name_exact
                      risk_kinds:
                      - pep
                      aliases:
                      - J. Villablanca
                      identity:
                      - key: date_of_birth
                        value: '1985-03-04'
                      - key: nationality
                        value: CL
                      peps:
                      - name: Municipal council member
                        country: CL
                        since: '2016'
                        until: '2020'
                        info: Former local office holder.
                      associates:
                      - name: Ana Soto
                        relation: business_partner
                - source: signer
                  index: 0
                  kind: signer
                  name: Pedro Ruiz
                  business_title: CFO
                  is_signer: true
                  person:
                    first_name: Pedro
                    last_name: Ruiz
                  aml:
                    screening_id: 8d2e3f4a-5b6c-4d7e-9f0a-1b2c3d4e5f6a
                    risk_level: low
                    status: clear
                    monitor: true
                    sanctions: clear
                    pep: clear
                    adverse_media: clear
                    screening_result: no_hits
                    matches_total: 0
                documents:
                - category: certificateOfIncorporation
                  filename: escritura.pdf
                  status: validated
                  outcome: MATCH
                  score: '0.98'
                  summary: Company name and registration number match the verified profile.
                  validated_at: '2026-07-21T09:10:00Z'
                - category: proofOfAddress
                  filename: cartola.pdf
                  status: rejected
                  outcome: NO_MATCH
                  error: Document older than 90 days.
                  validated_at: '2026-07-21T09:11:00Z'
                liveness:
                - role: ubo:0
                  session_id: lv_e763e3465bf34f1dab826a263c1eaaaa
                  purpose: gate
                  status: completed
                  outcome: PASS
                  passed_gate: true
                  liveness_score: '0.99'
                  antispoof_score: '0.97'
                  face_similarity: '0.95'
                  gestures_expected: 3
                  gestures_passed: 3
                  updated_at: '2026-07-20T09:05:00Z'
                  media:
                    has_selfie: true
                    has_video: false
                    expires_in_sec: 900
                    selfie_sha256: 6f8a3b1c2d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e
                - role: ubo:0
                  session_id: lv_4cdd3a82903940cebd8cc95a77cdacb3
                  purpose: media_recapture
                  status: completed
                  outcome: FAIL
                  passed_gate: false
                  liveness_score: '0.98'
                  antispoof_score: '0.59'
                  face_similarity: '0.55'
                  gestures_expected: 3
                  gestures_passed: 2
                  reasons:
                  - Smile was not detected.
                  updated_at: '2026-07-31T22:29:32Z'
                  media:
                    has_selfie: true
                    has_video: true
                    frame_gestures:
                    - center
                    - turn_right
                    - smile
                    video_mime_type: video/mp4
                    expires_in_sec: 900
                    selfie_sha256: a1b2c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9
                    frame_sha256:
                    - c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2
                    - d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f9a1b2c3
                aml:
                  screening_id: 5f0e1d2c-b4a5-4968-8776-5a4b3c2d1e0f
                  risk_level: low
                  status: clear
                  monitor: true
                  screened_at: '2026-07-21T09:14:00Z'
                  sanctions: clear
                  pep: clear
                  adverse_media: clear
                  screening_result: no_hits
                  rationale: No profiles matched the declared identity.
                  indicators:
                  - key: ind_sanctions
                    hit: false
                  - key: ind_pep
                    hit: false
                  - key: ind_adverse_media
                    hit: false
                  subject_rows:
                  - key: legal_name
                    value: JVA SpA
                  - key: registration_number
                    value: 77123456-7
                  matches_total: 0
                partial:
                - party_aml_unavailable
                content_sha256: 4f2c1de0a9b83b7d6c5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c
                verification_code: B9b2c41a0d3e54f6a8b7c1d2e3f4a5b6c0a1b2c3d4e5f6a7b8c9
                verification_url: https://api.example.com/verify/reports/B9b2c41a0d3e54f6a8b7c1d2e3f4a5b6c0a1b2c3d4e5f6a7b8c9
        '400':
          description: Formato o idioma inválido (invalid_format, invalid_language).
          content:
            application/json:
              example:
                error: invalid_format
                message: format must be pdf or json
        '403':
          description: Solo cuentas empresa pueden descargar informes de sus terceros (company_account_required).
          content:
            application/json:
              example:
                error: company_account_required
                message: this action requires a company account
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/kyb/submissions/{submissionID}/report:
    get:
      tags:
      - KYC / KYB
      operationId: getKybComplianceReport
      summary: Descargar el informe de compliance firmado (PDF)
      description: Informe de compliance firmado de una verificación KYB — evidencia para tus propios
        auditores. Gratis (el servicio se cobró al crear la verificación).
      parameters:
      - name: submissionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Informe de compliance firmado.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (contenido binario del informe)'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/contacts:
    get:
      tags:
      - Contacts
      operationId: listContacts
      summary: Listar contactos
      description: La libreta privada de tu cuenta, ordenada por favoritos y último uso. Los contactos
        se crean automáticamente con cada envío (transferencia, payout, retiro crypto), por CRUD manual
        o importando la agenda del celular.
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: q
        in: query
        schema:
          type: string
        description: Busca por nombre, alias, email o teléfono.
      - name: has_cbpay
        in: query
        schema:
          type: boolean
        description: Solo contactos enlazados (o no) a una cuenta CBPay.
      - name: favorite
        in: query
        schema:
          type: boolean
      responses:
        '200':
          description: Contactos paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  contacts:
                    type: array
                    items:
                      $ref: '#/components/schemas/Contact'
              example:
                page: 1
                page_size: 50
                contacts:
                - contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                  display_name: Carlos Soto
                  alias: Carlitos
                  phone: '+56987654321'
                  email: carlos@mail.com
                  has_cbpay: true
                  cbpay_account_id: 389d34a3-6c61-4782-b822-95ac173068dc
                  source: import
                  favorite: true
                  created_at: '2026-07-07T12:00:00Z'
                  updated_at: '2026-07-10T15:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - Contacts
      operationId: createContact
      summary: Crear un contacto
      description: Agrega un contacto a mano. El teléfono se normaliza a E.164 (los números locales usan
        el país de tu cuenta) y se matchea contra cuentas de tu operador para llenar `has_cbpay`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - display_name
              properties:
                display_name:
                  type: string
                alias:
                  type: string
                phone:
                  type: string
                email:
                  type: string
                favorite:
                  type: boolean
            example:
              display_name: Carlos Soto
              phone: '+56987654321'
              email: carlos@mail.com
              favorite: true
      responses:
        '201':
          description: Contacto creado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
              example:
                contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                display_name: Carlos Soto
                phone: '+56987654321'
                email: carlos@mail.com
                has_cbpay: true
                cbpay_account_id: 389d34a3-6c61-4782-b822-95ac173068dc
                source: manual
                favorite: true
                created_at: '2026-07-10T15:00:00Z'
                updated_at: '2026-07-10T15:00:00Z'
        '400':
          description: invalid_payload / invalid_phone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_phone
                message: phone could not be normalized to E.164 (send +<country><number>)
        '409':
          description: Ya existe un contacto con ese teléfono/email (duplicate).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: duplicate
                message: duplicate
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/contacts/import:
    post:
      tags:
      - Contacts
      operationId: importContacts
      summary: Importar la agenda del celular
      description: Sube hasta 1.000 contactos por request (pagina si hay más). Los teléfonos se normalizan
        a E.164; los contactos existentes no se duplican; `has_cbpay` te dice qué contactos ya tienen
        una cuenta activa de tu mismo operador (match por teléfono).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - contacts
              properties:
                contacts:
                  type: array
                  maxItems: 1000
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      phones:
                        type: array
                        items:
                          type: string
                      email:
                        type: string
            example:
              contacts:
              - name: Carlos Soto
                phones:
                - +56 9 8765 4321
              - name: Ana Perez
                phones:
                - '912345678'
      responses:
        '200':
          description: Resultado por contacto.
          content:
            application/json:
              schema:
                type: object
                properties:
                  imported:
                    type: integer
                  matched:
                    type: integer
                  total:
                    type: integer
                  contacts:
                    type: array
                    items:
                      type: object
              example:
                imported: 2
                matched: 1
                total: 2
                contacts:
                - name: Carlos Soto
                  phone: '+56987654321'
                  contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                  has_cbpay: true
                - name: Ana Perez
                  phone: '+56912345678'
                  contact_id: 9c1d2e3f-4a5b-6c7d-8e9f-0a1b2c3d4e5f
                  has_cbpay: false
        '400':
          description: invalid_payload / batch_too_large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: batch_too_large
                message: send up to 1000 contacts per request (paginate the upload)
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/contacts/{contactID}:
    get:
      tags:
      - Contacts
      operationId: getContact
      summary: Consultar un contacto
      description: El contacto más sus destinos guardados (cuenta CBPay, beneficiarios de payout por corredor,
        direcciones on-chain).
      parameters:
      - name: contactID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El contacto con sus destinos.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
              example:
                contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                display_name: Carlos Soto
                phone: '+56987654321'
                has_cbpay: true
                cbpay_account_id: 389d34a3-6c61-4782-b822-95ac173068dc
                source: import
                favorite: true
                destinations:
                - destination_id: bb22c3d4-5e6f-7a80-9b0c-1d2e3f4a5b6c
                  type: payout
                  country: CL
                  currency: CLP
                  method: bank_transfer
                  details:
                    name: Carlos Soto
                    tax_id: 12.345.678-5
                    bank_code: '012'
                    account_type: checking
                    account_number: '123456789'
                  last_used_at: '2026-07-09T18:30:00Z'
                  created_at: '2026-07-09T18:30:00Z'
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-10T15:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags:
      - Contacts
      operationId: updateContact
      summary: Editar un contacto
      description: Actualización parcial (nombre, alias, teléfono, email, favorito). Cambiar el teléfono
        re-ejecuta el match CBPay.
      parameters:
      - name: contactID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                alias:
                  type: string
                phone:
                  type: string
                email:
                  type: string
                favorite:
                  type: boolean
            example:
              alias: Carlitos
              favorite: true
      responses:
        '200':
          description: Contacto actualizado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Contact'
              example:
                contact_id: 3f8a1b2c-4d5e-6f70-8a9b-0c1d2e3f4a5b
                display_name: Carlos Soto
                alias: Carlitos
                phone: '+56987654321'
                has_cbpay: true
                source: import
                favorite: true
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-10T16:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    delete:
      tags:
      - Contacts
      operationId: deleteContact
      summary: Borrar un contacto
      description: Borra el contacto y sus destinos guardados.
      parameters:
      - name: contactID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Borrado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
              example:
                deleted: true
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/contacts/{contactID}/destinations:
    post:
      tags:
      - Contacts
      operationId: addContactDestination
      summary: Agregar un destino a un contacto
      description: 'Guarda un destino reutilizable a mano: `payout` (país + método + beneficiario), `crypto`
        (chain + dirección) o `cbpay` (requiere contacto enlazado). Los destinos se deduplican: repetir
        uno refresca su último uso.'
      parameters:
      - name: contactID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              properties:
                type:
                  type: string
                  enum:
                  - payout
                  - crypto
                  - cbpay
                country:
                  type: string
                currency:
                  type: string
                method:
                  type: string
                chain:
                  type: string
                address:
                  type: string
                beneficiary:
                  type: object
                  additionalProperties:
                    type: string
                label:
                  type: string
            example:
              type: payout
              country: CL
              currency: CLP
              method: bank_transfer
              beneficiary:
                name: Carlos Soto
                tax_id: 12.345.678-5
                bank_code: '012'
                account_type: checking
                account_number: '123456789'
      responses:
        '201':
          description: Destino guardado.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactDestination'
              example:
                destination_id: bb22c3d4-5e6f-7a80-9b0c-1d2e3f4a5b6c
                type: payout
                country: CL
                currency: CLP
                method: bank_transfer
                details:
                  name: Carlos Soto
                  tax_id: 12.345.678-5
                  bank_code: '012'
                  account_type: checking
                  account_number: '123456789'
                last_used_at: '2026-07-10T16:00:00Z'
                created_at: '2026-07-10T16:00:00Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/contacts/{contactID}/destinations/{destinationID}:
    delete:
      tags:
      - Contacts
      operationId: deleteContactDestination
      summary: Borrar un destino guardado
      parameters:
      - name: contactID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: destinationID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Borrado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: boolean
              example:
                deleted: true
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/swaps/quote:
    get:
      tags:
      - Swaps
      operationId: quoteSwap
      summary: Cotizar un swap
      description: 'Cotización indicativa gratis para convertir entre tus saldos USDT, USDC, BTC y GOLD
        (cualquier par, origen distinto del destino). La tasa que ves es la tasa de ejecución de tu cuenta
        — cotizado = recibido, sin comisiones aparte. Indicativa: la ejecución usa el precio del momento
        del POST (los pares entre stablecoins son estables).'
      parameters:
      - name: from_asset
        in: query
        required: true
        schema:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
      - name: to_asset
        in: query
        required: true
        schema:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
      - name: amount
        in: query
        required: true
        schema:
          type: string
        description: Monto en la moneda de origen (hasta sus decimales).
      responses:
        '200':
          description: Cotización indicativa.
          content:
            application/json:
              schema:
                type: object
                properties:
                  from_asset:
                    type: string
                  to_asset:
                    type: string
                  from_amount:
                    type: string
                  to_amount:
                    type: string
                  rate:
                    type: string
                    description: Unidades de destino por unidad de origen (tasa de ejecución marcada).
                  indicative:
                    type: boolean
              example:
                from_asset: USDT
                to_asset: BTC
                from_amount: '1000.000000'
                to_amount: '0.01568419'
                rate: '0.00001568'
                indicative: true
        '400':
          description: invalid_asset / invalid_pair / invalid_amount / amount_too_small / swap_asset_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_pair
                message: from_asset and to_asset must differ
        '503':
          description: Precio de ejecución no disponible o desactualizado (pricing_unavailable).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: pricing_unavailable
                message: execution price is unavailable or stale; retry later
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/merchants:
    get:
      tags:
      - QR Crypto POS
      operationId: listPosMerchants
      summary: "Listar merchants POS"
      description: "Listado paginado de los merchants registrados por la cuenta (procesador)."
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  merchants:
                    type: array
                    items:
                      $ref: '#/components/schemas/PosMerchant'
              example:
                page: 1
                page_size: 50
                merchants:
                - merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
                  account_id: 5138e8dd-64bd-43ef-aafe-8d9ef23bec9e
                  name: La Terraza Restaurant
                  verification_id: 9e2f41d0-6b3e-4b57-9d5c-2f2f0a97c001
                  external_ref: resto-001
                  fee_percent: '1'
                  fee_fixed: '0'
                  status: active
                  created_at: '2026-07-17T19:40:00Z'
                  updated_at: '2026-07-17T19:40:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - QR Crypto POS
      operationId: createPosMerchant
      summary: "Registrar un merchant POS"
      description: "Registra un merchant (restaurant, hotel, tienda) amarrado a una verificación KYC/KYB de terceros APROBADA — la identidad sale de la verificación. fee_percent/fee_fixed son la comisión informativa del procesador a su merchant (se calcula por cobro pagado y en el resumen; la plataforma jamás la cobra)."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - verification_id
              properties:
                verification_id:
                  type: string
                  format: uuid
                name:
                  type: string
                external_ref:
                  type: string
                fee_percent:
                  type: string
                fee_fixed:
                  type: string
            example:
              verification_id: 9e2f41d0-6b3e-4b57-9d5c-2f2f0a97c001
              name: La Terraza Restaurant
              external_ref: resto-001
              fee_percent: '1'
              fee_fixed: '0'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosMerchant'
              example:
                merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
                account_id: 5138e8dd-64bd-43ef-aafe-8d9ef23bec9e
                name: La Terraza Restaurant
                verification_id: 9e2f41d0-6b3e-4b57-9d5c-2f2f0a97c001
                external_ref: resto-001
                fee_percent: '1'
                fee_fixed: '0'
                status: active
                created_at: '2026-07-17T19:40:00Z'
                updated_at: '2026-07-17T19:40:00Z'
        '422':
          description: verification_required / verification_not_approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: verification_required
                message: an approved identity verification of the third party is required
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/merchants/{merchantID}:
    get:
      tags:
      - QR Crypto POS
      operationId: getPosMerchant
      summary: "Obtener un merchant POS"
      description: "Detalle del merchant. Solo la cuenta dueña (o el org admin) puede leerlo."
      parameters:
      - name: merchantID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosMerchant'
              example:
                merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
                account_id: 5138e8dd-64bd-43ef-aafe-8d9ef23bec9e
                name: La Terraza Restaurant
                verification_id: 9e2f41d0-6b3e-4b57-9d5c-2f2f0a97c001
                external_ref: resto-001
                fee_percent: '1'
                fee_fixed: '0'
                status: active
                created_at: '2026-07-17T19:40:00Z'
                updated_at: '2026-07-17T19:40:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags:
      - QR Crypto POS
      operationId: patchPosMerchant
      summary: "Actualizar un merchant POS"
      description: "Actualiza status (active/disabled — un merchant deshabilitado no puede generar cobros nuevos), la comisión informativa y la referencia externa. La identidad no se edita: viene de la verificación."
      parameters:
      - name: merchantID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                  - active
                  - disabled
                fee_percent:
                  type: string
                fee_fixed:
                  type: string
                external_ref:
                  type: string
            example:
              status: disabled
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosMerchant'
              example:
                merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
                account_id: 5138e8dd-64bd-43ef-aafe-8d9ef23bec9e
                name: La Terraza Restaurant
                verification_id: 9e2f41d0-6b3e-4b57-9d5c-2f2f0a97c001
                external_ref: resto-001
                fee_percent: '1'
                fee_fixed: '0'
                status: active
                created_at: '2026-07-17T19:40:00Z'
                updated_at: '2026-07-17T19:40:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/charges:
    get:
      tags:
      - QR Crypto POS
      operationId: listPosCharges
      summary: "Listar cobros POS"
      description: "Cobros de la cuenta con filtros merchant_id/status y rango de fechas obligatorio — la vista de conciliación por cliente."
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Start date (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: End date (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: merchant_id
        in: query
        schema:
          type: string
          format: uuid
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - paid
          - expired
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  charges:
                    type: array
                    items:
                      $ref: '#/components/schemas/PosCharge'
              example:
                page: 1
                page_size: 50
                charges:
                - charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                  payin_id: bd3d88ca-af9c-4c05-a6f8-0982a2d187d5
                  status: paid
                  amount: '25'
                  settlement_asset: USDT
                  crypto: tron:usdt
                  due: '25.000000'
                  received: '25.000000'
                  merchant:
                    id: d2875683-fc80-4c7b-876a-fb585d7c6982
                    name: La Terraza Restaurant
                    external_ref: resto-001
                  paid_at: '2026-07-17T19:42:00Z'
                  expires_at: '2026-07-17T19:55:00Z'
                  created_at: '2026-07-17T19:40:00Z'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - QR Crypto POS
      operationId: createPosCharge
      summary: "Crear un cobro POS (QR crypto con monto)"
      description: "Crea un cobro QR crypto con monto para un merchant. Mismo contrato del link de cobro universal: amount + settlement_asset (default de la cuenta si se omite); el due que paga el cliente se cotiza en el crypto del QR al crearlo (el pagador cubre la conversión — tú recibes tu meta exacta). La respuesta trae la dirección de depósito exclusiva, el QR de dirección cruda (qr_payload + qr_png_base64) y el due congelado, listos para el POS. idempotency_key es obligatoria: el retry devuelve el MISMO cobro y la misma dirección."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - merchant_id
              - amount
              - crypto
              - idempotency_key
              properties:
                merchant_id:
                  type: string
                  format: uuid
                amount:
                  type: string
                settlement_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                crypto:
                  type: string
                  enum:
                  - tron:usdt
                  - eth:usdt
                  - eth:usdc
                  - btc:btc
                reference:
                  type: string
                expires_in:
                  type: integer
                  minimum: 300
                  maximum: 86400
                  default: 900
                idempotency_key:
                  type: string
            example:
              merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
              amount: '25'
              crypto: tron:usdt
              reference: TICKET-0451
              expires_in: 900
              idempotency_key: pos-0451-1
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosCharge'
              example:
                charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                payin_id: bd3d88ca-af9c-4c05-a6f8-0982a2d187d5
                account_id: 5138e8dd-64bd-43ef-aafe-8d9ef23bec9e
                status: pending
                amount: '25'
                settlement_asset: USDT
                crypto: tron:usdt
                chain: tron
                asset: USDT
                address: TC5SToDEigQtie7Crf9et7ui7zDsvdDHeG
                due: '25.000000'
                received: '0.000000'
                qr_payload: TC5SToDEigQtie7Crf9et7ui7zDsvdDHeG
                qr_png_base64: iVBORw0KGgo...
                merchant:
                  id: d2875683-fc80-4c7b-876a-fb585d7c6982
                  name: La Terraza Restaurant
                  external_ref: resto-001
                reference: TICKET-0451
                expires_at: '2026-07-17T19:55:00Z'
                receipt_url: https://api.qbank.cl/platform/v1/payins/bd3d88ca-af9c-4c05-a6f8-0982a2d187d5/receipt
                created_at: '2026-07-17T19:40:00Z'
                updated_at: '2026-07-17T19:40:00Z'
        '422':
          description: merchant_disabled / settlement_asset_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: merchant_disabled
                message: this merchant is disabled; re-enable it before charging
        '503':
          description: pricing_unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: pricing_unavailable
                message: crypto pricing is temporarily unavailable
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/charges/{chargeID}:
    get:
      tags:
      - QR Crypto POS
      operationId: getPosCharge
      summary: "Obtener un cobro POS (polling del POS)"
      description: "Detalle del cobro para el loop de polling del POS. Mientras está pending, si ya hay un depósito on-chain detectado pero sin confirmar, la respuesta agrega confirming:true + detected_amount (señal de UX — el crédito llega con la confirmación). Los pagos parciales acumulan en received; los pagos tardíos a un cobro expirado igual acreditan y se ven aquí."
      parameters:
      - name: chargeID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosCharge'
              examples:
                confirming:
                  summary: Deposit detected on-chain, awaiting confirmations
                  value:
                    charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                    payin_id: bd3d88ca-af9c-4c05-a6f8-0982a2d187d5
                    status: pending
                    confirming: true
                    detected_amount: '25.000000'
                    amount: '25'
                    settlement_asset: USDT
                    crypto: tron:usdt
                    due: '25.000000'
                    received: '0.000000'
                    address: TC5SToDEigQtie7Crf9et7ui7zDsvdDHeG
                    qr_payload: TC5SToDEigQtie7Crf9et7ui7zDsvdDHeG
                    merchant:
                      id: d2875683-fc80-4c7b-876a-fb585d7c6982
                      name: La Terraza Restaurant
                    expires_at: '2026-07-17T19:55:00Z'
                paid:
                  summary: Target reached and credited
                  value:
                    charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                    payin_id: bd3d88ca-af9c-4c05-a6f8-0982a2d187d5
                    status: paid
                    amount: '25'
                    settlement_asset: USDT
                    crypto: tron:usdt
                    due: '25.000000'
                    received: '25.000000'
                    refunded_total: '2.000000'
                    refunds:
                    - refund_id: b92b1ac0-3f38-4613-89e4-3788c53c9778
                      charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                      withdrawal_id: 4630fe8c-1346-4baf-94f9-8b56c9dbd352
                      amount: '2.000000'
                      asset: USDT
                      to_address: TXHkw6bYtL2jExampleDest
                      status: completed
                      created_at: '2026-07-17T20:01:00Z'
                    merchant:
                      id: d2875683-fc80-4c7b-876a-fb585d7c6982
                      name: La Terraza Restaurant
                    paid_at: '2026-07-17T19:42:00Z'
                    expires_at: '2026-07-17T19:55:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/charges/{chargeID}/refund:
    post:
      tags:
      - QR Crypto POS
      operationId: refundPosCharge
      summary: "Devolver un cobro POS"
      description: "Devuelve (parte de) lo recibido al pagador como un retiro crypto normal desde el saldo de la cuenta (hold, fee de retiro y todos los controles de compliance del riel). to_address es siempre explícita — jamás se auto-devuelve al origen del depósito. Tope duro: la suma de devoluciones jamás supera lo recibido. idempotency_key obligatoria."
      parameters:
      - name: chargeID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              - to_address
              - idempotency_key
              properties:
                amount:
                  type: string
                to_address:
                  type: string
                idempotency_key:
                  type: string
                wallet_type:
                  type: string
                  enum:
                  - self_hosted
                  - vasp
                beneficiary_name:
                  type: string
                travel_address:
                  type: string
            example:
              amount: '25'
              to_address: TXHkw6bYtL2jExampleDest
              idempotency_key: pos-ref-0451-1
      responses:
        '202':
          description: Accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PosChargeRefund'
              example:
                refund_id: b92b1ac0-3f38-4613-89e4-3788c53c9778
                charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                withdrawal_id: 4630fe8c-1346-4baf-94f9-8b56c9dbd352
                amount: '25.000000'
                asset: USDT
                to_address: TXHkw6bYtL2jExampleDest
                status: processing
                tx_id: 7f1f2a3b4c...
                created_at: '2026-07-17T20:01:00Z'
                updated_at: '2026-07-17T20:01:00Z'
        '422':
          description: refund_exceeds_received / nothing_received
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: refund_exceeds_received
                message: the refund amount exceeds what this charge received minus prior refunds
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/charges/{chargeID}/refunds:
    get:
      tags:
      - QR Crypto POS
      operationId: listPosChargeRefunds
      summary: "Listar las devoluciones de un cobro POS"
      description: "Devoluciones del cobro; cada estado sigue el ciclo de su retiro (un retiro fallido reembolsa el débito y libera el tope)."
      parameters:
      - name: chargeID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  charge_id:
                    type: string
                  refunds:
                    type: array
                    items:
                      $ref: '#/components/schemas/PosChargeRefund'
              example:
                charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                refunds:
                - refund_id: b92b1ac0-3f38-4613-89e4-3788c53c9778
                  charge_id: 66773a3a-9911-4482-ae3c-09a481aba018
                  withdrawal_id: 4630fe8c-1346-4baf-94f9-8b56c9dbd352
                  amount: '2.000000'
                  asset: USDT
                  to_address: TXHkw6bYtL2jExampleDest
                  status: completed
                  created_at: '2026-07-17T20:01:00Z'
                  updated_at: '2026-07-17T20:05:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/pos/summary:
    get:
      tags:
      - QR Crypto POS
      operationId: getPosSummary
      summary: "Resumen de conciliación POS por merchant"
      description: "Agregado por merchant en un rango de fechas: conteos de cobros, bruto recaudado (meta de los cobros pagados), la comisión informativa del procesador, lo devuelto y el neto a repartir a cada merchant."
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Start date (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: End date (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: merchant_id
        in: query
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  from:
                    type: string
                  to:
                    type: string
                  merchants:
                    type: array
                    items:
                      type: object
              example:
                from: '2026-07-01'
                to: '2026-07-17'
                merchants:
                - merchant_id: d2875683-fc80-4c7b-876a-fb585d7c6982
                  name: La Terraza Restaurant
                  external_ref: resto-001
                  charges_count: 214
                  paid_count: 201
                  fee_percent: '1'
                  fee_fixed: '0'
                  totals:
                  - settlement_asset: USDT
                    gross: '5025.000000'
                    processor_fee: '50.250000'
                    net_for_merchant: '4972.750000'
                  refunded:
                  - asset: USDT
                    amount: '2.000000'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/swaps:
    post:
      tags:
      - Swaps
      operationId: createSwap
      summary: Crear un swap
      description: 'Convierte saldo entre dos de tus monedas, de forma síncrona y atómica — tu saldo cambia
        al instante y la plata nunca sale de tu cuenta (sin OTP). Los pares volátiles (BTC/GOLD) comparten
        los límites por operación y de volumen 24h con payouts y compras con tarjeta. Requiere clave de
        idempotencia: el replay devuelve el swap original y jamás re-ejecuta.'
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - from_asset
              - to_asset
              - amount
              properties:
                from_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                to_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                amount:
                  type: string
                  description: Monto en la moneda de origen (6 decimales para USDT/USDC/GOLD, 8 para BTC).
                idempotency_key:
                  type: string
                  description: >-
                    Clave elegida por el cliente. Los prefijos payin-convert: y checkout-swap: están reservados para las auto-conversiones del sistema y se rechazan con 400 reserved_idempotency_key.
            examples:
              usdt_btc:
                summary: USDT a BTC
                value:
                  from_asset: USDT
                  to_asset: BTC
                  amount: '1000'
                  idempotency_key: swap-2026-07-10-a
              cross:
                summary: BTC a GOLD directo
                value:
                  from_asset: BTC
                  to_asset: GOLD
                  amount: '0.01'
                  idempotency_key: swap-2026-07-10-b
              stables:
                summary: USDT a USDC
                value:
                  from_asset: USDT
                  to_asset: USDC
                  amount: '500'
                  idempotency_key: swap-2026-07-10-c
      responses:
        '201':
          description: Swap ejecutado (síncrono).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Swap'
              example:
                swap_id: 8a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                from_asset: USDT
                to_asset: BTC
                from_amount: '1000.000000'
                to_amount: '0.01568419'
                rate: '0.00001568'
                status: completed
                idempotency_key: swap-2026-07-10-a
                created_at: '2026-07-10T15:00:00Z'
        '200':
          description: Replay de idempotencia (swap original, jamás re-ejecutado).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Swap'
              example:
                swap_id: 8a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                from_asset: USDT
                to_asset: BTC
                from_amount: '1000.000000'
                to_amount: '0.01568419'
                rate: '0.00001568'
                status: completed
                idempotency_key: swap-2026-07-10-a
                created_at: '2026-07-10T15:00:00Z'
                idempotency_hit: true
        '400':
          description: invalid_asset / invalid_pair / invalid_amount / amount_too_small / swap_asset_disabled
            / idempotency_key_required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: amount_too_small
                message: amount is too small to convert at the current rate
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: verification_required / service_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: service_disabled
                message: this service is not enabled for your account
        '422':
          description: settlement_limit_exceeded / settlement_daily_limit_exceeded (topes volátiles compartidos
            con payouts y tarjetas).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: settlement_daily_limit_exceeded
                message: the account exceeded its 24h volume for volatile assets; retry later or use smaller
                  amounts
        '503':
          description: Precio de ejecución no disponible o desactualizado (pricing_unavailable).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: pricing_unavailable
                message: execution price is unavailable or stale; retry later
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Swaps
      operationId: listSwaps
      summary: Listar swaps
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: asset
        in: query
        schema:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
        description: Matchea cualquiera de las dos puntas de la conversión.
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      responses:
        '200':
          description: Swaps paginados.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  swaps:
                    type: array
                    items:
                      $ref: '#/components/schemas/Swap'
              example:
                page: 1
                page_size: 50
                swaps:
                - swap_id: 8a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  from_asset: USDT
                  to_asset: BTC
                  from_amount: '1000.000000'
                  to_amount: '0.01568419'
                  rate: '0.00001568'
                  status: completed
                  idempotency_key: swap-2026-07-10-a
                  created_at: '2026-07-10T15:00:00Z'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/swaps/{swapID}:
    get:
      tags:
      - Swaps
      operationId: getSwap
      summary: Consultar un swap
      parameters:
      - name: swapID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El swap.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Swap'
              example:
                swap_id: 8a1b2c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                from_asset: BTC
                to_asset: GOLD
                from_amount: '0.01000000'
                to_amount: '4.900990'
                rate: '490.09900990'
                status: completed
                idempotency_key: swap-2026-07-10-b
                created_at: '2026-07-10T15:05:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/webhooks/subscriptions:
    post:
      tags:
      - Webhooks
      operationId: createWebhookSubscription
      summary: Crear una suscripción de webhook
      description: 'Suscribe un callback HTTPS a los eventos de la cuenta que llama.


        Las entregas van firmadas: `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`
        con el timestamp en `X-Webhook-Timestamp`. Hasta 5 intentos con backoff incremental.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - event_type
              - callback_url
              - secret
              properties:
                event_type:
                  type: string
                  enum:
                  - payin_credited
                  - payin_expired
                  - payin_refunded
                  - payin_settlement_scheduled
                  - payout_status_changed
                  - transfer_received
                  - crypto_deposit_credited
                  - crypto_deposit_held
                  - crypto_deposit_alert
                  - crypto_withdrawal_status_changed
                  - banking_customer_status_changed
                  - banking_operation_status_changed
                  - aml_screening_updated
                  - kyc_verification_status_changed
                  - kyb_verification_status_changed
                  - kyc_link_completed
                  - kyb_link_completed
                  - kyc_document_validated
                  - kyb_document_validated
                  - kyc_liveness_completed
                  - wallet_deposit_received
                  - wallet_send_status_changed
                  - wallet_key_exported
                  - '*'
                callback_url:
                  type: string
                  format: uri
                  description: Solo HTTPS; localhost e IPs privadas se rechazan.
                secret:
                  type: string
                  minLength: 16
            example:
              event_type: payout_status_changed
              callback_url: https://api.myapp.com/webhooks/cbpay
              secret: a-strong-shared-secret-123
      responses:
        '201':
          description: Suscripción creada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  event_type:
                    type: string
                  callback_url:
                    type: string
                  status:
                    type: string
                    example: active
                  created_at:
                    type: string
                    format: date-time
                  secret_stored:
                    type: boolean
              example:
                id: 8e7f6a5b-4c3d-2e1f-0a9b-8c7d6e5f4a3b
                event_type: payout_status_changed
                callback_url: https://api.myapp.com/webhooks/cbpay
                status: active
                created_at: '2026-07-07T12:00:00Z'
                secret_stored: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Webhooks
      operationId: listWebhookSubscriptions
      summary: Listar suscripciones de webhook
      description: Lista las suscripciones de webhook de la cuenta (el secreto nunca se devuelve).
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Suscripciones.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                page: 1
                page_size: 50
                subscriptions:
                - id: 475b48fc-2858-4ad3-81e2-ec68742d8245
                  event_type: payout_status_changed
                  callback_url: https://api.example.com/webhooks/cbpay
                  status: active
                  created_at: '2026-07-07T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/webhooks/subscriptions/{subscriptionID}:
    patch:
      tags:
      - Webhooks
      operationId: updateWebhookSubscriptionStatus
      summary: Desactivar o reactivar una suscripción de webhook
      description: 'Alterna una suscripción de la cuenta entre `active` y `disabled`.
        Una suscripción `disabled` deja de recibir eventos NUEVOS; las entregas ya
        encoladas siguen saliendo. Idempotente: repetir el estado vigente es un no-op
        `200`. Las suscripciones nunca se borran — desactivar es la forma de
        desuscribirse. Ownership aplicado: una suscripción de otra cuenta responde
        `404` (es indistinguible de una inexistente).

        '
      parameters:
      - name: subscriptionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - status
              properties:
                status:
                  type: string
                  enum:
                  - active
                  - disabled
            examples:
              disable:
                summary: Desactivar la suscripción
                value:
                  status: disabled
              reactivate:
                summary: Reactivar la suscripción
                value:
                  status: active
      responses:
        '200':
          description: La suscripción con su estado vigente.
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  event_type:
                    type: string
                  callback_url:
                    type: string
                  status:
                    type: string
                    enum:
                    - active
                    - disabled
                  created_at:
                    type: string
                    format: date-time
              examples:
                disabled:
                  summary: Suscripción desactivada
                  value:
                    id: 8e7f6a5b-4c3d-2e1f-0a9b-8c7d6e5f4a3b
                    event_type: payout_status_changed
                    callback_url: https://api.myapp.com/webhooks/cbpay
                    status: disabled
                    created_at: '2026-07-07T12:00:00Z'
        '400':
          description: '`invalid_status`: el estado debe ser `active` o `disabled`.'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              examples:
                invalidStatus:
                  summary: Estado fuera de catálogo
                  value:
                    error: invalid_status
                    message: status must be active or disabled
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /healthz:
    get:
      tags:
      - Estado
      security: []
      operationId: healthz
      summary: Salud del servicio
      description: Chequeo público de disponibilidad de la API y su base de datos.
      responses:
        '200':
          description: Saludable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
              example:
                status: ok
        '503':
          description: Base de datos no alcanzable (`store_unavailable`).
          content:
            application/json:
              schema: &id016
                $ref: '#/components/schemas/Error'
              example:
                error: store_unavailable
                message: database not reachable
  /status/{orgToken}:
    get:
      security: []
      tags:
      - Estado
      operationId: statusPage
      summary: Página de status pública (HTML)
      description: >-
        Página de status hosteada y brandeada de la organización: estado en vivo de cada
        corredor de pago, uptime de 90 días e historial de incidentes. Pública (sin
        credenciales), segura para compartir con tus propios clientes o embeber en tu
        producto. El `orgToken` es el token opaco que comparte el operador (los org admins
        leen la URL completa como `status_page_url` en `GET /v1/org/branding`). Token
        inválido responde 404. Con rate limit por IP.
      parameters:
      - name: orgToken
        in: path
        required: true
        schema:
          type: string
        description: Token opaco de la página de status de la organización.
      responses:
        '200':
          description: Página de status brandeada (HTML).
          content:
            text/html:
              example: <!doctype html>…
        '404':
          description: Token inválido o desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: status page not found
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
  /v1/status/{orgToken}:
    get:
      security: []
      tags:
      - Estado
      operationId: statusJSON
      summary: Feed de status público (JSON)
      description: >-
        Los mismos datos de la página de status en JSON, para tu propio tooling de status
        o monitoreo: estado general, estado por corredor con uptime de 90 días y el
        historial reciente de incidentes (transiciones de estado). Público, con rate limit
        por IP.
      parameters:
      - name: orgToken
        in: path
        required: true
        schema:
          type: string
        description: Token opaco de la página de status de la organización.
      responses:
        '200':
          description: Estado actual del servicio.
          content:
            application/json:
              example:
                brand:
                  name: CBPay
                status: degraded
                generated_at: '2026-07-24T22:15:00Z'
                corridors:
                - flow: payout
                  country: VE
                  currency: VES
                  method: bank_transfer
                  status: down
                  since: '2026-07-24T22:10:00Z'
                  uptime_90d_pct: 99.62
                - flow: payout
                  country: MX
                  currency: MXN
                  method: bank_transfer
                  status: operational
                  since: '2026-05-02T00:00:00Z'
                  uptime_90d_pct: 100
                incidents:
                - flow: payout
                  country: VE
                  currency: VES
                  method: bank_transfer
                  from_status: operational
                  to_status: down
                  reason: consecutive infrastructure failures
                  at: '2026-07-24T22:10:00Z'
        '404':
          description: Token inválido o desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: status page not found
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
  /v1/crypto/wallets:
    post:
      tags:
      - Crypto
      operationId: createCryptoWallet
      summary: Reponer una wallet de depósito faltante
      description: 'Toda cuenta — persona y empresa — tiene exactamente **una wallet de depósito por par
        soportado** (`tron`/`usdt`, `eth`/`usdt`, `eth`/`usdc`, `btc`/`btc`), aprovisionadas automáticamente y gratis
        al registrarse. Las wallets de depósito solo **reciben** crypto que se abona al saldo virtual
        de la cuenta: no envían, y no se exportan ni importan (para eso están las wallets segregadas).
        Este endpoint existe solo para reponer un par faltante (caso excepcional): con el par ya aprovisionado
        responde `422 wallet_limit_reached` para todo tipo de cuenta.


        Una reposición manual cobra la comisión `wallet_creation` cuando CBPay la tiene configurada (fija
        por wallet; 0 = gratis, el valor por defecto); las wallets de nacimiento son siempre gratis. El
        cargo se reembolsa automáticamente si la creación falla.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - chain
              properties:
                chain:
                  type: string
                  enum:
                  - tron
                  - eth
                  - btc
                asset:
                  type: string
                  enum:
                  - usdt
                  - usdc
                  - btc
                  default: usdt
                  description: Activo de la wallet (usdc solo por `eth`; en `btc` el activo por defecto
                    es `btc`).
                label:
                  type: string
                  description: Etiqueta descriptiva opcional.
            examples:
              tron_usdt:
                summary: Reponer el par TRON USDT
                value:
                  chain: tron
              usdc_wallet:
                summary: Reponer el par USDC (solo Ethereum)
                value:
                  chain: eth
                  asset: usdc
              btc_wallet:
                summary: Reponer el par Bitcoin
                value:
                  chain: btc
      responses:
        '201':
          description: Wallet creada.
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/CryptoWallet'
                - type: object
                  properties:
                    creation_fee:
                      type: string
              example:
                wallet_id: b7e3a940-6f1d-4c2b-9a8e-5d4c3b2a1f0e
                chain: tron
                asset: USDT
                address: TQmZ1kfWzcmRDYkqLnybXnbWzPMYNvbYqT
                label: Main treasury
                type: deposit
                receive_only: true
                created_at: '2026-07-07T12:00:00Z'
                creation_fee: '1.000000'
        '400':
          description: invalid_chain / invalid_asset
          content:
            application/json:
              schema: &id008
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_chain
                message: unsupported chain "doge" (tron, eth, btc)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '422':
          description: La cuenta ya tiene su wallet de depósito de ese par (aplica a todo tipo de cuenta).
          content:
            application/json:
              schema: *id008
              example:
                error: wallet_limit_reached
                message: accounts hold one deposit wallet per network/asset pair (created automatically
                  with the account); use segregated wallets for additional wallets
    get:
      tags:
      - Crypto
      operationId: listCryptoWallets
      summary: Listar mis wallets
      description: Devuelve todas las wallets de la cuenta que llama, la más antigua primero.
      responses:
        '200':
          description: Las wallets de la cuenta.
          content:
            application/json:
              schema:
                type: object
                properties:
                  wallets:
                    type: array
                    items:
                      $ref: '#/components/schemas/CryptoWallet'
              example:
                wallets:
                - wallet_id: b7e3a940-6f1d-4c2b-9a8e-5d4c3b2a1f0e
                  chain: tron
                  asset: USDT
                  address: TQmZ1kfWzcmRDYkqLnybXnbWzPMYNvbYqT
                  label: ''
                  type: deposit
                  receive_only: true
                  created_at: '2026-07-07T12:00:00Z'
                - wallet_id: a1c9d8e7-2b3c-4d5e-6f70-8a9b0c1d2e3f
                  chain: eth
                  asset: USDT
                  address: '0x8f3Bc55D9F4a2E71c06b9a3D2e1F0A9B8C7D6E5F'
                  label: ''
                  type: deposit
                  receive_only: true
                  created_at: '2026-07-07T12:00:00Z'
                - wallet_id: fb88c2d1-7a4e-4b0c-8d3f-1e2a3b4c5d6e
                  chain: eth
                  asset: USDC
                  address: '0xa072E48Bc55D9F4a2E71c06b9a3D2e1F0A9B8C7D'
                  label: ''
                  type: deposit
                  receive_only: true
                  created_at: '2026-07-07T12:00:00Z'
                - wallet_id: 0d6f2a3b-4c5d-6e7f-8a9b-0c1d2e3f4a5b
                  chain: btc
                  asset: BTC
                  address: bc1q2w2w6cv5stsxpfygxk8ylvnqg9hu96pvvczvnc
                  label: ''
                  type: deposit
                  receive_only: true
                  created_at: '2026-07-07T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/segregated-wallets:
    post:
      tags:
      - Wallets segregadas
      operationId: createSegregatedWallet
      summary: Crear una wallet segregada
      description: 'Crea una wallet on-chain nueva de la cuenta que llama. Su saldo vive on-chain (nunca
        en el ledger) y queda exenta del barrido de tesorería. Pares soportados: `tron`/`usdt`, `eth`/`usdt`,
        `eth`/`usdc`, `btc`/`btc`. Las cuentas empresa pueden crear wallets ilimitadas; las cuentas persona tienen
        **1 por combinación red+activo** (la segunda responde `422 wallet_limit_reached`). Un `Idempotency-Key`
        opcional (o `idempotency_key` en el body) hace el reintento seguro: una repetición devuelve la
        misma wallet sin crear otra. Puede cobrar el fee `wallet_creation` (reembolsado si la creación
        falla).'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - chain
              properties:
                chain:
                  type: string
                  enum:
                  - tron
                  - eth
                  - btc
                asset:
                  type: string
                  enum:
                  - usdt
                  - usdc
                  - btc
                  default: usdt
                label:
                  type: string
                idempotency_key:
                  type: string
            examples:
              tron_usdt:
                summary: TRON USDT
                value:
                  chain: tron
                  asset: usdt
                  label: Acme Client
                  idempotency_key: wallet-client-001
              eth_usdc:
                summary: Ethereum USDC
                value:
                  chain: eth
                  asset: usdc
                  label: Project X
                  idempotency_key: wallet-project-x
              btc_btc:
                summary: Bitcoin BTC
                value:
                  chain: btc
                  asset: btc
                  label: BTC custody
                  idempotency_key: wallet-btc-001
      responses:
        '201':
          description: Wallet creada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWallet'
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                chain: tron
                asset: USDT
                address: TRmSZRaMAqLEevAdGwo3R43bRBXamWR5bd
                label: Acme Client
                type: segregated
                origin: created
                custody: cbpay
                exported: false
                created_at: '2026-07-11T12:00:00Z'
        '400':
          description: invalid_chain / invalid_asset
          content:
            application/json:
              schema: &id009
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_asset
                message: 'unsupported chain/asset pair (supported: tron/usdt, eth/usdt, eth/usdc, btc/btc)'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: El servicio está deshabilitado para la cuenta.
          content:
            application/json:
              schema: *id009
              example:
                error: service_disabled
                message: this service is not enabled for the account
        '409':
          description: Ya hay una wallet con esta idempotency key en aprovisionamiento.
          content:
            application/json:
              schema: *id009
              example:
                error: idempotency_conflict
                message: a wallet with this idempotency key is still being provisioned; retry with the
                  same key
        '422':
          description: La cuenta persona ya tiene su wallet segregada de ese par.
          content:
            application/json:
              schema: *id009
              example:
                error: wallet_limit_reached
                message: person accounts hold one segregated wallet per network/asset pair; company accounts
                  have no limit
    get:
      tags:
      - Wallets segregadas
      operationId: listSegregatedWallets
      summary: Listar mis wallets segregadas
      description: Lista las wallets segregadas de la cuenta, de la más nueva a la más antigua.
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Las wallets segregadas de la cuenta.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  wallets:
                    type: array
                    items:
                      $ref: '#/components/schemas/SegregatedWallet'
              example:
                page: 1
                page_size: 50
                wallets:
                - wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                  chain: tron
                  asset: USDT
                  address: TRmSZRaMAqLEevAdGwo3R43bRBXamWR5bd
                  label: Acme Client
                  type: segregated
                  origin: created
                  custody: cbpay
                  exported: false
                  created_at: '2026-07-11T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/segregated-wallets/import:
    post:
      tags:
      - Wallets segregadas
      operationId: importSegregatedWallet
      summary: Importar una wallet externa
      description: Importa una wallet que ya controlas entregando su `private_key_hex`. La llave viaja
        solo al custodio y **jamás se guarda ni se registra** en la plataforma. Exige sesión de usuario
        con 2FA (las API keys se rechazan con `human_session_required`), cuenta verificada y OTP. Cobra
        el fee `wallet_import` (reembolsado si el core rechaza).
      parameters:
      - name: X-OTP-Token
        in: header
        schema:
          type: string
        description: Token de un solo uso del desafío OTP (sesiones de usuario).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - chain
              - private_key_hex
              properties:
                chain:
                  type: string
                  enum:
                  - tron
                  - eth
                  - btc
                asset:
                  type: string
                  enum:
                  - usdt
                  - usdc
                  - btc
                  default: usdt
                private_key_hex:
                  type: string
                  description: Llave privada hex de 32 bytes (64 chars
                  prefijo 0x opcional).: null
                label:
                  type: string
                idempotency_key:
                  type: string
            examples:
              tron:
                summary: Importar una wallet TRON USDT
                value:
                  chain: tron
                  asset: usdt
                  private_key_hex: <64 hex>
                  label: Migrated wallet
                  idempotency_key: import-001
      responses:
        '201':
          description: Wallet importada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWallet'
              example:
                wallet_id: c8f4b2d3-1a2b-4c3d-9e8f-7a6b5c4d3e2f
                chain: tron
                asset: USDT
                address: TXimportedAddr00000000000000000000
                label: Migrated wallet
                type: segregated
                origin: imported
                custody: client
                exported: false
                created_at: '2026-07-11T12:02:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Se requiere verificación, sesión de usuario u OTP.
          content:
            application/json:
              schema: *id009
              example:
                error: human_session_required
                message: this operation handles private key material and requires a signed-in user session
                  with 2FA; API keys are not allowed
        '422':
          description: La cuenta persona ya tiene su wallet segregada de ese par.
          content:
            application/json:
              schema: *id009
              example:
                error: wallet_limit_reached
                message: person accounts hold one segregated wallet per network/asset pair; company accounts
                  have no limit
  /v1/segregated-wallets/{walletID}:
    get:
      tags:
      - Wallets segregadas
      operationId: getSegregatedWallet
      summary: Obtener una wallet segregada
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: La wallet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWallet'
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                chain: tron
                asset: USDT
                address: TRmSZRaMAqLEevAdGwo3R43bRBXamWR5bd
                label: Acme Client
                type: segregated
                origin: created
                custody: cbpay
                exported: false
                created_at: '2026-07-11T12:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/balance:
    get:
      tags:
      - Wallets segregadas
      operationId: getSegregatedWalletBalance
      summary: Obtener el saldo on-chain en vivo
      description: Devuelve el saldo on-chain en vivo de la wallet, incluyendo el gas nativo (TRX/ETH)
        para que sepas si puede pagar un fee de red.
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Saldo en vivo.
          content:
            application/json:
              schema:
                type: object
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                balances:
                  usdt:
                    amount_raw: '125000000'
                    amount_formatted: '125.000000'
                  trx:
                    amount_sun: '30000000'
                    amount_formatted: '30.000000'
                fee_policy:
                  min_fee_trx: '5.000000'
                  has_sufficient_fee_balance: true
                updated_at: '2026-07-11T12:03:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/deposits:
    get:
      tags:
      - Wallets segregadas
      operationId: listSegregatedWalletDeposits
      summary: Listar depósitos on-chain
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Depósitos recibidos por la wallet.
          content:
            application/json:
              schema:
                type: object
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                deposits:
                - tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                  amount_raw: '125000000'
                  from_address: TDonor00000000000000000000000000000
                  status: completed
                  created_at: '2026-07-11T12:01:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/transactions:
    get:
      tags:
      - Wallets segregadas
      operationId: listSegregatedWalletTransactions
      summary: Listar actividad on-chain
      description: Actividad on-chain completa (depósitos y envíos) de la wallet.
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Las transacciones on-chain de la wallet.
          content:
            application/json:
              schema:
                type: object
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                transactions:
                - tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                  direction: in
                  amount_raw: '125000000'
                  status: completed
                  event_time: '2026-07-11T12:01:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/sends:
    post:
      tags:
      - Wallets segregadas
      operationId: createSegregatedWalletSend
      summary: Enviar crypto desde la wallet
      description: 'Envía crypto **desde la wallet misma** (dirección de origen real, firmado por el custodio).
        `idempotency_key` es obligatoria. El gas corre por el cliente: si la wallet no tiene gas nativo,
        el envío se rechaza con `422 insufficient_gas` antes de cualquier cobro. Puede cobrar el fee `wallet_send`
        (del saldo de settlement de la cuenta en el ledger, nunca los fondos on-chain de la wallet; reembolsado
        si el core rechaza). Exige cuenta verificada y OTP. Ante una falla ambigua el envío queda `pending`
        — reintenta con la misma clave, jamás se re-envía.'
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: X-OTP-Token
        in: header
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - to_address
              - idempotency_key
              properties:
                asset:
                  type: string
                  enum:
                  - usdt
                  - usdc
                  - eth
                to_address:
                  type: string
                amount:
                  type: string
                  description: Monto decimal en la unidad del asset (usa este o amount_raw).
                amount_raw:
                  type: string
                  description: Monto en unidades mínimas on-chain.
                idempotency_key:
                  type: string
            examples:
              by_amount:
                summary: Por monto decimal
                value:
                  asset: usdt
                  to_address: TXYZdestination00000000000000000000
                  amount: '25.50'
                  idempotency_key: send-2026-07-11-a
              by_raw:
                summary: Por unidades mínimas
                value:
                  asset: usdt
                  to_address: TXYZdestination00000000000000000000
                  amount_raw: '25500000'
                  idempotency_key: send-2026-07-11-b
      responses:
        '202':
          description: Envío transmitido (asíncrono; estado final por webhook).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWalletSend'
              example:
                send_id: 9c8b7a6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                chain: tron
                asset: USDT
                to_address: TXYZdestination00000000000000000000
                amount_raw: '25500000'
                fee: '0.000000'
                fee_asset: USDT
                status: processing
                tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                idempotency_key: send-2026-07-11-a
                created_at: '2026-07-11T12:05:00Z'
        '200':
          description: Replay idempotente de un envío previo.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWalletSend'
              example:
                send_id: 9c8b7a6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                status: processing
                idempotency_key: send-2026-07-11-a
                idempotency_hit: true
        '422':
          description: Falta gas en la wallet, o el core rechazó el envío.
          content:
            application/json:
              schema: *id009
              example:
                error: insufficient_gas
                message: the wallet does not hold enough TRX to pay the network fee (minimum 5.000000
                  TRX); fund the wallet address with TRX and retry
    get:
      tags:
      - Wallets segregadas
      operationId: listSegregatedWalletSends
      summary: Listar envíos de la wallet
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Los envíos de la wallet.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  sends:
                    type: array
                    items:
                      $ref: '#/components/schemas/SegregatedWalletSend'
              example:
                page: 1
                page_size: 50
                sends:
                - send_id: 9c8b7a6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                  wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                  chain: tron
                  asset: USDT
                  to_address: TXYZdestination00000000000000000000
                  amount_raw: '25500000'
                  status: completed
                  tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                  created_at: '2026-07-11T12:05:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/sends/{sendID}:
    get:
      tags:
      - Wallets segregadas
      operationId: getSegregatedWalletSend
      summary: Obtener un envío
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: sendID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: El envío.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SegregatedWalletSend'
              example:
                send_id: 9c8b7a6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                chain: tron
                asset: USDT
                to_address: TXYZdestination00000000000000000000
                amount_raw: '25500000'
                status: completed
                tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
                created_at: '2026-07-11T12:05:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/export:
    post:
      tags:
      - Wallets segregadas
      operationId: exportSegregatedWalletKey
      summary: Exportar la llave privada
      description: 'Devuelve la llave privada de la wallet (custodia compartida: la wallet sigue operativa
        en la plataforma tras exportar). La operación más sensible del producto: exige sesión de usuario
        con 2FA (API keys rechazadas), cuenta verificada y un `reason` de al menos 20 caracteres que queda
        en la auditoría. Cada export dispara el webhook `wallet_key_exported`. Cobra el fee `wallet_export`.'
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: X-OTP-Token
        in: header
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - reason
              properties:
                reason:
                  type: string
                  description: Al menos 20 caracteres; queda en la auditoría.
            example:
              reason: Custody migration to the client cold wallet
      responses:
        '200':
          description: La llave privada (guárdala de forma segura).
          content:
            application/json:
              schema:
                type: object
              example:
                wallet:
                  wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                  chain: tron
                  asset: USDT
                  address: TRmSZRaMAqLEevAdGwo3R43bRBXamWR5bd
                  exported: true
                  exported_at: '2026-07-11T12:10:00Z'
                private_key_hex: <64 hex>
                export:
                  custody: shared
                  warning: anyone holding this private key controls the wallet funds; the wallet remains
                    operational in the platform
        '403':
          description: Se requiere sesión de usuario, verificación u OTP.
          content:
            application/json:
              schema: *id009
              example:
                error: human_session_required
                message: this operation handles private key material and requires a signed-in user session
                  with 2FA; API keys are not allowed
        '503':
          description: El export de llaves no está habilitado en este entorno.
          content:
            application/json:
              schema: *id009
              example:
                error: export_unavailable
                message: private key export is not enabled on this environment
  /v1/segregated-wallets/{walletID}/auto-forward:
    get:
      tags:
      - Wallets segregadas
      operationId: getSegregatedWalletAutoForward
      summary: Obtener la regla de auto-forward
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: La regla de auto-forward actual.
          content:
            application/json:
              schema:
                type: object
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                configured: true
                auto_forward:
                  enabled: true
                  linked_address: TColdWallet00000000000000000000000
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags:
      - Wallets segregadas
      operationId: upsertSegregatedWalletAutoForward
      summary: Configurar auto-forward
      description: Reenvía a una dirección tuya todo lo que llegue a la wallet. Redirige fondos futuros,
        así que exige cuenta verificada y OTP.
      parameters:
      - name: walletID
        in: path
        required: true
        schema:
          type: string
      - name: X-OTP-Token
        in: header
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                linked_address:
                  type: string
                  description: Dirección destino (requerida para activar).
                enabled:
                  type: boolean
                  default: true
            examples:
              enable:
                summary: Activar
                value:
                  linked_address: TColdWallet00000000000000000000000
                  enabled: true
              disable:
                summary: Desactivar
                value:
                  enabled: false
      responses:
        '200':
          description: La regla actualizada.
          content:
            application/json:
              schema:
                type: object
              example:
                wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
                auto_forward:
                  enabled: true
                  linked_address: TColdWallet00000000000000000000000
        '403':
          description: Se requiere verificación u OTP.
          content:
            application/json:
              schema: *id009
              example:
                error: verification_required
                message: identity verification is required before using this service
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/segregated-wallets/{walletID}/signatures:
    post:
      tags:
      - Pruebas de firma
      operationId: signSegregatedWalletMessage
      summary: Firmar un mensaje con una wallet segregada
      description: "Crea una prueba de firma: la wallet segregada (`custody=cbpay`) firma\n
        un sobre estructurado **CBPay Signature Proof** (anti-phishing) con el estándar\n
        nativo de la red — EIP-191 para ETH/EVM, TIP-191 para TRON. La prueba queda\n
        persistida y es verificable públicamente en `verify_url` (sin auth).\n\n
        **Requiere OTP** (código de un solo uso) cuando la acción `sign_message` está\n
        protegida en la política de la cuenta. La wallet debe estar `custody=cbpay`:\n
        una wallet exportada (`custody=client`) ya no puede firmar (`custody_transferred`).\n\n
        El sobre vive 10 minutos; una prueba firmada sigue siendo verificable después\n
        de expirar. Emite el webhook `wallet_signature_created` y un email de seguridad\n
        al titular de la cuenta.\n\n
        Errores específicos: `invalid_purpose`, `invalid_statement`,\n
        `custody_transferred`, `proof_not_signable`, `sign_rejected`,\n
        `nonce_failed`, `core_unavailable`."
      parameters:
      - name: walletID
        in: path
        required: true
        description: ID de la wallet segregada.
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [purpose]
              properties:
                purpose:
                  type: string
                  enum: [wallet_ownership, treasury_attestation]
                  description: Propósito de la prueba (default `wallet_ownership`). `wallet_link` no se acepta aquí — usa `POST /v1/wallet-links/challenges` para vincular una wallet externa.
                statement:
                  type: string
                  description: Declaración opcional que la wallet atestigua (máx. 140 runas).
            example:
              purpose: wallet_ownership
              statement: I control this wallet
      responses:
        '201':
          description: Prueba de firma creada y firmada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  proof:
                    $ref: '#/components/schemas/SignatureProof'
              example:
                proof:
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_ownership
                  statement: I control this wallet
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: signed
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:03:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  signature: '8ba1f109551bd432803012645ac136ddd64dba72a0c9e0b1f5b1a2c3d4e5f60718293a4b5c6d7e8f90112233445566778899aabbccddeeff00112233445566771b'
                  message_hash: '3f8a1c4e9b2d6f70a5c8e1b4d7f2093a4c6e8f10b3d5f709e2a4c6d8f0b1c3d5'
                  verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
        '400':
          description: Payload inválido (`invalid_json`, `invalid_purpose`, `invalid_statement`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_purpose
                message: purpose must be one of wallet_ownership, treasury_attestation; to link an external wallet use POST /v1/wallet-links/challenges
        '403':
          description: 'La cuenta o sesión no pasó el gate: `human_session_required` (las API keys no pueden firmar), `verification_required` (KYC no aprobado), `account_blocked`, `service_disabled`, o falta/OTP inválido (`otp_required`, `otp_invalid`).'
        '404':
          description: La wallet no existe o no pertenece a la cuenta.
        '409':
          description: La wallet fue exportada (`custody_transferred`).
        '422':
          description: La wallet no puede firmar (`proof_not_signable`) o la firma fue rechazada (`sign_rejected`).
        '502':
          description: El servicio de firma no respondió (`core_unavailable`).
        '503':
          description: La organización no tiene credencial del core configurada (`org_credential_missing`) o el servicio OTP no está disponible (`otp_unavailable`).
    get:
      tags:
      - Pruebas de firma
      operationId: listSegregatedWalletSignatures
      summary: Listar pruebas de firma de una wallet segregada
      description: Lista paginada de las pruebas de firma emitidas con una wallet segregada
        de la cuenta.
      parameters:
      - name: walletID
        in: path
        required: true
        description: ID de la wallet segregada.
        schema:
          type: string
          format: uuid
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Lista paginada de pruebas de firma.
          content:
            application/json:
              example:
                proofs:
                - proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_ownership
                  statement: I control this wallet
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: signed
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:03:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                page: 1
                page_size: 50
        '400':
          description: Rango de fechas inválido (`invalid_range`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_range
                message: invalid date range
        '404':
          description: La wallet no existe o no pertenece a la cuenta.
  /v1/signature-proofs:
    get:
      tags:
      - Pruebas de firma
      operationId: listSignatureProofs
      summary: Listar pruebas de firma de la cuenta
      description: "Lista paginada de todas las pruebas de firma de la cuenta\n
        (wallets segregadas y vínculos de wallets externas), con filtros de fecha\n
        (`from`/`to`, `YYYY-MM-DD` UTC) y filtro opcional por `purpose`."
      parameters:
      - name: from
        in: query
        description: Fecha inicial (`YYYY-MM-DD`, UTC, inclusiva).
        schema:
          type: string
          format: date
      - name: to
        in: query
        description: Fecha final (`YYYY-MM-DD`, UTC, inclusiva).
        schema:
          type: string
          format: date
      - name: purpose
        in: query
        description: Filtra por propósito de la prueba.
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Lista paginada de pruebas de firma.
          content:
            application/json:
              example:
                proofs:
                - proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_ownership
                  statement: I control this wallet
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: signed
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:03:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                page: 1
                page_size: 50
        '400':
          description: Rango de fechas inválido (`invalid_range`).
  /v1/signature-proofs/{proofID}:
    get:
      tags:
      - Pruebas de firma
      operationId: getSignatureProof
      summary: Detalle de una prueba de firma
      description: Devuelve el detalle completo de una prueba de firma de la cuenta,
        incluyendo la firma, el hash del mensaje y el link público de verificación.
      parameters:
      - name: proofID
        in: path
        required: true
        description: ID de la prueba de firma.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Detalle de la prueba de firma.
          content:
            application/json:
              schema:
                type: object
                properties:
                  proof:
                    $ref: '#/components/schemas/SignatureProof'
              example:
                proof:
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_ownership
                  statement: I control this wallet
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: signed
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:03:12Z'
                  created_at: '2026-08-21T14:03:11Z'
                  signature: '8ba1f109551bd432803012645ac136ddd64dba72a0c9e0b1f5b1a2c3d4e5f60718293a4b5c6d7e8f90112233445566778899aabbccddeeff00112233445566771b'
                  message_hash: '3f8a1c4e9b2d6f70a5c8e1b4d7f2093a4c6e8f10b3d5f709e2a4c6d8f0b1c3d5'
                  verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
        '404':
          description: La prueba no existe o no pertenece a la cuenta.
  /v1/signature-proofs/{proofID}/revoke:
    post:
      tags:
      - Pruebas de firma
      operationId: revokeSignatureProof
      summary: Revocar una prueba de firma
      description: "Revoca una prueba de firma: la verificación pública deja de\n
        reportarla como válida. La prueba queda en el historial (append-only).\n
        Idempotente: revocar una prueba ya revocada devuelve el mismo estado."
      parameters:
      - name: proofID
        in: path
        required: true
        description: ID de la prueba de firma.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Prueba revocada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  proof:
                    $ref: '#/components/schemas/SignatureProof'
              example:
                proof:
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_ownership
                  statement: I control this wallet
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: revoked
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:03:12Z'
                  revoked_at: '2026-08-21T16:40:02Z'
                  created_at: '2026-08-21T14:03:11Z'
                  verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
        '404':
          description: La prueba no existe o no pertenece a la cuenta.
        '409':
          description: La prueba no está en un estado revocable (`invalid_state`).
  /v1/wallet-links/challenges:
    post:
      tags:
      - Vínculos de wallets
      operationId: createWalletLinkChallenge
      summary: Crear un desafío nonce para vincular una wallet externa
      description: "Crea un desafío nonce para vincular una wallet EXTERNA\n
        (`custody=client`) a la cuenta. El cliente firma el `envelope` con su wallet\n
        (EIP-191 para ETH/EVM, TIP-191 para TRON) y lo envía a `POST\n
        /v1/wallet-links/verify`. El desafío expira en 10 minutos y es de un solo uso.\n\n
        Errores específicos: `unsupported_chain`, `invalid_address`, `nonce_failed`."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chain, address]
              properties:
                chain:
                  type: string
                  enum: [eth, tron]
                address:
                  type: string
                  description: Dirección de la wallet externa (formato de la red).
            example:
              chain: eth
              address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
      responses:
        '201':
          description: Desafío creado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  challenge:
                    type: object
                    properties:
                      link_id:
                        type: string
                      chain:
                        type: string
                      address:
                        type: string
                      nonce:
                        type: string
                      envelope:
                        type: string
                        description: Texto exacto que la wallet debe firmar.
                      expires_at:
                        type: string
              example:
                challenge:
                  link_id: d52e7b91-4c68-4f89-1b23-5d7e9f0a2c46
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_link\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nAccount: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z"
                  expires_at: '2026-08-21T14:13:11Z'
        '400':
          description: Payload inválido (`invalid_address`, `unsupported_chain`).
        '500':
          description: No se pudo generar el nonce (`nonce_failed`).
  /v1/wallet-links/verify:
    post:
      tags:
      - Vínculos de wallets
      operationId: verifyWalletLink
      summary: Verificar la firma del desafío y vincular la wallet
      description: "Verifica la firma del desafío nonce y vincula la wallet externa a\n
        la cuenta. La wallet queda `custody=client`: el cliente conserva la llave y\n
        puede gastar por fuera; el vínculo solo prueba la titularidad de la dirección.\n\n
        Errores específicos: `invalid_payload`, `invalid_signature`,\n
        `signature_mismatch`, `verification_failed`, `challenge_consumed`,\n
        `challenge_expired`, `proof_not_signable`."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [nonce, signature]
              properties:
                nonce:
                  type: string
                  description: Nonce del desafío (de `POST /v1/wallet-links/challenges`).
                signature:
                  type: string
                  description: Firma del envelope (EIP-191 / TIP-191).
            example:
              nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
              signature: '8ba1f109551bd432803012645ac136ddd64dba72a0c9e0b1f5b1a2c3d4e5f60718293a4b5c6d7e8f90112233445566778899aabbccddeeff00112233445566771b'
      responses:
        '200':
          description: Wallet vinculada.
          content:
            application/json:
              schema:
                type: object
                properties:
                  link:
                    $ref: '#/components/schemas/WalletLink'
                  proof:
                    $ref: '#/components/schemas/SignatureProof'
              example:
                link:
                  link_id: d52e7b91-4c68-4f89-1b23-5d7e9f0a2c46
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  status: linked
                  expires_at: '2026-08-21T14:13:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  verified_at: '2026-08-21T14:05:41Z'
                proof:
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  purpose: wallet_link
                  envelope: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_link\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nAccount: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z"
                  nonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7
                  signature: '8ba1f109551bd432803012645ac136ddd64dba72a0c9e0b1f5b1a2c3d4e5f60718293a4b5c6d7e8f90112233445566778899aabbccddeeff00112233445566771b'
                  message_hash: 5c35a1c2e9f84d1b7a06e3f2d49871c5b6a0e4d38f71c2b5963a84d0e1f2c3b4
                  proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
                  status: signed
                  issued_at: '2026-08-21T14:03:11Z'
                  expires_at: '2026-08-21T14:13:11Z'
                  signed_at: '2026-08-21T14:05:41Z'
                  created_at: '2026-08-21T14:03:11Z'
                  verify_url: 'https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a'
        '400':
          description: Payload inválido (`invalid_payload`, `invalid_signature`, `verification_failed`).
        '404':
          description: Nonce de desafío desconocido o ajeno (`not_found`).
        '409':
          description: El desafío ya fue consumido o la prueba no pudo persistirse (`challenge_consumed`, `proof_not_signable`).
        '410':
          description: El desafío expiró (`challenge_expired`).
        '422':
          description: La firma no corresponde a la dirección del desafío (`signature_mismatch`).
  /v1/wallet-links:
    get:
      tags:
      - Vínculos de wallets
      operationId: listWalletLinks
      summary: Listar wallets vinculadas
      description: Lista paginada de las wallets externas (`custody=client`) vinculadas
        a la cuenta.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Lista paginada de vínculos.
          content:
            application/json:
              example:
                links:
                - link_id: d52e7b91-4c68-4f89-1b23-5d7e9f0a2c46
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  status: linked
                  expires_at: '2026-08-21T14:13:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  verified_at: '2026-08-21T14:05:41Z'
                page: 1
                page_size: 50
                total: 1
  /v1/wallet-links/{linkID}:
    delete:
      tags:
      - Vínculos de wallets
      operationId: revokeWalletLink
      summary: Revocar un vínculo de wallet
      description: "Revoca el vínculo de una wallet externa. El vínculo queda en el\n
        historial (append-only) con estado `revoked`. Idempotente: revocar un vínculo\n
        ya revocado devuelve el mismo estado."
      parameters:
      - name: linkID
        in: path
        required: true
        description: ID del vínculo.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Vínculo revocado.
          content:
            application/json:
              schema:
                type: object
                properties:
                  link:
                    $ref: '#/components/schemas/WalletLink'
              example:
                link:
                  link_id: d52e7b91-4c68-4f89-1b23-5d7e9f0a2c46
                  account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
                  chain: eth
                  address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
                  status: revoked
                  expires_at: '2026-08-21T14:13:11Z'
                  created_at: '2026-08-21T14:03:11Z'
                  proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
                  verified_at: '2026-08-21T14:05:41Z'
                  revoked_at: '2026-08-21T18:12:03Z'
        '404':
          description: El vínculo no existe o no pertenece a la cuenta.
        '409':
          description: El vínculo no está en un estado revocable (`invalid_state`).
  /v1/payouts/qr/scan:
    post:
      tags:
      - Payouts
      operationId: payoutQrScan
      summary: Escanear un QR de payout (Bolivia, Brasil)
      description: 'Paso 1 del payout por QR — Bolivia (QR interoperable local) y Brasil (QR PIX, incluyendo
        el código "copia e cola"): lee un QR de cobro y devuelve los datos del destinatario más el `provider_reference`
        necesario para confirmar. **Sin costo** — no se debita nada hasta el paso de confirmación. Por
        defecto es Bolivia; para Brasil envía `country: "BR"` y `currency: "BRL"`. Brasil soporta QR PIX
        **estáticos** (con la llave embebida); un QR dinámico (payload con URL del PSP) responde `400` —
        pide al beneficiario su llave PIX y usa el método `pix`. Un `amount` vacío en la respuesta del
        scan significa QR de monto abierto.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - qr_payload
              properties:
                country:
                  type: string
                  enum:
                  - BO
                  - BR
                  default: BO
                currency:
                  type: string
                  enum:
                  - BOB
                  - USD
                  - BRL
                  default: BOB
                qr_payload:
                  type: string
                  description: Contenido crudo del QR escaneado (en Brasil, también el código PIX "copia
                    e cola").
            example:
              qr_payload: <QR content>
              currency: BOB
      responses:
        '200':
          description: Datos del destinatario para la confirmación del usuario.
          content:
            application/json:
              schema:
                type: object
                properties:
                  scan_id:
                    type: string
                  provider_reference:
                    type: string
                  beneficiary_name:
                    type: string
                  destination_account:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  glosa:
                    type: string
                  status:
                    type: string
              example:
                scan_id: 5c1d2e3f-4a5b-6c7d-8e9f-0a1b2c3d4e5f
                provider_reference: QR-REF-889923
                beneficiary_name: Juan Quispe
                destination_account: '****4521'
                amount: '700.00'
                currency: BOB
                glosa: ''
                status: scanned
        '400':
          description: Falta qr_payload (invalid_request) o QR ilegible/no soportado (invalid_qr_payload
            — BR Code corrupto, checksum inválido o QR PIX dinámico; el mensaje explica la razón exacta).
          content:
            application/json:
              schema: &id010
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_qr_payload
                message: 'dynamic pix qr codes are not supported yet: pay with method pix using the beneficiary
                  pix key'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
        '502':
          description: Core no disponible (core_unavailable).
          content:
            application/json:
              schema: *id010
              example:
                error: core_unavailable
                message: could not scan the QR
  /v1/payouts/qr/confirm:
    post:
      tags:
      - Payouts
      operationId: payoutQrConfirm
      summary: Confirmar un payout por QR (Bolivia, Brasil)
      description: 'Paso 2 del payout por QR (Bolivia y Brasil/PIX), cobrado exactamente igual que un
        payout normal: el monto local se convierte a **la tasa de tu cuenta** y se debita `usdt_amount
        + comisión fija`. El resultado es **síncrono** — la respuesta trae el estado final (`completed`,
        o `failed` con reembolso automático). Requiere una llave de idempotencia como toda operación que
        mueve dinero. En Bolivia la referencia escaneada es de un solo uso (un QR = un pago); en Brasil
        un **QR PIX estático es reutilizable por diseño** — págalo de nuevo con una llave distinta, y un
        intento fallido jamás inutiliza el QR. `amount` es siempre obligatorio; en Brasil, si el QR PIX
        trae monto fijo debe coincidir exacto — un mismatch responde `422` con el payout en `status:
        failed` y el reembolso ya aplicado (los QR de monto abierto pagan lo que envíes).'
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - provider_reference
              - amount
              properties:
                country:
                  type: string
                  enum:
                  - BO
                  - BR
                  default: BO
                currency:
                  type: string
                  enum:
                  - BOB
                  - USD
                  - BRL
                  default: BOB
                provider_reference:
                  type: string
                  description: Del paso de escaneo.
                amount:
                  type: string
                  description: Monto local como string decimal positivo.
                description:
                  type: string
                settlement_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  description: Saldo virtual a debitar para esta operación. Omítelo para usar el asset
                    de settlement predeterminado de tu cuenta.
                idempotency_key:
                  type: string
            example:
              provider_reference: QR-REF-889923
              amount: '700.00'
              currency: BOB
              description: QR lunch payment
              idempotency_key: qr-2026-07-07-a
      responses:
        '202':
          description: Resultado final (síncrono).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example:
                payout_id: 8e2a1b0c-9d8e-7f60-5a4b-3c2d1e0f9a8b
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: qr-2026-07-07-a
                country: BO
                currency: BOB
                method: qr
                local_amount: '700.00'
                fx_rate: '6.91'
                usdt_amount: '101.302460'
                fee: '0.300000'
                total_debit: '101.602460'
                settlement_asset: USDT
                settlement_amount: '101.602460'
                settlement_rate: '1'
                beneficiary: &id011
                  qr_reference: QR-REF-889923
                description: QR lunch payment
                status: completed
                status_code: ''
                status_message: ''
                bank_reference: '00761123456'
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:01Z'
        '200':
          description: Respuesta idempotente de un payout por QR existente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example:
                payout_id: 8e2a1b0c-9d8e-7f60-5a4b-3c2d1e0f9a8b
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: qr-2026-07-07-a
                country: BO
                currency: BOB
                method: qr
                local_amount: '700.00'
                fx_rate: '6.91'
                usdt_amount: '101.302460'
                fee: '0.300000'
                total_debit: '101.602460'
                settlement_asset: USDT
                settlement_amount: '101.602460'
                settlement_rate: '1'
                beneficiary: *id011
                description: QR lunch payment
                status: completed
                status_code: ''
                status_message: ''
                bank_reference: '00761123456'
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:01Z'
                idempotency_hit: true
        '400':
          description: invalid_request / idempotency_key_required / invalid_amount
          content:
            application/json:
              schema: *id010
              example:
                error: invalid_request
                message: provider_reference (from the scan) is required
        '401':
          $ref: '#/components/responses/Unauthorized'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '403':
          description: Cuenta no activa (account_blocked).
          content:
            application/json:
              schema: *id010
              example:
                error: account_blocked
                message: account is not active
        '422':
          description: Rechazado por el procesador; el débito fue reembolsado (payout con status failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payout'
              example:
                payout_id: 8e2a1b0c-9d8e-7f60-5a4b-3c2d1e0f9a8b
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                idempotency_key: qr-2026-07-07-a
                country: BO
                currency: BOB
                method: qr
                local_amount: '700.00'
                fx_rate: '6.91'
                usdt_amount: '101.302460'
                fee: '0.300000'
                total_debit: '101.602460'
                settlement_asset: USDT
                settlement_amount: '101.602460'
                settlement_rate: '1'
                beneficiary: *id011
                description: QR lunch payment
                status: failed
                status_code: core_rejected
                status_message: QR already paid
                bank_reference: ''
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:01Z'
  /v1/stored-cards:
    get:
      tags:
      - Tarjetas guardadas
      operationId: listStoredCards
      summary: 'Listar tarjetas guardadas'
      description: 'Tarjetas guardadas por tus pagadores en el checkout hosted (mandato COF): solo datos de display (marca, últimos 4, expiración) — el número de tarjeta jamás existe en esta plataforma. Filtra por `payer_reference` para mostrarle a un cliente recurrente sus tarjetas. Requiere `from`/`to`.'
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: payer_reference
        in: query
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
          enum:
          - active
          - revoked
      responses:
        '200':
          description: 'Tarjetas guardadas paginadas.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  stored_cards:
                    type: array
                    items:
                      type: object
              example:
                page: 1
                page_size: 50
                stored_cards:
                - stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                  payer_reference: customer-1042
                  country: BO
                  currency: BOB
                  brand: visa
                  last4: '2701'
                  expiry_month: '12'
                  expiry_year: '2028'
                  status: active
                  created_at: '2026-07-20T18:00:00Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/stored-cards/{storedCardID}:
    get:
      tags:
      - Tarjetas guardadas
      operationId: getStoredCard
      summary: 'Obtener una tarjeta guardada'
      parameters:
      - name: storedCardID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La tarjeta guardada.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  stored_card_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  country:
                    type: string
                  currency:
                    type: string
                  brand:
                    type: string
                  last4:
                    type: string
                  expiry_month:
                    type: string
                  expiry_year:
                    type: string
                  seed_payin_id:
                    type: string
                    format: uuid
                  status:
                    type: string
                    enum:
                    - active
                    - revoked
                  revoked_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
              example:
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                payer_reference: customer-1042
                country: BO
                currency: BOB
                brand: visa
                last4: '2701'
                expiry_month: '12'
                expiry_year: '2028'
                seed_payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
                status: active
                created_at: '2026-07-20T18:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    delete:
      tags:
      - Tarjetas guardadas
      operationId: revokeStoredCard
      summary: 'Revocar una tarjeta guardada'
      description: 'Revoca la credencial upstream y marca el espejo `revoked` (la historia jamás se borra). Los cobros iniciados por el comercio y los pagos con tarjeta guardada dejan de funcionar al instante. Emite el webhook `stored_card_revoked`.'
      parameters:
      - name: storedCardID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La tarjeta guardada, ahora revocada.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  stored_card_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  country:
                    type: string
                  currency:
                    type: string
                  brand:
                    type: string
                  last4:
                    type: string
                  expiry_month:
                    type: string
                  expiry_year:
                    type: string
                  seed_payin_id:
                    type: string
                    format: uuid
                  status:
                    type: string
                    enum:
                    - active
                    - revoked
                  revoked_at:
                    type: string
                    format: date-time
                  created_at:
                    type: string
                    format: date-time
              example:
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                payer_reference: customer-1042
                country: BO
                currency: BOB
                brand: visa
                last4: '2701'
                expiry_month: '12'
                expiry_year: '2028'
                seed_payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
                status: revoked
                created_at: '2026-07-20T18:00:00Z'
                revoked_at: '2026-07-21T10:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/stored-cards/{storedCardID}/charges:
    post:
      tags:
      - Tarjetas guardadas
      operationId: chargeStoredCard
      summary: 'Cobrar una tarjeta guardada (iniciado por el comercio)'
      description: 'Cobra la tarjeta guardada SIN el pagador presente (MIT bajo el mandato de las marcas, citando la semilla 3-D Secure consentida original). Usa `recurring: true` para cobros de suscripción a intervalos fijos; false (default) para cobros no programados. Requiere `idempotency_key` — un retry con la misma clave devuelve el payin original y jamás cobra dos veces. El cobro aprobado acredita el saldo automático (webhook `payin_credited`); un cobro declinado responde 422 con el payin marcado failed.'
      parameters:
      - name: storedCardID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - amount
              - idempotency_key
              properties:
                amount:
                  type: string
                currency:
                  type: string
                description:
                  type: string
                recurring:
                  type: boolean
                  default: false
                idempotency_key:
                  type: string
            example:
              amount: '45.00'
              description: Monthly subscription
              recurring: true
              idempotency_key: sub-2026-07-cust1042
      responses:
        '201':
          description: 'Cobro aceptado: los aprobados acreditan automático; consulta el estado del payin.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  payin_id:
                    type: string
                  status:
                    type: string
                  reference:
                    type: string
                  transaction_id:
                    type: string
                  note:
                    type: string
              example:
                payin_id: 3c5b002c-af76-4235-8145-9f07fef073fd
                status: pending
                reference: 3c5b002c-af76-4235-8145-9f07fef073fd
                transaction_id: '7846012604816236704806'
                note: charge approved; the balance is credited automatically (payin_credited webhook)
        '200':
          description: 'Replay idempotente — el payin original.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  payin_id:
                    type: string
                  status:
                    type: string
                  idempotency_hit:
                    type: boolean
              example:
                payin_id: 3c5b002c-af76-4235-8145-9f07fef073fd
                status: credited
                reference: 3c5b002c-af76-4235-8145-9f07fef073fd
                idempotency_hit: true
        '422':
          description: 'Tarjeta declinada por el emisor, tarjeta guardada revocada, o el cobro fue rechazado antes de capturar (p. ej. la tarjeta guardada tiene una dirección de facturación incompleta en archivo).'
          content:
            application/json:
              schema:
                type: object
              examples:
                declined:
                  value:
                    payin_id: cb781262-a53f-4ec8-a833-013edcf907d3
                    status: failed
                    reference: cb781262-a53f-4ec8-a833-013edcf907d3
                    failure_reason: DECLINED
                revoked:
                  value:
                    error: stored_card_revoked
                    message: the stored card is revoked
                core_rejected:
                  value:
                    error: core_rejected
                    message: 'bill_to is incomplete and the stored credential has no billing address on file: send bill_to (first_name, last_name, email, address1, locality, postal_code, country) or re-register the card'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/subscriptions:
    post:
      tags:
      - Suscripciones
      operationId: createSubscription
      summary: 'Crear una suscripción'
      description: 'Cobros recurrentes sobre una tarjeta guardada (la plataforma lleva el calendario por ti). El primer período se cobra síncrono en la creación salvo que start_at sea futuro (trial). Cada cobro es un payin de tarjeta normal (se acredita por payin_credited); un decline reintenta diario hasta 3 veces y luego la suscripción pasa a past_due. idempotency_key es obligatoria (un retry devuelve la suscripción original). Requiere una tarjeta guardada activa del pagador.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - stored_card_id
              - amount
              - interval
              - idempotency_key
              properties:
                stored_card_id:
                  type: string
                  format: uuid
                amount:
                  type: string
                currency:
                  type: string
                interval:
                  type: string
                  enum:
                  - daily
                  - weekly
                  - monthly
                  - yearly
                description:
                  type: string
                start_at:
                  type: string
                  format: date-time
                idempotency_key:
                  type: string
            example:
              stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
              amount: '45.00'
              interval: monthly
              description: Monthly plan
              idempotency_key: plan-cust1042-monthly
      responses:
        '201':
          description: 'Suscripción creada (first_charge presente cuando el primer período se cobró en la creación).'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '200':
          description: 'Replay idempotente — la suscripción original.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Suscripciones
      operationId: listSubscriptions
      summary: 'Listar suscripciones'
      description: 'Tus suscripciones (los org admin ven toda la org con account_id). Filtra por status, stored_card_id o payer_reference. Requiere from/to.'
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: status
        in: query
        schema:
          type: string
          enum:
          - active
          - paused
          - past_due
          - canceled
      - name: stored_card_id
        in: query
        schema:
          type: string
      - name: payer_reference
        in: query
        schema:
          type: string
      responses:
        '200':
          description: 'Tus suscripciones (los org admin ven toda la org con account_id). Filtra por status, stored_card_id o payer_reference. Requiere from/to.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  subscriptions:
                    type: array
                    items:
                      type: object
              example:
                page: 1
                page_size: 50
                subscriptions:
                - subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                  stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                  payer_reference: customer-1042
                  amount: '45.00'
                  currency: BOB
                  interval: monthly
                  status: active
                  period: 1
                  next_charge_at: '2026-08-20T18:00:00Z'
                  created_at: '2026-07-20T18:00:00Z'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/subscriptions/{subscriptionID}:
    get:
      tags:
      - Suscripciones
      operationId: getSubscription
      summary: 'Obtener una suscripción'
      parameters:
      - name: subscriptionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La suscripción.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/subscriptions/{subscriptionID}/pause:
    post:
      tags:
      - Suscripciones
      operationId: pauseSubscription
      summary: 'Pausar una suscripción'
      description: 'Detiene los cobros (desde active). Reanudar NO recupera los períodos perdidos.'
      parameters:
      - name: subscriptionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La suscripción.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '409':
          description: The subscription state does not allow the transition.
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/subscriptions/{subscriptionID}/resume:
    post:
      tags:
      - Suscripciones
      operationId: resumeSubscription
      summary: 'Reanudar una suscripción'
      description: 'Reactiva desde paused o past_due. Desde past_due reintenta el período pendiente una vez (un nuevo decline vuelve a past_due). Desde paused con fecha futura conserva el calendario original (sin cobro anticipado).'
      parameters:
      - name: subscriptionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La suscripción.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '409':
          description: The subscription state does not allow the transition.
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/subscriptions/{subscriptionID}/cancel:
    post:
      tags:
      - Suscripciones
      operationId: cancelSubscription
      summary: 'Cancelar una suscripción'
      description: 'Terminal. Revocar la tarjeta guardada cancela también sus suscripciones automáticamente.'
      parameters:
      - name: subscriptionID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'La suscripción.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription_id:
                    type: string
                    format: uuid
                  account_id:
                    type: string
                    format: uuid
                  stored_card_id:
                    type: string
                    format: uuid
                  payer_reference:
                    type: string
                  amount:
                    type: string
                  currency:
                    type: string
                  description:
                    type: string
                  interval:
                    type: string
                    enum:
                    - daily
                    - weekly
                    - monthly
                    - yearly
                  status:
                    type: string
                    enum:
                    - active
                    - paused
                    - past_due
                    - canceled
                  period:
                    type: integer
                  next_charge_at:
                    type: string
                    format: date-time
                  failed_attempts:
                    type: integer
                  last_payin_id:
                    type: string
                    format: uuid
                  cancel_reason:
                    type: string
                  created_at:
                    type: string
                    format: date-time
              example:
                subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
                payer_reference: customer-1042
                amount: '45.00'
                currency: BOB
                description: Monthly plan
                interval: monthly
                status: active
                period: 1
                next_charge_at: '2026-08-20T18:00:00Z'
                created_at: '2026-07-20T18:00:00Z'
        '409':
          description: The subscription state does not allow the transition.
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/collect:
    post:
      tags:
      - Payins
      operationId: payinCollect
      summary: Cobro activo (pull)
      description: 'Cobra al pagador directamente en los corredores que soportan cobros pull (ej. Venezuela
        `c2p` / `debito_inmediato`). Síncrono: si el cobro se aprueba, el depósito se convierte a tu `payin_rate`
        vigente y se acredita en la misma llamada, neto de la comisión de payin. Si el pagador rechaza,
        `paid` es false, el payin queda marcado como failed y no se cobra nada.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - method
              - amount
              - idempotency_key
              properties:
                country:
                  type: string
                  default: VE
                currency:
                  type: string
                  default: VES
                method:
                  type: string
                  description: Método de cobro (ej. c2p, debito_inmediato).
                amount:
                  type: string
                  description: Monto local como string decimal positivo.
                description:
                  type: string
                our_reference:
                  type: string
                payer_document:
                  type: string
                payer_phone:
                  type: string
                payer_bank:
                  type: string
                payer_account:
                  type: string
                payer_name:
                  type: string
                payer_account_type:
                  type: string
                otp:
                  type: string
                otp_reference:
                  type: string
                idempotency_key:
                  type: string
                  description: Obligatoria — el collect ejecuta un cargo real; reintentos con la misma
                    clave no vuelven a cobrar.
            example:
              method: c2p
              amount: '1200.00'
              description: Order 5512
              payer_document: V12345678
              payer_phone: '04141234567'
              payer_bank: '0102'
              otp: '12345678'
              idempotency_key: collect-2026-07-07-a
      responses:
        '200':
          description: 'Resultado del cobro. `paid: true` significa que el depósito fue acreditado.'
          content:
            application/json:
              schema:
                type: object
                properties:
                  payin_id:
                    type: string
                  kind:
                    type: string
                  country:
                    type: string
                  currency:
                    type: string
                  method:
                    type: string
                  local_amount:
                    type: string
                  status:
                    type: string
                  fx_rate:
                    type: string
                  usdt_gross:
                    type: string
                  fee:
                    type: string
                  usdt_credited:
                    type: string
                  paid:
                    type: boolean
                  provider_reference:
                    type: string
                  status_code:
                    type: string
                  status_message:
                    type: string
                  failure:
                    type: object
                    description: 'Presente solo cuando el payin queda `failed` en cobros activos: dónde se originó el rechazo (`provider` = el banco del pagador, `core` = la validación previa al cobro) más el código y mensaje concretos.'
                    properties:
                      source:
                        type: string
                      code:
                        type: string
                      message:
                        type: string
              example:
                payin_id: 7b3c9e2f-1a2b-3c4d-5e6f-7a8b9c0d1e2f
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                kind: collect
                country: VE
                currency: VES
                method: c2p
                local_amount: '1200.00'
                status: credited
                reference: REF-102030
                fx_rate: '36.50'
                usdt_gross: '32.876712'
                fee: '0.300000'
                usdt_credited: '32.576712'
                paid: true
                provider_reference: PRV-889911
                status_code: '00'
                status_message: approved
                created_at: '2026-07-07T12:00:00Z'
                updated_at: '2026-07-07T12:00:02Z'
        '400':
          description: invalid_request / invalid_amount
          content:
            application/json:
              schema: &id012
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_request
                message: method is required (e.g. c2p, debito_inmediato)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Cuenta no activa (account_blocked).
          content:
            application/json:
              schema: *id012
              example:
                error: account_blocked
                message: account is not active
        '502':
          description: Core no disponible (core_unavailable).
          content:
            application/json:
              schema: *id012
              example:
                error: core_unavailable
                message: could not execute the collection
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
  /v1/payins/collect/otp:
    post:
      tags:
      - Payins
      operationId: payinCollectOtp
      summary: Solicitar un OTP de cobro
      description: Solicita la contraseña de un solo uso que algunos métodos pull necesitan antes del
        cobro (ej. débito inmediato). Sin costo.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - method
              - amount
              properties:
                country:
                  type: string
                  default: VE
                currency:
                  type: string
                  default: VES
                method:
                  type: string
                amount:
                  type: string
                payer_document:
                  type: string
                payer_phone:
                  type: string
                payer_bank:
                  type: string
                payer_account:
                  type: string
            example:
              method: debito_inmediato
              amount: '1200.00'
              payer_document: V12345678
              payer_phone: '04141234567'
              payer_bank: '0102'
              payer_account: 01020123456789012345
      responses:
        '200':
          description: OTP enviado al pagador.
          content:
            application/json:
              schema:
                type: object
                properties:
                  method:
                    type: string
                  result:
                    type: object
                    additionalProperties: true
              example:
                method: debito_inmediato
                result:
                  status: sent
                  otp_reference: OTP-5521
        '400':
          description: invalid_request
          content:
            application/json:
              schema: *id012
              example:
                error: invalid_request
                message: method is required
        '401':
          $ref: '#/components/responses/Unauthorized'
        '502':
          description: Core no disponible (core_unavailable).
          content:
            application/json:
              schema: *id012
              example:
                error: core_unavailable
                message: could not request the OTP
  /v1/payins/deposit-accounts:
    post:
      tags:
      - Payins
      operationId: createDepositAccount
      summary: Crear una cuenta de depósito dedicada
      description: Aprovisiona un destino de depósito fijo ligado a tu cuenta en los corredores que lo
        soportan (ej. una CLABE mexicana o una CVU argentina). Cada transferencia que llega a él se acredita
        automáticamente, neta de la comisión de payin. La creación es gratuita.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                country:
                  type: string
                  default: MX
                currency:
                  type: string
                  default: MXN
                method:
                  type: string
                  default: bank_transfer
            examples:
              mx_clabe:
                summary: México — CLABE dedicada (MXN)
                value:
                  country: MX
                  currency: MXN
              ar_cvu:
                summary: Argentina — CVU dedicada (ARS)
                value:
                  country: AR
                  currency: ARS
      responses:
        '201':
          description: Cuenta de depósito creada y ligada a la cuenta que llama.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DepositInstrument'
              example: &id013
                instrument_id: a1d4b7c0-1234-5678-9abc-def012345678
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                country: MX
                currency: MXN
                method: bank_transfer
                instrument: '734180000151000006'
                details:
                  clabe: '734180000151000006'
                  status: active
                status: active
                created_at: '2026-07-07T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Cuenta no activa (account_blocked).
          content:
            application/json:
              schema: *id012
              example:
                error: account_blocked
                message: account is not active
        '502':
          description: Core no disponible (core_unavailable).
          content:
            application/json:
              schema: *id012
              example:
                error: core_unavailable
                message: could not create the deposit account
    get:
      tags:
      - Payins
      operationId: listDepositAccounts
      summary: Listar mis cuentas de depósito
      description: Lista los destinos de depósito dedicados ligados a la cuenta que llama.
      responses:
        '200':
          description: Cuentas de depósito de la cuenta que llama.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deposit_accounts:
                    type: array
                    items:
                      $ref: '#/components/schemas/DepositInstrument'
              example:
                deposit_accounts:
                - *id013
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/payins/deposit-instructions:
    get:
      tags:
      - Payins
      operationId: getDepositInstructions
      summary: Previsualizar las instrucciones de depósito de un corredor
      description: |
        Devuelve el texto de cuenta bancaria que tu organización publica para
        un corredor `bank_transfer` — el mismo contenido que se embebe
        automáticamente en la respuesta de un payin anunciado. Útil para
        renderizar una pantalla "¿dónde envío la plata?" antes de que el
        pagador cree el payin, o para mostrar las instrucciones vigentes en
        una página estática.

        Solo los corredores con una instrucción activa configurada por el
        admin devuelven contenido; el resto responde `404 not_found` (nada
        que previsualizar — el corredor no lo requiere o nadie lo configuró
        aún).
      parameters:
      - name: country
        in: query
        required: true
        schema:
          type: string
        example: CL
      - name: currency
        in: query
        required: true
        schema:
          type: string
        example: CLP
      - name: method
        in: query
        schema:
          type: string
          default: bank_transfer
      responses:
        '200':
          description: Instrucciones de depósito activas del corredor.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deposit_instructions:
                    $ref: '#/components/schemas/DepositInstructions'
                  deposit_instructions_swift:
                    $ref: '#/components/schemas/DepositInstructions'
                    description: >-
                      Variante internacional SWIFT del corredor (solo US/USD,
                      cuando está configurada). Mismo shape, con su propio
                      `qr_payload` y `qr_png_base64`.
              examples:
                cl_bank_transfer:
                  summary: Chile (CLP)
                  value:
                    deposit_instructions:
                      id: 9f0c1a2b-3d4e-4f5a-8b9c-1a2b3c4d5e6f
                      country: CL
                      currency: CLP
                      method: bank_transfer
                      purpose: payin
                      revision: 3
                      active: true
                      reference_required: true
                      bank_name: Banco de Ejemplo
                      account_number: '0001112223'
                      account_type: checking
                      holder_name: Ejemplo Org SpA
                      created_at: '2026-07-15T14:02:00Z'
                      qr_payload: |
                        Bank: Banco de Ejemplo
                        Account type: checking
                        Account number: 0001112223
                        Holder: Ejemplo Org SpA
                      qr_png_base64: iVBORw0KGgoAAAANSU...
                us_bank_transfer:
                  summary: 'Estados Unidos (USD — doble riel: ABA doméstico + SWIFT internacional)'
                  value:
                    deposit_instructions:
                      id: 7b1e4d5a-2c3b-4a59-8877-6f5e4d3c2b1a
                      country: US
                      currency: USD
                      method: bank_transfer
                      purpose: payin
                      revision: 1
                      active: true
                      reference_required: true
                      bank_name: Partner Bank, N.A.
                      account_number: '000123456789'
                      account_type: checking
                      holder_name: CBPay Operations LLC
                      holder_tax_id: 88-1234567
                      routing_number: '021000021'
                      holder_address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                      created_at: '2026-08-07T10:00:00Z'
                      qr_payload: |
                        Bank: Partner Bank, N.A.
                        Account type: checking
                        Account number: 000123456789
                        Routing number (ABA): 021000021
                        Holder: CBPay Operations LLC
                        Holder address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                        Tax ID: 88-1234567
                      qr_png_base64: iVBORw0KGgoAAAANSU...
                    deposit_instructions_swift:
                      id: 8c2f5e6b-3d4c-5b6a-9988-7f6e5d4c3b2a
                      country: US
                      currency: USD
                      method: swift
                      purpose: payin
                      revision: 1
                      active: true
                      reference_required: true
                      bank_name: Partner Bank International
                      account_number: '9870001234'
                      account_type: checking
                      holder_name: CBPay Operations LLC
                      holder_tax_id: 88-1234567
                      swift: PRTBPRI3
                      bank_address: 200 Example Blvd, San Juan, PR 00901, PR
                      intermediary_bank_name: Intermediary Bank N.A.
                      intermediary_bank_swift: INTRUS33
                      holder_address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                      notes: Select Puerto Rico as the final beneficiary bank country
                      created_at: '2026-08-07T10:00:00Z'
                      qr_payload: |
                        Bank: Partner Bank International
                        Account type: checking
                        Account number: 9870001234
                        SWIFT: PRTBPRI3
                        Bank address: 200 Example Blvd, San Juan, PR 00901, PR
                        Intermediary bank: Intermediary Bank N.A.
                        Intermediary SWIFT: INTRUS33
                        Holder: CBPay Operations LLC
                        Holder address: 25 SW 9th Street, Suite 406, Miami, FL 33130, US
                        Tax ID: 88-1234567
                        Note: Select Puerto Rico as the final beneficiary bank country
                      qr_png_base64: iVBORw0KGgoAAAANSU...
        '400':
          description: Faltan country o currency (invalid_payload).
          content:
            application/json:
              schema: *id012
              example:
                error: invalid_payload
                message: country and currency are required
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: No hay instrucciones de depósito activas para este corredor
            (not_found).
          content:
            application/json:
              schema: *id012
              example:
                error: not_found
                message: no deposit instructions configured for this corridor
  /v1/banking/customer:
    post:
      tags:
      - Banking
      operationId: createBankingCustomer
      summary: Crear mi perfil bancario
      description: Abre el perfil bancario de la cuenta (una vez por cuenta). Si omites `type`/`name`/`email`,
        se completan desde tu cuenta CBPay. Se cobra la comisión fija `banking_customer` desde tu settlement
        asset (USDT por defecto); se reembolsa si el corredor rechaza.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                currency:
                  type: string
                  example: USD
                type:
                  type: string
                  enum:
                  - INDIVIDUAL
                  - COMPANY
                name:
                  type: string
                email:
                  type: string
                address:
                  type: object
                  additionalProperties: true
            example:
              currency: USD
              address:
                countryIso: CL
                city: Santiago
      responses:
        '201':
          description: Perfil bancario creado (estado draft).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                provider_id: BNK-88f13a
                status: draft
                data:
                  item:
                    type: INDIVIDUAL
                    name: Ana Perez Rojas
                created_at: '2026-07-07T12:00:00Z'
                banking_fee: '5.000000'
                banking_fee_asset: USDT
        '202':
          description: Solicitud retenida para revisión (la organización tiene la revisión de solicitudes activada).
            El recurso se crea solo cuando compliance aprueba la revisión; la comisión de emisión/apertura se cobra al
            retener y se reembolsa automáticamente si se rechaza. Sigue el resultado con el webhook
            `txn_review_status_changed` o `GET /v1/me/txn-reviews`.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                status: in_review
                kind: banking_application
                review_id: 7a1c2e94-3b5f-4c8a-9d2e-6f0a1b2c3d4e
                message: application received; it is pending review by our compliance team
        '401':
          $ref: '#/components/responses/Unauthorized'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '409':
          description: Esta cuenta ya tiene perfil bancario (banking_customer_exists).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: banking_customer_exists
                message: this account already has a banking customer
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
    get:
      tags:
      - Banking
      operationId: getBankingCustomer
      summary: Consultar mi perfil bancario
      description: Devuelve el perfil bancario de la cuenta con su estado de verificación en vivo.
      responses:
        '200':
          description: Perfil bancario con estado normalizado (draft, submitted, under_review, approved,
            rejected).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                provider_id: BNK-88f13a
                status: approved
                data:
                  item:
                    type: INDIVIDUAL
                    name: Ana Perez Rojas
                created_at: '2026-07-07T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
  /v1/banking/customer/documents:
    post:
      tags:
      - Banking
      operationId: uploadBankingDocument
      summary: Subir un documento de verificación
      description: Sube un documento KYC (base64) al perfil bancario. Gratis.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - attach
              properties:
                attach:
                  type: string
                  description: Base64 file content.
                filename:
                  type: string
                type:
                  type: string
                  example: PASSPORT
                info:
                  type: string
            example:
              type: PASSPORT
              filename: passport.pdf
              attach: JVBERi0xLjQK...
      responses:
        '201':
          description: Documento adjuntado.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                status: draft
                data:
                  documentId: DOC-1129
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
  /v1/banking/customer/submit:
    post:
      tags:
      - Banking
      operationId: submitBankingCustomer
      summary: Enviar mi perfil a revisión
      description: Envía el perfil bancario a verificación. Gratis. Sigue el avance con GET /v1/banking/customer
        o el webhook banking_customer_status_changed.
      responses:
        '202':
          description: Perfil enviado (estado submitted).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                status: submitted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
  /v1/banking/accounts:
    post:
      tags:
      - Banking
      operationId: createBankingAccount
      summary: Abrir una cuenta bancaria
      description: Abre una cuenta bancaria para el perfil aprobado (una por moneda habilitada). Se cobra
        la comisión fija `banking_account`; se reembolsa si el corredor rechaza. `data` trae los datos
        para recibir (número de cuenta/IBAN, routing, banco).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - currency
              properties:
                currency:
                  type: string
                  example: USD
                name:
                  type: string
            example:
              currency: USD
              name: Operating USD
      responses:
        '201':
          description: Cuenta bancaria creada.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                account_id: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                provider_id: BNK-ACC-4471
                status: active
                data:
                  currency: USD
                  name: Operating USD
                  accountNumber: '0532013000'
                  routingNumber: 011000138
                banking_fee: '1.000000'
                banking_fee_asset: USDT
        '202':
          description: Solicitud retenida para revisión (la organización tiene la revisión de solicitudes activada).
            El recurso se crea solo cuando compliance aprueba la revisión; la comisión de emisión/apertura se cobra al
            retener y se reembolsa automáticamente si se rechaza. Sigue el resultado con el webhook
            `txn_review_status_changed` o `GET /v1/me/txn-reviews`.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                status: in_review
                kind: banking_application
                review_id: 7a1c2e94-3b5f-4c8a-9d2e-6f0a1b2c3d4e
                message: application received; it is pending review by our compliance team
        '401':
          $ref: '#/components/responses/Unauthorized'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
    get:
      tags:
      - Banking
      operationId: listBankingAccounts
      summary: Listar mis cuentas bancarias
      responses:
        '200':
          description: Cuentas bancarias de la cuenta que llama.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                items:
                - id: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                  provider_id: BNK-ACC-4471
                  status: active
                  payload:
                    currency: USD
                meta:
                  page: 1
                  page_size: 50
                  retrieved: 1
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
  /v1/banking/accounts/{bankAccountID}:
    get:
      tags:
      - Banking
      operationId: getBankingAccount
      summary: Consultar una cuenta bancaria
      description: >-
        Devuelve el detalle EN VIVO de una de tus cuentas bancarias (nombre,
        moneda, estado y los requisitos para recibir fondos — rieles wire y
        locales — bajo `data`). Úsalo para mostrar las instrucciones de
        depósito de una cuenta específica.
        Si el banco no puede servir la cuenta en vivo, el endpoint devuelve el último snapshot espejado y `source` es `mirror` (en caso normal, `live`).
      parameters:
      - name: bankAccountID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Detalle en vivo de la cuenta bancaria.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                account_id: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                provider_id: BNK-ACC-4471
                source: live
                data:
                  name: Operating USD
                  currencyCode: USD
                  status: ACCEPT
                  requisites:
                  - transferMethod: SWIFT
                    accountNumber: '0532013000'
                    bankName: Example Bank N.A.
                    swiftCode: EXAMUS33
                  - transferMethod: ACH
                    accountNumber: '0532013000'
                    routingNumber: '011000138'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: La cuenta bancaria no pertenece a este perfil.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: not_found
                message: resource not found
        '409':
          description: Aún no hay perfil banking (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
  /v1/banking/accounts/{bankAccountID}/balance:
    get:
      tags:
      - Banking
      operationId: getBankingAccountBalance
      summary: Consultar el saldo de una cuenta bancaria
      parameters:
      - name: bankAccountID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Saldo en vivo en el banco. La forma recomendada de leer
            el monto es el resumen neutro de nivel superior (`available`,
            `held`, `currency` — strings decimales planos, presentes cuando el
            rail soporta el resumen); el objeto `balance` conserva el detalle
            completo nativo del rail (nombre visible, datos para recibir
            fondos).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                account_id: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                provider_id: BNK-ACC-4471
                available: '1250.00'
                held: '0.00'
                currency: USD
                balance:
                  name: OPERATING USD
                  currency: USD
                  balance:
                    money:
                      units: '1250'
                      nanos: 0
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: La cuenta bancaria no pertenece a este perfil.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: not_found
                message: resource not found
  /v1/banking/counterparties:
    post:
      tags:
      - Banking
      operationId: createBankingCounterparty
      summary: Registrar un beneficiario
      description: Registra un beneficiario tercero con sus datos bancarios. Gratis; la cuenta del beneficiario
        pasa por moderación antes de poder recibir pagos WITHDRAW.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                profile:
                  type: object
                  additionalProperties: true
                accounts:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
            example:
              description: ACME supplier
              profile:
                name: ACME LLC
                additionalInfo:
                  type: CORPORATION
              accounts:
              - currencyCode: USD
                bank:
                  name: Test Bank
                  number: 011000138
                fiat:
                  number: '0532013000'
                  routingNumber: 011000138
                  additionalInformation:
                    type: TYPE_FIAT_US
                    accountType: CHECKING
                    supportedRails:
                    - ACH
      responses:
        '201':
          description: Beneficiario registrado (cuentas pendientes de moderación).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                counterparty_id: b2c3d4e5-f6a7-b8c9-d0e1-f2a3b4c5d6e7
                provider_id: BNK-CP-7301
                status: active
                data: {}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
    get:
      tags:
      - Banking
      operationId: listBankingCounterparties
      summary: Listar mis beneficiarios
      responses:
        '200':
          description: Beneficiarios registrados.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                items:
                - id: b2c3d4e5-f6a7-b8c9-d0e1-f2a3b4c5d6e7
                  status: active
                meta:
                  page: 1
                  page_size: 50
                  retrieved: 1
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
  /v1/banking/counterparties/{counterpartyID}/accounts:
    post:
      tags:
      - Banking
      operationId: addBankingCounterpartyAccount
      summary: Agregar una cuenta a un beneficiario
      parameters:
      - name: counterpartyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
            example:
              currencyCode: USD
              fiat:
                number: '0532013001'
                routingNumber: 011000138
                additionalInformation:
                  type: TYPE_FIAT_US
                  accountType: SAVINGS
                  supportedRails:
                  - ACH
      responses:
        '201':
          description: Cuenta agregada (pendiente de moderación).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                counterparty_id: b2c3d4e5-f6a7-b8c9-d0e1-f2a3b4c5d6e7
                data:
                  status: MODERATION
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: El beneficiario no pertenece a este perfil.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: not_found
                message: resource not found
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
  /v1/banking/operations/prepare:
    post:
      tags:
      - Banking
      operationId: prepareBankingOperation
      summary: Cotizar un pago bancario
      description: Valida y cotiza una operación (comisiones, saldos) sin mover dinero. Gratis.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - currency
              - type
              - sourceRequisit
              - destinationRequisit
              - amount
              properties:
                currency:
                  type: string
                  example: USD
                type:
                  type: string
                  enum:
                  - TRANSFER
                  - WITHDRAW
                paymentType:
                  type: string
                  description: EMPTY for TRANSFER; rail code (e.g. SEPA_CT) for WITHDRAW.
                sourceRequisit:
                  type: object
                  properties:
                    account:
                      type: string
                destinationRequisit:
                  type: object
                  properties:
                    account:
                      type: string
                      description: Destination account (TRANSFER).
                    beneficiar:
                      type: string
                      description: Beneficiary account (WITHDRAW).
                amount:
                  type: object
                  properties:
                    currencyCode:
                      type: string
                    units:
                      type: string
                    nanos:
                      type: integer
                comment:
                  type: string
                idempotency_key:
                  type: string
            example:
              currency: USD
              type: WITHDRAW
              paymentType: SEPA_CT
              sourceRequisit:
                account: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
              destinationRequisit:
                beneficiar: BNK-CPACC-11
              amount:
                currencyCode: USD
                units: '250'
                nanos: 0
      responses:
        '200':
          description: Vista previa de la cotización.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                preview:
                  fee:
                    units: '1'
                    nanos: 500000000
                  valid: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
  /v1/banking/operations:
    post:
      tags:
      - Banking
      operationId: createBankingOperation
      summary: Enviar un pago bancario
      description: Ejecuta un TRANSFER (entre tus propias cuentas bancarias) o WITHDRAW (a un beneficiario
        registrado). Requiere clave de idempotencia; reintentos con la misma clave devuelven la operación
        original sin volver a cobrar. Se cobra la comisión fija `banking_operation`; se reembolsa si el
        corredor rechaza. El estado final llega por el webhook banking_operation_status_changed.
      parameters:
      - $ref: '#/components/parameters/idempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - currency
              - type
              - sourceRequisit
              - destinationRequisit
              - amount
              properties:
                currency:
                  type: string
                  example: USD
                type:
                  type: string
                  enum:
                  - TRANSFER
                  - WITHDRAW
                paymentType:
                  type: string
                  description: EMPTY for TRANSFER; rail code (e.g. SEPA_CT) for WITHDRAW.
                sourceRequisit:
                  type: object
                  properties:
                    account:
                      type: string
                destinationRequisit:
                  type: object
                  properties:
                    account:
                      type: string
                      description: Destination account (TRANSFER).
                    beneficiar:
                      type: string
                      description: Beneficiary account (WITHDRAW).
                amount:
                  type: object
                  properties:
                    currencyCode:
                      type: string
                    units:
                      type: string
                    nanos:
                      type: integer
                comment:
                  type: string
                idempotency_key:
                  type: string
            examples:
              withdraw:
                summary: WITHDRAW a un beneficiario (SEPA)
                value:
                  currency: USD
                  type: WITHDRAW
                  paymentType: SEPA_CT
                  sourceRequisit:
                    account: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                  destinationRequisit:
                    beneficiar: BNK-CPACC-11
                  amount:
                    currencyCode: USD
                    units: '250'
                    nanos: 0
                  comment: Invoice 8841
                  idempotency_key: acme-payment-0071
              transfer:
                summary: TRANSFER entre cuentas propias
                value:
                  currency: USD
                  type: TRANSFER
                  paymentType: EMPTY
                  sourceRequisit:
                    account: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                  destinationRequisit:
                    account: d5e2f3a4-b5c6-d7e8-f9a0-b1c2d3e4f5a6
                  amount:
                    currencyCode: USD
                    units: '100'
                    nanos: 0
                  idempotency_key: internal-move-0012
      responses:
        '202':
          description: Operación aceptada (estado final por webhook).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                operation_id: 7e8a9b0c-1d2e-3f4a-5b6c-7d8e9f0a1b2c
                provider_id: BNK-OP-90213
                status: pending
                idempotency_key: platform:9b1deb4d:acme-payment-0071
                data:
                  type: WITHDRAW
                  paymentType: SEPA_CT
                banking_fee: '2.000000'
                banking_fee_asset: BANK_EUR
        '200':
          description: Replay idempotente de una operación existente (sin cobro nuevo).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                operation_id: 7e8a9b0c-1d2e-3f4a-5b6c-7d8e9f0a1b2c
                provider_id: BNK-OP-90213
                status: pending
                idempotency_key: platform:9b1deb4d:acme-payment-0071
                data:
                  type: WITHDRAW
                  paymentType: SEPA_CT
                idempotency_hit: true
        '400':
          description: idempotency_key_required / payload inválido.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '402':
          $ref: '#/components/responses/InsufficientFunds'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
        '502':
          description: Error del corredor bancario; la comisión (si hubo) se reembolsó (banking_request_failed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: banking_request_failed
                  message: request rejected
    get:
      tags:
      - Banking
      operationId: listBankingOperations
      summary: Listar mis pagos bancarios
      description: >-
        Historial de tus operaciones bancarias, incluidos los depósitos
        entrantes y los cargos descubiertos automáticamente. Los ítems llevan
        la vista neutra enriquecida: los campos opcionales direction, amount
        (string decimal NETO — el acreditado para `in`, la comisión neta para
        `out`), currency, counterparty y reference aparecen solo cuando el
        proveedor bancario los reporta.
      parameters:
      - name: type
        in: query
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
      - name: from
        in: query
        schema:
          type: string
      - name: to
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
      - name: page_size
        in: query
        schema:
          type: integer
      responses:
        '200':
          description: Historial de operaciones (vista neutra enriquecida).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                items:
                - id: 7e8a9b0c-1d2e-3f4a-5b6c-7d8e9f0a1b2c
                  status: completed
                  type: withdraw
                  direction: out
                  amount: '250.00'
                  currency: USD
                  counterparty: ACME SUPPLIES GMBH
                  reference: SEPA-20260804-9911
                - id: 4c6d8e0f-2a4b-6c8d-0e2f-4a6c8e0b2d4f
                  status: completed
                  type: wire_transfer
                  direction: in
                  amount: '120.00'
                  currency: USD
                  counterparty: ACME SUPPLIES LLC
                  reference: FEDWIRE-20260804-77881
                meta:
                  page: 1
                  page_size: 50
                  retrieved: 2
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Aún no hay perfil bancario (no_banking_customer).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: no_banking_customer
                message: create your banking customer first (POST /v1/banking/customer)
  /v1/banking/operations/{operationID}:
    get:
      tags:
      - Banking
      operationId: getBankingOperation
      summary: Consultar un pago bancario
      description: >-
        Operación con estado en vivo y la vista neutra enriquecida: los campos
        opcionales direction, amount (string decimal neto), currency,
        counterparty y reference aparecen solo cuando el proveedor bancario los
        reporta.
      parameters:
      - name: operationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Operación con estado en vivo (vista neutra enriquecida).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                operation_id: 7e8a9b0c-1d2e-3f4a-5b6c-7d8e9f0a1b2c
                provider_id: BNK-OP-90213
                status: completed
                idempotency_key: platform:9b1deb4d:acme-payment-0071
                data:
                  type: WITHDRAW
                  paymentType: SEPA_CT
                direction: out
                amount: '250.00'
                currency: USD
                counterparty: ACME SUPPLIES GMBH
                reference: SEPA-20260804-9911
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: La operación no pertenece a esta cuenta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: not_found
                message: resource not found
  /v1/reports/statement:
    get:
      tags:
      - Saldos
      operationId: getAccountStatement
      summary: Cartola / estado de cuenta (JSON, PDF o Excel)
      description: 'Consolida todos los movimientos del período — payouts, payins, depósitos y retiros
        crypto, transferencias internas y cargos por servicio — en una cartola auditable con cuadratura
        exacta: opening_balance + total_in − total_out = closing_balance, verificada contra el ledger
        (`balanced`). El resumen y los movimientos cubren el saldo operativo USDT; cada otro saldo virtual
        (USDC, BTC, GOLD) con actividad concilia por separado dentro de `assets`. Pide `format=pdf` o
        `format=xlsx` para descargar el archivo con branding CBPay (Content-Disposition: attachment).'
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: 'Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive). Rango máximo: 400 días.'
      - name: format
        in: query
        schema:
          type: string
          enum:
          - json
          - pdf
          - xlsx
          default: json
        description: json para la vista web; pdf/xlsx descargan el archivo con branding.
      - name: lang
        in: query
        schema:
          type: string
          enum:
          - en
          - es
          - zh
          default: en
        description: Idioma del PDF/Excel. Por defecto ingles.
      responses:
        '200':
          description: La cartola (JSON) o los bytes del archivo (pdf/xlsx).
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                account:
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  display_name: Example Company SpA
                  email: ops@example.com
                  type: company
                period:
                  from: '2026-01-01'
                  to: '2026-07-07'
                  timezone: UTC
                generated_at: '2026-07-07T15:00:00Z'
                summary:
                  opening_balance: '0.000000'
                  total_in: '985633.540000'
                  total_out: '38099.870000'
                  net_change: '947533.670000'
                  closing_balance: '947533.670000'
                  balanced: true
                  counts:
                    payouts: 51
                    payins: 12
                    crypto_deposits: 18
                    crypto_withdrawals: 3
                    transfers: 4
                    service_charges: 2
                    movements: 771
                  fees_by_service:
                    payout: '15.300000'
                    funding: '897.550000'
                  total_fees: '912.850000'
                assets:
                - asset: GOLD
                  opening_balance: '0.000000'
                  total_in: '12.500000'
                  total_out: '2.000000'
                  net_change: '10.500000'
                  closing_balance: '10.500000'
                  balanced: true
                  movements:
                  - created_at: '2026-07-09T10:00:00Z'
                    type: adjustment
                    reference: adjustment gold-load-1
                    amount: '12.500000'
                    balance_after: '12.500000'
                breakdown:
                  by_product:
                  - product: payouts
                    count: 51
                    usdt_in: '0.000000'
                    usdt_out: '38099.870000'
                    fees: '15.300000'
                  by_country:
                  - flow: payouts
                    country: BO
                    currency: BOB
                    count: 14
                    local_amount: '28748.58'
                    usdt_amount: '2902.210000'
                  by_currency:
                  - currency: BOB
                    payout_local: '28748.58'
                    payin_local: '700.00'
                  by_month:
                  - month: 2026-01
                    usdt_in: '985633.540000'
                    usdt_out: '35100.000000'
                payouts:
                - created_at: '2026-03-19T22:57:00Z'
                  payout_id: 0d4f2a10-1b2c-3d4e-5f60-718293a4b5c6
                  idempotency_key: bo-90
                  country: BO
                  currency: BOB
                  method: bank_transfer
                  beneficiary: Juan Quispe
                  detail: '1234567890'
                  local_amount: '90.00'
                  fx_rate: '6.91'
                  usdt_amount: '13.024600'
                  fee: '0.300000'
                  fee_percent: '0.200000'
                  fee_fixed: '0.100000'
                  total_debit: '13.324600'
                  status: completed
                  bank_reference: '00761123456'
                payins: []
                crypto_deposits: []
                crypto_withdrawals: []
                transfers: []
                card_transactions:
                - created_at: '2026-03-20T10:12:00Z'
                  transaction_id: 5f6a7b8c-9d0e-1f2a-3b4c-5d6e7f8a9b0c
                  card_id: c0de0791-0000-0000-0000-000000000003
                  kind: purchase
                  merchant: AMAZON.COM
                  amount_usd: '25.00'
                  spend_asset: USDT
                  spend_amount: '25.000000'
                  status: settled
                swaps:
                - created_at: '2026-03-21T09:00:00Z'
                  swap_id: 1a2b3c4d-5e6f-7a8b-9c0d-1e2f3a4b5c6d
                  from_asset: USDT
                  to_asset: BTC
                  from_amount: '10.000000'
                  to_amount: '0.00015433'
                  rate: '0.00001543'
                  status: completed
                banking_operations:
                - created_at: '2026-03-22T14:30:00Z'
                  operation_id: 2b3c4d5e-6f7a-8b9c-0d1e-2f3a4b5c6d7e
                  direction: out
                  type: wire
                  currency: USD
                  amount: '150.00'
                  counterparty: Acme Inc
                  status: completed
                service_charges:
                - created_at: '2026-03-23T08:00:00Z'
                  type: banking_fee
                  service: banking_customer
                  fee_model: fixed
                  reference: cust-001
                  amount: '-0.500000'
                  balance_after: '139.100000'
                movements:
                - created_at: '2026-03-19T22:57:00Z'
                  type: payout_debit
                  reference: payout 0d4f2a10-1b2c-3d4e-5f60-718293a4b5c6
                  amount: '-13.324600'
                  balance_after: '139.600000'
        '400':
          description: invalid_range / invalid_format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_range
                message: from and to are required (YYYY-MM-DD)
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/transfers/{transferID}:
    get:
      tags:
      - Transferencias
      operationId: getTransfer
      summary: Consultar una transferencia
      description: Devuelve una transferencia; visible solo para sus dos partes.
      parameters:
      - name: transferID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: La transferencia.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example: *id014
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          description: No existe o no eres parte.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: not_found
                message: resource not found
  /v1/cards:
    post:
      tags:
      - Cards
      operationId: createCard
      summary: Crear una tarjeta
      description: 'Emite una tarjeta (virtual o física) que gasta Just-In-Time del saldo central de la
        cuenta en el asset de gasto de la tarjeta (`spending_asset`: USDT/USDC 1:1 con el USD, o BTC/GOLD
        convertidos al precio del momento de cada evento; USDT por defecto) — sin prefondeo: cada compra
        se autoriza en tiempo real contra el saldo disponible y los límites propios de la tarjeta. Las
        cuentas persona pueden tener **1 virtual + 1 física**; las cuentas empresa crean tarjetas **ilimitadas**,
        para la empresa o para personas designadas (ej. empleados, pasando sus documentos de identidad).
        La emisión cobra el fee `card_creation_virtual` / `card_creation_physical` cuando está configurado
        (0 = gratis); el cargo se reembolsa automáticamente si la emisión falla. Las tarjetas físicas
        nacen `pending_activation` hasta que el titular confirma la recepción.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - idempotency_key
              properties:
                physical:
                  type: boolean
                  default: false
                idempotency_key:
                  type: string
                  description: Obligatoria (también como header Idempotency-Key). Un retry con la misma
                    clave devuelve la tarjeta original y nunca cobra dos veces el fee de emisión.
                spending_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  default: USDT
                  description: Saldo desde el que gasta la tarjeta. USDT/USDC van a la par del USD (1:1
                    exacto, sin comisión de conversión). BTC/GOLD se convierten al precio efectivo del
                    momento de cada evento, con colchón de reserva en la autorización (pricing fail-closed
                    y límites de assets volátiles compartidos con payouts).
                limits:
                  type: object
                  description: Límites de gasto opcionales en USD (se aplican al autorizar; 0 u omitido
                    = sin límite).
                  properties:
                    per_transaction:
                      type: string
                    daily:
                      type: string
                    monthly:
                      type: string
                cardholder:
                  type: object
                  description: 'Datos del titular. En la PRIMERA emisión de la cuenta el titular se crea
                    en el emisor: la identidad y los documentos se COMPLETAN SOLOS desde la verificación
                    de identidad aprobada de la cuenta (los campos explícitos ganan); solo agregas los
                    campos propios del emisor (código de ocupación del catálogo, salary_usd). Omítelo
                    en las tarjetas siguientes: se reutiliza el titular ya verificado. Las cuentas empresa
                    también pueden usar kind "person" más el verification_id del KYC APROBADO de ESA persona
                    para emitir a una persona designada (su identidad y documentos salen de la verificación).'
                  properties:
                    verification_id:
                      type: string
                      format: uuid
                      description: 'OBLIGATORIO para personas designadas (kind "person" en cuentas empresa):
                        submission_id del KYC de terceros APROBADO de la persona.'
            examples:
              person_first_card:
                summary: Persona — PRIMERA tarjeta (la identidad se completa desde la verificación)
                value:
                  physical: false
                  idempotency_key: card-v-1
                  cardholder:
                    occupation: '52201'
                    salary_usd: 1800
              subsequent_card:
                summary: Tarjeta siguiente — titular ya verificado (sin datos)
                value:
                  physical: true
                  idempotency_key: card-f-ops-1
                  spending_asset: USDC
                  limits:
                    per_transaction: '500.00'
                    monthly: '5000.00'
              company_for_employee:
                summary: Empresa — tarjeta para una persona designada (exige KYC aprobado)
                value:
                  physical: false
                  idempotency_key: card-emp-77
                  limits:
                    monthly: '1500.00'
                  cardholder:
                    kind: person
                    verification_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                    occupation: '52201'
                    salary_usd: 1800
      responses:
        '201':
          description: Tarjeta creada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardObject'
              example:
                card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                physical: false
                cardholder_kind: account
                cardholder_name: Client X SpA
                status: active
                spending_asset: USDT
                limits:
                  monthly: '5000.000000'
                created_at: '2026-07-08T12:00:00Z'
                updated_at: '2026-07-08T12:00:00Z'
                creation_fee: '3.000000'
        '202':
          description: Solicitud retenida para revisión (la organización tiene la revisión de solicitudes activada).
            El recurso se crea solo cuando compliance aprueba la revisión; la comisión de emisión/apertura se cobra al
            retener y se reembolsa automáticamente si se rechaza. Sigue el resultado con el webhook
            `txn_review_status_changed` o `GET /v1/me/txn-reviews`.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                status: in_review
                kind: card_application
                review_id: 7a1c2e94-3b5f-4c8a-9d2e-6f0a1b2c3d4e
                message: application received; it is pending review by our compliance team
        '400':
          description: idempotency_key_required / invalid_amount
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: idempotency_key_required
                message: provide idempotency_key (body or Idempotency-Key header)
        '402':
          description: El saldo no alcanza para el fee de emisión (insufficient_funds).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: insufficient_funds
                message: account balance is not enough for this operation
        '409':
          description: card_limit_reached (cuentas persona) / cardholder_kyc_pending
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: card_limit_reached
                message: person accounts can hold at most 1 virtual card (cancel the existing one first)
        '422':
          description: verification_required / verification_not_approved / verification_kind_mismatch
            (las personas designadas necesitan el verification_id de un KYC de terceros aprobado).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: verification_required
                message: 'an approved identity verification of the third party is required: pass verification_id'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
    get:
      tags:
      - Cards
      operationId: listCards
      summary: Listar tarjetas
      description: Con credencial de cuenta lista las tarjetas de la cuenta; con credencial org admin
        lista todas las de la organización (filtra con `account_id`).
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending_activation
          - active
          - frozen
          - cancelled
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Desde esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Hasta esta fecha (YYYY-MM-DD, zona horaria de la organización, inclusive).
      responses:
        '200':
          description: Tarjetas paginadas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  cards:
                    type: array
                    items:
                      $ref: '#/components/schemas/CardObject'
              example:
                page: 1
                page_size: 50
                cards:
                - card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  physical: false
                  cardholder_kind: account
                  status: active
                  spending_asset: USDT
                  limits: {}
                  created_at: '2026-07-08T12:00:00Z'
                  updated_at: '2026-07-08T12:00:00Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /v1/cards/{cardID}:
    get:
      tags:
      - Cards
      operationId: getCard
      summary: Consultar una tarjeta
      parameters:
      - $ref: '#/components/parameters/cardID'
      responses:
        '200':
          description: La tarjeta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardObject'
              example:
                card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                physical: false
                cardholder_kind: account
                status: active
                spending_asset: USDT
                limits:
                  monthly: '5000.000000'
                created_at: '2026-07-08T12:00:00Z'
                updated_at: '2026-07-08T12:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags:
      - Cards
      operationId: updateCard
      summary: Actualizar límites, asset de gasto o congelar/descongelar
      description: Actualiza los límites de gasto (envía "0" para quitar un límite), cambia el saldo desde
        el que gasta (`spending_asset`, solo compras futuras) y congela o descongela la tarjeta. Una tarjeta
        congelada rechaza toda autorización al instante.
      parameters:
      - $ref: '#/components/parameters/cardID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                frozen:
                  type: boolean
                spending_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  description: Nuevo saldo de gasto. Solo afecta compras futuras; las autorizaciones en
                    vuelo conservan (y devuelven a) el asset con el que debitaron.
                limits:
                  type: object
                  properties:
                    per_transaction:
                      type: string
                    daily:
                      type: string
                    monthly:
                      type: string
            examples:
              freeze:
                summary: Congelar la tarjeta
                value:
                  frozen: true
              set_spending_asset:
                summary: Gastar desde el saldo USDC
                value:
                  spending_asset: USDC
              set_limits:
                summary: Actualizar límites
                value:
                  limits:
                    per_transaction: '200.00'
                    daily: '0'
      responses:
        '200':
          description: Tarjeta actualizada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardObject'
              example:
                card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                physical: false
                cardholder_kind: account
                status: frozen
                spending_asset: USDC
                limits:
                  per_transaction: '200.000000'
                created_at: '2026-07-08T12:00:00Z'
                updated_at: '2026-07-08T13:00:00Z'
        '400':
          description: invalid_amount / spending_asset_unavailable / settlement_asset_disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              examples:
                invalid_amount:
                  value:
                    error: invalid_amount
                    message: limits must be positive decimal strings (or "0" to remove)
                spending_asset_unavailable:
                  value:
                    error: spending_asset_unavailable
                    message: this asset is not available for card purchases
        '409':
          description: card_cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: card_cancelled
                message: a cancelled card cannot be updated
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/cards/{cardID}/activate:
    post:
      tags:
      - Cards
      operationId: activateCard
      summary: Activar una tarjeta física
      description: Confirma que el titular recibió la tarjeta física. Solo tarjetas en `pending_activation`.
      parameters:
      - $ref: '#/components/parameters/cardID'
      responses:
        '200':
          description: Tarjeta activada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardObject'
              example:
                card_id: 4d3c2b1a-9e8f-7a6b-5c4d-3e2f1a0b9c8d
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                physical: true
                cardholder_kind: account
                status: active
                limits: {}
                created_at: '2026-07-01T12:00:00Z'
                updated_at: '2026-07-08T13:00:00Z'
        '409':
          description: card_not_pending
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: card_not_pending
                message: only cards pending activation can be activated
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/cards/{cardID}/cancel:
    post:
      tags:
      - Cards
      operationId: cancelCard
      summary: Cancelar una tarjeta
      description: Cancela la tarjeta de forma permanente (irreversible). Cobra el fee `card_cancellation`
        cuando está configurado (0 = gratis); se reembolsa si la cancelación falla aguas arriba.
      parameters:
      - $ref: '#/components/parameters/cardID'
      responses:
        '200':
          description: Tarjeta cancelada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardObject'
              example:
                card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                physical: false
                cardholder_kind: account
                status: cancelled
                limits: {}
                created_at: '2026-07-08T12:00:00Z'
                updated_at: '2026-07-09T10:00:00Z'
                cancellation_fee: '1.000000'
        '402':
          description: El saldo no alcanza para el fee de cancelación (insufficient_funds).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: insufficient_funds
                message: account balance is not enough for this operation
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/cards/{cardID}/reveal:
    post:
      tags:
      - Cards
      operationId: revealCard
      summary: Revelar PAN y CVV
      description: Devuelve los datos sensibles de la tarjeta (PAN, CVV, expiración) como pass-through
        de una sola vez para mostrárselos al titular. Solo la cuenta dueña puede llamarlo (nunca el org
        admin). **Nunca almacenes ni loguees esta respuesta** — la plataforma tampoco la persiste (PCI).
      parameters:
      - $ref: '#/components/parameters/cardID'
      responses:
        '200':
          description: Datos sensibles (mostrar una sola vez).
          content:
            application/json:
              example:
                card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                pan: '5339880000001234'
                cvv: '123'
                exp_date: '202907'
                note: 'sensitive data: display once, never store'
        '404':
          $ref: '#/components/responses/NotFound'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/cards/{cardID}/transactions:
    get:
      tags:
      - Cards
      operationId: listCardTransactions
      summary: Listar transacciones de la tarjeta
      description: 'Compras y su ciclo de vida: `authorized` (hold en tiempo real), `settled` (confirmada
        en la liquidación), `reversed` (anulada, fondos devueltos al mismo saldo) y `declined` (con la
        razón: insufficient_funds, card_limit_exceeded, card_frozen...). `spend_asset` / `spend_amount`
        indican desde qué saldo se debitó la compra y cuánto en ese asset; `amount_usd` / `amount_usdt`
        siguen siendo el valor USD de referencia. Cuando el operador configura una comisión por compra
        (`card_purchase_virtual` / `card_purchase_physical`), `fee_asset` / `fee_amount` exponen la
        comisión (estimada en `authorized`, definitiva en `settled`) y `fee_refunded_amount` aparece
        cuando una reversa o ajuste a la baja prorratea la devolución; sin comisión configurada los
        campos `fee_*` se omiten.'
      parameters:
      - $ref: '#/components/parameters/cardID'
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: status
        in: query
        schema:
          type: string
          enum:
          - authorized
          - settled
          - reversed
          - declined
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Transacciones paginadas.
          content:
            application/json:
              schema:
                type: object
                properties:
                  page:
                    type: integer
                  page_size:
                    type: integer
                  transactions:
                    type: array
                    items:
                      type: object
              example:
                page: 1
                page_size: 50
                transactions:
                - transaction_id: 5e4d3c2b-1a09-8f7e-6d5c-4b3a2c1d0e9f
                  card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
                  kind: purchase
                  merchant: AMZN Mktp
                  mcc: '5999'
                  amount_usd: '16.17'
                  amount_usdt: '16.170000'
                  spend_asset: USDC
                  spend_amount: '16.170000'
                  fee_asset: USDC
                  fee_amount: '0.214255'
                  status: settled
                  auth_number: '040031'
                  created_at: '2026-07-08T15:00:00Z'
                  updated_at: '2026-07-09T02:00:00Z'
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/otp/settings:
    get:
      tags:
      - Seguridad (OTP)
      operationId: getOTPSettings
      summary: Mi política OTP
      description: Política OTP efectiva de la cuenta que llama — si el feature está habilitado, qué acciones
        exigen código y por qué canal, más el teléfono (enmascarado) y su estado de verificación.
      responses:
        '200':
          description: Política efectiva.
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                  phone:
                    type: string
                    description: Teléfono enmascarado ("" si no está cargado).
                  phone_verified:
                    type: boolean
                  actions:
                    type: array
                    items:
                      type: object
                      properties:
                        action:
                          type: string
                        required:
                          type: boolean
                        channel:
                          type: string
                          enum:
                          - sms
                          - whatsapp
              example:
                enabled: true
                phone: '********5678'
                phone_verified: true
                actions:
                - action: login
                  required: true
                  channel: sms
                - action: payout
                  required: true
                  channel: whatsapp
                - action: crypto_withdrawal
                  required: true
                  channel: sms
                - action: transfer
                  required: false
                  channel: sms
                - action: banking_operation
                  required: false
                  channel: sms
                - action: card_reveal
                  required: true
                  channel: sms
                - action: api_key_create
                  required: true
                  channel: sms
                - action: member_add
                  required: false
                  channel: sms
                - action: phone_change
                  required: true
                  channel: sms
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/otp/challenges:
    post:
      tags:
      - Seguridad (OTP)
      operationId: createOTPChallenge
      summary: Pedir un código OTP
      description: Envía un código de un solo uso al teléfono de la cuenta por el canal configurado para
        la acción. Requiere sesión de usuario (JWT) — las API keys reciben `403 session_required`. Aplican
        límites de envío por hora.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - action
              properties:
                action:
                  type: string
                  enum:
                  - login
                  - payout
                  - crypto_withdrawal
                  - transfer
                  - banking_operation
                  - card_reveal
                  - api_key_create
                  - member_add
                  - phone_change
            example:
              action: payout
      responses:
        '201':
          description: Desafío creado; el código va en camino.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OTPChallenge'
              example: &id015
                challenge_id: 6f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                action: payout
                channel: whatsapp
                phone: '********5678'
                status: pending
                created_at: '2026-07-08T21:00:00Z'
                expires_at: '2026-07-08T21:10:00Z'
        '403':
          description: Credencial API key (`session_required`) o teléfono recién enlazado sin verificación y sin factor
            alternativo (`phone_binding_cooldown`). Con el teléfono en cooldown, el desafío cae automáticamente a la app
            autenticadora (si está enrolada) o al email de login verificado; el canal efectivo llega en la respuesta del
            desafío.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: session_required
                message: OTP verification requires a user session (JWT), not an API key
        '409':
          description: La cuenta no tiene teléfono (`phone_required`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: phone_required
                message: the account has no phone number; set it with PATCH /v1/me before requesting OTP
                  codes
        '429':
          description: Cuota de envíos alcanzada (`too_many_attempts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: too_many_attempts
                message: too many verification attempts; wait before retrying
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags:
      - Seguridad (OTP)
      operationId: listOTPChallenges
      summary: Listar mis desafíos OTP
      parameters:
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      - name: from
        in: query
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusive).
      - name: to
        in: query
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusive).
      responses:
        '200':
          description: Historial de desafíos (auditoría).
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                challenges:
                - challenge_id: 6f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  action: payout
                  channel: whatsapp
                  phone: '********5678'
                  status: consumed
                  created_at: '2026-07-08T21:00:00Z'
                  expires_at: '2026-07-08T21:10:00Z'
                  verified_at: '2026-07-08T21:01:12Z'
                  consumed_at: '2026-07-08T21:01:40Z'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/otp/challenges/{challengeID}:
    get:
      tags:
      - Seguridad (OTP)
      operationId: getOTPChallenge
      summary: Consultar un desafío OTP
      parameters:
      - name: challengeID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: El desafío.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OTPChallenge'
              example: *id015
        '404':
          $ref: '#/components/responses/NotFound'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/otp/challenges/{challengeID}/verify:
    post:
      tags:
      - Seguridad (OTP)
      operationId: verifyOTPChallenge
      summary: Verificar el código
      description: Valida el código recibido por SMS/WhatsApp y devuelve el `otp_token` de un solo uso
        que autoriza la acción protegida (se envía en el header `X-OTP-Token`). 5 intentos por desafío.
      parameters:
      - name: challengeID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
                  minLength: 4
                  maxLength: 10
            example:
              code: '482913'
      responses:
        '200':
          description: Código aprobado; token de un solo uso emitido.
          content:
            application/json:
              example:
                challenge_id: 6f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
                action: payout
                otp_token: otp_Zk9uY2XIr1EYE0lq8xqlM3VayVZYX4aa11bb22cc33
                expires_at: '2026-07-08T21:10:00Z'
                note: 'single use: send it in the X-OTP-Token header of the protected action'
        '401':
          description: El código no coincide (`invalid_code`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_code
                message: the code is incorrect; check it and try again
        '409':
          description: El desafío expiró o ya se usó (`challenge_not_pending`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: challenge_not_pending
                message: the challenge already expired or was used; create a new one
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/services:
    get:
      tags:
      - Cuenta
      operationId: getServices
      summary: Servicios habilitados
      description: Mapa efectivo de qué productos puede usar esta cuenta ahora mismo (payouts, payins,
        transfers, crypto, banking, kyc, cards). Cuando un servicio está deshabilitado, sus endpoints
        de acción responden 403 service_disabled; las lecturas y el dinero en tránsito nunca se ven afectados.
        Úsalo para decidir qué mostrar en tu UI.
      responses:
        '200':
          description: Mapa efectivo de servicios.
          content:
            application/json:
              schema:
                type: object
                properties:
                  account_id:
                    type: string
                  services:
                    type: object
                    additionalProperties:
                      type: boolean
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                services:
                  payouts: true
                  payins: true
                  transfers: true
                  crypto: true
                  banking: true
                  kyc: true
                  cards: false
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/settlement:
    get:
      tags:
      - Cuenta
      operationId: getSettlement
      summary: Obtener mi configuración de settlement
      description: Devuelve desde qué saldo virtual se debitan por defecto tus operaciones (payouts y
        comisiones de servicios), en qué saldo se acreditan por defecto tus payins (`default_payin_asset`),
        qué assets permite tu organización, y el límite por operación (equivalente en USDT) que aplica
        a los assets volátiles (BTC/GOLD).
      responses:
        '200':
          description: Configuración de settlement vigente.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSettings'
              example:
                default_settlement_asset: USDT
                default_payin_asset: USDT
                enabled_assets:
                - USDT
                - USDC
                - BTC
                - GOLD
                volatile_op_limit_usdt: '10000.000000'
                volatile_daily_limit_usdt: '50000.000000'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
    put:
      tags:
      - Cuenta
      operationId: updateSettlement
      summary: Definir mis saldos predeterminados (payouts y payins)
      description: Cambia tus saldos predeterminados. `default_settlement_asset` es el saldo desde el
        que se debitan tus payouts y comisiones de servicios cuando el request no envía un
        `settlement_asset` explícito. `default_payin_asset` es el saldo en que se acreditan tus payins —
        el crédito sigue entrando en USDT y el neto se auto-convierte vía el motor de swaps al precio
        real, sin spread de swap adicional (el payin ya pagó su comisión y su tasa; los payins de
        checkout y POS conservan el asset de su link). Cualquiera
        de USDT, USDC, BTC o GOLD (si tu organización lo tiene habilitado). Envía uno o ambos campos. El
        cambio aplica solo a operaciones nuevas; nada en tránsito se re-cotiza.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                default_settlement_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  description: Saldo desde el que se debitan por defecto payouts y comisiones. Opcional.
                default_payin_asset:
                  type: string
                  enum:
                  - USDT
                  - USDC
                  - BTC
                  - GOLD
                  description: Saldo en que se acreditan por defecto los payins. El crédito entra en
                    USDT y el neto se auto-convierte vía el motor de swaps al precio real (sin
                    spread de swap adicional). Opcional.
            example:
              default_settlement_asset: BTC
              default_payin_asset: USDC
      responses:
        '200':
          description: Configuración de settlement actualizada.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SettlementSettings'
              example:
                default_settlement_asset: BTC
                default_payin_asset: USDC
                enabled_assets:
                - USDT
                - USDC
                - BTC
                - GOLD
                volatile_op_limit_usdt: '10000.000000'
                volatile_daily_limit_usdt: '50000.000000'
        '400':
          description: Asset inválido o deshabilitado (`invalid_settlement_asset`, `settlement_asset_disabled`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: settlement_asset_disabled
                message: settlement asset is disabled for this organization
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/AccountRequired'
  /v1/cards/catalog/occupations:
    get:
      tags:
      - Cards
      operationId: listCardOccupations
      summary: Catálogo de ocupaciones
      description: Official issuer occupation codes (for the `occupation` field when designating a person
        cardholder). Search with `q` (matches code or label). Use the `code` value; a free-text occupation
        is rejected with 400 invalid_occupation.
      parameters:
      - name: q
        in: query
        schema:
          type: string
        description: Filtro opcional (case-insensitive) por código o etiqueta.
      responses:
        '200':
          description: Entradas del catálogo.
          content:
            application/json:
              example:
                items:
                - code: '12101'
                  label: Director General
                - code: '21313'
                  label: Computer Systems Designer
                meta:
                  retrieved: 2
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/cards/catalog/business-activities:
    get:
      tags:
      - Cards
      operationId: listCardBusinessActivities
      summary: Catálogo de giros
      description: Official issuer economic-activity codes (for `kind_of_business` when creating a company
        cardholder). Search with `q`. Use the `code` value; a free-text value is rejected with 400 invalid_kind_of_business.
      parameters:
      - name: q
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Entradas del catálogo.
          content:
            application/json:
              example:
                items:
                - code: J6201
                  label: Computer programming activities
                meta:
                  retrieved: 1
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/analytics/summary:
    get:
      tags:
      - Analytics
      operationId: analyticsSummary
      summary: Resumen de la cuenta para dashboards
      description: 'Aggregated view of the whole account flow in a single call: gross volume in/out, transaction
        counts, new banking users, per-service sections (payouts, payins, deposits, withdrawals, transfers,
        swaps, cards, banking, verifications, aml, contacts) with their dimensions (country, currency,
        method, status, chain, merchant), service spending and valued balances. Amounts are USD decimal
        strings; empty buckets are zero-filled. Account credential only.

        '
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: granularity
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: day
      responses:
        '200':
          description: Account analytics summary.
          content:
            application/json:
              example:
                account_id: ae8cf540-22a9-414d-82cc-8ac04732be4f
                from: '2026-07-01'
                to: '2026-07-10'
                granularity: day
                timezone: America/New_York
                gross_volume:
                  in: '636936.87'
                  out: '270118.87'
                  total: '907055.74'
                  series:
                  - date: '2026-07-01'
                    in: '51023.10'
                    out: '31210.44'
                    total: '82233.54'
                  previous_period:
                    in: '512300.00'
                    out: '241000.10'
                    total: '753300.10'
                  change_pct: '20.41'
                  unpriced_assets: []
                transactions:
                  total: 92
                  series:
                  - date: '2026-07-01'
                    in: 3
                    out: 5
                    total: 8
                  previous_period:
                    total: 71
                  change_pct: '29.58'
                new_users:
                  total: 11
                  series:
                  - date: '2026-07-01'
                    count: 1
                  previous_period:
                    total: 6
                  change_pct: '83.33'
                by_country:
                - country: BR
                  payouts:
                    count: 18
                    volume_usd: '3410.20'
                  payins:
                    count: 4
                    volume_usd: '820.00'
                  total_usd: '4230.20'
                sections:
                  payouts:
                    count: 24
                    volume_usd: '5120.40'
                    fees_usd: '7.20'
                    by_status:
                    - key: completed
                      count: 21
                      volume_usd: '4980.10'
                    by_country:
                    - country: BR
                      count: 18
                      volume_usd: '3410.20'
                      local_volume:
                        BRL: '17550'
                    by_method:
                    - key: pix
                      count: 18
                      volume_usd: '3410.20'
                  cards:
                    count: 12
                    volume_usd: '230.50'
                    active_cards: 1
                    by_status:
                    - status: settled
                      count: 10
                      volume_usd: '205.00'
                    top_merchants:
                    - merchant: AMAZON
                      count: 4
                      volume_usd: '98.20'
                  banking:
                    new_third_parties: 11
                    new_accounts: 14
                    operations: 6
                    volume:
                      in:
                        count: 4
                        volume_usd: '1200.00'
                      out:
                        count: 9
                        volume_usd: '3450.00'
                      volume_usd: '4650.00'
                    fees_usd: '18.00'
                  verifications:
                    submissions:
                    - kind: kyc
                      status: approved
                      count: 3
                    fees_usd: '9.00'
                    fees_by_kind:
                      kyc_verification:
                        count: 3
                        fees_usd: '6.00'
                      kyb_verification:
                        count: 1
                        fees_usd: '3.00'
                  adjustments:
                    count: 2
                    volume_usd: '2000.00'
                spending:
                  total_usd: '34.50'
                  by_service:
                    banking_customer:
                      count: 11
                      fees_usd: '11.00'
                balances:
                  items:
                  - asset: USDT
                    available: '1520.250000'
                    held: '0.000000'
                    usd_estimate: '1520.25'
                  net_worth_usd_estimate: '1833.93'
        '400':
          description: invalid_range / invalid_granularity
        '403':
          description: account_required
  /v1/banking/third-parties:
    post:
      tags:
      - Banking
      operationId: createBankingThirdParty
      summary: Da de alta un usuario banking de tercero (solo empresas)
      description: 'Las cuentas empresa dan de alta a sus clientes finales como usuarios banking separados,
        con cuentas a su nombre. Exige el verification_id de una verificación KYC/KYB APROBADA del tercero:
        el tipo sale del kind de la verificación (KYC = INDIVIDUAL, KYB = COMPANY), la identidad se completa
        sola desde el perfil verificado (los campos explícitos ganan) y los documentos ya validados se
        re-entregan automáticamente al proveedor bancario (documents_synced). Cobra el fee de perfil bancario
        (se reembolsa si el core rechaza). Las cuentas persona reciben 403 company_required.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - verification_id
              properties:
                verification_id:
                  type: string
                  format: uuid
                  description: submission_id de una verificación KYC/KYB de terceros APROBADA.
                type:
                  type: string
                  enum:
                  - INDIVIDUAL
                  - COMPANY
                  description: 'Opcional: debe calzar con el kind de la verificación.'
                name:
                  type: string
                  description: 'Opcional: se completa desde el perfil verificado.'
                email:
                  type: string
                  format: email
                  description: 'Opcional: se completa desde el perfil verificado.'
                country:
                  type: string
            example:
              verification_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
      responses:
        '201':
          description: Third-party banking user created.
          content:
            application/json:
              example:
                third_party_id: 7f2a1b3c-0000-1111-2222-333344445555
                customer_id: cus_abc123
                kind: third_party
                status: pending
                verification_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
                documents_synced: 2
                registered_at: '2026-07-10T15:00:00Z'
                banking_fee: '1.000000'
                banking_fee_asset: USDT
        '202':
          description: Solicitud retenida para revisión (la organización tiene la revisión de solicitudes activada).
            El recurso se crea solo cuando compliance aprueba la revisión; la comisión de emisión/apertura se cobra al
            retener y se reembolsa automáticamente si se rechaza. Sigue el resultado con el webhook
            `txn_review_status_changed` o `GET /v1/me/txn-reviews`.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                status: in_review
                kind: banking_application
                review_id: 7a1c2e94-3b5f-4c8a-9d2e-6f0a1b2c3d4e
                message: application received; it is pending review by our compliance team
        '403':
          description: company_required
        '422':
          description: verification_required / verification_not_approved / verification_kind_mismatch
            / verification_invalid
    get:
      tags:
      - Banking
      operationId: listBankingThirdParties
      summary: Lista los usuarios banking de terceros
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Third parties owned by the calling account.
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                third_parties:
                - id: 7f2a1b3c-0000-1111-2222-333344445555
                  customer_id: cus_abc123
                  kind: third_party
                  type: INDIVIDUAL
                  name: Carlos Mendes
                  created_at: '2026-07-10T15:00:00Z'
  /v1/banking/third-parties/{thirdPartyID}:
    get:
      tags:
      - Banking
      operationId: getBankingThirdParty
      summary: Consulta un usuario banking de tercero (estado KYC en vivo)
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Third party with live verification status.
          content:
            application/json:
              example:
                third_party_id: 7f2a1b3c-0000-1111-2222-333344445555
                kind: third_party
                status: approved
        '404':
          description: not_found (does not exist or not owned by your account)
  /v1/banking/third-parties/{thirdPartyID}/documents:
    post:
      tags:
      - Banking
      operationId: uploadBankingThirdPartyDocument
      summary: Sube un documento KYC del tercero (gratis)
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
            example:
              type: PASSPORT
              file_base64: '...'
      responses:
        '201':
          description: Document uploaded.
          content:
            application/json:
              example:
                status: received
  /v1/banking/third-parties/{thirdPartyID}/submit:
    post:
      tags:
      - Banking
      operationId: submitBankingThirdParty
      summary: Envia el tercero a verificacion (gratis)
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '202':
          description: Submitted to verification.
          content:
            application/json:
              example:
                status: pending_review
  /v1/banking/third-parties/{thirdPartyID}/accounts:
    post:
      tags:
      - Banking
      operationId: createBankingThirdPartyAccount
      summary: Abre una cuenta bancaria para el tercero
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
            example:
              currency: USD
              name: Carlos account
      responses:
        '201':
          description: Bank account created for the third party.
          content:
            application/json:
              example:
                account_id: c4d10000-aaaa-bbbb-cccc-ddddeeeeffff
                status: active
                banking_fee: '1.000000'
                banking_fee_asset: USDT
        '202':
          description: Solicitud retenida para revisión (la organización tiene la revisión de solicitudes activada).
            El recurso se crea solo cuando compliance aprueba la revisión; la comisión de emisión/apertura se cobra al
            retener y se reembolsa automáticamente si se rechaza. Sigue el resultado con el webhook
            `txn_review_status_changed` o `GET /v1/me/txn-reviews`.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
              example:
                status: in_review
                kind: banking_application
                review_id: 7a1c2e94-3b5f-4c8a-9d2e-6f0a1b2c3d4e
                message: application received; it is pending review by our compliance team
    get:
      tags:
      - Banking
      operationId: listBankingThirdPartyAccounts
      summary: Lista las cuentas bancarias del tercero
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Bank accounts of the third party.
          content:
            application/json:
              example:
                items:
                - id: c4d10000-aaaa-bbbb-cccc-ddddeeeeffff
                  currency: USD
  /v1/banking/third-parties/{thirdPartyID}/accounts/{bankAccountID}/balance:
    get:
      tags:
      - Banking
      operationId: getBankingThirdPartyAccountBalance
      summary: Saldo de una cuenta bancaria del tercero
      parameters:
      - name: thirdPartyID
        in: path
        required: true
        schema:
          type: string
      - name: bankAccountID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Saldo en vivo. La forma recomendada de leer el monto es
            el resumen neutro de nivel superior (`available`, `held`,
            `currency` — strings decimales planos, presentes cuando el rail
            soporta el resumen); el objeto `balance` conserva el detalle
            completo nativo del rail.
          content:
            application/json:
              example:
                account_id: c4d1e2f3-a4b5-c6d7-e8f9-a0b1c2d3e4f5
                provider_id: BNK-ACC-4471
                available: '0.00'
                held: '0.00'
                currency: USD
                balance:
                  name: OPERATING USD
                  currency: USD
                  balance:
                    money:
                      units: '0'
                      nanos: 0
        '404':
          description: not_found
  /v1/auth/password/forgot:
    post:
      tags:
      - Autenticación
      operationId: forgotPassword
      summary: Solicitar un código para recuperar la contraseña
      description: Envía un código de recuperación al email del miembro (por defecto) o a su teléfono
        verificado (`channel:"sms"`). SIEMPRE responde 200 con el mismo cuerpo exista o no la cuenta —
        así se evita la enumeración de cuentas. Completa el reset con `POST /v1/auth/password/reset`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - email
              properties:
                org:
                  type: string
                email:
                  type: string
                  format: email
                channel:
                  type: string
                  enum:
                  - email
                  - sms
                  - whatsapp
                  default: email
            example:
              org: cbpay
              email: taylor@example.com
      responses:
        '200':
          description: Acuse genérico (nunca revela si la cuenta existe).
          content:
            application/json:
              example:
                status: ok
                note: if an account matches, a verification code was sent; complete the reset with POST
                  /v1/auth/password/reset
  /v1/auth/password/reset:
    post:
      tags:
      - Autenticación
      operationId: resetPassword
      summary: Restablecer la contraseña con un código
      description: Valida el código enviado por `forgot` y fija una contraseña nueva. Todas las sesiones
        se revocan. Un único error genérico (`invalid_code`) cubre org/miembro/código inválidos para no
        filtrar existencia de cuentas.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - email
              - code
              - new_password
              properties:
                org:
                  type: string
                email:
                  type: string
                  format: email
                code:
                  type: string
                new_password:
                  type: string
                  minLength: 8
                channel:
                  type: string
                  enum:
                  - email
                  - sms
                  - whatsapp
                  default: email
            example:
              org: cbpay
              email: taylor@example.com
              code: '123456'
              new_password: mi-nueva-clave-fuerte
      responses:
        '200':
          description: Contraseña restablecida; todas las sesiones revocadas.
          content:
            application/json:
              example:
                status: password_reset
                note: all sessions were revoked; log in with the new password
        '401':
          description: Código inválido o expirado, o ninguna cuenta coincide (`invalid_code`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: invalid_code
                message: the code is invalid or expired
                or no account matches: null
  /v1/auth/passkey/login/begin:
    post:
      tags:
      - Passkeys
      operationId: passkeyLoginBegin
      summary: Iniciar login con passkey (sin contraseña)
      description: Inicia una ceremonia WebAuthn de login discoverable. Pasa el `options.publicKey` devuelto
        a `navigator.credentials.get()` y envía el resultado a `finish`. El challenge es de un solo uso
        y expira en 2 minutos.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              properties:
                org:
                  type: string
            example:
              org: cbpay
      responses:
        '200':
          description: Opciones de assertion WebAuthn + id del challenge.
          content:
            application/json:
              example:
                challenge_id: 5f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
                options:
                  publicKey:
                    challenge: base64url…
                    rpId: cbpayapp.com
                expires_at: '2026-07-11T00:02:00Z'
        '503':
          description: Passkeys no configuradas para la org (`passkeys_unavailable`).
  /v1/auth/passkey/login/finish:
    post:
      tags:
      - Passkeys
      operationId: passkeyLoginFinish
      summary: Completar login con passkey (sin contraseña)
      description: Valida la assertion del autenticador y emite la sesión. Un passkey ya son dos factores
        (dispositivo + biometría), por eso este login no pasa por el OTP de login.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - org
              - challenge_id
              - credential
              properties:
                org:
                  type: string
                challenge_id:
                  type: string
                credential:
                  type: object
                  description: El PublicKeyCredential JSON de navigator.credentials.get().
            example:
              org: cbpay
              challenge_id: 5f1c02aa-93a1-4f0e-a7d1-1f2e3c4b5a69
              credential:
                id: AaBb…
                type: public-key
                response: {}
      responses:
        '200':
          description: Sesión emitida.
          content:
            application/json:
              example:
                access_token: eyJ…
                expires_at: '2026-07-12T00:00:00Z'
                refresh_token: rt_6a1e6c22-….XXXX…
                refresh_expires_at: '2026-08-10T00:00:00Z'
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                role: owner
        '401':
          description: No se pudo verificar la passkey (`invalid_credential`).
  /v1/me/password:
    post:
      tags:
      - Cuenta
      operationId: changePassword
      summary: Cambiar mi contraseña
      description: Cambia la contraseña del miembro que llama. Exige la contraseña actual (los miembros
        creados por login social sin contraseña fijan la primera aquí, dejando `current_password` vacío)
        y, cuando la política OTP lo pide, un `X-OTP-Token` de la acción `password_change`. Todas las
        demás sesiones se revocan y se devuelve una sesión nueva.
      parameters:
      - name: X-OTP-Token
        in: header
        required: false
        schema:
          type: string
        description: Token OTP de un solo uso cuando la acción password_change lo exige.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - new_password
              properties:
                current_password:
                  type: string
                new_password:
                  type: string
                  minLength: 8
            example:
              current_password: clave-vieja
              new_password: mi-nueva-clave-fuerte
      responses:
        '200':
          description: Contraseña cambiada; sesiones anteriores revocadas; nueva sesión devuelta.
          content:
            application/json:
              example:
                status: password_changed
                access_token: eyJ…
                expires_at: '2026-07-12T00:00:00Z'
                refresh_token: rt_6a1e6c22-….XXXX…
                refresh_expires_at: '2026-08-10T00:00:00Z'
                note: all previous sessions were revoked
        '403':
          description: Contraseña actual incorrecta (`invalid_password`) u OTP requerido (`otp_required`).
  /v1/me/email/change:
    post:
      tags:
      - Cuenta
      operationId: changeEmail
      summary: Cambiar mi email de login
      description: Inicia el cambio del email de login. Se envía un código al email NUEVO; confirma con
        `POST /v1/me/email/confirm`. Cuando la política OTP lo pide, también se exige un `X-OTP-Token`
        de la acción `email_change`. Verificar el email nuevo es obligatorio — evita que un atacante apunte
        el login a un buzón que no controla.
      parameters:
      - name: X-OTP-Token
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - new_email
              properties:
                new_email:
                  type: string
                  format: email
            example:
              new_email: taylor.new@example.com
      responses:
        '200':
          description: Código enviado al email nuevo.
          content:
            application/json:
              example:
                status: code_sent
                email: t***@***.com
                expires_at: '2026-07-11T00:30:00Z'
                note: confirm the change with POST /v1/me/email/confirm using the code sent to the new
                  email
        '409':
          description: El email nuevo ya está en uso (`email_in_use`).
  /v1/me/email/verify:
    post:
      tags:
      - Cuenta
      operationId: verifyEmail
      summary: Verificar mi email actual
      description: Envía un código al email ACTUAL del miembro para marcarlo verificado (requisito para
        usar el canal OTP de email en acciones de dinero). Confirma con `POST /v1/me/email/confirm`.
      responses:
        '200':
          description: Código enviado (o ya verificado).
          content:
            application/json:
              example:
                status: code_sent
                email: t***@***.com
                note: confirm with POST /v1/me/email/confirm using the code sent to your email
  /v1/me/email/confirm:
    post:
      tags:
      - Cuenta
      operationId: confirmEmail
      summary: Confirmar un cambio o verificación de email
      description: Confirma un cambio de email pendiente (o su verificación) con el código recibido. En
        un cambio, el email de login — y, para owners, el email de contacto de la cuenta — se actualizan
        y se avisa al email anterior.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
            example:
              code: '123456'
      responses:
        '200':
          description: Email confirmado.
          content:
            application/json:
              example:
                status: email_confirmed
                email: taylor.new@example.com
        '409':
          description: No hay cambio de email pendiente (`no_pending_email`).
  /v1/me/alias:
    put:
      tags:
      - Cuenta
      operationId: setMyAlias
      summary: Fijar mi alias permanente
      description: 'Fija el alias público de la cuenta UNA vez. El alias es permanente y no se puede cambiar.
        Formato: 4-20 caracteres (a-z, 0-9, punto, guion bajo, guion), empieza y termina en alfanumérico;
        las palabras reservadas se rechazan. Otros pueden enviarte dinero por alias.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - alias
              properties:
                alias:
                  type: string
            example:
              alias: taylor.code
      responses:
        '200':
          description: Alias fijado (de forma permanente).
          content:
            application/json:
              example:
                alias: taylor.code
                note: the alias is permanent and cannot be changed
        '409':
          description: Ya fijado (`alias_already_set`) o tomado (`alias_taken`).
  /v1/me/qr:
    get:
      tags:
      - Cuenta
      operationId: getMyQR
      summary: El QR de mi perfil
      description: Devuelve el token QR inmutable de la cuenta, su payload URI (`cbpay:pay?to=<token>`)
        y un PNG listo para mostrar. El QR solo permite que otros te ENVÍEN dinero; nunca cambia.
      responses:
        '200':
          description: Token QR, payload y PNG.
          content:
            application/json:
              example:
                qr_token: 9f8c7b6a5d4e3f2a1b0c9d8e7f6a5b4c
                payload: cbpay:pay?to=9f8c7b6a5d4e3f2a1b0c9d8e7f6a5b4c
                png_base64: iVBORw0KGgo…
                alias: taylor.code
                note: the QR identifies your account to RECEIVE transfers; it never changes
  /v1/me/avatar:
    put:
      tags:
      - Cuenta
      operationId: putMyAvatar
      summary: Subir mi foto de perfil
      description: Sube (o reemplaza) el avatar de la cuenta. El cuerpo son los bytes crudos de la imagen
        (JPEG, PNG o WebP), máx 512 KB. El tipo se detecta por los magic bytes del archivo, nunca por
        el header. Cuando la imagen se publica en el CDN público, `avatar_url` es una URL absoluta que
        carga sin autenticación; si no, es la ruta autenticada de la API.
      requestBody:
        required: true
        content:
          image/jpeg:
            schema:
              type: string
              format: binary
          image/png:
            schema:
              type: string
              format: binary
          image/webp:
            schema:
              type: string
              format: binary
      responses:
        '200':
          description: Avatar actualizado.
          content:
            application/json:
              example:
                status: avatar_updated
                content_type: image/png
                size_bytes: 20481
                avatar_url: https://cdn.cbpayapp.com/public/avatars/1fa63bd1-0000-4000-8000-000000000001/9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
        '413':
          description: El avatar supera 512 KB (`image_too_large`).
        '415':
          description: Tipo de imagen no soportado (`unsupported_image`).
    delete:
      tags:
      - Cuenta
      operationId: deleteMyAvatar
      summary: Eliminar mi foto de perfil
      responses:
        '200':
          description: Avatar eliminado.
          content:
            application/json:
              example:
                status: avatar_deleted
  /v1/avatars/{accountID}:
    get:
      tags:
      - Cuenta
      operationId: getAvatar
      summary: Obtener el avatar de una cuenta
      description: Sirve la imagen de avatar de una cuenta de la misma organización (se usa en la vista
        previa del destinatario en transferencias y contactos). Los avatares publicados en el CDN público
        responden con un `302` hacia la URL del CDN; los legados se sirven directo.
      parameters:
      - name: accountID
        in: path
        required: true
        schema:
          type: string
      responses:
        '302':
          description: Redirección a la URL pública del avatar en el CDN.
        '200':
          description: Los bytes de la imagen (Content-Type = tipo de la imagen guardada).
          content:
            image/png:
              schema:
                type: string
                format: binary
        '404':
          description: La cuenta no tiene avatar (`not_found`).
  /v1/resolve:
    get:
      tags:
      - Cuenta
      operationId: resolveRecipient
      summary: Resolver un destinatario por alias o QR
      description: Devuelve una vista previa mínima (alias, nombre, tipo, avatar) de una cuenta de la
        misma organización, para confirmar antes de enviar. Provee `alias` o `qr` (el token crudo o el
        payload completo `cbpay:pay?to=…`). Responde un 404 uniforme y tiene rate limit para evitar enumeración.
      parameters:
      - name: alias
        in: query
        required: false
        schema:
          type: string
      - name: qr
        in: query
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Vista previa del destinatario.
          content:
            application/json:
              example:
                account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                display_name: Taylor Code
                type: person
                alias: taylor.code
                avatar_url: https://cdn.cbpayapp.com/public/avatars/1fa63bd1-0000-4000-8000-000000000001/9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
        '404':
          description: Ninguna cuenta coincide (`not_found`).
        '429':
          description: Demasiadas consultas (`too_many_attempts`).
  /v1/otp/preferences:
    get:
      tags:
      - Seguridad (OTP)
      operationId: getOTPPreferences
      summary: Mis preferencias de 2FA
      description: Muestra la política 2FA efectiva de la cuenta con, por acción, si está bloqueada por
        la organización (la política de la org es el piso que la cuenta no puede bajar).
      responses:
        '200':
          description: Preferencias efectivas.
          content:
            application/json:
              example:
                enabled: true
                totp_enrolled: true
                actions:
                - action: login
                  required: true
                  channel: totp
                  source: account
                  locked_by_org: false
                - action: payout
                  required: true
                  channel: sms
                  source: org_default
                  locked_by_org: true
    put:
      tags:
      - Seguridad (OTP)
      operationId: putOTPPreferences
      summary: Actualizar mis preferencias de 2FA
      description: 'Permite al owner de la cuenta configurar su propio 2FA. Siempre puedes endurecer (activar
        acciones, subir a un canal más fuerte: totp > email > sms/whatsapp) pero nunca bajar del piso
        de la organización. Relajar (desactivar una acción activa o bajar de canal) exige un `X-OTP-Token`
        de la acción `security_settings`. Elegir `channel:totp` exige la app autenticadora ya enrolada.
        Activar el 2FA de la acción `login` por canal telefónico (`sms`/`whatsapp`) exige el teléfono de
        la cuenta ya verificado (completa cualquier desafío OTP por SMS/WhatsApp); si no, el request se
        rechaza con `409 phone_verification_required` — evita que te bloquees con un número mal escrito.'
      parameters:
      - name: X-OTP-Token
        in: header
        required: false
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - action
              - required
              properties:
                action:
                  type: string
                  enum:
                  - login
                  - payout
                  - crypto_withdrawal
                  - transfer
                  - banking_operation
                  - card_reveal
                  - api_key_create
                  - member_add
                  - phone_change
                  - password_change
                  - email_change
                  - security_settings
                required:
                  type: boolean
                channel:
                  type: string
                  enum:
                  - sms
                  - whatsapp
                  - email
                  - totp
                  default: sms
            example:
              action: payout
              required: true
              channel: totp
      responses:
        '200':
          description: Política efectiva actualizada.
          content:
            application/json:
              example:
                enabled: true
                actions:
                - action: payout
                  required: true
                  channel: totp
                  source: account
        '403':
          description: Bloqueado por la org (`policy_locked_by_org` / `channel_locked_by_org`) u OTP requerido
            para relajar (`otp_required`).
        '409':
          description: App autenticadora sin enrolar para `channel:totp` (`totp_enrollment_required`),
            o teléfono sin verificar al activar el 2FA de login por canal telefónico (`phone_verification_required`).
  /v1/me/totp:
    get:
      tags:
      - Passkeys
      operationId: getMyTOTP
      summary: Estado de mi app autenticadora
      description: Si una app autenticadora (TOTP) está activa y cuántos códigos de respaldo quedan.
      responses:
        '200':
          description: Estado TOTP.
          content:
            application/json:
              example:
                enrolled: true
                recovery_codes_remaining: 8
    delete:
      tags:
      - Passkeys
      operationId: disableMyTOTP
      summary: Quitar mi app autenticadora
      description: Desactiva TOTP. Exige un código autenticador (o de respaldo) válido y respeta el piso
        de la org — si la organización exige la app autenticadora, no se puede quitar.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
            example:
              code: '123456'
      responses:
        '200':
          description: TOTP desactivado.
          content:
            application/json:
              example:
                enrolled: false
                status: disabled
        '403':
          description: Código inválido (`otp_invalid`) o requerido por la org (`policy_locked_by_org`).
  /v1/me/totp/enroll:
    post:
      tags:
      - Passkeys
      operationId: enrollMyTOTP
      summary: Iniciar el enrolamiento de la app autenticadora
      description: Genera un secreto TOTP y devuelve el `otpauth://` más un PNG de QR para escanear en
        Google Authenticator / Authy. Exige la contraseña actual (si el miembro tiene una). Confirma con
        `POST /v1/me/totp/confirm`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
            example:
              password: mi-clave-fuerte
      responses:
        '200':
          description: Secreto + otpauth URI + PNG del QR.
          content:
            application/json:
              example:
                totp_secret: JBSWY3DPEHPK3PXP
                otpauth_uri: otpauth://totp/CBPay:taylor@example.com?secret=JBSWY3DPEHPK3PXP&issuer=CBPay
                qr_png_base64: iVBORw0KGgo…
                note: scan the QR (or type the secret) in your authenticator app, then confirm with POST
                  /v1/me/totp/confirm
        '409':
          description: Ya hay una app autenticadora activa (`totp_already_enrolled`).
  /v1/me/totp/confirm:
    post:
      tags:
      - Passkeys
      operationId: confirmMyTOTP
      summary: Confirmar y activar la app autenticadora
      description: Activa TOTP con el primer código válido y devuelve 10 códigos de respaldo de un solo
        uso (se muestran una sola vez).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
            example:
              code: '123456'
      responses:
        '200':
          description: TOTP activo; códigos de respaldo devueltos una vez.
          content:
            application/json:
              example:
                enrolled: true
                recovery_codes:
                - ABCD-EFGH
                - JKLM-NPQR
                note: 'store these recovery codes now: they are shown only once and each works a single
                  time'
        '403':
          description: Código inválido (`otp_invalid`).
  /v1/me/totp/recovery-codes:
    post:
      tags:
      - Passkeys
      operationId: regenerateRecoveryCodes
      summary: Regenerar códigos de respaldo
      description: Emite un set nuevo de códigos de respaldo e invalida los anteriores. Exige un código
        autenticador o de respaldo válido.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - code
              properties:
                code:
                  type: string
            example:
              code: '123456'
      responses:
        '200':
          description: Nuevos códigos de respaldo (los anteriores quedan anulados).
          content:
            application/json:
              example:
                recovery_codes:
                - WXYZ-2345
                - 6789-ABCD
                note: previous recovery codes are no longer valid
  /v1/me/passkeys:
    get:
      tags:
      - Passkeys
      operationId: listMyPasskeys
      summary: Listar mis passkeys
      responses:
        '200':
          description: Las passkeys registradas de la cuenta.
          content:
            application/json:
              example:
                passkeys:
                - passkey_id: 3b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  name: MacBook de Taylor
                  created_at: '2026-07-11T00:00:00Z'
                  last_used_at: '2026-07-11T00:05:00Z'
  /v1/me/passkeys/register/begin:
    post:
      tags:
      - Passkeys
      operationId: passkeyRegisterBegin
      summary: Iniciar el registro de una passkey
      description: Inicia una ceremonia WebAuthn de registro. Exige la contraseña actual (si el miembro
        tiene una) — un factor de acceso nuevo nunca se agrega desde una sesión robada. Pasa `options.publicKey`
        a `navigator.credentials.create()`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
            example:
              password: mi-clave-fuerte
      responses:
        '200':
          description: Opciones de creación WebAuthn + id del challenge.
          content:
            application/json:
              example:
                challenge_id: 7b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                options:
                  publicKey:
                    challenge: base64url…
                    rp:
                      id: cbpayapp.com
                expires_at: '2026-07-11T00:02:00Z'
        '503':
          description: Passkeys no configuradas para la org (`passkeys_unavailable`).
  /v1/me/passkeys/register/finish:
    post:
      tags:
      - Passkeys
      operationId: passkeyRegisterFinish
      summary: Completar el registro de una passkey
      description: Valida la attestation y guarda la passkey bajo un nombre amigable.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - challenge_id
              - credential
              properties:
                challenge_id:
                  type: string
                name:
                  type: string
                  description: Nombre amigable del dispositivo, ej. "MacBook de Taylor".
                credential:
                  type: object
                  description: El PublicKeyCredential JSON de navigator.credentials.create().
            example:
              challenge_id: 7b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              name: MacBook de Taylor
              credential:
                id: AaBb…
                type: public-key
                response: {}
      responses:
        '201':
          description: Passkey registrada.
          content:
            application/json:
              example:
                passkey_id: 3b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                name: MacBook de Taylor
                created_at: '2026-07-11T00:00:00Z'
        '409':
          description: Esta credencial del dispositivo ya está registrada (`passkey_exists`).
  /v1/me/passkeys/{passkeyID}:
    delete:
      tags:
      - Passkeys
      operationId: deleteMyPasskey
      summary: Quitar una passkey
      description: Quita una passkey. Exige la contraseña actual (si el miembro tiene una) y se bloquea
        si es el único método de acceso que queda.
      parameters:
      - name: passkeyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
            example:
              password: mi-clave-fuerte
      responses:
        '200':
          description: Passkey eliminada.
          content:
            application/json:
              example:
                passkey_id: 3b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                status: deleted
        '409':
          description: No se puede quitar el único método de acceso (`last_login_method`).
  /v1/me/sessions:
    get:
      tags:
      - Cuenta
      operationId: listMySessions
      summary: Listar mis sesiones activas
      description: Lista las sesiones (dispositivos) del miembro con IP, user agent, método de login y
        si cada una es la actual.
      responses:
        '200':
          description: Las sesiones del miembro.
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                sessions:
                - session_id: 8b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  created_at: '2026-07-11T00:00:00Z'
                  expires_at: '2026-07-12T00:00:00Z'
                  login_method: password
                  current: true
                  active: true
                  ip: 200.1.2.3
                  user_agent: Mozilla/5.0
  /v1/me/sessions/{sessionID}:
    delete:
      tags:
      - Cuenta
      operationId: revokeMySession
      summary: Revocar una de mis sesiones
      description: Cierra un dispositivo específico.
      parameters:
      - name: sessionID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Sesión revocada.
          content:
            application/json:
              example:
                session_id: 8b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                status: revoked
  /v1/me/sessions/revoke-all:
    post:
      tags:
      - Cuenta
      operationId: revokeAllMySessions
      summary: Revocar todas mis otras sesiones
      description: Cierra todas las sesiones salvo la actual.
      responses:
        '200':
          description: Otras sesiones revocadas.
          content:
            application/json:
              example:
                status: revoked
                sessions_revoked: 3
  /v1/me/security/events:
    get:
      tags:
      - Cuenta
      operationId: listMySecurityEvents
      summary: Mi actividad de seguridad
      description: Historial append-only de eventos de seguridad de la cuenta (logins, cambios de credenciales,
        factores agregados/quitados). Exige filtros de fecha `from`/`to`.
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Eventos de seguridad.
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                events:
                - event_id: a1b2c3d4-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  member_id: 1c2d3e4f-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  event: password_changed
                  created_at: '2026-07-11T00:00:00Z'
                  ip: 200.1.2.3
        '400':
          $ref: '#/components/responses/InvalidRange'
  /v1/events:
    get:
      tags:
      - Eventos en tiempo real
      operationId: streamEvents
      summary: Stream de eventos en tiempo real (SSE)
      description: |-
        Abre una conexión `text/event-stream` que entrega en tiempo real todos los eventos de la cuenta — los mismos que viajan por webhook, sin que expongas un endpoint público.
        
        Cada evento lleva `id:` (cursor monótono), `event:` (el tipo de evento) y `data:` (JSON con el mismo `event_id` del webhook, así ambos canales se cruzan).
        
        Reconecta con el header `Last-Event-ID` (o `?last_event_id=`) y el servidor replaya lo que te perdiste antes de seguir en vivo. El `EventSource` nativo no puede mandar el header `Authorization`: usa `fetch` con `ReadableStream`.
        
        El alcance sale de la credencial: una credencial de cuenta recibe sus propios eventos; una credencial org-admin con `ops:read` recibe el canal de toda la organización y puede acotarlo con `?account_id=`.
        
        La conexión manda `: ping` cada 20s, revalida la sesión cada 60s (una sesión revocada cierra el stream con el evento de control `unauthorized`) y se cierra a los 30 minutos con un evento de control `reconnect`.
      parameters:
      - name: Last-Event-ID
        in: header
        required: false
        description: 'Cursor de reconexión. Alternativa al header `Last-Event-ID` para clientes con `fetch`.'
        schema:
          type: string
        example: '4821'
      - name: last_event_id
        in: query
        required: false
        description: 'Cursor de reconexión. Alternativa al header `Last-Event-ID` para clientes con `fetch`.'
        schema:
          type: string
      - name: types
        in: query
        required: false
        description: 'Tipos de evento separados por coma para filtrar. Un tipo desconocido responde `400 invalid_event_type`.'
        schema:
          type: string
        example: payin_credited,payout_status_changed
      - name: snapshot
        in: query
        required: false
        description: 'Con `true`, el primer evento es `snapshot` con el estado actual absoluto (saldos y, para org-admins, salud operativa) antes de los deltas en vivo.'
        schema:
          type: boolean
      - name: account_id
        in: query
        required: false
        description: 'Solo org-admin: acota el canal de la organización a una sola cuenta.'
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'Stream de eventos abierto. El cuerpo queda abierto y se escribe incrementalmente.'
          content:
            text/event-stream:
              schema:
                type: string
              example: |
                : cbpay event stream

                retry: 3000

                id: 4822
                event: payin_credited
                data: {"event_id":"7c1f0b2a-5c3d-4e6f-9a1b-2c3d4e5f6a7b","type":"payin_credited","account_id":"c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10","created_at":"2026-07-25T18:04:11Z","cursor":"4822","data":{"payin_id":"9b8a7c6d-5e4f-4a3b-8c2d-1e0f9a8b7c6d","usdt_credited":"99.700000"}}

                : ping
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          description: 'Demasiados streams simultáneos (`too_many_streams`) o demasiadas aperturas de stream desde esta IP (`rate_limited`, 600 por hora — la cuota cuenta intentos, no conexiones vivas).'
          content:
            application/json:
              examples:
                too_many_streams:
                  summary: Límite de streams simultáneos alcanzado
                  value:
                    error: too_many_streams
                    message: too many concurrent event streams for this account; close one before opening another
                rate_limited:
                  summary: Demasiadas aperturas de stream desde esta IP
                  value:
                    error: rate_limited
                    message: too many requests
  /v1/events/history:
    get:
      tags:
      - Eventos en tiempo real
      operationId: listEventHistory
      summary: Historial de eventos
      description: 'El mismo log que alimenta el stream, consultable. Exige filtros de fecha `from`/`to`. La retención es de 90 días: el log es un buffer de notificación, los hechos financieros viven en sus propios recursos inmutables (movimientos, payins, payouts).'
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
      - name: event_type
        in: query
        required: false
        description: 'Filtra por un único tipo de evento.'
        schema:
          type: string
      - name: account_id
        in: query
        required: false
        description: 'Solo org-admin: acota el canal de la organización a una sola cuenta.'
        schema:
          type: string
          format: uuid
      - $ref: '#/components/parameters/page'
      - $ref: '#/components/parameters/pageSize'
      responses:
        '200':
          description: 'Eventos del rango solicitado.'
          content:
            application/json:
              example:
                page: 1
                page_size: 50
                total: 2
                events:
                - event_id: 7c1f0b2a-5c3d-4e6f-9a1b-2c3d4e5f6a7b
                  type: payin_credited
                  account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                  created_at: '2026-07-25T18:04:11Z'
                  cursor: '4822'
                  data:
                    payin_id: 9b8a7c6d-5e4f-4a3b-8c2d-1e0f9a8b7c6d
                    usdt_credited: '99.700000'
                - event_id: 1a2b3c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                  type: balance_adjusted
                  account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                  created_at: '2026-07-25T18:02:00Z'
                  cursor: '4821'
                  data:
                    account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                    asset: USDT
                    amount: '10.000000'
                    direction: credit
                    reason: manual top-up
                    available: '109.700000'
                    held: '0.000000'
        '400':
          $ref: '#/components/responses/InvalidRange'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /v1/events/{eventID}:
    get:
      tags:
      - Eventos en tiempo real
      operationId: getEvent
      summary: Detalle de un evento
      description: 'Detalle de un evento por su `event_id` público (el mismo que viaja en el webhook). Un evento fuera de tu alcance responde `404`, jamás `403`.'
      parameters:
      - name: eventID
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: 'El evento.'
          content:
            application/json:
              example:
                event_id: 7c1f0b2a-5c3d-4e6f-9a1b-2c3d4e5f6a7b
                type: payin_credited
                account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                created_at: '2026-07-25T18:04:11Z'
                cursor: '4822'
                data:
                  payin_id: 9b8a7c6d-5e4f-4a3b-8c2d-1e0f9a8b7c6d
                  usdt_credited: '99.700000'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /v1/me/txn-reviews:
    get:
      tags:
      - Account
      operationId: listMyTxnReviews
      summary: Mis revisiones de operaciones
      description: |-
        Operaciones de dinero de la cuenta retenidas por el firewall transaccional (payouts, retiros crypto, payins, transferencias banking) con su estado de revisión. Filtra por `status` y rango de fechas (`from`/`to`, zona horaria de la organización, ambos inclusive). Cuando una revisión llega a `info_requested`, sube los documentos pedidos con `POST /v1/me/txn-reviews/{reviewID}/files` — la operación sigue retenida hasta que el equipo de cumplimiento decida.
      parameters:
      - name: status
        in: query
        description: '`in_review`, `info_requested`, `released`, `rejected` or `all` (empty = open: `in_review` + `info_requested`).'
        schema:
          type: string
          enum:
          - in_review
          - info_requested
          - released
          - rejected
          - all
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      - name: page
        in: query
        required: false
        schema:
          type: integer
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
      responses:
        '200':
          description: Mis revisiones de operaciones.
          content:
            application/json:
              example:
                reviews:
                - id: 7a3f2b1c-0000-4000-8000-000000000001
                  kind: payout
                  resource_id: 8b4c3d2e-1111-4111-8111-111111111111
                  status: info_requested
                  amount_label: '1500.00'
                  asset: USD
                  country: MX
                  method: spei
                  counterparty: Juan Pérez
                  info_request:
                    message: Please upload the invoice that justifies this payment
                    requested_at: '2026-08-06T15:20:00Z'
                  created_at: '2026-08-06T14:32:00Z'
                  updated_at: '2026-08-06T15:20:00Z'
                page: 1
                page_size: 50
                total: 1
        '400':
          description: Invalid filter.
          content:
            application/json:
              examples:
                invalid_status:
                  summary: Unknown status
                  value:
                    error: invalid_status
                    message: status must be one of in_review, info_requested, released, rejected, all
                invalid_range:
                  summary: Bad date range
                  value:
                    error: invalid_range
                    message: invalid date range
  /v1/me/txn-reviews/{reviewID}:
    get:
      tags:
      - Account
      operationId: getMyTxnReview
      summary: Detalle de una revisión
      description: 'Detalle de una revisión de la cuenta, con el mensaje de la solicitud de información (`info_request`) y los archivos ya subidos. El motivo interno de la retención y las notas de cumplimiento nunca se exponen (anti tipping-off). Una revisión rechazada incluye además `decision_note` (el mismo texto del email de rechazo) y `decided_at`.'
      parameters:
      - name: reviewID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: The review with its uploaded files.
          content:
            application/json:
              example:
                review:
                  id: 7a3f2b1c-0000-4000-8000-000000000001
                  kind: payout
                  resource_id: 8b4c3d2e-1111-4111-8111-111111111111
                  status: info_requested
                  amount_label: '1500.00'
                  asset: USD
                  country: MX
                  method: spei
                  counterparty: Juan Pérez
                  info_request:
                    message: Please upload the invoice that justifies this payment
                    requested_at: '2026-08-06T15:20:00Z'
                  files:
                  - id: f1e2d3c4-0000-4000-8000-0000000000aa
                    review_id: 7a3f2b1c-0000-4000-8000-000000000001
                    file_name: invoice-221.pdf
                    content_type: application/pdf
                    size_bytes: 482110
                    uploaded_by: account
                    created_at: '2026-08-06T15:40:00Z'
                  created_at: '2026-08-06T14:32:00Z'
                  updated_at: '2026-08-06T15:40:00Z'
        '404':
          description: The review does not belong to the account.
          content:
            application/json:
              example:
                error: not_found
                message: review not found
  /v1/me/txn-reviews/{reviewID}/files:
    post:
      tags:
      - Account
      operationId: uploadMyTxnReviewFile
      summary: Sube un archivo para una solicitud de información
      description: |-
        Sube un documento de respaldo para una revisión en `info_requested`. Envía el binario crudo con su `Content-Type` (PDF, PNG, JPEG, WEBP, TXT, CSV, DOC(X) o XLS(X), hasta 50 MB) y el nombre del archivo en el query param `name`. Al subir un archivo la revisión vuelve a `in_review` para que el equipo de cumplimiento la re-evalúe.
      parameters:
      - name: reviewID
        in: path
        required: true
        schema:
          type: string
      - name: name
        in: query
        required: true
        description: Original filename (max 200 chars, no path separators).
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/pdf:
            schema:
              type: string
              format: binary
          image/jpeg:
            schema:
              type: string
              format: binary
          image/png:
            schema:
              type: string
              format: binary
          text/csv:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: File uploaded; the review moved back to `in_review`.
          content:
            application/json:
              example:
                file:
                  id: f1e2d3c4-0000-4000-8000-0000000000aa
                  review_id: 7a3f2b1c-0000-4000-8000-000000000001
                  file_name: invoice-221.pdf
                  content_type: application/pdf
                  size_bytes: 482110
                  uploaded_by: account
                  created_at: '2026-08-06T15:40:00Z'
                status: in_review
        '400':
          description: Validation error.
          content:
            application/json:
              examples:
                invalid_name:
                  summary: Missing filename
                  value:
                    error: invalid_name
                    message: name is required (query param, max 200 chars, no path separators)
                empty_file:
                  summary: Empty body
                  value:
                    error: empty_file
                    message: the file body is empty
        '404':
          description: The review does not belong to the account.
          content:
            application/json:
              example:
                error: not_found
                message: review not found
        '409':
          description: The review is not waiting for information.
          content:
            application/json:
              example:
                error: not_awaiting_info
                message: this review is not waiting for additional information
        '413':
          description: The file exceeds 50 MB.
          content:
            application/json:
              example:
                error: file_too_large
                message: max file size is 50 MB
        '415':
          description: Unsupported content type.
          content:
            application/json:
              example:
                error: unsupported_file_type
                message: 'allowed types: pdf, png, jpeg, webp, txt, csv, doc(x), xls(x) — send the file''s Content-Type header'
        '422':
          description: The review already has 20 files.
          content:
            application/json:
              example:
                error: file_limit_reached
                message: a review accepts at most 20 files
  /v1/me/txn-reviews/{reviewID}/files/{fileID}/download:
    get:
      tags:
      - Account
      operationId: downloadMyTxnReviewFile
      summary: Descarga un archivo que subiste
      description: 'Descarga un archivo subido previamente por la cuenta para esta revisión. Devuelve el binario crudo con el `Content-Type` original. Los archivos de otras cuentas u otras revisiones responden `404 not_found` (nunca `403`, para no filtrar existencia).'
      parameters:
      - name: reviewID
        in: path
        required: true
        schema:
          type: string
      - name: fileID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: The raw file bytes with the original `Content-Type`.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            text/csv:
              schema:
                type: string
                format: binary
        '404':
          description: The review or file does not belong to the account.
          content:
            application/json:
              example:
                error: not_found
                message: review not found

  /v1/qscore/reports:
    get:
      tags: [Qscore]
      summary: Listar informes crediticios
      operationId: listQscoreReports
      description: 'Devuelve los informes crediticios de tu cuenta, del más reciente al más antiguo. Los

        filtros de fecha `from` y `to` (zona horaria de la organización, ambos inclusivos) son obligatorios. Los informes

        de otras cuentas nunca son visibles (aislamiento por cuenta: un id de informe

        perteneciente a otra cuenta devuelve `404 not_found`).'
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
        description: 'Fecha de inicio YYYY-MM-DD (zona horaria de la organización, inclusiva). Una fecha inválida

          devuelve 400 invalid_range.'
        example: '2026-08-01'
      - name: to
        in: query
        required: true
        schema:
          type: string
        description: Fecha de término YYYY-MM-DD (zona horaria de la organización, inclusiva).
        example: '2026-08-08'
      - name: subject_id
        in: query
        required: false
        schema:
          type: string
        description: Filtra por id de sujeto (de GET /v1/qscore/subjects/{docId}).
        example: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum: [pending, ready, failed]
        description: Filtra por estado del informe.
        example: ready
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
        description: Número de página (>= 1).
        example: 1
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 50
          maximum: 200
        description: Resultados por página (máx. 200).
        example: 50
      responses:
        '200':
          description: 'Página de informes. Cada ítem es el objeto informe persistido; el

            campo de versión del modelo de score es `score_model_version` en este listado

            (los endpoints de un informe devuelven el mismo valor como `model_version`).'
          content:
            application/json:
              examples:
                ready:
                  summary: Página con un informe listo
                  value:
                    items:
                    - report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                      account_id: 5f9d2c10-3e8b-4a1d-9c7a-1d2f3a4b5c6d
                      subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                      purpose: credit_evaluation
                      status: ready
                      lang: es
                      score: 742
                      band: B
                      score_model_version: v1
                      reason_codes:
                      - code: CREDIT_HISTORY_DEPTH
                        direction: positive
                        weight: medium
                      - code: ACTIVE_TRADELINES
                        direction: positive
                        weight: low
                      verify_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                      created_at: '2026-08-08T15:04:22Z'
                      completed_at: '2026-08-08T15:04:24Z'
                    meta:
                      page: 1
                      page_size: 50
                      total: 1
        '400':
          description: invalid_range
          content:
            application/json:
              example:
                error: invalid_range
                message: from/to must be valid YYYY-MM-DD dates
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado, cuenta sin verificar o scope insuficiente)
    post:
      tags: [Qscore]
      summary: Crear un informe crediticio
      operationId: createQscoreReport
      description: 'Emite un informe crediticio nuevo de un sujeto (persona o empresa),

        cobrado como fee standalone (`risk_report_person` / `risk_report_company`)

        al momento de la emisión. La idempotencia es obligatoria: reintentar con la

        misma `idempotency_key` devuelve el informe original con `idempotency_hit:

        true` y jamás cobra dos veces. El informe se calcula de forma síncrona: la

        respuesta es normalmente el informe terminado (status `ready`) o uno fallido

        (status `failed` con `error_code`/`error_message`). El campo `purpose`

        es obligatorio (ley de protección de datos) y `doc_id` se valida y

        normaliza por país (p. ej. el RUT chileno con dígito verificador).'
      requestBody:
        required: true
        content:
          application/json:
            examples:
              person:
                summary: Informe de persona (RUT chileno)
                value:
                  doc_id: 12.345.678-5
                  country: CL
                  subject_type: person
                  purpose: credit_evaluation
                  lang: es
                  idempotency_key: qscore-9f2b8d11
              company:
                summary: Informe de empresa (RUT chileno de empresa)
                value:
                  doc_id: 76.543.210-3
                  country: CL
                  subject_type: company
                  purpose: supplier_onboarding
                  lang: en
                  idempotency_key: qscore-7a31c4e5
      responses:
        '201':
          description: 'Informe creado y calculado. `model_version` identifica la

            versión del modelo de score. Con status `ready`, el objeto `report`

            normalizado completo va embebido; con status `failed`,

            `error_code`/`error_message` explican por qué y la clave de idempotencia

            se libera para poder reintentar.'
          content:
            application/json:
              examples:
                ready:
                  summary: Informe listo (score calculado)
                  value:
                    report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                    subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                    status: ready
                    purpose: credit_evaluation
                    lang: es
                    score: 742
                    band: B
                    model_version: v1
                    reason_codes:
                    - code: CREDIT_HISTORY_DEPTH
                      direction: positive
                      weight: medium
                    - code: ACTIVE_TRADELINES
                      direction: positive
                      weight: low
                    verify_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                    created_at: '2026-08-08T15:04:22Z'
                    completed_at: '2026-08-08T15:04:24Z'
                    report:
                      meta:
                        report_id: QSR-3F5C9F2D7D21
                        lang: es
                        purpose: credit_evaluation
                        generated_at: '2026-08-08T15:04:24Z'
                        verification_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                        verification_url: /verify/qscore/Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                      identity:
                        subject_type: person
                        doc_id: 12.345.678-5
                        name: Example Name
                        country: CL
                      score:
                        score: 742
                        band: B
                        model_version: v1
                        reason_codes:
                        - code: CREDIT_HISTORY_DEPTH
                          direction: positive
                          weight: medium
                        - code: ACTIVE_TRADELINES
                          direction: positive
                          weight: low
                        computed_at: '2026-08-08T15:04:24Z'
                      summary:
                      - No delinquencies or adverse events on record.
                      - Active credit history with reported tradelines.
                      tradelines:
                      - source: res_chile
                        creditor: Commercial creditor
                        kind: loan
                        status: current
                        currency: CLP
                        balance: '1250000'
                        opened_at: '2025-03-14'
                        reported_at: '2026-08-01'
                      delinquencies: []
                      sources:
                      - source: res_chile
                        label: Commercial bulletin
                        records: 1
                        fetched_at: '2026-08-08T15:04:23Z'
                ready_company:
                  summary: Informe de empresa con benchmark de pares por industria
                  value:
                    report_id: 7c9a1f3d-2e44-4b8a-9d51-0a1b2c3d4e5f
                    subject_id: 5b7d0e2a-2c34-4f6a-8b11-7d8e9f0a1b2c
                    status: ready
                    purpose: credit_evaluation
                    lang: es
                    score: 712
                    band: B
                    model_version: qscore-v2
                    reason_codes:
                    - code: ACTIVE_TRADELINES
                      direction: positive
                      weight: medium
                    verify_code: Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                    created_at: '2026-08-08T14:52:03Z'
                    completed_at: '2026-08-08T14:52:05Z'
                    report:
                      meta:
                        report_id: QSR-7C9A1F3D2E44
                        lang: es
                        purpose: credit_evaluation
                        generated_at: '2026-08-08T14:52:05Z'
                        verification_code: Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                        verification_url: /verify/qscore/Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                      identity:
                        subject_type: company
                        doc_id: 76.123.456-0
                        name: Example Company SpA
                        country: CL
                      score:
                        score: 712
                        band: B
                        model_version: qscore-v2
                        reason_codes:
                        - code: ACTIVE_TRADELINES
                          direction: positive
                          weight: medium
                        computed_at: '2026-08-08T14:52:05Z'
                      peer_benchmark:
                        available: true
                        segment_code: '6499'
                        segment_label: Other financial service activities
                        peers: 12
                        percentile: 75
                        median_score: 640
                      summary:
                      - No delinquencies or adverse events on record.
                      - Active credit history with reported tradelines.
                      tradelines:
                      - source: res_chile
                        creditor: Commercial creditor
                        kind: loan
                        status: current
                        currency: CLP
                        balance: '1250000'
                        opened_at: '2025-03-14'
                        reported_at: '2026-08-01'
                      delinquencies: []
                      sources:
                      - source: res_chile
                        label: Commercial bulletin
                        records: 1
                        fetched_at: '2026-08-08T14:52:04Z'
                no_data:
                  summary: Sujeto sin datos de buró (banda SC)
                  value:
                    report_id: 9b2e7a41-3c10-4e7b-8f25-9d31a2b7c5e2
                    subject_id: 5c31a9d4-6b20-4f18-9a71-3e7b2d90c4aa
                    status: ready
                    purpose: hiring
                    lang: en
                    band: SC
                    model_version: v1
                    reason_codes:
                    - code: NO_DATA
                      direction: negative
                      weight: high
                    verify_code: Q9b2e7a413c104e7b8f259d31a2b7c5e2b2c3d4e5f60718293a4
                    created_at: '2026-08-08T15:10:02Z'
                    completed_at: '2026-08-08T15:10:03Z'
                replay:
                  summary: Replay idempotente (200 con idempotency_hit)
                  value:
                    report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                    subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                    status: ready
                    purpose: credit_evaluation
                    lang: es
                    score: 742
                    band: B
                    model_version: v1
                    reason_codes:
                    - code: CREDIT_HISTORY_DEPTH
                      direction: positive
                      weight: medium
                    verify_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                    created_at: '2026-08-08T15:04:22Z'
                    completed_at: '2026-08-08T15:04:24Z'
                    idempotency_hit: true
        '400':
          description: invalid_payload | purpose_required | invalid_purpose | invalid_doc_id | invalid_subject_type | idempotency_key_required
          content:
            application/json:
              examples:
                purpose:
                  summary: Falta el purpose
                  value:
                    error: purpose_required
                    message: purpose is required (data protection law)
                doc_id:
                  summary: Documento de identidad inválido
                  value:
                    error: invalid_doc_id
                    message: doc_id is not valid for the given country
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found (no se encontró un recurso padre de la cuenta)
        '409':
          description: idempotency_conflict (misma clave con payload distinto o aún procesando)
  /v1/qscore/reports/{id}:
    get:
      tags: [Qscore]
      summary: Obtener un informe crediticio
      operationId: getQscoreReport
      description: 'Devuelve el detalle completo de un informe crediticio de tu

        cuenta. Con `status` `ready` el objeto `report` normalizado completo va

        embebido; con `failed`, `error_code`/`error_message` explican por qué.

        Un informe de otra cuenta devuelve `404 not_found` (aislamiento por

        cuenta).'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        example: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
      responses:
        '200':
          description: Detalle del informe.
          content:
            application/json:
              examples:
                ready_person:
                  summary: Informe de persona listo
                  value:
                    report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                    subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                    status: ready
                    purpose: credit_evaluation
                    lang: es
                    score: 742
                    band: B
                    model_version: v1
                    reason_codes:
                    - code: CREDIT_HISTORY_DEPTH
                      direction: positive
                      weight: medium
                    - code: ACTIVE_TRADELINES
                      direction: positive
                      weight: low
                    verify_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                    created_at: '2026-08-08T15:04:22Z'
                    completed_at: '2026-08-08T15:04:24Z'
                    report:
                      meta:
                        report_id: QSR-3F5C9F2D7D21
                        lang: es
                        purpose: credit_evaluation
                        generated_at: '2026-08-08T15:04:24Z'
                        verification_code: Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                        verification_url: /verify/qscore/Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a
                      identity:
                        subject_type: person
                        doc_id: 12.345.678-5
                        name: Example Name
                        country: CL
                      score:
                        score: 742
                        band: B
                        model_version: v1
                        reason_codes:
                        - code: CREDIT_HISTORY_DEPTH
                          direction: positive
                          weight: medium
                        computed_at: '2026-08-08T15:04:24Z'
                      summary:
                      - No delinquencies or adverse events on record.
                      sources:
                      - source: res_chile
                        label: Commercial bulletin
                        records: 1
                        fetched_at: '2026-08-08T15:04:23Z'
                ready_company:
                  summary: Informe de empresa con benchmark de pares por industria
                  value:
                    report_id: 7c9a1f3d-2e44-4b8a-9d51-0a1b2c3d4e5f
                    subject_id: 5b7d0e2a-2c34-4f6a-8b11-7d8e9f0a1b2c
                    status: ready
                    purpose: credit_evaluation
                    lang: es
                    score: 712
                    band: B
                    model_version: qscore-v2
                    reason_codes:
                    - code: ACTIVE_TRADELINES
                      direction: positive
                      weight: medium
                    verify_code: Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                    created_at: '2026-08-08T14:52:03Z'
                    completed_at: '2026-08-08T14:52:05Z'
                    report:
                      meta:
                        report_id: QSR-7C9A1F3D2E44
                        lang: es
                        purpose: credit_evaluation
                        generated_at: '2026-08-08T14:52:05Z'
                        verification_code: Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                        verification_url: /verify/qscore/Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fa1b2c3d4e5f60718293a
                      identity:
                        subject_type: company
                        doc_id: 76.123.456-0
                        name: Example Company SpA
                        country: CL
                      score:
                        score: 712
                        band: B
                        model_version: qscore-v2
                        reason_codes:
                        - code: ACTIVE_TRADELINES
                          direction: positive
                          weight: medium
                        computed_at: '2026-08-08T14:52:05Z'
                      peer_benchmark:
                        available: true
                        segment_code: '6499'
                        segment_label: Other financial service activities
                        peers: 12
                        percentile: 75
                        median_score: 640
                      summary:
                      - No delinquencies or adverse events on record.
                      - Active credit history with reported tradelines.
                      tradelines:
                      - source: res_chile
                        creditor: Commercial creditor
                        kind: loan
                        status: current
                        currency: CLP
                        balance: '1250000'
                        opened_at: '2025-03-14'
                        reported_at: '2026-08-01'
                      delinquencies: []
                      sources:
                      - source: res_chile
                        label: Commercial bulletin
                        records: 1
                        fetched_at: '2026-08-08T14:52:04Z'
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found
          content:
            application/json:
              example:
                error: not_found
                message: report not found
  /v1/qscore/reports/{id}/pdf:
    get:
      tags: [Qscore]
      summary: Descargar el PDF del informe
      operationId: getQscoreReportPDF
      description: 'Descarga el PDF brandeado de un informe crediticio en el idioma en

        que fue emitido (`lang`), con el `verify_code` impreso para verificación

        pública. Solo disponible cuando el informe está `ready` — si no, `404

        pdf_not_ready`. El PDF se genera al vuelo en cada request; guárdalo si

        necesitas una copia inmutable.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        example: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
      responses:
        '200':
          description: Archivo PDF del informe.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found | pdf_not_ready
          content:
            application/json:
              example:
                error: pdf_not_ready
                message: the report PDF is not available yet
  /v1/qscore/my-report:
    post:
      tags: [Qscore]
      summary: Genera tu propio informe crediticio (self)
      operationId: createQscoreSelfReport
      description: 'Genera (o reutiliza) el informe crediticio Qscore del titular de
        la cuenta autenticada — el derecho de acceso ARCO / de protección de datos
        a la propia información crediticia. A diferencia de `POST
        /v1/qscore/reports`, este endpoint es GRATUITO (no cobra comisión) y la
        identidad del sujeto se deriva de la cuenta verificada: el `tax_id`
        validado durante el KYC/KYB. El request NO acepta `doc_id` — pedir el
        informe de un tercero por este endpoint es imposible por diseño
        (anti-oráculo).

        Límite de frecuencia: un informe NUEVO cada 30 días. Si ya existe un
        informe self `ready` dentro de esa ventana, el endpoint responde `200`
        con el informe existente e `idempotency_hit: true` (reutilizado); si no,
        genera uno nuevo y responde `201`. Un doble envío el mismo día devuelve
        el informe ya creado (idempotencia determinista por cuenta + sujeto +
        día). Los informes self quedan excluidos del conteo de consultas del
        sujeto, así que revisar tu propio informe jamás penaliza tu score. El
        informe se computa de forma síncrona: la respuesta es el informe
        terminado (status `ready`) o uno fallido (status `failed` con
        `error_code`/`error_message`).'
      requestBody:
        required: false
        content:
          application/json:
            examples:
              default:
                summary: Idioma por defecto (es)
                value: {}
              lang:
                summary: Elegir el idioma del informe
                value:
                  lang: en
      responses:
        '200':
          description: 'Ya existía un informe self `ready` dentro de la ventana
            de 30 días; se devuelve con `idempotency_hit: true` (no se genera
            uno nuevo).'
          content:
            application/json:
              example:
                report_id: 7d2c1a90-4e55-4b3a-9f0c-1a2b3c4d5e6f
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                status: ready
                purpose: self_access
                lang: es
                score: 742
                band: B
                model_version: qscore-v2
                reason_codes:
                - code: CREDIT_HISTORY_DEPTH
                  direction: positive
                  weight: medium
                verify_code: Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                created_at: '2026-08-02T11:20:10Z'
                completed_at: '2026-08-02T11:20:12Z'
                idempotency_hit: true
        '201':
          description: 'Informe self creado y computado. Cuando el status es
            `ready`, el objeto `report` completo normalizado va embebido; cuando
            es `failed`, `error_code`/`error_message` explican por qué.'
          content:
            application/json:
              examples:
                ready:
                  summary: Informe listo (score computado)
                  value:
                    report_id: 7d2c1a90-4e55-4b3a-9f0c-1a2b3c4d5e6f
                    subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                    status: ready
                    purpose: self_access
                    lang: es
                    score: 742
                    band: B
                    model_version: qscore-v2
                    reason_codes:
                    - code: CREDIT_HISTORY_DEPTH
                      direction: positive
                      weight: medium
                    - code: ACTIVE_TRADELINES
                      direction: positive
                      weight: low
                    verify_code: Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                    created_at: '2026-08-08T15:04:22Z'
                    completed_at: '2026-08-08T15:04:24Z'
                    report:
                      meta:
                        report_id: QSR-7D2C1A904E55
                        lang: es
                        purpose: self_access
                        generated_at: '2026-08-08T15:04:24Z'
                        verification_code: Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                        verification_url: /verify/qscore/Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                      identity:
                        subject_type: person
                        doc_id: 12.345.678-5
                        name: Example Name
                        country: CL
                      score:
                        score: 742
                        band: B
                        model_version: qscore-v2
                        reason_codes:
                        - code: CREDIT_HISTORY_DEPTH
                          direction: positive
                          weight: medium
                        computed_at: '2026-08-08T15:04:24Z'
                      summary:
                      - No delinquencies or adverse events on record.
                      sources:
                      - source: bureau
                        label: Commercial bulletin
                        records: 1
                        fetched_at: '2026-08-08T15:04:23Z'
                no_data:
                  summary: Titular sin datos de buró (banda SC)
                  value:
                    report_id: 9b2e7a41-3c10-4e7b-8f25-9d31a2b7c5e2
                    subject_id: 5c31a9d4-6b20-4f18-9a71-3e7b2d90c4aa
                    status: ready
                    purpose: self_access
                    lang: en
                    band: SC
                    model_version: qscore-v2
                    reason_codes:
                    - code: NO_DATA
                      direction: negative
                      weight: high
                    verify_code: Q9b2e7a413c104e7b8f259d31a2b7c5e2b2c3d4e5f60718293a4
                    created_at: '2026-08-08T15:10:02Z'
                    completed_at: '2026-08-08T15:10:03Z'
        '400':
          description: invalid_tax_id | invalid_payload (p. ej. lang no soportado)
          content:
            application/json:
              example:
                error: invalid_tax_id
                message: the verified tax id of the account is not valid for its country
        '401':
          description: unauthorized
        '403':
          description: 'kyc_required (la cuenta no tiene un KYC/KYB aprobado) |
            forbidden (servicio deshabilitado)'
          content:
            application/json:
              example:
                error: kyc_required
                message: an approved identity verification is required to pull your own report
        '409':
          description: no_tax_id (la cuenta verificada no tiene tax id registrado)
          content:
            application/json:
              example:
                error: no_tax_id
                message: the account has no verified tax id
    get:
      tags: [Qscore]
      summary: Obtén tu último informe crediticio self
      operationId: getQscoreSelfReport
      description: 'Devuelve el informe self más reciente del titular de la cuenta
        autenticada (en cualquier estado), sin generar uno nuevo. Responde `404
        not_found` si el titular nunca generó un informe self. Cuando el `status`
        es `ready` el objeto `report` completo normalizado va embebido; cuando es
        `failed`, `error_code`/`error_message` explican por qué.'
      responses:
        '200':
          description: Último informe self.
          content:
            application/json:
              example:
                report_id: 7d2c1a90-4e55-4b3a-9f0c-1a2b3c4d5e6f
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                status: ready
                purpose: self_access
                lang: es
                score: 742
                band: B
                model_version: qscore-v2
                reason_codes:
                - code: CREDIT_HISTORY_DEPTH
                  direction: positive
                  weight: medium
                verify_code: Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                created_at: '2026-08-08T15:04:22Z'
                completed_at: '2026-08-08T15:04:24Z'
                report:
                  meta:
                    report_id: QSR-7D2C1A904E55
                    lang: es
                    purpose: self_access
                    generated_at: '2026-08-08T15:04:24Z'
                    verification_code: Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                    verification_url: /verify/qscore/Q7d2c1a904e554b3a9f0c1a2b3c4d5e6fa1b2c3d4e5f60718293a
                  identity:
                    subject_type: person
                    doc_id: 12.345.678-5
                    name: Example Name
                    country: CL
                  score:
                    score: 742
                    band: B
                    model_version: qscore-v2
                    computed_at: '2026-08-08T15:04:24Z'
                  summary:
                  - No delinquencies or adverse events on record.
        '401':
          description: unauthorized
        '403':
          description: kyc_required | forbidden
        '404':
          description: not_found (nunca se generó un informe self)
          content:
            application/json:
              example:
                error: not_found
                message: resource not found
  /v1/qscore/my-report/pdf:
    get:
      tags: [Qscore]
      summary: Descarga el PDF de tu propio informe
      operationId: getQscoreSelfReportPDF
      description: 'Descarga el PDF brandeado del informe self más reciente del
        titular de la cuenta autenticada, en el idioma en que fue emitido
        (`lang`), con el `verify_code` impreso para verificación pública en `GET
        /verify/qscore/{code}`. Solo disponible cuando el informe está `ready` —
        si no, `404 pdf_not_ready`. La respuesta es una descarga binaria
        (`Content-Disposition: attachment; filename="qscore_self_<id>.pdf"`).'
      responses:
        '200':
          description: Archivo PDF del informe self.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
              example: '%PDF-1.7 (binary report content)'
        '401':
          description: unauthorized
        '403':
          description: kyc_required | forbidden
        '404':
          description: not_found | pdf_not_ready
          content:
            application/json:
              example:
                error: pdf_not_ready
                message: the report PDF is not available yet
  /v1/qscore/my-seal:
    post:
      tags: [Qscore]
      operationId: activateMyQscoreSeal
      summary: Activar mi sello público verificable Qscore
      description: 'Activa el sello público Qscore de la cuenta empresa autenticada.
        Elegibilidad (evaluada en vivo): banda A o B con una evaluación de no más de
        90 días — `seal_not_eligible` en caso contrario; el producto es solo para
        empresas (`seal_companies_only` en cuentas persona). No requiere body y es
        idempotente por diseño: activar cuando el sello ya está activo responde 200
        con el sello vigente y `idempotency_hit: true` — jamás se crea un duplicado.
        Se envía un email de aviso de activación al titular (branding de la org).'
      responses:
        '201':
          description: Sello activado.
          content:
            application/json:
              example:
                seal:
                  id: becb6a82-3f52-4d7a-9c2f-8a1d4e5f6a70
                  status: active
                  verify_code: SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                  verify_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                  badge_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2/badge.svg
                  created_at: '2026-08-09T15:04:22Z'
                subject_id: c9558a4b-374f-4599-b638-c93ec5489e84
                eligibility:
                  eligible: true
                  band: A
                  score: 845
                  evaluated_at: '2026-08-07'
        '200':
          description: El sello ya estaba activo (replay seguro).
          content:
            application/json:
              example:
                seal:
                  id: becb6a82-3f52-4d7a-9c2f-8a1d4e5f6a70
                  status: active
                  verify_code: SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                  verify_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                  badge_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2/badge.svg
                  created_at: '2026-08-09T15:04:22Z'
                subject_id: c9558a4b-374f-4599-b638-c93ec5489e84
                eligibility:
                  eligible: true
                  band: A
                  score: 845
                  evaluated_at: '2026-08-07'
                idempotency_hit: true
        '400':
          description: El tax_id de la cuenta falta o es inválido.
          content:
            application/json:
              examples:
                invalid_tax_id:
                  summary: Tax ID inválido
                  value:
                    error: invalid_tax_id
                    message: the tax_id on your account is not a valid document for its
                      country
        '403':
          description: kyc_required (la cuenta no tiene un KYC/KYB aprobado) | forbidden
            (servicio deshabilitado)
          content:
            application/json:
              example:
                error: kyc_required
                message: identity verification (KYC/KYB) is required to activate your
                  public seal
        '409':
          description: Conflicto de regla de negocio.
          content:
            application/json:
              examples:
                seal_companies_only:
                  summary: Cuenta persona
                  value:
                    error: seal_companies_only
                    message: the public seal is only available for company accounts
                seal_not_eligible:
                  summary: Banda bajo B o evaluación envejecida
                  value:
                    error: seal_not_eligible
                    message: your Qscore does not currently qualify for a public seal
                no_tax_id:
                  summary: La cuenta no tiene tax_id
                  value:
                    error: no_tax_id
                    message: complete your account's tax_id to activate your public seal
                identity_mismatch:
                  summary: El tax_id no calza con la identidad verificada
                  value:
                    error: identity_mismatch
                    message: the tax_id on your account does not match your verified
                      identity
    get:
      tags: [Qscore]
      operationId: getMyQscoreSeal
      summary: Consultar el estado de mi sello Qscore
      description: 'Devuelve el sello de la cuenta autenticada (`null` si no tiene)
        junto con su elegibilidad en vivo. Un sello activo incluye `verify_code`,
        `verify_url` y `badge_url`; un sello revocado conserva solo `revoked_at`
        (sus URLs dejan de funcionar permanentemente). Las cuentas persona siempre
        reciben `seal: null` con `eligibility.reason: "companies_only"`.'
      responses:
        '200':
          description: Sello actual y elegibilidad en vivo.
          content:
            application/json:
              examples:
                active:
                  summary: Empresa con sello activo
                  value:
                    subject_id: c9558a4b-374f-4599-b638-c93ec5489e84
                    seal:
                      id: becb6a82-3f52-4d7a-9c2f-8a1d4e5f6a70
                      status: active
                      verify_code: SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                      verify_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2
                      badge_url: https://api.qbank.cl/platform/verify/qscore/seal/SA710B276F30387B2A96BF2BBF7632D138e23224b7d36b7b064a35f958b6b230a2/badge.svg
                      created_at: '2026-08-09T15:04:22Z'
                    eligibility:
                      eligible: true
                      band: A
                      score: 845
                      evaluated_at: '2026-08-07'
                no_seal:
                  summary: Empresa que nunca activó (o tras revocar)
                  value:
                    subject_id: c9558a4b-374f-4599-b638-c93ec5489e84
                    seal: null
                    eligibility:
                      eligible: false
                      reason: score_stale
                      band: B
                      score: 702
                      evaluated_at: '2026-04-29'
                person:
                  summary: Cuenta persona (solo empresas)
                  value:
                    subject_id: e2518c9a-5f5e-4f6d-b6f8-991dd296b6cd
                    seal: null
                    eligibility:
                      eligible: false
                      reason: companies_only
        '400':
          description: El tax_id de la cuenta falta o es inválido.
          content:
            application/json:
              example:
                error: invalid_tax_id
                message: the tax_id on your account is not a valid document for its
                  country
        '403':
          description: kyc_required (la cuenta no tiene un KYC/KYB aprobado) | forbidden
            (servicio deshabilitado)
          content:
            application/json:
              example:
                error: kyc_required
                message: identity verification (KYC/KYB) is required to use your Qscore
                  seal
        '409':
          description: Conflicto de regla de negocio.
          content:
            application/json:
              examples:
                no_tax_id:
                  summary: La cuenta no tiene tax_id
                  value:
                    error: no_tax_id
                    message: complete your account's tax_id to use your Qscore seal
                identity_mismatch:
                  summary: El tax_id no calza con la identidad verificada
                  value:
                    error: identity_mismatch
                    message: the tax_id on your account does not match your verified
                      identity
    delete:
      tags: [Qscore]
      operationId: revokeMyQscoreSeal
      summary: Revocar mi sello Qscore
      description: 'Revoca el sello activo de la cuenta autenticada. Inmediato y
        PERMANENTE para ese sello: su URL pública y su badge pasan a "no vigente"
        (la URL sigue respondiendo — nunca rompe una página que lo haya embebido).
        No se puede deshacer, pero la cuenta puede activar un sello NUEVO (con un
        código distinto) en cualquier momento, mientras siga siendo elegible. Se
        envía un email de aviso de revocación al titular (branding de la org).'
      responses:
        '200':
          description: Sello revocado.
          content:
            application/json:
              example:
                seal:
                  id: becb6a82-3f52-4d7a-9c2f-8a1d4e5f6a70
                  status: revoked
                  revoked_at: '2026-08-09T18:41:07Z'
        '400':
          description: El tax_id de la cuenta falta o es inválido.
          content:
            application/json:
              example:
                error: invalid_tax_id
                message: the tax_id on your account is not a valid document for its
                  country
        '403':
          description: kyc_required (la cuenta no tiene un KYC/KYB aprobado) | forbidden
            (servicio deshabilitado)
          content:
            application/json:
              example:
                error: kyc_required
                message: identity verification (KYC/KYB) is required to use your Qscore
                  seal
        '404':
          description: No hay sello activo que revocar.
          content:
            application/json:
              example:
                error: no_active_seal
                message: you have no active seal
        '409':
          description: Conflicto de regla de negocio.
          content:
            application/json:
              examples:
                no_tax_id:
                  summary: La cuenta no tiene tax_id
                  value:
                    error: no_tax_id
                    message: complete your account's tax_id to use your Qscore seal
                identity_mismatch:
                  summary: El tax_id no calza con la identidad verificada
                  value:
                    error: identity_mismatch
                    message: the tax_id on your account does not match your verified
                      identity
  /v1/qscore/subjects/{docID}:
    get:
      tags: [Qscore]
      summary: Consultar un sujeto
      operationId: getQscoreSubject
      description: 'Devuelve el registro del sujeto para un documento de identidad (no

        emite un informe nuevo ni cobra nada). Los sujetos se crean automáticamente

        la primera vez que se pide un informe para ese documento. El parámetro de

        ruta `docId` es el documento tal como fue emitido (p. ej. un RUT chileno como

        `12.345.678-5`); se valida y normaliza por país. Si el sujeto ya tiene

        scores, el último se resume en `last_score` / `last_band` / `last_score_at`

        (`last_score` está ausente para la banda SC). Un sujeto inexistente devuelve

        `404 not_found`.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        description: Documento de identidad tal como fue emitido en el país (p. ej. `12.345.678-5` para Chile).
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (CL por defecto).
        example: CL
      responses:
        '200':
          description: Registro del sujeto con el resumen de su último score.
          content:
            application/json:
              example:
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                country: CL
                doc_id: 12.345.678-5
                subject_type: person
                display_name: Example Name
                last_score: 742
                last_band: B
                last_score_at: '2026-08-08T15:04:24Z'
        '400':
          description: invalid_doc_id
          content:
            application/json:
              example:
                error: invalid_doc_id
                message: doc_id is not valid for the given country
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found (el sujeto no existe)
          content:
            application/json:
              example:
                error: not_found
                message: subject not found
  /v1/qscore/subjects/{docID}/score:
    get:
      tags: [Qscore]
      summary: Score vigente de un sujeto
      operationId: getQscoreSubjectScore
      description: 'Devuelve solo el último score de un sujeto — la consulta barata

        para chequeos recurrentes sin emitir (ni pagar) un informe nuevo. Devuelve

        `404 no_score` si aún no se ha calculado ningún informe para ese sujeto.

        Para la banda SC (sin datos), `score` está ausente.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (CL por defecto).
        example: CL
      responses:
        '200':
          description: Último score del sujeto.
          content:
            application/json:
              example:
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                doc_id: 12.345.678-5
                country: CL
                score: 742
                band: B
                model_version: v1
                computed_at: '2026-08-08T15:04:24Z'
        '400':
          description: invalid_doc_id
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found (el sujeto no existe) | no_score
          content:
            application/json:
              example:
                error: no_score
                message: no score has been computed for this subject yet
  /v1/qscore/subjects/{docID}/disputes:
    get:
      tags: [Qscore]
      summary: Listar disputas del sujeto (ARCO)
      operationId: listQscoreSubjectDisputes
      description: 'Lista las disputas ARCO abiertas para un sujeto (derechos del

        titular de los datos: rectificación y cancelación de datos crediticios).

        Filtrable por `status` (`open`, `under_review`, `resolved_corrected`,

        `resolved_rejected`). El ciclo de vida de la disputa se opera desde el panel

        org admin o por tu organización vía `PATCH /v1/org/qscore/disputes/{id}`;

        cada cambio se notifica al solicitante por email. Una disputa resuelta jamás

        borra el registro disputado: la corrección es un registro nuevo que lo

        reemplaza.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (CL por defecto).
        example: CL
      - name: status
        in: query
        required: false
        schema:
          type: string
          enum: [open, under_review, resolved_corrected, resolved_rejected]
        description: Filtra por estado de la disputa.
        example: open
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 50
          maximum: 200
        example: 50
      responses:
        '200':
          description: Página de disputas.
          content:
            application/json:
              example:
                items:
                - dispute_id: 6a1c4e92-3b7d-4f51-9e82-1c7f3a5d8b64
                  subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                  report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                  record_source: res_chile
                  record_ref: BOL-2026-04512
                  reason: The reported default was paid in full on 2026-07-30.
                  status: open
                  created_by: 5f9d2c10-3e8b-4a1d-9c7a-1d2f3a4b5c6d
                  created_at: '2026-08-08T16:11:40Z'
                meta:
                  page: 1
                  page_size: 50
                  total: 1
        '400':
          description: invalid_doc_id
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found (el sujeto no existe)
    post:
      tags: [Qscore]
      summary: Abrir una disputa (ARCO)
      operationId: createQscoreDispute
      description: 'Abre una disputa ARCO sobre un registro específico de un sujeto

        (p. ej. una deuda que no le pertenece o que ya fue pagada), en cumplimiento

        de los derechos de rectificación del titular de los datos. El sujeto ya debe

        existir (lo crea el primer informe). `record_source` identifica la familia de

        fuente de datos del registro disputado y `record_ref` el registro dentro de

        esa fuente (ambos aparecen en el informe como `source` / `ref` de cada

        tradeline o evento adverso). La disputa nace en estado `open` y su ciclo de

        vida lo gestiona tu organización.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (CL por defecto).
        example: CL
      requestBody:
        required: true
        content:
          application/json:
            example:
              record_source: res_chile
              record_ref: BOL-2026-04512
              reason: The reported default was paid in full on 2026-07-30.
              report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
      responses:
        '201':
          description: Disputa creada en estado `open`.
          content:
            application/json:
              example:
                dispute_id: 6a1c4e92-3b7d-4f51-9e82-1c7f3a5d8b64
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                record_source: res_chile
                record_ref: BOL-2026-04512
                reason: The reported default was paid in full on 2026-07-30.
                status: open
                created_by: 5f9d2c10-3e8b-4a1d-9c7a-1d2f3a4b5c6d
                created_at: '2026-08-08T16:11:40Z'
        '400':
          description: invalid_payload | invalid_doc_id
          content:
            application/json:
              example:
                error: invalid_payload
                message: record_source and reason are required
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found (el sujeto no existe)
  /v1/qscore/disputes/{id}:
    get:
      tags: [Qscore]
      summary: Obtener una disputa
      operationId: getQscoreDispute
      description: 'Devuelve el detalle y el estado actual de una disputa ARCO de tu

        cuenta. Una disputa de otra cuenta devuelve `404 not_found` (aislamiento por

        cuenta).'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        example: 6a1c4e92-3b7d-4f51-9e82-1c7f3a5d8b64
      responses:
        '200':
          description: Detalle de la disputa.
          content:
            application/json:
              example:
                dispute_id: 6a1c4e92-3b7d-4f51-9e82-1c7f3a5d8b64
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                record_source: res_chile
                record_ref: BOL-2026-04512
                reason: The reported default was paid in full on 2026-07-30.
                status: under_review
                resolution_note: Under review with the data source.
                created_by: 5f9d2c10-3e8b-4a1d-9c7a-1d2f3a4b5c6d
                created_at: '2026-08-08T16:11:40Z'
        '401':
          description: unauthorized
        '403':
          description: forbidden
        '404':
          description: not_found
          content:
            application/json:
              example:
                error: not_found
                message: not found
  /v1/qscore/subjects/{docID}/monitoring:
    put:
      tags: [Qscore]
      summary: Suscribirse a un sujeto (monitoreo)
      operationId: putQscoreMonitoring
      description: 'Crea o actualiza la suscripción de monitoreo de tu cuenta a
        un sujeto (persona o empresa). El monitoreo es gratuito, pero exige que
        tu organización ya haya comprado un informe `ready` de ese sujeto — la
        línea base de la suscripción se siembra desde esa data comprada. Por
        diseño (anti-oráculo), un sujeto inexistente y un sujeto existente sin
        informe comprado reciben LA MISMA respuesta `403 report_required`: este
        endpoint jamás revela si un documento existe en el buró.

        La suscripción es idempotente por (cuenta, sujeto): re-crearla devuelve
        la suscripción existente con su línea base preservada. `only_material`
        (default false) limita el trigger `new_records` a registros materialmente
        negativos (cobranzas abiertas, instrumentos protestados, quiebras,
        juicios). `monitor_since_score` (1..999) arma el trigger
        `score_drop_below`: una alerta se dispara cuando el score cruza ese umbral
        a la baja.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (default CL). También puede enviarse
          en el body del request como `country`.
        example: CL
      requestBody:
        required: false
        content:
          application/json:
            example:
              only_material: true
              monitor_since_score: 500
      responses:
        '200':
          description: Suscripción de monitoreo (creada o actualizada).
          content:
            application/json:
              example:
                monitoring_id: 7c9e2f14-5b6a-4c8d-9e1f-3a7b5c2d8e91
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                active: true
                only_material: true
                created_at: '2026-08-08T16:11:40Z'
                doc_id: 12.345.678-5
                country: CL
                subject_type: person
                monitor_since_score: 500
        '400':
          description: invalid_doc_id | invalid_payload
          content:
            application/json:
              example:
                error: invalid_payload
                message: monitor_since_score must be between 1 and 999
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar) | report_required
          content:
            application/json:
              example:
                error: report_required
                message: purchase a report for this subject before enabling monitoring
    get:
      tags: [Qscore]
      summary: Obtener una suscripción de monitoreo
      operationId: getQscoreMonitoring
      description: 'Devuelve la suscripción de monitoreo vigente de tu cuenta
        para un sujeto, incluidos el último score observado, el conteo de
        registros y los timestamps del último chequeo (`last_checked_at`) y de
        la última alerta (`last_event_at`). Devuelve `404 not_found` si no hay
        suscripción.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (default CL).
        example: CL
      responses:
        '200':
          description: Suscripción de monitoreo.
          content:
            application/json:
              example:
                monitoring_id: 7c9e2f14-5b6a-4c8d-9e1f-3a7b5c2d8e91
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                active: true
                only_material: true
                created_at: '2026-08-08T16:11:40Z'
                last_event_at: '2026-08-08T18:02:11Z'
                last_checked_at: '2026-08-08T18:05:00Z'
                doc_id: 12.345.678-5
                country: CL
                subject_type: person
                monitor_since_score: 500
                last_score: 487
                last_record_count: 2
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found
          content:
            application/json:
              example:
                error: not_found
                message: resource not found
    delete:
      tags: [Qscore]
      summary: Desuscribir un sujeto
      operationId: deleteQscoreMonitoring
      description: 'Desactiva la suscripción de monitoreo de tu cuenta para un
        sujeto (`active: false`). No se envían más webhooks
        `risk_monitoring_alert`; puedes suscribirte de nuevo con `PUT`. Devuelve
        `404 not_found` si no hay suscripción.'
      parameters:
      - name: docID
        in: path
        required: true
        schema:
          type: string
        example: 12.345.678-5
      - name: country
        in: query
        required: false
        schema:
          type: string
          default: CL
        description: País ISO del documento (default CL).
        example: CL
      responses:
        '200':
          description: Suscripción desactivada.
          content:
            application/json:
              example:
                subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                doc_id: 12.345.678-5
                active: false
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found
          content:
            application/json:
              example:
                error: not_found
                message: resource not found
  /v1/qscore/monitoring:
    get:
      tags: [Qscore]
      summary: Listar suscripciones de monitoreo
      operationId: listQscoreMonitoring
      description: 'Lista las suscripciones de monitoreo de tu cuenta (las más
        recientes primero), paginadas. `page` parte en 1; `page_size` por
        defecto 50 (máximo 200).'
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
        example: 1
      - name: page_size
        in: query
        required: false
        schema:
          type: integer
          default: 50
        example: 50
      - name: active
        in: query
        required: false
        schema:
          type: string
          enum: ['true']
        description: Envía `active=true` para listar solo las suscripciones activas.
        example: 'true'
      responses:
        '200':
          description: Página de suscripciones de monitoreo.
          content:
            application/json:
              example:
                items:
                - monitoring_id: 7c9e2f14-5b6a-4c8d-9e1f-3a7b5c2d8e91
                  subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
                  active: true
                  only_material: true
                  created_at: '2026-08-08T16:11:40Z'
                  doc_id: 12.345.678-5
                  country: CL
                  subject_type: person
                  monitor_since_score: 500
                  last_score: 487
                  last_record_count: 2
                page: 1
                page_size: 50
                total: 1
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
  /v1/qscore/batches:
    get:
      tags: [Qscore]
      summary: Listar lotes de scoring
      operationId: listQscoreBatches
      description: 'Lista los lotes de scoring de tu cuenta (los más recientes
        primero), paginados. `page` parte en 1; `page_size` por defecto 50
        (máximo 200). Los filtros de fecha `from` y `to` (zona horaria de la organización, ambos inclusivos)
        son obligatorios. Los lotes de otras cuentas nunca son visibles
        (aislamiento por cuenta: un batch id de otra cuenta responde 404).'
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha de inicio (YYYY-MM-DD, zona horaria de la organización, inclusiva).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha de término (YYYY-MM-DD, zona horaria de la organización, inclusiva).
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - processing
          - completed
          - completed_with_errors
          - failed
        description: Filtra por estado del lote.
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Página de lotes.
          content:
            application/json:
              example:
                items:
                - batch_id: b7f2c1a4-3e5d-4f8a-9c2b-1d0e6a8f4c5d
                  account_id: 4a9c2e71-8b3d-4f2a-9c1e-5d7b2a8f3e41
                  status: completed_with_errors
                  purpose: credit_evaluation
                  country: CL
                  lang: es
                  total_items: 4
                  processed_items: 4
                  succeeded_items: 3
                  failed_items: 1
                  estimated_fee_usdt: '14.50'
                  idempotency_key: portfolio-2026-08-09-a
                  created_at: '2026-08-09T14:32:10Z'
                  started_at: '2026-08-09T14:34:02Z'
                  completed_at: '2026-08-09T14:41:37Z'
                meta:
                  page: 1
                  page_size: 50
                  total: 1
        '400':
          description: invalid_range
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
    post:
      tags: [Qscore]
      summary: Crear un lote de scoring
      operationId: createQscoreBatch
      description: 'Crea un lote de scoring asíncrono (scoring de cartera): los
        sujetos se validan por adelantado, el lote queda en cola y un worker en
        segundo plano emite un informe Qscore completo por cada ítem válido.
        Envía `subjects` (array JSON, de 1 a 5000 entradas) **o** `subjects_csv`
        (un string CSV con encabezado `doc_id[,subject_type]`) — exactamente uno
        de los dos. Las filas con un documento inválido (`invalid_doc_id`) o un
        documento duplicado dentro del lote (`duplicate_in_batch`, reportado con
        su forma normalizada) se rechazan al crear y
        se reportan en `rejected_items`; el lote solo procesa las válidas. Un
        `subject_type` omitido o no reconocido nunca es un error: para `CL` se
        infiere de la serie del RUT (primer dígito 5–9 → `company`, cualquier
        otro → `person`). Si
        todas las filas se rechazan, la solicitud falla con `no_valid_items` y
        no se crea nada. La idempotencia es obligatoria: reintentar con la misma
        `idempotency_key` devuelve el lote original con `idempotency_hit: true`
        y nunca duplica el lote ni los cobros. Cada ítem cobra el fee standalone
        del informe (`risk_report_person` / `risk_report_company`) al procesarse,
        y un ítem con falla terminal se reembolsa automáticamente.
        `estimated_fee_usdt` es la estimación inicial para los ítems válidos.
        Cuando el lote termina recibes un webhook `risk_batch_completed` y un
        email — los informes individuales no emiten su propio webhook ni email.'
      requestBody:
        required: true
        content:
          application/json:
            examples:
              json_subjects:
                summary: Sujetos JSON (dos filas rechazadas por adelantado)
                value:
                  country: CL
                  purpose: credit_evaluation
                  lang: es
                  subjects:
                  - doc_id: 12.345.678-5
                  - doc_id: 15.678.234-3
                  - doc_id: 11.222.333-9
                  - doc_id: 76.543.210-3
                    subject_type: company
                  - doc_id: 12.345.678-9
                  - doc_id: 12345678-5
                  idempotency_key: portfolio-2026-08-09-a
              csv_subjects:
                summary: Payload CSV
                value:
                  country: CL
                  purpose: credit_evaluation
                  subjects_csv: "doc_id,subject_type\n12.345.678-5,person\n76.543.210-3,company\n96.543.210-8,company"
                  idempotency_key: portfolio-2026-08-09-b
      responses:
        '202':
          description: 'Lote aceptado para procesamiento asíncrono.
            `rejected_items` lista las filas rechazadas por adelantado (solo
            presente cuando al menos una fila fue rechazada).'
          content:
            application/json:
              example:
                batch_id: b7f2c1a4-3e5d-4f8a-9c2b-1d0e6a8f4c5d
                status: pending
                purpose: credit_evaluation
                country: CL
                lang: es
                total_items: 4
                processed_items: 0
                succeeded_items: 0
                failed_items: 0
                estimated_fee_usdt: '14.50'
                created_at: '2026-08-09T14:32:10Z'
                rejected_count: 2
                rejected_items:
                - line: 5
                  doc_id: 12.345.678-9
                  error_code: invalid_doc_id
                  error: doc_id is not valid for CL
                - line: 6
                  doc_id: 12345678-5
                  error_code: duplicate_in_batch
                  error: doc_id appears more than once in the batch
        '200':
          description: 'Replay idempotente — el lote original, con
            `idempotency_hit: true`.'
          content:
            application/json:
              example:
                batch_id: b7f2c1a4-3e5d-4f8a-9c2b-1d0e6a8f4c5d
                status: processing
                purpose: credit_evaluation
                country: CL
                lang: es
                total_items: 4
                processed_items: 2
                succeeded_items: 2
                failed_items: 0
                estimated_fee_usdt: '14.50'
                created_at: '2026-08-09T14:32:10Z'
                idempotency_hit: true
        '400':
          description: 'invalid_payload | purpose_required | invalid_purpose |
            idempotency_key_required | no_valid_items | too_many_items'
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
  /v1/qscore/batches/{id}:
    get:
      tags: [Qscore]
      summary: Obtener un lote de scoring
      operationId: getQscoreBatch
      description: 'Devuelve el detalle del lote: contadores en vivo,
        `started_at` / `completed_at`, y `error_code` / `error_message` cuando el
        lote mismo falló. Aislamiento por cuenta: un batch id de otra cuenta
        responde 404.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Detalle del lote.
          content:
            application/json:
              example:
                batch_id: b7f2c1a4-3e5d-4f8a-9c2b-1d0e6a8f4c5d
                status: completed_with_errors
                purpose: credit_evaluation
                country: CL
                lang: es
                total_items: 4
                processed_items: 4
                succeeded_items: 3
                failed_items: 1
                estimated_fee_usdt: '14.50'
                created_at: '2026-08-09T14:32:10Z'
                started_at: '2026-08-09T14:34:02Z'
                completed_at: '2026-08-09T14:41:37Z'
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found
  /v1/qscore/batches/{id}/items:
    get:
      tags: [Qscore]
      summary: Listar ítems del lote
      operationId: listQscoreBatchItems
      description: 'Devuelve los ítems del lote, paginados. `status` filtra por
        estado del ítem (`pending`, `ready`, `failed`). Cada ítem `ready` expone
        su `report_id`, `score` y `band`; cada ítem `failed` expone `error_code`
        / `error_message`.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - ready
          - failed
        description: Filtra por estado del ítem.
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Página de ítems del lote.
          content:
            application/json:
              example:
                items:
                - item_id: e1f0a9b8-7c6d-4e5f-8a9b-0c1d2e3f4a5b
                  report_id: 3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01
                  doc_id: 12.345.678-5
                  subject_type: person
                  status: ready
                  score: 715
                  band: B
                  created_at: '2026-08-09T14:32:10Z'
                  completed_at: '2026-08-09T14:34:52Z'
                - item_id: f2a1b0c9-8d7e-4f6a-9b0c-1d2e3f4a5b6c
                  report_id: 7c9a1f3d-2e44-4b8a-9d51-0a1b2c3d4e5f
                  doc_id: 15.678.234-3
                  subject_type: person
                  status: ready
                  score: 430
                  band: D
                  created_at: '2026-08-09T14:32:10Z'
                  completed_at: '2026-08-09T14:35:41Z'
                - item_id: a3b2c1d0-9e8f-4a7b-8c1d-2e3f4a5b6c7d
                  report_id: 9d0e1f2a-3b4c-4d5e-8f6a-7b8c9d0e1f2a
                  doc_id: 76.543.210-3
                  subject_type: company
                  status: ready
                  score: 604
                  band: C
                  created_at: '2026-08-09T14:32:10Z'
                  completed_at: '2026-08-09T14:36:22Z'
                - item_id: b4c3d2e1-0f9a-4b8c-9d2e-3f4a5b6c7d8e
                  doc_id: 11.222.333-9
                  subject_type: person
                  status: failed
                  score: null
                  error_code: generation_failed
                  error_message: the report could not be generated; the fee was refunded
                  created_at: '2026-08-09T14:32:10Z'
                  completed_at: '2026-08-09T14:37:05Z'
                meta:
                  page: 1
                  page_size: 50
                  total: 4
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found
  /v1/qscore/batches/{id}/results.csv:
    get:
      tags: [Qscore]
      summary: Exportar los resultados del lote como CSV
      operationId: exportQscoreBatchResults
      description: 'Descarga los resultados del lote como un archivo CSV con BOM
        UTF-8 (abre correctamente en Excel). Los encabezados visibles siguen el
        locale de la cuenta; las claves crudas de las celdas son
        `doc_id,subject_type,status,score,band,verify_code,report_id,error_code`.
        `verify_code` es el
        código de verificación pública del informe (utilizable en
        `/verify/qscore/{code}`). Las filas fallidas llevan su `error_code` y
        ningún score.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Archivo CSV (text/csv, UTF-8 con BOM).
          content:
            text/csv:
              example: "Documento,Tipo de sujeto,Estado,Score,Banda,Codigo de verificacion,ID de informe,Codigo de error\n12.345.678-5,person,ready,715,B,Q3f5c9f2d7d214b8c9a2d2d5f6a1b8c01a1b2c3d4e5f60718293a,3f5c9f2d-7d21-4b8c-9a2d-2d5f6a1b8c01,\n15.678.234-3,person,ready,430,D,Q7c9a1f3d2e444b8a9d510a1b2c3d4e5fb2c3d4e5f60718293a4b5,7c9a1f3d-2e44-4b8a-9d51-0a1b2c3d4e5f,\n76.543.210-3,company,ready,604,C,Q9d0e1f2a3b4c4d5e8f6a7b8c9d0e1f2ac3d4e5f60718293a4b5c6,9d0e1f2a-3b4c-4d5e-8f6a-7b8c9d0e1f2a,\n11.222.333-9,person,failed,,,,,generation_failed\n"
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio deshabilitado o cuenta sin verificar)
        '404':
          description: not_found
  /v1/qscore/consents:
    get:
      tags: [Qscore]
      summary: Listar links de consentimiento
      operationId: listRiskConsents
      description: 'Lista los links de consentimiento de tu cuenta (mas recientes
        primero), paginado. Los filtros de fecha `from` y `to` (zona horaria de la organización, ambos
        inclusivos) son obligatorios; `page` parte en 1 y `page_size` por defecto
        es 50 (maximo 200). Los consents de otras cuentas jamas son visibles
        (aislamiento por cuenta).'
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha inicial (YYYY-MM-DD, zona horaria de la organización, inclusiva).
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
        description: Fecha final (YYYY-MM-DD, zona horaria de la organización, inclusiva).
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - granted
          - revoked
          - expired
        description: Filtrar por estado del consent.
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: page_size
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Lista paginada de consents.
          content:
            application/json:
              example:
                consents:
                - consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                  subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
                  channel: link
                  status: granted
                  purpose: credit_evaluation
                  consent_url: https://business.cbpayapp.com/consent/cns_4f9a2b7c1d8e4a0f9b3c6d5e8a7f1c2d
                  email: maria@example.com
                  holder_name: Maria Jose Contreras Soto
                  created_at: '2026-08-09T15:00:00Z'
                  updated_at: '2026-08-10T09:21:44Z'
                  expires_at: '2026-08-16T15:00:00Z'
                  granted_at: '2026-08-10T09:21:44Z'
                total: 1
                page: 1
                page_size: 50
        '400':
          description: invalid_range
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio risk deshabilitado o cuenta no verificada)
    post:
      tags: [Qscore]
      summary: Crear un link de consentimiento
      operationId: createRiskConsent
      description: 'Crea un link de autorizacion para el sujeto (flujo de
        consentimiento estilo Destacame): recibes una `consent_url` compartible y,
        si pasas `email`, la plataforma envia el link brandeado al titular por
        correo. El titular abre la pagina publica, conecta su banco via open
        finance y autoriza; un consent otorgado desbloquea la data bancaria
        positiva del sujeto en sus informes Qscore. `country` y `doc_id`
        identifican al titular (el documento se valida con su digito verificador);
        `subject_type` se infiere del documento si se omite. `purpose` es
        obligatorio por la ley de proteccion de datos y `self_access` no esta
        permitido aqui (el informe self del titular vive en `POST
        /v1/qscore/my-report`). `expires_in_days` por defecto es 7 (maximo 30). La
        idempotencia es obligatoria: reintentar con la misma `idempotency_key`
        devuelve el consent original con `idempotency_hit: true` y jamas duplica
        el link ni re-envia el correo.'
      requestBody:
        required: true
        content:
          application/json:
            example:
              country: CL
              doc_id: 12.345.678-5
              purpose: credit_evaluation
              email: maria@example.com
              expires_in_days: 7
              idempotency_key: consent-2026-08-09-001
      responses:
        '201':
          description: Consent creado (estado `pending`).
          content:
            application/json:
              example:
                consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
                channel: link
                status: pending
                purpose: credit_evaluation
                consent_url: https://business.cbpayapp.com/consent/cns_4f9a2b7c1d8e4a0f9b3c6d5e8a7f1c2d
                email: maria@example.com
                created_at: '2026-08-09T15:00:00Z'
                updated_at: '2026-08-09T15:00:00Z'
                expires_at: '2026-08-16T15:00:00Z'
        '200':
          description: 'Replay idempotente — el consent original, con `idempotency_hit: true`.'
          content:
            application/json:
              example:
                consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
                channel: link
                status: pending
                purpose: credit_evaluation
                consent_url: https://business.cbpayapp.com/consent/cns_4f9a2b7c1d8e4a0f9b3c6d5e8a7f1c2d
                email: maria@example.com
                created_at: '2026-08-09T15:00:00Z'
                updated_at: '2026-08-09T15:00:00Z'
                expires_at: '2026-08-16T15:00:00Z'
                idempotency_hit: true
        '400':
          description: 'invalid_payload | purpose_required | invalid_purpose |
            invalid_doc_id | invalid_subject_type | invalid_email | idempotency_key_required'
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio risk deshabilitado) o verification_required
  /v1/qscore/consents/{id}:
    get:
      tags: [Qscore]
      summary: Obtener un link de consentimiento
      operationId: getRiskConsent
      description: 'Devuelve el detalle del consent. La propiedad es estricta: un
        id de otra cuenta devuelve 404. Un consent `pending` cuyo TTL ya vencio se
        marca `expired` de forma lazy al leerlo.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Detalle del consent.
          content:
            application/json:
              example:
                consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
                channel: link
                status: granted
                purpose: credit_evaluation
                consent_url: https://business.cbpayapp.com/consent/cns_4f9a2b7c1d8e4a0f9b3c6d5e8a7f1c2d
                email: maria@example.com
                holder_name: Maria Jose Contreras Soto
                created_at: '2026-08-09T15:00:00Z'
                updated_at: '2026-08-10T09:21:44Z'
                expires_at: '2026-08-16T15:00:00Z'
                granted_at: '2026-08-10T09:21:44Z'
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio risk deshabilitado o cuenta no verificada)
        '404':
          description: not_found
  /v1/qscore/consents/{id}/revoke:
    post:
      tags: [Qscore]
      summary: Revocar un link de consentimiento
      operationId: revokeRiskConsent
      description: 'Revoca el consent (estado `revoked`) y emite el webhook
        `risk_consent_revoked`. Un consent ya decidido por el titular (`granted`,
        o ya `revoked`/`expired`) no puede transicionar de nuevo y devuelve `409
        already_decided`.'
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Consent revocado.
          content:
            application/json:
              example:
                consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
                subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
                channel: link
                status: revoked
                purpose: credit_evaluation
                consent_url: https://business.cbpayapp.com/consent/cns_4f9a2b7c1d8e4a0f9b3c6d5e8a7f1c2d
                email: maria@example.com
                created_at: '2026-08-09T15:00:00Z'
                updated_at: '2026-08-10T10:02:11Z'
                expires_at: '2026-08-16T15:00:00Z'
                revoked_at: '2026-08-10T10:02:11Z'
        '401':
          description: unauthorized
        '403':
          description: forbidden (servicio risk deshabilitado o cuenta no verificada)
        '404':
          description: not_found
        '409':
          description: already_decided
  /consent/{token}:
    get:
      security: []
      tags:
      - Consentimiento
      operationId: getPublicConsent
      summary: Leer una solicitud de consentimiento (publico, titular)
      description: 'Vista publica de la solicitud de consentimiento detras de una
        `consent_url`. El token de la URL ES la capacidad (128 bits aleatorios,
        unico por consent): no se requiere login, y un token desconocido responde
        un 404 generico (anti-enumeracion). La respuesta es minima a proposito:
        `doc_id` va enmascarado a sus ultimos 4 caracteres, y el email, la
        identidad del titular y los ids internos jamas se exponen. Con rate limit
        por IP.'
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: Token opaco del consent embebido en la `consent_url`.
      responses:
        '200':
          description: Resumen de la solicitud para el titular.
          content:
            application/json:
              example:
                status: pending
                purpose: credit_evaluation
                country: CL
                subject_type: person
                doc_id: '********678-5'
                org:
                  name: ACME SpA
                  website: https://acme.example.com
                  logo_url: https://cdn.example.com/acme/logo.png
                expires_at: '2026-08-16T15:00:00Z'
        '404':
          description: Token desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: consent not found
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
  /consent/{token}/begin:
    post:
      security: []
      tags:
      - Consentimiento
      operationId: beginPublicConsent
      summary: Iniciar la conexion bancaria (publico, titular)
      description: 'Abre la sesion de conexion bancaria del titular: devuelve el
        `widget_token` y la `public_key` que el widget de open finance necesita
        para renderizar el selector de instituciones. Solo un consent `pending`
        puede iniciar; cualquier otro estado devuelve `409 already_decided`. Con
        rate limit por IP.'
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Sesion de conexion bancaria lista.
          content:
            application/json:
              example:
                widget_token: wtoken_2c7f9a1b4e5d40aa
                public_key: pk_prod_9a8f7e6d5c
                expires_at: '2026-08-09T15:30:00Z'
        '404':
          description: Token desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: consent not found
        '409':
          description: El consent ya fue decidido.
          content:
            application/json:
              example:
                error: already_decided
                message: this consent link is no longer pending
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
  /consent/{token}/complete:
    post:
      security: []
      tags:
      - Consentimiento
      operationId: completePublicConsent
      summary: Otorgar el consentimiento tras conectar el banco (publico, titular)
      description: 'Completa el flujo despues de que el titular conecto su banco en
        el widget: la plataforma intercambia el `exchange_token`, exige que el
        vinculo bancario este `active` (`409 link_inactive`) y que el documento
        del titular verificado por el banco calce exactamente con el documento del
        sujeto (`409 holder_mismatch` — una cuenta de OTRO documento jamas otorga
        el consentimiento). Al completar, el consent queda `granted`, se sella la
        evidencia legal (IP y user agent), se gatilla la derivacion de data
        positiva y se emite el webhook `risk_consent_granted`.'
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            example:
              exchange_token: etok_7f3a9c1e2b4d46aa
      responses:
        '200':
          description: Consent otorgado.
          content:
            application/json:
              example:
                status: granted
                granted_at: '2026-08-10T09:21:44Z'
                holder_name: Maria Jose Contreras Soto
        '404':
          description: Token desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: consent not found
        '409':
          description: 'already_decided | link_inactive | holder_mismatch'
          content:
            application/json:
              example:
                error: holder_mismatch
                message: the connected account belongs to a different document
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
        '502':
          description: El agregador bancario no pudo completar el intercambio.
          content:
            application/json:
              example:
                error: provider_error
                message: the banking connection could not be completed
  /consent/{token}/decline:
    post:
      security: []
      tags:
      - Consentimiento
      operationId: declinePublicConsent
      summary: Rechazar la solicitud de consentimiento (publico, titular)
      description: 'El titular rechaza la solicitud: el consent queda `revoked`, se
        sella la evidencia legal y se emite el webhook `risk_consent_revoked`. Un
        consent ya decidido devuelve `409 already_decided`.'
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Consent rechazado.
          content:
            application/json:
              example:
                status: revoked
        '404':
          description: Token desconocido.
          content:
            application/json:
              example:
                error: not_found
                message: consent not found
        '409':
          description: El consent ya fue decidido.
          content:
            application/json:
              example:
                error: already_decided
                message: this consent link is no longer pending
        '429':
          description: Demasiadas solicitudes desde esta IP.
          content:
            application/json:
              example:
                error: rate_limited
                message: too many requests
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 'JWT de sesión (de register/login) o llave de API (`pk_...`).

        `X-API-Key: <token>` se acepta como header alternativo.

        '
  parameters:
    receiptLang:
      name: lang
      in: query
      schema:
        type: string
        enum:
        - en
        - es
        - zh
        default: en
      description: Idioma del comprobante PDF. Por defecto ingles.
    cardID:
      name: cardID
      in: path
      required: true
      schema:
        type: string
    page:
      name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
    pageSize:
      name: page_size
      in: query
      schema:
        type: integer
        default: 50
        maximum: 200
    idempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      required: false
      description: Alternativa al campo `idempotency_key` del body.
      schema:
        type: string
  responses:
    ReceiptPDF:
      description: Comprobante PDF brandeado con el QR de verificacion firmado.
      content:
        application/pdf:
          schema:
            type: string
            format: binary
          example: '%PDF-1.7 (binary receipt content)'
    BadRequest:
      description: Solicitud inválida (ver el código `error` para la validación específica).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: invalid_json
            message: 'request body is not valid JSON: unknown field'
    InsufficientFunds:
      description: El saldo de la cuenta no alcanza para esta operación (`insufficient_funds`).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: insufficient_funds
            message: account balance is not enough for this operation
    InvalidRange:
      description: Filtro de fechas `from`/`to` inválido (`invalid_range`).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: invalid_range
            message: invalid from date (YYYY-MM-DD)
    AccountRequired:
      description: Este endpoint requiere una credencial de cuenta (`account_required`).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: account_required
            message: this endpoint requires an account credential
    Forbidden:
      description: Nivel de credencial no permitido para este endpoint (`forbidden`).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: forbidden
            message: credentials required
    NotFound:
      description: Recurso no encontrado (`not_found`).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error: not_found
            message: resource not found
    Unauthorized:
      description: Credencial faltante o inválida (`unauthorized`).
      content:
        application/json:
          schema: *id016
          example:
            error: unauthorized
            message: invalid or missing credentials
  schemas:
    PosMerchant:
      type: object
      properties:
        merchant_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        name:
          type: string
        verification_id:
          type: string
          format: uuid
        external_ref:
          type: string
        fee_percent:
          type: string
        fee_fixed:
          type: string
        status:
          type: string
          enum:
          - active
          - disabled
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PosCharge:
      type: object
      properties:
        charge_id:
          type: string
          format: uuid
        payin_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        status:
          type: string
          enum:
          - pending
          - paid
          - expired
        amount:
          type: string
        settlement_asset:
          type: string
        crypto:
          type: string
        chain:
          type: string
        asset:
          type: string
        address:
          type: string
        due:
          type: string
        received:
          type: string
        refunded_total:
          type: string
        qr_payload:
          type: string
        qr_png_base64:
          type: string
        confirming:
          type: boolean
        detected_amount:
          type: string
        merchant:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            external_ref:
              type: string
        reference:
          type: string
        quote_expires_at:
          type: string
          format: date-time
        paid_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        conversion_status:
          type: string
        refunds:
          type: array
          items:
            $ref: '#/components/schemas/PosChargeRefund'
        receipt_url:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PosChargeRefund:
      type: object
      properties:
        refund_id:
          type: string
          format: uuid
        charge_id:
          type: string
          format: uuid
        withdrawal_id:
          type: string
        amount:
          type: string
        asset:
          type: string
        to_address:
          type: string
        status:
          type: string
        tx_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CatalogItem:
      type: object
      description: Una entrada de catálogo — `value` es lo que envías a la API, `label` lo que muestras.
      properties:
        value:
          type: string
          example: CL
        label:
          type: string
          example: Chile
    OTPChallenge:
      type: object
      properties:
        challenge_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        action:
          type: string
          enum:
          - login
          - payout
          - crypto_withdrawal
          - transfer
          - banking_operation
          - card_reveal
          - api_key_create
          - member_add
          - phone_change
        channel:
          type: string
          enum:
          - sms
          - whatsapp
        phone:
          type: string
          description: Teléfono enmascarado al que viajó el código.
        status:
          type: string
          enum:
          - pending
          - verified
          - consumed
          - expired
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        verified_at:
          type: string
          format: date-time
        consumed_at:
          type: string
          format: date-time
    CardObject:
      type: object
      properties:
        card_id:
          type: string
        account_id:
          type: string
        physical:
          type: boolean
        cardholder_kind:
          type: string
          enum:
          - account
          - person
        cardholder_name:
          type: string
        status:
          type: string
          enum:
          - pending_activation
          - active
          - frozen
          - cancelled
        spending_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          description: Saldo desde el que gastan las compras de la tarjeta. USDT/USDC 1:1 con el USD;
            BTC/GOLD convertidos al precio del momento de cada evento. USDT por defecto. Cambiarlo solo
            afecta compras futuras.
        limits:
          type: object
          description: Límites de gasto, siempre medidos en USD, independientes del asset de gasto.
          properties:
            per_transaction:
              type: string
            daily:
              type: string
            monthly:
              type: string
        creation_fee:
          type: string
        cancellation_fee:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    VerificationLinkRequest:
      type: object
      required:
      - external_customer_id
      - idempotency_key
      properties:
        external_customer_id:
          type: string
          description: Tu referencia del cliente verificado (vuelve en cada webhook).
        idempotency_key:
          type: string
        country:
          type: string
          description: Solo KYB. Corredor del wizard (us, cl, ve, br, mx, co, pe, bo, py, ar, generic).
        generic_country:
          type: string
          description: ISO alpha-2 cuando country=generic.
        currency:
          type: string
        label:
          type: string
        expires_in_days:
          type: integer
        metadata:
          type: object
    VerificationLink:
      type: object
      properties:
        link_id:
          type: string
          format: uuid
        kind:
          type: string
          enum:
          - kyc
          - kyb
        external_customer_id:
          type: string
        url:
          type: string
          description: URL del wizard hosteado para compartir con la persona/empresa verificada.
        status:
          type: string
          enum:
          - pending
          - opened
          - completed
          - expired
        country:
          type: string
        label:
          type: string
        expires_at:
          type: integer
        submission_id:
          type: string
          description: Presente cuando el link se completa.
        verification_fee:
          type: string
        idempotency_key:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    VerificationSubmission:
      type: object
      properties:
        submission_id:
          type: string
          format: uuid
        kind:
          type: string
          enum:
          - kyc
          - kyb
        external_customer_id:
          type: string
        status:
          type: string
          enum:
          - pending_review
          - in_review
          - changes_requested
          - more_info_required
          - escalated
          - approved
          - approved_partial
          - rejected
        risk_band:
          type: string
        liveness_pending:
          type: boolean
          description: Solo KYC. True hasta superar la prueba de vida en video.
        pending_documents:
          type: array
          items:
            type: string
        rejection_reason:
          type: string
        aml_decision:
          type: string
          description: Solo KYB.
        verification_fee:
          type: string
        idempotency_key:
          type: string
        documents_gate:
          type: object
          description: Pre-flight del approve — si ok es false, el approve explícito responde 409. Sirve para saber si el approve pasaría antes de llamarlo.
          properties:
            ok:
              type: boolean
            matched:
              type: integer
            total:
              type: integer
            unresolved:
              type: array
              items:
                type: object
                properties:
                  category:
                    type: string
                  effective_outcome:
                    type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    VerificationDocumentPresignRequest:
      type: object
      required:
      - category
      - filename
      - content_type
      properties:
        category:
          type: string
          description: 'KYC: identity, proofOfResidence. KYB: legalPresence, ownershipStructure, controlStructure,
            companyDetails.'
        filename:
          type: string
        content_type:
          type: string
          enum:
          - application/pdf
          - image/png
          - image/jpeg
        file_size:
          type: integer
          description: Bytes; máximo 15 MB.
    VerificationDocumentConfirmRequest:
      type: object
      required:
      - key
      - category
      - filename
      - content_type
      properties:
        key:
          type: string
          description: La key devuelta por el paso presign.
        category:
          type: string
        filename:
          type: string
        content_type:
          type: string
    VerificationDocumentValidation:
      type: object
      properties:
        id:
          type: string
          description: ID de la validación (el valor que se pasa al endpoint de override de validaciones de documentos).
        category:
          type: string
        status:
          type: string
          enum:
          - pending
          - running
          - completed
          - failed
        outcome:
          type: string
          enum:
          - MATCH
          - REVIEW
          - NO_MATCH
        effective_outcome:
          type: string
          description: El outcome que manda ahora — el override manual si existe, si no el del motor OCR.
          enum:
          - MATCH
          - REVIEW
          - NO_MATCH
        manual_review:
          type: object
          description: Presente cuando un admin sobreescribió manualmente esta validación. La vista tenant solo expone outcome y reviewed_at.
          properties:
            outcome:
              type: string
              enum:
              - match
              - no_match
              - review
            reviewed_at:
              type: string
              format: date-time
        score:
          type: number
        summary:
          type: string
        filename:
          type: string
    Contact:
      type: object
      properties:
        contact_id:
          type: string
          format: uuid
        display_name:
          type: string
        alias:
          type: string
        phone:
          type: string
          description: E.164.
        email:
          type: string
        has_cbpay:
          type: boolean
          description: True cuando el teléfono corresponde a una cuenta activa de tu operador.
        cbpay_account_id:
          type: string
          format: uuid
          description: Cuenta enlazada (presente cuando has_cbpay es true).
        source:
          type: string
          enum:
          - manual
          - import
          - transfer
          - payout
          - crypto
        favorite:
          type: boolean
        destinations:
          type: array
          description: Presente en el endpoint de detalle.
          items:
            $ref: '#/components/schemas/ContactDestination'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ContactDestination:
      type: object
      properties:
        destination_id:
          type: string
          format: uuid
        type:
          type: string
          enum:
          - cbpay
          - payout
          - crypto
        country:
          type: string
        currency:
          type: string
        method:
          type: string
        chain:
          type: string
        address:
          type: string
        details:
          type: object
          description: Beneficiario de payout completo en destinos payout.
        label:
          type: string
        last_used_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    Swap:
      type: object
      properties:
        swap_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
          description: Cuenta dueña. Permite a los org admins atribuir cada registro en la vista org-wide.
        from_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
        to_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
        from_amount:
          type: string
        to_amount:
          type: string
        rate:
          type: string
          description: Unidades de destino por unidad de origen (tasa de ejecución marcada).
        status:
          type: string
          example: completed
        idempotency_key:
          type: string
        created_at:
          type: string
          format: date-time
        idempotency_hit:
          type: boolean
          description: Presente en replays.
    Error:
      type: object
      properties:
        error:
          type: string
          description: Código de error legible por máquina (snake_case).
          example: insufficient_funds
        message:
          type: string
          description: Explicación legible por humanos.
    Account:
      type: object
      properties:
        id:
          type: string
          format: uuid
        org_id:
          type: string
          format: uuid
        type:
          type: string
          enum:
          - person
          - company
        status:
          type: string
          enum:
          - active
          - blocked
          - closed
        kyc_status:
          type: string
          enum:
          - none
          - pending
          - approved
          - rejected
        email:
          type: string
        display_name:
          type: string
        tax_id:
          type: string
        phone:
          type: string
        country:
          type: string
        locale:
          type: string
          enum:
          - en
          - es
          - zh
          description: Locale humano preferido. Las respuestas JSON y los webhooks siguen en ingles.
          example: en
        profile:
          type: object
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Member:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
        role:
          type: string
          enum:
          - owner
          - operator
          - viewer
        status:
          type: string
          enum:
          - active
          - disabled
        created_at:
          type: string
          format: date-time
    ApiKeyIssued:
      type: object
      properties:
        api_key_id:
          type: string
          format: uuid
        key_id:
          type: string
        token:
          type: string
          description: Llave en texto plano (`pk_...`). Se muestra exactamente una vez.
        label:
          type: string
        note:
          type: string
    BalancesResponse:
      type: object
      properties:
        account_id:
          type: string
          format: uuid
        balances:
          type: array
          items:
            type: object
            properties:
              asset:
                type: string
                example: USDT
              available:
                type: string
                example: '100.000000'
              held:
                type: string
                description: Reservado por payouts/retiros en curso.
                example: '5.000000'
              updated_at:
                type: string
                format: date-time
    MovementsResponse:
      type: object
      properties:
        account_id:
          type: string
          format: uuid
        page:
          type: integer
        page_size:
          type: integer
        movements:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              asset:
                type: string
              amount:
                type: string
                description: Con signo; negativos los débitos, positivos los créditos.
              type:
                type: string
                description: Tipo de asiento del ledger.
              reference_type:
                type: string
              reference_id:
                type: string
              description:
                type: string
              balance_after:
                type: string
              created_at:
                type: string
                format: date-time
    FeeConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        org_id:
          type: string
          format: uuid
        account_id:
          type: string
          description: Vacío cuando la comisión es un valor por defecto de CBPay (no un override de cuenta).
        service:
          type: string
          enum:
          - payout
          - payin
          - payin_card
          - funding
          - withdrawal
          - compliance_person
          - compliance_company
          - compliance_rescreen
          - compliance_transaction
          - compliance_monitoring
          - banking_deposit
          - banking_transfer_ach
          - banking_transfer_swift
          - banking_transfer_wire
          - banking_transfer_sepa
        country:
          type: string
        currency:
          type: string
          description: >-
            Dimensión opcional de moneda ISO-4217. Solo la soporta payin_card — la misma cuenta puede tener un % distinto por moneda procesada (ej. BOB vs USD). Vacía aplica a cualquier moneda.
        asset:
          type: string
          example: USDT
        percent:
          type: string
          example: '1.5'
        fixed_amount:
          type: string
          example: '0.30'
        settlement_hours:
          type: integer
          description: >-
            Solo payins con tarjeta (`payin_card`) — demora de acreditación del SALDO
            configurable en horas. `0` (default) liquida el saldo apenas se confirma el cobro
            con tarjeta; un valor positivo confirma el payin como `credited` de inmediato
            (webhook y cierre del checkout incluidos) pero deja el SALDO programado con un
            `settle_at` hasta que vence la demora, y luego lo liquida automáticamente. La
            demora cubre todo cobro pagado con tarjeta: payins directos con tarjeta y
            cobros checkout/POS pagados con tarjeta (un checkout pagado por
            QR/crypto/fintoc sigue acreditándose de inmediato). En otro
            servicio o negativo se rechaza con `invalid_settlement_hours`.
          example: 24
        status:
          type: string
          example: active
        updated_at:
          type: string
          format: date-time
    SettlementSettings:
      type: object
      properties:
        default_settlement_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          description: Saldo desde el que se debitan las operaciones cuando no se envía un override por
            request.
        default_payin_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          description: Saldo en que se acreditan por defecto los payins. El crédito entra en USDT y el
            neto se auto-convierte vía el motor de swaps al precio real, sin spread de swap
            adicional. Checkout/POS conservan el asset de su link.
        enabled_assets:
          type: array
          items:
            type: string
          description: Assets que tu organización permite actualmente como origen del settlement.
        volatile_op_limit_usdt:
          type: string
          description: Límite por operación (equivalente en USDT) para los assets volátiles (BTC/GOLD).
        volatile_daily_limit_usdt:
          type: string
          description: Tope de volumen 24h (equivalente en USDT) por cuenta para los assets volátiles
            (BTC/GOLD).
      example:
        default_settlement_asset: USDT
        default_payin_asset: USDT
        enabled_assets:
        - USDT
        - USDC
        - BTC
        - GOLD
        volatile_op_limit_usdt: '10000.000000'
        volatile_daily_limit_usdt: '50000.000000'
    Payout:
      type: object
      properties:
        payout_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        idempotency_key:
          type: string
        country:
          type: string
        currency:
          type: string
        method:
          type: string
        local_amount:
          type: string
        fx_rate:
          type: string
          description: La tasa de tu cuenta al momento de la ejecución (unidades locales por 1 USDT).
        usdt_amount:
          type: string
        fee:
          type: string
        total_debit:
          type: string
        settlement_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          description: Saldo virtual desde el que realmente se debitó la operación.
        settlement_amount:
          type: string
          description: Monto exacto debitado del saldo del asset de settlement (en unidades de ese asset).
            Es igual a `total_debit` cuando el asset es USDT. Si el payout falla se reembolsa este monto
            exacto — nunca se re-cotiza.
        settlement_rate:
          type: string
          description: Precio USD efectivo por unidad del asset de settlement usado para convertir el
            total en USDT ("1" para USDT/USDC).
        beneficiary:
          type: object
        description:
          type: string
        status:
          type: string
          enum:
          - pending
          - processing
          - completed
          - failed
        status_code:
          type: string
        status_message:
          type: string
        bank_reference:
          type: string
          description: Referencia de la transaccion en el banco/rail (ej. el id de transferencia del banco, con el que el beneficiario cruza el pago con su banco). En US ACH/wire/SWIFT es la referencia CBF que vuelve al crear; en el resto de corredores queda vacia hasta que el rail la reporta.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        idempotency_hit:
          type: boolean
          description: Presente (true) cuando la respuesta es una repetición idempotente.
    Payin:
      type: object
      properties:
        payin_id:
          type: string
          format: uuid
        account_id:
          type: string
        kind:
          type: string
          enum:
          - qr
          - collect
          - push
          - card
          - checkout
          - pos
        country:
          type: string
        currency:
          type: string
        method:
          type: string
        local_amount:
          type: string
          description: >-
            Monto en la moneda local, como texto decimal plano (por ejemplo
            "5000000"). Nunca en notación científica.
        settlement_asset:
          type: string
          enum:
          - USDT
          - USDC
          - BTC
          - GOLD
          description: El saldo virtual en que se liquida el payin. Los payins de checkout y POS
            denominan el cobro en el asset de su link (sin `currency`/`local_amount` hasta que se
            usa un método de pago local; el monto del cobro es `asset_amount` en este asset). Los
            demás payins acreditados lo traen cuando la cuenta configuró un `default_payin_asset`
            distinto de USDT — el neto acreditado se auto-convierte a este asset.
        asset_amount:
          type: string
          description: Solo payins de checkout y POS — el monto del cobro denominado en
            `settlement_asset` (presente en todo estado, incluidos pendiente y vencido).
        conversion_status:
          type: string
          enum:
          - pending_retry
          - done
          description: Estado de la auto-conversión — en payins de checkout/POS cuando el pago
            llegó en un asset distinto de `settlement_asset`, y en los demás payins cuando la
            cuenta configuró un `default_payin_asset` distinto de USDT. `pending_retry` se
            reintenta automáticamente. Se omite cuando no aplica conversión.
        status:
          type: string
          enum:
          - pending
          - credited
          - unassigned
          - expired
          - failed
        reference:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        settle_at:
          type: string
          format: date-time
          description: >-
            Solo cobros pagados con tarjeta que tienen una demora de settlement
            configurada — payins directos con tarjeta y cobros checkout/POS pagados
            con tarjeta — cuándo queda disponible el SALDO de este payin (`created_at`
            más las `settlement_hours` de la comisión `payin_card`). El payin queda
            confirmado de inmediato: el estado es `credited` y el webhook
            `payin_credited` se emite al momento del pago; lo que espera al `settle_at`
            es solo el crédito del saldo (y la auto-conversión, cuando está
            configurada). Un payin cuya demora ya venció al aprobarse o asignarse se
            liquida de inmediato. Ausente cuando el saldo se liquida al confirmarse.
        settlement_pending:
          type: boolean
          description: >-
            Presente (true) mientras el saldo de un payin `credited` pagado con
            tarjeta sigue programado para su `settle_at` — cubre payins directos con
            tarjeta y cobros checkout/POS pagados con tarjeta. Mientras esté
            presente, el payin no se puede devolver (`422 settlement_pending`) — la
            plata aún no está en tu saldo. Ausente una vez que el saldo cae en el
            ledger.
        settled_at:
          type: string
          format: date-time
          description: >-
            Cuándo cayó realmente en tu ledger el saldo de este payin acreditado —
            al confirmarse para payins sin demora de settlement, o cuando corrió el
            settlement programado (o un org-admin lo liberó antes) para payins con
            tarjeta diferidos.
        fx_rate:
          type: string
          description: Tu tasa de payin al momento del abono (presente una vez que el pago fue recibido
            y convertido).
        usdt_gross:
          type: string
        fee:
          type: string
        usdt_credited:
          type: string
          description: Monto abonado al saldo. También presente SIN `fx_rate` en cobros
            checkout/POS liquidados en crypto o vía transferencia CBPay (no aplica cotización FX).
        refund_status:
          type: string
          enum:
          - partial
          - full
          description: Presente solo cuando el payin tiene devoluciones o un contracargo. Se deriva de los montos acumulados — el `status` del payin nunca se reescribe.
        refunded_amount:
          type: string
          description: USDT acumulado debitado por devoluciones y contracargos de este payin.
        refunded_local:
          type: string
          description: Monto acumulado devuelto en la moneda local original.
        failure:
          type: object
          description: 'Presente solo cuando el payin queda `failed` en cobros activos: dónde se originó el rechazo (`provider` = el banco del pagador, `core` = la validación previa al cobro) más el código y mensaje concretos.'
          properties:
            source:
              type: string
            code:
              type: string
            message:
              type: string
        payer_source:
          type: string
          enum:
          - declared
          - account_identity
          - none
          - bank_event
          description: >-
            Transferencias anunciadas y depósitos sin asignar — de dónde salió la identidad del
            pagador. `declared` la enviaste tú, `account_identity` se usó por defecto el documento
            verificado del titular, `none` no había identidad disponible (solo la referencia puede
            conciliar), `bank_event` (depósitos sin asignar) se leyó de la transferencia que llegó.
        payer:
          type: object
          description: Identidad del pagador asociada al payin, cuando se conoce.
          properties:
            name:
              type: string
            document:
              type: string
            account:
              type: string
        deposit_instructions:
          $ref: '#/components/schemas/DepositInstructions'
          description: >-
            Snapshot congelado de la cuenta bancaria de destino anunciada al pagador
            (payins `bank_transfer` anunciados en corredores con instrucciones
            configuradas — hoy CL, PY y US). El texto nunca cambia tras la creación;
            la imagen QR se regenera al leer con tu branding vigente. En el corredor
            US/USD este bloque es el riel doméstico (routing number ABA).
        deposit_instructions_swift:
          $ref: '#/components/schemas/DepositInstructions'
          description: >-
            Snapshot congelado del riel internacional SWIFT (BIC + banco
            corresponsal), presente solo en el corredor US/USD cuando tu
            organización tiene configurada la variante swift. El pagador elige
            cualquiera de los dos rieles; ambos comparten la misma `reference`.
        match_method:
          type: string
          enum:
          - charge_link
          - reference
          - payer_document
          - payer_account
          - payer_name
          - amount_single_candidate
          - dedicated_clabe
          - collect_settlement
          - manual_assign
          description: >-
            Cómo se concilió la transferencia con este payin (traza de auditoría). `charge_link`
            significa que el abono pagó el cobro creado para este payin (QR, link de checkout o
            tarjeta) y se vinculó uno a uno por ese cobro — la señal más fuerte, sin
            heurísticas. `manual_assign` significa que un admin de la org lo ruteó a mano.
        match_reason:
          type: string
          description: >-
            Por qué el depósito NO se pudo conciliar automáticamente, en payins `unassigned` —
            `no_match`, `ambiguous_amount` (dos o más anuncios comparten el monto),
            `ambiguous_payer`, `claim_lost` (otro evento tomó el anuncio primero) o
            `assigned_to:<payin_id>`.
        candidate_count:
          type: integer
          description: >-
            Cantidad de anuncios pendientes que coincidían con el monto y la moneda de un depósito
            `unassigned` (2 o más significa que el depósito era ambiguo).
    PayinRefund:
      type: object
      properties:
        refund_id:
          type: string
          format: uuid
        payin_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        kind:
          type: string
          enum:
          - refund
          - void
          - chargeback
          description: '`chargeback` lo impone el emisor de la tarjeta — se asienta automático y puede dejar el saldo negativo.'
        status:
          type: string
          enum:
          - pending
          - completed
          - failed
        currency:
          type: string
          description: Moneda del payin original.
        local_amount:
          type: string
        usdt_debited:
          type: string
          description: Monto debitado del saldo USDT. La comisión y el spread FX del payin original NO se devuelven.
        requested_by:
          type: string
          enum:
          - account
          - admin
          - issuer
        reason:
          type: string
        failure_reason:
          type: string
          description: Presente cuando `status` es `failed` — el débito se revirtió completo.
        idempotency_key:
          type: string
        idempotency_hit:
          type: boolean
        reconciliation_required:
          type: boolean
          description: Presente mientras la devolución está `pending` — el resultado lo resuelve la conciliación y jamás se re-despacha.
        balance_after:
          type: string
          description: Presente en el webhook `payin_refunded` — el saldo USDT después del movimiento (puede ser negativo en un contracargo).
        receipt_url:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Transfer:
      type: object
      properties:
        transfer_id:
          type: string
          format: uuid
        from_account_id:
          type: string
          format: uuid
        to_account_id:
          type: string
          format: uuid
        asset:
          type: string
          example: USDT
        amount:
          type: string
        description:
          type: string
        status:
          type: string
          example: completed
        created_at:
          type: string
          format: date-time
        idempotency_hit:
          type: boolean
    CryptoWithdrawal:
      type: object
      properties:
        withdrawal_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        idempotency_key:
          type: string
        chain:
          type: string
          enum:
          - tron
          - eth
          - btc
        asset:
          type: string
          example: USDT
        to_address:
          type: string
        amount:
          type: string
        fee:
          type: string
        total_debit:
          type: string
        status:
          type: string
          enum:
          - pending
          - processing
          - completed
          - failed
        status_code:
          type: string
        status_message:
          type: string
        tx_id:
          type: string
        travel_rule_status:
          type: string
          enum:
          - not_required
          - self_hosted_attested
          - approved
          description: Presente cuando el retiro fue evaluado contra la política Travel Rule (FATF R.16).
        beneficiary_name:
          type: string
          description: Nombre del beneficiario declarado (retiros con Travel Rule).
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        idempotency_hit:
          type: boolean
    CryptoWallet:
      type: object
      properties:
        wallet_id:
          type: string
          format: uuid
        type:
          type: string
          enum:
          - deposit
          description: 'Discriminador de producto: las wallets de depósito solo reciben crypto que acredita
            el saldo virtual.'
        receive_only:
          type: boolean
          description: Siempre true. Las wallets de depósito no envían, no se exportan ni se importan
            (para eso están las segregadas).
        chain:
          type: string
          enum:
          - tron
          - eth
          - btc
        asset:
          type: string
          example: USDT
        address:
          type: string
        label:
          type: string
          description: Etiqueta descriptiva opcional.
        created_at:
          type: string
          format: date-time
    SegregatedWallet:
      type: object
      properties:
        wallet_id:
          type: string
          format: uuid
        type:
          type: string
          enum:
          - segregated
          description: Discriminador de producto de las wallets segregadas.
        chain:
          type: string
          enum:
          - tron
          - eth
          - btc
        asset:
          type: string
          example: USDT
        address:
          type: string
        label:
          type: string
        origin:
          type: string
          enum:
          - created
          - imported
          description: Cómo entró la wallet a la plataforma.
        exported:
          type: boolean
          description: Si la llave privada se exportó alguna vez (custodia compartida).
        exported_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    SegregatedWalletSend:
      type: object
      properties:
        send_id:
          type: string
          format: uuid
        wallet_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        chain:
          type: string
          enum:
          - tron
          - eth
          - btc
        asset:
          type: string
          example: USDT
        to_address:
          type: string
        amount_raw:
          type: string
          description: Monto en unidades mínimas on-chain.
        fee:
          type: string
          description: Fee de servicio cobrado del saldo de settlement del ledger.
        fee_asset:
          type: string
          example: USDT
        status:
          type: string
          enum:
          - pending
          - processing
          - completed
          - failed
        status_code:
          type: string
        status_message:
          type: string
        tx_id:
          type: string
        idempotency_key:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SignatureProof:
      type: object
      description: Prueba criptográfica de que una wallet firmó un mensaje estructurado
        CBPay (EIP-191 ETH/EVM, TIP-191 TRON). Verificable públicamente vía verify_url.
        El envelope es el mensaje anti-phishing exacto que firmó la wallet (líneas Domain /
        Purpose / Wallet / Nonce / Issued / Expires / Statement; la línea Account
        solo aparece para proofs `wallet_link`). Los proofs viven 10 minutos
        (`expires_at`); un proof firmado sigue siendo verificable públicamente tras expirar.
      properties:
        proof_id:
          type: string
          format: uuid
        wallet_id:
          type: string
          format: uuid
          description: Presente para proofs custodiales (server-side); omitido para proofs wallet-link.
        account_id:
          type: string
          format: uuid
        chain:
          type: string
          enum:
          - eth
          - tron
        address:
          type: string
          description: Dirección on-chain que produjo la firma.
        purpose:
          type: string
          enum:
          - wallet_ownership
          - treasury_attestation
          - wallet_link
        statement:
          type: string
          description: Declaración de texto libre (máx 140 runas). Omitida cuando está vacía.
        envelope:
          type: string
          description: El mensaje estructurado exacto que firmó la wallet.
          example: "CBPay Signature Proof\nDomain: https://api.qbank.cl/platform\nPurpose: wallet_ownership\nWallet: 0x71C7656EC7ab88b098defB751B7401B5f6d8976F\nNonce: 9f2c41d2a887f3e4b219c6d2e8f1a5b7\nIssued: 2026-08-21T14:03:11Z\nExpires: 2026-08-21T14:13:11Z\nStatement: \"I control this wallet\""
        nonce:
          type: string
          description: Nonce anti-replay de 32 caracteres hex.
        proof_code:
          type: string
          description: Código de capacidad de verificación pública (53 caracteres, empieza con G).
          example: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
        status:
          type: string
          enum:
          - issued
          - signed
          - expired
          - revoked
        signature:
          type: string
          description: Presente una vez que status es signed.
        message_hash:
          type: string
          description: Presente una vez que status es signed.
        issued_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          description: issued_at + 10 minutos.
        signed_at:
          type: string
          format: date-time
        revoked_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        verify_url:
          type: string
          example: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
    WalletLink:
      type: object
      description: Wallet externa (custody=client) vinculada a la cuenta firmando un
        desafío nonce. El desafío expira 10 minutos después de emitirse; una vez
        vinculada, el vínculo permanece activo hasta ser revocado.
      properties:
        link_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        chain:
          type: string
          enum:
          - eth
          - tron
        address:
          type: string
        status:
          type: string
          enum:
          - pending
          - linked
          - revoked
          - expired
        expires_at:
          type: string
          format: date-time
          description: Expiración del desafío (10 minutos tras su creación).
        proof_id:
          type: string
          format: uuid
          description: Prueba de firma creada al verificar (presente una vez vinculada).
        verified_at:
          type: string
          format: date-time
        revoked_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    DepositInstrument:
      type: object
      properties:
        instrument_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        country:
          type: string
        currency:
          type: string
        method:
          type: string
        instrument:
          type: string
          description: El destino de depósito a compartir con los pagadores (ej. la CLABE).
        details:
          type: object
          additionalProperties: true
        status:
          type: string
          enum:
          - active
          - disabled
        created_at:
          type: string
          format: date-time
    DepositInstructions:
      type: object
      description: |
        El texto de cuenta bancaria que tu organización publica para un
        corredor `bank_transfer`. Administrado por el equipo admin,
        append-only (una revisión nueva jamás sobrescribe el texto que un
        payin ya congeló).
      properties:
        id:
          type: string
          format: uuid
        country:
          type: string
        currency:
          type: string
        method:
          type: string
        purpose:
          type: string
          enum:
          - payin
          - otc
        revision:
          type: integer
        active:
          type: boolean
        reference_required:
          type: boolean
        bank_name:
          type: string
        account_number:
          type: string
        account_type:
          type: string
        holder_name:
          type: string
        holder_tax_id:
          type: string
        holder_email:
          type: string
        routing_number:
          type: string
          description: >-
            Número de ruta ABA del banco destino (corredor US/USD — ACH y
            wires domésticos). Ausente en corredores que no lo usan.
        swift:
          type: string
          description: >-
            SWIFT/BIC del banco destino (corredor US/USD — wires
            internacionales). Ausente en corredores que no lo usan.
        bank_address:
          type: string
          description: >-
            Dirección registrada del banco destino. Presente cuando el banco
            del corredor la exige en el formulario de transferencia (US/USD).
        intermediary_bank_name:
          type: string
          description: >-
            Banco corresponsal (intermediario) — solo cuando la cuenta del
            corredor recibe wires internacionales a través de uno.
        intermediary_bank_swift:
          type: string
          description: SWIFT/BIC del banco corresponsal, cuando aplica.
        holder_address:
          type: string
          description: >-
            Dirección postal del titular de la cuenta. Los formularios de wire
            de los bancos de EE. UU. la exigen; el QR de cada riel incluye una
            línea "Holder address" cuando tiene valor.
        notes:
          type: string
          description: >-
            Nota operativa libre para el banco emisor (ej. qué país seleccionar
            como banco beneficiario final). Se muestra al pagador tal cual; el
            QR de cada riel incluye una línea "Note" cuando tiene valor.
        created_at:
          type: string
          format: date-time
        qr_payload:
          type: string
          description: |
            Texto plano multilínea con los datos bancarios (y, cuando el
            corredor lo exige, el monto y la referencia). Pensado para que
            lo lea una persona, no para que lo escanee una app bancaria.
        qr_png_base64:
          type: string
          description: |
            PNG en base64 que renderiza `qr_payload` como código QR, con el
            símbolo de marca de tu organización centrado.
webhooks:
  payin_credited:
    post:
      summary: Payin acreditado
      description: 'Se recibió un cobro fiat y se acreditó a la cuenta en USDT. Para links de cobro
        universal (`kind: checkout`) el payload trae además `settled_via` (ej. `qr`,
        `crypto:tron:usdt`, `cbpay`), `settlement_asset` y `asset_amount`; los pagos crypto agregan
        `crypto_amount` y los pagos desde la app CBPay agregan `transfer_id`, `asset` y `amount`. Cuando
        la cuenta configuró un `default_payin_asset` distinto de USDT, el payload trae además
        `settlement_asset` y `conversion_status` de la conversión automática post-crédito.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                payin_id: &id017
                  type: string
                account_id: *id017
                country: *id017
                currency: *id017
                local_amount: *id017
                fx_rate: *id017
                usdt_credited: *id017
                fee: *id017
                settlement_asset: *id017
                conversion_status: *id017
            example:
              payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              country: BO
              currency: BOB
              local_amount: '700.00'
              fx_rate: '6.91'
              usdt_credited: '100.302460'
              fee: '1.000000'
              settlement_asset: USDC
              conversion_status: done
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  subscription_status_changed:
    post:
      summary: 'Una suscripción cambió de estado (active, paused, past_due tras el dunning, o canceled).'
      requestBody:
        content:
          application/json:
            example:
              event_type: subscription_status_changed
              subscription_id: 7a1c9e2d-3b4f-5061-8273-9a4b5c6d7e8f
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
              status: past_due
              period: 3
              reason: dunning_exhausted
      responses:
        '200':
          description: ACK
  card_stored:
    post:
      summary: 'Un pagador guardó una tarjeta en el checkout hosted (consentimiento explícito sobre una semilla 3-D Secure aprobada).'
      requestBody:
        content:
          application/json:
            example:
              event_type: card_stored
              stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              payer_reference: customer-1042
              brand: visa
              last4: '2701'
              country: BO
              currency: BOB
              seed_payin_id: 4f8a1b2c-3d4e-5f60-7a8b-9c0d1e2f3a4b
      responses:
        '200':
          description: ACK
  stored_card_revoked:
    post:
      summary: 'Una tarjeta guardada fue revocada (por ti vía DELETE /v1/stored-cards/{id}).'
      requestBody:
        content:
          application/json:
            example:
              event_type: stored_card_revoked
              stored_card_id: 5f0f2c9e-6a4b-4c7e-9a1d-2b3c4d5e6f70
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              payer_reference: customer-1042
              brand: visa
              last4: '2701'
      responses:
        '200':
          description: ACK
  payin_expired:
    post:
      summary: Payin expirado
      description: 'Un cobro activo (QR o checkout hosteado) llegó a estado final sin recibir el pago:
        el payin pasa de `pending` a `expired` (o `failed`). No se mueve dinero; crea un payin nuevo para
        reintentar el cobro.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                payin_id:
                  type: string
                account_id:
                  type: string
                status:
                  type: string
                  enum:
                  - expired
                  - failed
                country:
                  type: string
                currency:
                  type: string
                local_amount:
                  type: string
                reference:
                  type: string
            example:
              payin_id: 567d0d1a-49bc-437e-92e3-77e921112e49
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              status: expired
              country: BO
              currency: BOB
              local_amount: '60.99'
              reference: CBK7Q2M4XZ9P
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  payin_refunded:
    post:
      summary: Cobro devuelto
      description: 'Una devolución de un cobro con tarjeta alcanzó su estado final, o el emisor impuso un
        contracargo. `status: completed` significa que la plata volvió al tarjetahabiente y el débito
        quedó liquidado en tu saldo; `status: failed` significa que el procesador la rechazó y el débito
        se revirtió completo. Un `kind: chargeback` se asienta automático y puede dejar `balance_after`
        negativo (deuda que se netea contra abonos futuros).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secreto, timestamp + "." + cuerpo))`. Responde 2xx;
        hasta 5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PayinRefund'
            example:
              event_type: payin_refunded
              refund_id: 6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c
              payin_id: d0135ed5-8e9c-4f8b-a522-8ec100470426
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: refund
              status: completed
              currency: USD
              local_amount: '150.00'
              usdt_debited: '150.000000'
              balance_after: '850.000000'
              requested_by: account
              idempotency_key: refund-order-8842
              receipt_url: https://api.qbank.cl/platform/v1/payin-refunds/6f1a2b3c-4d5e-4f6a-8b9c-0d1e2f3a4b5c/receipt
              created_at: '2026-07-25T13:10:00Z'
              updated_at: '2026-07-25T13:10:03Z'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  payin_settlement_scheduled:
    post:
      summary: Acreditación de cobro programada
      description: 'Un cobro pagado con tarjeta — un payin directo con tarjeta o un
        cobro checkout/POS pagado con tarjeta — quedó pagado y su SALDO se programó para un
        `settle_at` futuro porque tu organización configuró una demora de settlement
        (`settlement_hours`) en el servicio `payin_card`. Se emite exactamente una vez al
        confirmarse el pago (idempotente — un retry de la misma confirmación no lo re-emite).
        El `status` es `credited` desde el inicio: el payin queda confirmado y `payin_credited`
        se emite al momento del pago — lo que espera al `settle_at` es solo el SALDO.
        `usdt_net` es el monto que se acreditará al vencimiento (bruto − comisión); los montos
        pueden venir vacíos (`""`) en filas históricas sin bruto/comisión. Al llegar el
        `settle_at`, el worker de settlement libera el saldo (el payin pasa a llevar
        `settled_at` en vez de `settlement_pending`).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secreto, timestamp + "." + cuerpo))`. Responde 2xx;
        hasta 5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            example:
              event_type: payin_settlement_scheduled
              payin_id: 8b3e1c2d-4f5a-6b7c-8d9e-0f1a2b3c4d5e
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              country: US
              currency: USD
              local_amount: '498.75'
              fx_rate: '1.010202'
              usdt_gross: '493.811881'
              fee: '27.159654'
              usdt_net: '466.652227'
              status: credited
              settle_at: '2026-08-12T14:08:33Z'
              receipt_url: https://api.qbank.cl/platform/v1/payins/8b3e1c2d-4f5a-6b7c-8d9e-0f1a2b3c4d5e/receipt
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  payout_status_changed:
    post:
      summary: Estado de payout cambiado
      description: 'Un payout alcanzó un nuevo estado (processing, completed o failed).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                payout_id: *id017
                account_id: *id017
                country: *id017
                currency: *id017
                local_amount: *id017
                usdt_amount: *id017
                total_debit: *id017
                status: *id017
                status_code: *id017
                bank_reference: *id017
            example:
              payout_id: 7d5c2f0a-1e9b-4a6d-8f3c-0b2a1d9e8c7f
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              country: MX
              currency: MXN
              local_amount: '1500.00'
              usdt_amount: '85.714286'
              total_debit: '86.014286'
              status: completed
              status_code: ''
              bank_reference: '00761123456'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  transfer_received:
    post:
      summary: Transferencia recibida
      description: 'La cuenta recibió una transferencia interna.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                transfer_id: *id017
                from_account_id: *id017
                to_account_id: *id017
                asset: *id017
                amount: *id017
                description: *id017
                created_at: *id017
            example:
              transfer_id: 2a7b9c1d-4e5f-6a70-8b9c-0d1e2f3a4b5c
              from_account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              to_account_id: 5e6f7a8b-9c0d-1e2f-3a4b-5c6d7e8f9a0b
              asset: USDT
              amount: '25.000000'
              description: Expense split
              created_at: '2026-07-07T12:00:00Z'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  txn_review_status_changed:
    post:
      summary: Cambio de estado de una revisión transaccional
      description: 'Una operación de la cuenta cambió su estado de revisión (`in_review`
        cuando el firewall transaccional la retiene, `info_requested`, `released`, `rejected`).

        El payload es deliberadamente neutro: solo estado y resumen de la operación — jamás
        incluye motivos internos de revisión ni notas de cumplimiento. Un estado `rejected`
        también puede provenir del barrido por plazo de revisión de la organización
        (rechazo automático); el email y el detalle de la revisión llevan el aviso
        estándar de plazo.

        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: *id017
                review_id: *id017
                kind: *id017
                resource_id: *id017
                status: *id017
                previous_status: *id017
                amount: *id017
                asset: *id017
            example:
              account_id: ae8cf540-1234-5678-9abc-def012345678
              review_id: 7a3f2b1c-0000-4000-8000-000000000001
              kind: payout
              resource_id: 8b4c3d2e-1111-4111-8111-111111111111
              status: released
              previous_status: in_review
              amount: '1500.00'
              asset: USD
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  crypto_deposit_credited:
    post:
      summary: Depósito crypto acreditado
      description: 'Un depósito on-chain fue confirmado y acreditado (neto de la comisión de fondeo) al
        saldo del activo de la wallet (`USDT` o `USDC`).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                account_id: *id017
                chain: *id017
                asset: *id017
                tx_id: *id017
                amount: *id017
                fee: *id017
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
              amount: '499.000000'
              fee: '1.000000'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  crypto_deposit_held:
    post:
      summary: Depósito crypto retenido por revisión de compliance
      description: Un depósito on-chain entrante NO se acreditó porque la dirección remitente presenta
        riesgo severo (sanciones / fondos ilícitos directos). El equipo de compliance del operador revisa
        el hold y lo libera (acredita) o lo rechaza.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              hold_id: c1d2e3f4-5a6b-4c7d-8e9f-0a1b2c3d4e5f
              chain: tron
              asset: usdt
              tx_id: 8a5b3c92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b1
              risk: Severe
              status: held
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  crypto_deposit_alert:
    post:
      summary: Depósito crypto acreditado con alerta de riesgo
      description: Un depósito entrante se acreditó normal, pero la dirección remitente presenta riesgo
        alto. Informativo — no requiere acción sobre los fondos.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: usdt
              tx_id: 9c6d4e92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b2
              risk: High
              status: credited
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  crypto_withdrawal_status_changed:
    post:
      summary: Estado de retiro crypto cambiado
      description: 'Un retiro on-chain alcanzó un nuevo estado.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                withdrawal_id: *id017
                account_id: *id017
                chain: *id017
                asset: *id017
                tx_id: *id017
                status: *id017
                amount: *id017
            example:
              withdrawal_id: 6c5d4e3f-2a1b-0c9d-8e7f-6a5b4c3d2e1f
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
              status: completed
              amount: '100.000000'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  banking_customer_status_changed:
    post:
      summary: Cambió la verificación del perfil bancario
      description: >-
        Se emite cuando cambia el estado de verificación del perfil bancario (submitted, under_review,
        approved, rejected) — tanto del perfil propio de la cuenta (`customer_kind: self`) como de
        los terceros que registraste (`customer_kind: third_party`, con `third_party_id` igual al de
        GET /v1/banking/third-parties/{id}).
      requestBody:
        content:
          application/json:
            examples:
              self:
                summary: Perfil bancario propio
                value:
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
                  customer_kind: self
                  kyc_status: approved
              third_party:
                summary: Tercero registrado
                value:
                  account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
                  customer_id: 5a6b7c8d-9e0f-1a2b-3c4d-5e6f7a8b9c0d
                  customer_kind: third_party
                  third_party_id: 77aa88bb-1c2d-3e4f-5a6b-7c8d9e0f1a2b
                  kyc_status: approved
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la recepción.
  banking_operation_status_changed:
    post:
      summary: Cambió el estado de un pago bancario
      description: Se emite cuando un pago bancario alcanza un nuevo estado (processing, completed, failed).
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              customer_id: 9f2b1c3d-4e5f-6a7b-8c9d-0e1f2a3b4c5d
              operation_id: 7e8a9b0c-1d2e-3f4a-5b6c-7d8e9f0a1b2c
              type: withdraw
              status: completed
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la recepción.
  card_transaction:
    post:
      summary: Transacción de tarjeta
      description: 'Se dispara en cada cambio del ciclo de vida de una transacción de tarjeta: authorized
        (hold en tiempo real), reversed (fondos devueltos) y ajustes de liquidación. Mismos headers de
        firma que toda entrega.'
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
              transaction_id: 5e4d3c2b-1a09-8f7e-6d5c-4b3a2c1d0e9f
              status: authorized
              amount_usdt: '16.170000'
              merchant: AMZN Mktp
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar.
  card_status_changed:
    post:
      summary: Estado de tarjeta cambiado
      description: Se dispara cuando una tarjeta cambia de estado (active, frozen, cancelled), incluidos
        los congelamientos automáticos cuando la mensualidad no se puede cobrar.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              card_id: 3c2b1a09-8d7e-6f5a-4b3c-2d1e0f9a8b7c
              status: frozen
              reason: monthly_fee_unpaid
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar.
  kyc_verification_status_changed:
    post:
      summary: Cambió el estado de una verificación KYC
      description: 'Se emite en cada cambio del ciclo de una submission KYC (submitted, in_review, changes_requested,
        more_info_required, escalated, approved, approved_partial, rejected). Tu propio onboarding llega
        con self_onboarding: true. decision_source indica si la decisión la tomó el motor automático (auto)
        o un revisor humano (admin); está presente en las decisiones finales.'
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyc
              event: approved
              submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
              external_customer_id: cust_789
              status: approved
              risk_band: low
              decision: approved
              decision_source: auto
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyb_verification_status_changed:
    post:
      summary: Cambió el estado de una verificación KYB
      description: Se emite en cada cambio del ciclo de una submission KYB, incluidas las señales AML/riesgo/reporte
        de la revisión. decision_source indica si la decisión la tomó el motor automático (auto) o un revisor
        humano (admin); está presente en las decisiones finales.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyb
              event: approved
              submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
              external_customer_id: cust_456
              status: approved
              risk_band: low
              decision_source: auto
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyc_link_completed:
    post:
      summary: Link KYC completado
      description: Se emite cuando tu cliente completa un link KYC hosteado; nace la submission.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyc
              event: link_completed
              link_id: b2c3d4e5-f6a7-4b8c-9d0e-1f2a3b4c5d6e
              submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
              external_customer_id: cust_123
              status: completed
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyb_link_completed:
    post:
      summary: Link KYB completado
      description: Se emite cuando tu cliente completa un link KYB hosteado; nace la submission.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyb
              event: link_completed
              link_id: d4e5f6a7-b8c9-4d0e-1f2a-3b4c5d6e7f80
              submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
              external_customer_id: cust_456
              status: completed
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyc_document_validated:
    post:
      summary: Terminó el OCR de un documento KYC
      description: Se emite cuando termina la validación OCR de un documento subido por API.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyc
              submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
              external_customer_id: cust_789
              category: identity
              outcome: MATCH
              score: 0.97
              summary: Document matches the submitted identity
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyb_document_validated:
    post:
      summary: Terminó el OCR de un documento KYB
      description: Se emite cuando termina la validación OCR de un documento KYB.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyb
              submission_id: e5f6a7b8-c9d0-4e1f-2a3b-4c5d6e7f8091
              external_customer_id: cust_456
              category: legalPresence
              outcome: REVIEW
              score: 0.72
              summary: Registry extract requires manual review
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  kyc_liveness_completed:
    post:
      summary: Prueba de vida completada
      description: Se emite cuando la prueba de vida en video se completa desde un liveness link.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              kind: kyc
              submission_id: c3d4e5f6-a7b8-4c9d-0e1f-2a3b4c5d6e7f
              external_customer_id: cust_789
              outcome: PASS
              passed: true
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  aml_screening_updated:
    post:
      summary: Novedades del AML screening
      description: Se emite cuando el screening AML termina, un caso cambia, el riesgo cambia o una transacción
        monitoreada es revisada.
      requestBody:
        content:
          application/json:
            example:
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              screening_event: compliance_risk_changed
              customer_id: cus_8f2e1a3b4c5d
              data:
                risk_level: high
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  wallet_deposit_received:
    post:
      summary: Depósito recibido en wallet segregada
      description: 'Se confirmó un depósito on-chain en una wallet segregada. **No** toca el ledger (el
        saldo vive on-chain en la wallet).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx.'
      requestBody:
        content:
          application/json:
            example:
              wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
              amount_raw: '125000000'
              from_address: TDonor00000000000000000000000000000
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  wallet_send_status_changed:
    post:
      summary: Cambió el estado de un envío de wallet segregada
      description: Un envío desde una wallet segregada alcanzó un nuevo estado (processing, completed
        o failed).
      requestBody:
        content:
          application/json:
            example:
              send_id: 9c8b7a6d-5e4f-3a2b-1c0d-9e8f7a6b5c4d
              wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              tx_id: b1946ac92492d2347c6235b4d2611184e0f0f8c9d3a1e2b3c4d5e6f708192a3b
              status: completed
              amount_raw: '25500000'
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  wallet_key_exported:
    post:
      summary: Se exportó la llave de una wallet segregada
      description: 'Alerta de seguridad: se exportó la llave privada de una wallet segregada (custodia
        compartida). El payload nunca incluye la llave.'
      requestBody:
        content:
          application/json:
            example:
              wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              address: TRmSZRaMAqLEevAdGwo3R43bRBXamWR5bd
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  wallet_external_movement:
    post:
      summary: Movimiento externo en una wallet segregada
      description: El sync de actividad detectó un movimiento on-chain de una wallet segregada que no
        pasó por la plataforma (esperable en custodia del cliente, p. ej. tras importar o exportar la
        llave). El registro de actividad de la wallet queda completo igual.
      requestBody:
        content:
          application/json:
            example:
              wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              direction: out
              tx_id: 9a3c1e5f7b2d4a6c8e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d
              amount_raw: '25000000'
              custody: client
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  wallet_key_compromise_suspected:
    post:
      summary: Sospecha de llave comprometida (alarma crítica)
      description: 'CRÍTICO: salió plata de una wallet segregada con custodia de la plataforma sin pasar
        por la plataforma. Por diseño es imposible salvo que la llave privada se haya filtrado. Trata
        la llave como comprometida, mueve los fondos restantes a una wallet nueva y contacta soporte de
        inmediato.'
      requestBody:
        content:
          application/json:
            example:
              wallet_id: b7e3a1c2-9f4d-4a8b-8c1e-2d3f4a5b6c7d
              account_id: 9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d
              chain: tron
              asset: USDT
              direction: out
              tx_id: 9a3c1e5f7b2d4a6c8e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d
              amount_raw: '25000000'
              custody: cbpay
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  corridor_status_changed:
    post:
      summary: Cambió la disponibilidad de un corredor
      description: >-
        Un corredor de pago cambió de disponibilidad (`operational` / `degraded` / `down`) —
        se dispara en caídas Y en recuperaciones. Evento broadcast: no está ligado a una
        operación tuya, así que no trae `account_id`. Consúmelo de forma idempotente
        (dedupe por delivery id). Ver la guía de estado del servicio.
      requestBody:
        content:
          application/json:
            example:
              flow: payout
              country: VE
              currency: VES
              method: bank_transfer
              status: down
              previous_status: operational
              since: '2026-07-24T22:10:00Z'
              reason: consecutive infrastructure failures
      responses:
        '200':
          description: Responde cualquier 2xx para confirmar la entrega.
  balance_adjusted:
    post:
      summary: Ajuste de saldo aplicado
      description: 'Un ajuste administrativo acreditó o debitó el saldo. Se emite solo cuando el asiento entró de verdad: un ajuste a la espera de un segundo aprobador, o el replay de una clave de idempotencia ya usada, no emite.'
      requestBody:
        content:
          application/json:
            example:
              event_id: 1a2b3c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d
              type: balance_adjusted
              account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
              created_at: '2026-07-25T18:02:00Z'
              data:
                account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                asset: USDT
                amount: '10.000000'
                direction: credit
                reason: manual top-up
                available: '109.700000'
                held: '0.000000'
      responses:
        '200':
          description: 'Recibido.'
  account_status_changed:
    post:
      summary: Estado de la cuenta cambiado
      description: 'El estado administrativo de la cuenta cambió (`active`, `blocked`, `closed`). Reenviar el mismo estado no emite.'
      requestBody:
        content:
          application/json:
            example:
              event_id: 2b3c4d5e-6f7a-4b8c-9d0e-1f2a3b4c5d6e
              type: account_status_changed
              account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
              created_at: '2026-07-25T18:06:30Z'
              data:
                account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                status: blocked
                previous_status: active
      responses:
        '200':
          description: 'Recibido.'
  member_security_event:
    post:
      summary: Evento de seguridad de la cuenta
      description: 'Un hecho de seguridad de la cuenta (inicio de sesión, cambio de contraseña, factor agregado o quitado, sesión revocada). Mismos campos que `GET /v1/me/security/events`.'
      requestBody:
        content:
          application/json:
            example:
              event_id: 3c4d5e6f-7a8b-4c9d-8e0f-2a3b4c5d6e7f
              type: member_security_event
              account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
              created_at: '2026-07-25T18:09:05Z'
              data:
                account_id: c57f05b6-8f2e-4d51-9a0c-5b7e2d3f4a10
                event_type: password_changed
                member_id: 1c2d3e4f-3b7d-4bad-9bdd-2b0d7b3dcb6d
                ip: 200.1.2.3
                user_agent: Mozilla/5.0
      responses:
        '200':
          description: 'Recibido.'
  risk_report_ready:
    post:
      summary: Informe crediticio Qscore listo
      description: 'Un informe crediticio Qscore comprado con `POST /v1/qscore/reports`
        terminó de generarse con éxito. El payload lleva la identidad del informe,
        el score/banda resultante y el `verify_code` público (compartible con el
        sujeto como prueba de autenticidad en `GET /verify/qscore/{code}`). Si la
        generación falla, el informe queda `status: failed` y no se emite evento.
        El evento también se emite cuando el informe comprado ya estaba disponible
        para el sujeto (reuso idempotente).

        El mismo evento se dispara para un informe SELF generado por el titular
        con `POST /v1/qscore/my-report`; en ese caso el payload agrega
        `purpose: "self_access"` (ausente en los informes comprados).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                report_id:
                  type: string
                subject_id:
                  type: string
                doc_id:
                  type: string
                country:
                  type: string
                subject_type:
                  type: string
                  enum: [person, company]
                purpose:
                  type: string
                  description: 'Presente solo en informes self: `self_access`.'
                  enum: [self_access]
                score:
                  type: integer
                band:
                  type: string
                  enum: [A, B, C, D, E, SC]
                verify_code:
                  type: string
            example:
              report_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
              subject_id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
              doc_id: '12983319'
              country: CL
              subject_type: person
              score: 742
              band: B
              verify_code: Q3fa85f6457174562b3fc2c963f66afa6a1b2c3d4e5f
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  risk_score_changed:
    post:
      summary: Cambió el score Qscore
      description: 'Un informe Qscore recién generado produjo un score distinto al
        último conocido del sujeto (p. ej. en una re-consulta de monitoreo o una
        compra nueva). Se emite solo cuando el score efectivamente cambia; el payload
        lleva el par score/banda anterior y nuevo para que decidas si actuar.
        Suscríbete a este evento para monitorear un sujeto en el tiempo sin polling.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                subject_id:
                  type: string
                report_id:
                  type: string
                old_score:
                  type: integer
                new_score:
                  type: integer
                old_band:
                  type: string
                new_band:
                  type: string
            example:
              subject_id: 7c9e6679-7425-40de-944b-e07fc1f90ae7
              report_id: 3fa85f64-5717-4562-b3fc-2c963f66afa6
              old_score: 742
              new_score: 688
              old_band: B
              new_band: B
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  risk_monitoring_alert:
    post:
      summary: Alerta de monitoreo Qscore
      description: 'Un sujeto que la cuenta monitorea vía `PUT /v1/qscore/subjects/{docID}/monitoring`
        cambió desde la última revisión. `triggers` lista qué disparó la alerta:
        `score_drop_below` (el score cruzó hacia abajo el umbral `monitor_since_score`
        de la suscripción: anterior >= umbral, nuevo < umbral), `new_records` (el
        buró registró eventos nuevos; con `only_material: true` solo cuentan los
        eventos materialmente negativos — mora abierta, protesto, quiebra, demanda) o
        `records_removed` (el buró ahora muestra menos eventos que el baseline, es
        decir, un registro se cerró o se eliminó). Las suscripciones activas se
        re-evalúan cada ~5 minutos; la primera pasada tras suscribirse solo siembra
        el baseline y jamás alerta con el historial que ya mostraba el informe
        comprado. `score` y `previous_score` se omiten mientras el sujeto no tenga
        score clasificable, y `new_records` se omite cuando ningún evento nuevo
        disparó la alerta.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                monitoring_id:
                  type: string
                subject_id:
                  type: string
                doc_id:
                  type: string
                country:
                  type: string
                subject_type:
                  type: string
                  enum: [person, company]
                triggers:
                  type: array
                  items:
                    type: string
                    enum: [score_drop_below, new_records, records_removed]
                score:
                  type: integer
                previous_score:
                  type: integer
                band:
                  type: string
                  enum: [A, B, C, D, E, SC]
                record_count:
                  type: integer
                new_records:
                  type: array
                  items:
                    type: object
                    properties:
                      source:
                        type: string
                      record_type:
                        type: string
                      reported_at:
                        type: string
                      amount:
                        type: string
                      currency:
                        type: string
                      status:
                        type: string
                detected_at:
                  type: string
            example:
              monitoring_id: 7c9e2f14-5b6a-4c8d-9e1f-3a7b5c2d8e91
              subject_id: 8f5fb0d2-1d45-4a0e-9d5c-2d39f2b7a112
              doc_id: 12.345.678-5
              country: CL
              subject_type: person
              triggers: [score_drop_below, new_records]
              score: 487
              previous_score: 512
              band: D
              record_count: 3
              new_records:
              - source: res_chile
                record_type: debt_collection
                reported_at: '2026-08-05T00:00:00Z'
                amount: '450000'
                currency: CLP
                status: open
              detected_at: '2026-08-08T18:41:12Z'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  risk_batch_completed:
    post:
      summary: Lote de scoring Qscore completado
      description: 'Un lote de scoring creado con `POST /v1/qscore/batches`
        terminó de procesar todos sus ítems. Esta es la única señal de
        finalización del lote: los informes individuales **no** emiten su propio
        webhook `risk_report_ready` (y tampoco se envía un email por ítem).
        `status` es `completed` cuando todos los ítems tuvieron éxito,
        `completed_with_errors` cuando al menos un ítem falló de forma terminal
        (su fee fue reembolsado) y `failed` cuando el lote mismo no pudo
        procesarse. Los contadores siempre cuadran: `succeeded_items +
        failed_items = total_items`. Lee los resultados por ítem con `GET
        /v1/qscore/batches/{id}/items` o descárgalos con `GET
        /v1/qscore/batches/{id}/results.csv`.


        Toda entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`,
        `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp` y `X-Webhook-Signature =
        hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta 5
        intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                batch_id:
                  type: string
                status:
                  type: string
                  enum: [completed, completed_with_errors, failed]
                total_items:
                  type: integer
                succeeded_items:
                  type: integer
                failed_items:
                  type: integer
                country:
                  type: string
                purpose:
                  type: string
                  enum: [credit_evaluation, tenant_screening, hiring, supplier_onboarding, other]
            example:
              batch_id: b7f2c1a4-3e5d-4f8a-9c2b-1d0e6a8f4c5d
              status: completed_with_errors
              total_items: 4
              succeeded_items: 3
              failed_items: 1
              country: CL
              purpose: credit_evaluation
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  risk_consent_granted:
    post:
      summary: Consent Qscore otorgado por el titular
      description: 'Un link de consentimiento creado con `POST /v1/qscore/consents`
        fue otorgado: el titular abrio la pagina publica, conecto su banco via
        open finance y autorizo la solicitud (su documento verificado calzo con el
        documento del sujeto). El consent ahora desbloquea la data bancaria
        positiva del sujeto en los informes Qscore nuevos. `previous_status` es
        `pending` en la practica; viaja para que un replay sea facil de
        reconciliar.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                consent_id:
                  type: string
                subject_id:
                  type: string
                country:
                  type: string
                doc_id:
                  type: string
                subject_type:
                  type: string
                  enum: [person, company]
                purpose:
                  type: string
                  enum: [credit_evaluation, tenant_screening, hiring, supplier_onboarding, other]
                status:
                  type: string
                  enum: [granted]
                previous_status:
                  type: string
                holder_name:
                  type: string
                openfinance_link_id:
                  type: string
                granted_at:
                  type: string
                  format: date-time
            example:
              consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
              subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
              country: CL
              doc_id: 12.345.678-5
              subject_type: person
              purpose: credit_evaluation
              status: granted
              previous_status: pending
              holder_name: Maria Jose Contreras Soto
              openfinance_link_id: lnk_9b2c4d6e8f
              granted_at: '2026-08-10T09:21:44Z'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  risk_consent_revoked:
    post:
      summary: Consent Qscore revocado
      description: 'Un link de consentimiento fue revocado, ya sea porque el
        titular rechazo la solicitud en la pagina publica o porque tu cuenta llamo
        `POST /v1/qscore/consents/{id}/revoke`. Un consent revocado deja de
        alimentar data bancaria positiva a los informes Qscore nuevos.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                consent_id:
                  type: string
                subject_id:
                  type: string
                country:
                  type: string
                doc_id:
                  type: string
                subject_type:
                  type: string
                  enum: [person, company]
                purpose:
                  type: string
                  enum: [credit_evaluation, tenant_screening, hiring, supplier_onboarding, other]
                status:
                  type: string
                  enum: [revoked]
                previous_status:
                  type: string
                revoked_at:
                  type: string
                  format: date-time
            example:
              consent_id: c1a2b3d4-5e6f-4a7b-8c9d-0e1f2a3b4c5d
              subject_id: 7e8f9a0b-1c2d-4e3f-8a9b-0c1d2e3f4a5b
              country: CL
              doc_id: 12.345.678-5
              subject_type: person
              purpose: credit_evaluation
              status: revoked
              previous_status: pending
              revoked_at: '2026-08-10T10:02:11Z'
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  wallet_signature_created:
    post:
      summary: Prueba de firma de wallet creada
      description: 'Se creo una prueba de firma con una wallet segregada (`POST
        /v1/segregated-wallets/{walletID}/signatures`). La wallet firmo un mensaje
        con el estandar nativo de la red (EIP-191 para ETH/EVM, TIP-191 para TRON)
        dentro del sobre anti-phishing CBPay Signature Proof. La prueba se verifica
        publicamente en `verify_url` (sin auth).


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                proof_id:
                  type: string
                account_id:
                  type: string
                wallet_id:
                  type: string
                chain:
                  type: string
                  enum: [eth, tron]
                address:
                  type: string
                purpose:
                  type: string
                proof_code:
                  type: string
                  description: Codigo publico de la prueba (empieza con `G`).
                verify_url:
                  type: string
                  description: URL publica de verificacion (sin auth).
            example:
              proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
              account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
              wallet_id: b7e39c14-2d56-4e78-8a01-3c5d7e9f1b24
              chain: eth
              address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
              purpose: wallet_ownership
              proof_code: Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
              verify_url: https://api.qbank.cl/platform/v1/public/signature-proofs/Gc41d2a887f3e4b219c6d2e8f1a5b7d90a1b2c3d4e5f60718293a
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
  wallet_linked:
    post:
      summary: Wallet externa vinculada
      description: 'Se vinculo una wallet externa (`custody=client`) a la cuenta
        resolviendo un desafio nonce (`POST /v1/wallet-links/challenges` + `POST
        /v1/wallet-links/verify`). La wallet sigue bajo custodia del cliente; el
        vinculo solo prueba la titularidad de la direccion.


        Cada entrega incluye `X-Webhook-Event`, `X-Webhook-Event-ID`, `X-Webhook-Delivery-ID`, `X-Webhook-Timestamp`
        y `X-Webhook-Signature = hex(HMAC-SHA256(secret, timestamp + "." + body))`. Responde 2xx; hasta
        5 intentos con backoff incremental.'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                link_id:
                  type: string
                account_id:
                  type: string
                chain:
                  type: string
                  enum: [eth, tron]
                address:
                  type: string
                proof_id:
                  type: string
                  description: Prueba de firma que respalda el vinculo.
            example:
              link_id: d52e7b91-4c68-4f89-1b23-5d7e9f0a2c46
              account_id: ae8c1f02-3b45-4c67-9d12-8f0e5a6b7c8d
              chain: eth
              address: '0x71C7656EC7ab88b098defB751B7401B5f6d8976F'
              proof_id: c41d2a88-7f3e-4b21-9c6d-2e8f1a5b7d90
      responses:
        '200':
          description: Devuelve cualquier 2xx para confirmar la entrega.
